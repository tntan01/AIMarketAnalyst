# Gate72 — Kế hoạch sửa nhỏ có checkpoint cho CODER2

**KẾT LUẬN HIỆN HÀNH — TASK72 APPROVED, 2026-09-12.** F00…F11 hoàn tất; A/B/C/D PASS; R72-01…09 CLOSED trong phạm vi gate. Xem response mục0 và fix-progress §A3.157. Không còn việc sửa gate72; task73 chưa thực hiện, không rollout production. Các chỉ dẫn NEXT/CHANGES_REQUESTED/WAITING_REVIEW trước bên dưới giữ lịch sử.

**QUYẾT ĐỊNH HIỆN HÀNH — checkpoint C PASS (2026-09-12), fix-progress §A3.155.** A/B/C đã PASS; NEXT=F11, nghiệm thu end-to-end và bản bàn giao trình D. Full reviewer982P, probes16P; chưa APPROVED gate72/task73. Các kết luận C trước bên dưới là lịch sử đã thay thế.

**Hiện hành — C lần2 CHANGES_REQUESTED (§A3.153):** C-R2/same-pool đạt; chỉ còn C-R1 caller fallback open time thành reclaimed_at canonical. G1/G2 đã chốt tại §A3.153, không sửa policy mới; chưa F11/task73.

**Hiện hành — C CHANGES_REQUESTED (2026-09-12), fix-progress §A3.151.** Full979P chưa đủ: sửa C-R1 canonical lineage/time ở caller và C-R2 history cùng pool mâu thuẫn trước F11. Giữ A/B PASS, không task73; bản trình IMPLEMENTED/WAITING_REVIEW dưới đây là lịch sử coder.

**NEXT hiện hành: F07/r1 (§A3.146 fix-progress)** — sửa boundary legacy của `_smc_for_timeframe`, khôi phục compatibility có regression; không migrate producer hoặc rollout canonical trên Analyze/Scanner. Canonical thiếu records/provenance vẫn fail closed. Giữ A/B PASS, chưa F08/task73; trạng thái trước bên dưới là lịch sử.

**NEXT hiện hành: F06/r1 — nối usable_at từ swing producer**, theo quyết định TL fix-progress §A3.143. Confirmed usable non-provisional swing: usable_at=confirmed_at (close nến xác nhận), khai tại producer; không fallback tại pool/sweep. Báo xong rồi dừng trước F07; A/B vẫn PASS, chưa C/task73.

**QUYẾT ĐỊNH HIỆN HÀNH — F00…F11 IMPLEMENTED; CHECKPOINT D `WAITING_REVIEW` (2026-09-12, bản trình [smc-task-72-response.md](smc-task-72-response.md)).** A/B/C **PASS** (§A3.129/§A3.141/§A3.155). **Acceptance 129P/0F, 16 probes 16P/0F**, task57–71 108P, retained 854P, full §5 983P/0F (cùng snapshot); 6/6 fingerprint bảo vệ khớp ledger F00; `git diff --check` sạch. G1/G2 chốt §A3.153; C-R1 (gồm reclaim-time)/C-R2 đã sửa. **Chưa** CLOSED 9 finding, **chưa** APPROVED gate72, **chưa** task73 — TL quyết định cuối. Các nhận xét cũ bên dưới là lịch sử đã được quyết định này thay thế.

**Hiện hành — B lần3 CHANGES_REQUESTED (§A3.139):** B-R1 và terminal/timestamp B-R2 đạt; chỉ sửa tiếp history B-R2 (enrich thiếu tick đổi visit closed_by_invalidation thành reaction sau terminal). Giữ phần đã đạt, chưa F06; các quyết định cũ bên dưới là lịch sử.

**Cập nhật TL — B lần2 (2026-09-11): CHANGES_REQUESTED, chỉ còn B-R2.** B-R1 đạt; B-R2 còn ghi đè terminal/timestamp đã biết khi derivation hết tuổi. Xem fix-progress §A3.137; sửa cùng mapping ba regression và trình lại B, không mở lại A/B-R1, chưa F06.

**Quyết định hiện hành checkpoint B: CHANGES_REQUESTED (2026-09-11).** Xem fix-progress §A3.135: chỉ sửa B-R1 (terminal lifecycle bị zone False che) và B-R2 (thiếu metadata dựng invalidation0/mất terminal history), trình lại B; chưa F06/task73. Giữ A PASS. Các trạng thái bản trình bên dưới là lịch sử, không thay quyết định này.

**Trạng thái: PLAN_READY; gate72 vẫn CHANGES_REQUESTED, 9 finding OPEN.**

**Điểm đang thực hiện:** checkpoint A lần4 **PASS** — quyết định Tech Lead 2026-09-11 tại [fix-progress §A3.129](smc-task-72-fix-progress.md). Coder làm **F02 → F03 → F04 → F05 → trình B**. Không làm lại A3-001…090. Gate72 vẫn CHANGES_REQUESTED, 9 findings OPEN; chưa F06/task73.

**Checklist A3-001…090:** hoàn tất chuẩn bị F01 và refresh A lần4; A3-090 IMPLEMENTED, checkpoint A PASS (§A3.129). Checklist, review A lần3 và các phần “Xong” bên dưới giữ lịch sử, không phải 90 task mới của kế hoạch144 bước.

Tech Lead lập ngày 2026-09-11 theo yêu cầu người dùng. Owner implementation: **CODER2**; ADMIN điều phối, Tech Lead duyệt checkpoints và quyết định gate. Mục tiêu là phát hiện sai expected/interface trước khi sửa code và bắt regression ngay trong cụm nhỏ, không hứa không bao giờ cần chỉnh sửa thêm.

`F00…F11` dưới đây là **subtask sửa các task57–71**, không phải task mới trong checklist1–144, không đổi số task và không mở rộng phạm vi cần task145. Chưa task73, chưa production rollout/auto-entry. Không tạo framework/service/database mới.

## 1. Nguồn quyết định và quy tắc làm việc

Đọc [review gate72](smc-task-72-review.md), [lifecycle spec](smc-lifecycle-spec.md), parameter table P7/P8/P9 và BQLC phần ownership/D1. Bộ [reviewer probes](probes/test_smc_gate72_review.py) có 16 tests, baseline **14 failed / 2 passed**. Baseline Coder: **108 tests task57–71**, **854 tests SMC/scanner** đều pass nhưng có fixture sai; không lấy số pass làm oracle.

- Làm đúng thứ tự **F00 → F01 → checkpoint A → F02…F05 → checkpoint B → F06…F10 → checkpoint C → F11 → checkpoint D**.
- Mỗi subtask: viết/đối chiếu test với contract → ghi RED trước sửa → sửa đúng owner → chạy targeted + toàn bộ tests đã xanh của các bước trước → ghi bằng chứng. Một nhóm thay đổi có một root cause; không gộp refactor unrelated.
- Chỉ một writer cho các file core dùng chung. Không cho nhiều coder cùng sửa `smc_context.py`/`smc_lifecycle.py`/`smc_sweep_linking.py` trong worktree này. Giữ dirty changes có sẵn; không reset hoặc tự commit toàn repo.
- Không sửa reviewer probe/contract/R56 golden để làm xanh. Được sửa các OHLC fixtures sai đã chỉ ra ở R72-09, nhưng phải giữ ý nghĩa ca test và có bảng old→new→expected được duyệt tại A. Nếu một probe cần đổi vì API thực sự đổi, trình TL trước, không tự sửa assertion.
- Không hard-code theo case ID/zone ID; không dùng current output để sinh expected. Luôn có positive, negative, boundary và BUY/SELL ở quy tắc có hướng.
- Nếu test của bước sau đã fail trong baseline thì được ghi rõ chưa xử lý; không cần làm tất cả xanh ở bước đầu. **Không được tạo regression mới ở bước đã đạt.** Nếu dựng lại fixture đúng làm lộ fail mới, ghi input và root cause để TL duyệt phạm vi tại A, không âm thầm đổi expected.
- Không có phép duyệt tự động bằng pytest. Qua checkpoint cần Tech Lead ghi PASS cho checkpoint tương ứng trước khi ADMIN/CODER2 chuyển cụm. PASS checkpoint không phải APPROVED gate72.

## 2. Bảng giao việc

| Subtask | Công việc nhỏ | Finding / task gốc | Phụ thuộc | Sản phẩm kiểm tra được |
|---|---|---|---|---|
| F00 | Chụp baseline và lập ledger | Tất cả / gate72 | — | Hash, lệnh/count, danh sách fail, các file dirty |
| F01 | Sửa fixture sai; trình matrix expected và interface | 09; chuẩn bị 01…08 / 60,62,65,71 | F00 | OHLC hợp lệ, test IDs cho toàn bộ acceptance; chưa core edits |
| **A** | **Tech Lead duyệt expected và phạm vi** | Không phải duyệt implementation | F01 | PASS hoặc chỉ rõ ô cần sửa; chưa làm F02 nếu chưa PASS |
| F02 | Forward tick/ATR và phân biệt missing data | 07 / 59,63,65 | A | Metadata contract và boundary probes đạt |
| F03 | Đồng bộ invalid state/usability | 08 / 63,65 | F02 | Context → typed round-trip không còn cờ mâu thuẫn |
| F04 | Chặn reaction tại/sau terminal expiry | 06 / 60,64,65 | F03 | Timeline prefix/expiry/reaction đạt |
| F05 | D1 consumer loại evidence của vùng terminal | 05 / 70,71 | F04 | D1 lifecycle thật trước/sau invalid/expired đạt |
| **B** | **Tech Lead duyệt cụm lifecycle → D1** | 05…08 | F05 | PASS cụm; giữ các probes liquidity RED đã biết |
| F06 | Giữ identity/provenance/usable time của pool | 01 / 66 | B | Pool source records và equal-pool tests đạt |
| F07 | Sweep chỉ dùng pool usable đúng thời điểm | 01 / 67,71 | F06 | Pool → sweep positive/negative/prefix đạt |
| F08 | Xét mọi claim trước chọn owner | 02 / 68,69,71 | F07 | Context owner theo claim time, không theo distance rank |
| F09 | Chọn contribution chỉ trong owner children | 03 / 69,71 | F08 | Owner-present tổng1, non-owner0, duplicate/permutation ổn định |
| F10 | Giữ assignment/consumption qua replay/restore | 04 / 69,71 | F09 | Owner không bị lấy lại; thiếu history fail closed |
| **C** | **Tech Lead duyệt cụm pool → sweep → ownership** | 01…04 | F10 | PASS cụm; toàn bộ16 reviewer probes phải xanh |
| F11 | Chạy end-to-end, regression và dossier | 09 + tất cả / 65,71,72 | C | Matrix nguồn thật, full test results và manifest bản trình |
| **D** | **Tech Lead re-review gate72 và đồng bộ hồ sơ** | gate72 | F11 | APPROVED hoặc CHANGES_REQUESTED có bằng chứng |

## 3. Chỉ dẫn từng subtask

### F00 — Baseline và ledger; không sửa code/tests

1. Đọc toàn bộ 9 finding, xác minh hashes theo review trước khi nhận phần code hiện tại. Hash thay đổi không mặc nhiên là lỗi: nếu khác, ghi revision và diff liên quan, không overwrite người khác.
2. Chạy riêng16 reviewer probes,108 task tests và854 baseline; ghi đúng actual, không copy count từ tài liệu. Liệt kê node IDs fail và mapping R72.
3. Tạo `docs/plans/smc-task-72-fix-progress.md` theo template §6. Ghi file đang dirty và hash core/protected artifacts để tách thay đổi lượt này.

**Đạt khi:** tái lập được vấn đề và có baseline đủ so sánh. Nếu không tái lập, báo dữ liệu/phiên bản khác trước khi sửa; không đoán fix.

### F01 — Sửa oracle trước implementation

**Chỉ được đổi:** fixtures/test factories task57–71 cần thiết, thêm acceptance tests và hồ sơ fix; **không đổi `core/`**.

1. Audit các factory OHLC task57–71 bằng shared `validate_smc_candles`, ưu tiên task60/task62. Tách rõ fixtures invalid-data có chủ đích khỏi positive market data. Không bắt một test dữ liệu lỗi giả vờ là positive.
2. Dựng lại các rows sai OHLC, giữ mục tiêu exit/no-reaction/reaction/full-fill. Ví dụ close dưới low phải thay bằng một candle hợp lệ mà vẫn đạt đúng tolerance/reaction window đã định; không chỉ hạ low tùy tiện vì có thể biến outside thành overlap.
3. Tạo `docs/plans/smc-task-72-acceptance-matrix.md`: mỗi hàng có case ID, finding, input/metadata/cutoff, expected, contract reference, test node ID. Bao phủ toàn bộ §4 trước khi sửa core. Expected ghi tay/đối chiếu giá, không từ production output.
4. Viết tests mới trong `tests/test_smc_gate72_fix_acceptance.py` hoặc file phù hợp hiện có, tên test thể hiện behavior. Tests phải collect/run được, lỗi thiếu interface mới thể hiện assertion có ý nghĩa, không để cả suite chết vì import error.
5. Trong matrix, ghi bảng interface dự kiến: caller → owner → dữ liệu trả về. Chỉ rõ cách giữ pool source/usable time, truyền assignment history/history completeness, biểu diễn metadata unknown và canonical/legacy boundary. Tên field mới là đề xuất implementation để TL duyệt ở A, không phải tự thêm business policy.
6. Nếu cần chọn boundary chưa đủ rõ trong tài liệu (ví dụ pool mới usable ngay tại close của candle đang xét), ghi case cụ thể và đề xuất vào ô cần TL quyết định. **Không tự chốt bằng việc cho code hiện tại quyết định.**

**Đạt khi:** R72-09 fixture-validity probe pass; mỗi acceptance obligation có test/input/expected rõ. Tests behavior của các finding chưa sửa có thể RED. Ghi đầy đủ fail mới lộ ra sau chỉnh fixture, không gọi chúng là regression ngoài scope rồi bỏ qua.

**DỪNG CHECKPOINT A:** gửi TL matrix, diff tests, ledger và kết quả. Không làm F02 trước khi A được PASS.

### F02 — Metadata lifecycle đúng nguồn (R72-07) — **IMPLEMENTED 2026-09-11**

**Trạng thái:** IMPLEMENTED (**F02/r1** đã khép blocker regression legacy — xem fix-progress §A3.131). Sửa `core/smc_lifecycle.py` + `core/smc_context.py` + `core/smc_models.py`: threshold metadata-dependent trả `None` + reason thay vì `0.0`; forward tick argument; conflict cùng scope ⇒ unknown; `metadata_state`/`metadata_reason` đi xuyên lifecycle → context → typed. **Chưa** PASS checkpoint B.

**Owner:** `enrich_zones` → lifecycle threshold resolution; chỉ thêm model fields nếu cần biểu diễn unknown đã duyệt tại A.

- Forward tick đã cung cấp qua argument, không bắt caller phải copy vào mọi zone. Ghi quy tắc khi item và argument đều có; conflict/missing handling phải khớp matrix A, không lấy tùy bên làm xanh.
- Thiếu metadata bắt buộc trên canonical path không trở thành tolerance/buffer0. Không dùng `or 0`, broker digits hay latest/future ATR thay nguồn causal thiếu. Explicit override chỉ bỏ requirement của rule tương ứng, không bypass mọi rule.
- Giữ legacy compatibility bằng boundary nhận diện đã duyệt, không dùng fallback chỉ vì canonical record thiếu field.
- Chạy `r72_07` BUY/SELL, missing tick/ATR, invalid/nonfinite metadata, item-vs-argument parity và inside/equal/outside buffer.

**Đạt khi:** tick=.1/ATR1 và BUY close99.98 đối với zone[100,110] không broken; SELL mirror tương tự; missing canonical data có trạng thái/reason fail-closed được duyệt.

### F03 — Một trạng thái terminal canonical (R72-08) — **IMPLEMENTED 2026-09-11**

**Trạng thái:** IMPLEMENTED (không blocker). Sửa `core/smc_context.py` + `core/smc_models.py`: `enrich_zones` chiếu canonical terminal (`invalid` ⇒ status invalid + `usable=False` + `broken=True` + giữ `invalidated_at`/`invalidation_index`/reason/history), invalidation thắng expiry; `SmcZone` có `usable` và canonical `invalid` thắng cờ legacy trong `__post_init__`. Bằng chứng: fix-progress §A3.132. **Chưa** PASS checkpoint B.

**Owner:** `enrich_zones` projection và typed adapter nếu cần.

- Khi lifecycle invalidates: status invalid, usable false, broken canonical nhất quán; giữ invalidation timestamp/reason và visit history, original bounds/ID/setup. Không dựng breaker.
- Không tái đánh giá dữ liệu bằng cờ legacy rồi override terminal. Không coi missing metadata là bằng chứng vùng đã invalid; unknown và invalid tách nhau.
- Chạy `r72_08`, context → SmcZone round-trip, repeated enrichment/serialization; control chưa phá buffer vẫn confirmed theo trạng thái đầu vào.

**Đạt khi:** không có output vừa broken vừa confirmed/usable=true; F02 giữ xanh.

### F04 — Reaction không vượt expiry (R72-06) — **IMPLEMENTED 2026-09-11**

**Trạng thái:** IMPLEMENTED (không blocker). Sửa `core/smc_lifecycle.py`: thêm `_expiry_boundary_index` tính **cùng** anchor/availability/lifetime như vòng lặp chính, và `_follow_through_reaction_at` nhận `terminal_index` để **cắt cửa sổ follow-through** tại biên lifetime ⇒ reaction tại/sau terminal không được cấp, reaction hợp lệ trước terminal giữ nguyên. Bằng chứng: fix-progress §A3.133. **Chưa** PASS checkpoint B.

**Owner:** `analyze_zone_lifecycle` / follow-through helper.

- Dùng cùng tuổi/availability/terminal boundary của main lifecycle khi đánh giá từng reaction candle. Có thể xử lý tuần tự hoặc giới hạn search; không bắt layout thuật toán.
- Ưu tiên invalidation/expiry trước reaction. Hết hạn tại candle21 của D1 lifetime20 không được biến exit20 thành reacted21.
- Không xóa reaction đã xảy ra hợp lệ trước terminal. Same-candle exit→reaction trước expiry vẫn đạt; giữ prefix/cold parity.
- Chạy `r72_06`, expiry trước/đúng/sau prospective reaction, invalidation precedence, D1/H4 lifetime boundary, BUY/SELL.

**Đạt khi:** prefix tới20 completed_unreacted không bị append candle21 expired đổi thành reacted; không chỉ thêm guard riêng cho D1.

### F05 — D1 evidence đọc lifecycle terminal (R72-05) — **IMPLEMENTED 2026-09-11**

**Trạng thái:** IMPLEMENTED (không blocker). Sửa `core/smc_confluence.py::build_d1_reaction_evidence`: cổng canonical terminal đọc **cả** typed/serialized zone **và** actual `ZoneLifecycle`, gồm `lifecycle_broken`/`broken`/status `invalid|expired|stale` và mốc `invalidated_at`/`expired_at` **bao gồm** tại cutoff ⇒ `valid=False`, `score=0`, reason `D1_REACTION_STALE`; terminal thắng cờ legacy; prefix trước terminal vẫn positive. Bằng chứng: fix-progress §A3.134. **Checkpoint B: WAITING_REVIEW** (bản trình tại §A3.134).

**Owner:** `build_d1_reaction_evidence`, không sửa scoring chặng D.

- Kiểm canonical broken/invalid/expired ở cutoff trước khi cấp active reaction. Giữ history để audit nhưng valid=false/score0 sau terminal.
- Dùng actual ZoneLifecycle và serialized mapping; đừng chỉ test dict tự gán `completed_reacted`.
- Positive trước invalidation còn đạt; legacy reacted/proximity=true không cứu terminal; legacy false không xóa canonical reaction hợp lệ. Không reintroduce proximity bonus.

**Đạt khi:** `r72_05` và hai chiều đối chứng đạt, F02–F04 giữ xanh.

**DỪNG CHECKPOINT B:** trình cụm lifecycle → typed projection → D1, kèm timeline và cumulative tests. Chưa làm F06 nếu B chưa PASS.

> **Đã trình B lần4 — `WAITING_REVIEW` (2026-09-11):** khép nốt nghĩa vụ **history** của B-R2 — `enrich_zones` **bảo toàn history đã khai** (`visits` + counts/retest anchors) khi metadata không đủ tái tính, thay vì để `item.update(lifecycle.to_dict())` ghi đè; visit terminal không bị tái viết thành reaction sau terminal. Node `known_invalid` được **mở rộng thêm vế history** (dùng history do `_probe.lifecycle` thật sinh). Acceptance **29 failed / 92 passed** (121 node), full §5 **29 failed / 946 passed**, probes **5/11**, task57–71 **108**, retained **854**; RED còn lại **29, 0 RED mới**, toàn bộ F06…F10. Bản trình: fix-progress §A3.140.
>
> **Đã trình B lần3 — `WAITING_REVIEW`:** B-R2 tiếp tục được sửa ở lượt **B-R2/r1** — **precedence** bảo toàn terminal: nhánh giữ terminal đã khai nay đứng **trước** `lifecycle_broken`/`lifecycle_expired`, nên khi metadata không đủ tái tính, terminal đã biết **không** bị expiry/broken tính lại ghi đè (hai diagnostic cửa sổ vượt lifetime trong §A3.137 đã đúng). Hai regression B-R2 được **mở rộng** thêm vế vượt lifetime (giữ vế cửa sổ ngắn). Acceptance **29 failed / 92 passed**, full §5 **29 failed / 946 passed**, probes **5/11**, task57–71 **108**, retained **854**; RED còn lại **29, 0 RED mới**, toàn bộ F06…F10. Bản trình: fix-progress §A3.138.
>
> **Đã trình B lần2 — `WAITING_REVIEW`:** sửa xong **B-R1** (`core/smc_confluence.py`: terminal từ **một trong hai nguồn** không bị cờ non-terminal/default của nguồn kia che — OR theo field thay vì “zone thắng”) và **B-R2** (`core/smc_lifecycle.py` bỏ dựng invalidation bằng `0.0` khi metadata thiếu; `core/smc_context.py` bảo toàn terminal đã khai khi không đủ dữ liệu tái tính). Thêm **3 regression test** (collection 118 → 121). Acceptance **29 failed / 92 passed**, full §5 **29 failed / 946 passed**, probes **5 failed / 11 passed**, task57–71 **108 passed**, retained **854 passed**; RED còn lại vẫn **29, 0 RED mới**, toàn bộ thuộc F06…F10. Bản trình: fix-progress §A3.136.
>
> **Đã trình B lần1 — `WAITING_REVIEW`:** F02→F05 IMPLEMENTED; acceptance **29 failed / 89 passed** (baseline A lần4: 69/49; **0 RED mới**), probes **5 failed / 11 passed**, task57–71 **108 passed**, retained **854 passed**, full §5 **29 failed / 943 passed**. Toàn bộ RED còn lại thuộc **F06…F10** (r72_01 ×18, r72_02 ×5, r72_03 ×2, r72_04 ×4). Bản trình: fix-progress §A3.134.

### F06 — Pool không mất nguồn/thời điểm usable (phần R72-01) — **IMPLEMENTED 2026-09-11, còn 1 điểm cần TL quyết định**

**Trạng thái:** IMPLEMENTED (không blocker cho phần đã chốt). Sửa `core/smc_context.py`: `detect_liquidity_pools` phát container canonical `records` cho cả **swing pool một source** lẫn **equal pool** — `pool_id` (identity = `kind` + `source_ids` đã sort + causal record của từng source; **không** dùng level/thứ tự input/rolling index/observation time), `kind` (`swing_low`/`swing_high`/`equal_low`/`equal_high`), `level`, `source_ids` sorted, `sources` giữ `{swing_id, confirmed_at, usable_at, provisional}` và `usable_at = max(source.usable_at)`. Bốn key numeric **giữ nguyên** `list[float]` làm legacy projection (không trộn float/dict); equal giữ mean theo contract. Source thiếu `swing_id`/`usable_at` ⇒ **không** cấp record (fail closed), **không** tự tạo nguồn và **không** coi là legacy. Bằng chứng: fix-progress §A3.142.

**Điểm cần TL quyết định:** **đã chốt tại [fix-progress §A3.143](smc-task-72-fix-progress.md)** — confirmed/usable/non-provisional ⇒ producer phát `usable_at = confirmed_at` (close nến xác nhận bên phải, không phải pivot/open/observation time); provisional/unusable không khai mốc usability có hiệu lực. **F06/r1 IMPLEMENTED 2026-09-11:** `_confirmed_swing_points` phát `usable_at` cho cả seam internal/external (không đổi pivot detection/confirmation delay/ID); pool dựng từ producer thật nay có `records`. Đây là producer khai mốc từ semantics nó sở hữu — **không** phải fallback chung: pool/sweep vẫn fail closed khi payload canonical thiếu `usable_at`. Bằng chứng: §A3.144.

**Owner:** pool detector và model/adapter nhỏ nếu cần; theo interface A. **Chưa** đụng `detect_liquidity_sweeps` (canonical/legacy tường minh, nối `source_pool_id` → `records.pool_id` và cổng `usable_at <= reclaimed_at` thuộc F07).

- Giữ pool identity, kind, source swing IDs và thời điểm từng source usable. Equal pool giữ cả sources, không tìm ngược source bằng việc average price phải bằng level một swing.
- Source confirmed/usable tại snapshot không đồng nghĩa đã usable tại candle quá khứ. Temporal provenance thiếu trên canonical path phải fail closed.
- Pool identity không phụ thuộc input list order/rolling index; không đổi danh tính chỉ để né consumed. Nếu giữ numeric levels cho legacy, đó chỉ là projection, không là nguồn authority canonical.
- Test single source, two-source equal pool confirmed khác thời điểm, missing/provisional source, permutation và append-future không backdate pool.

**Đạt khi:** cấu trúc pool đủ cho F07 kiểm causal, helper pool tests xanh; không đòi probe sweep R72-01 xanh chỉ bằng mock source.

### F07 — Sweep causal qua pool thật (phần còn lại R72-01) — **IMPLEMENTED 2026-09-11**

**Trạng thái:** IMPLEMENTED. Sửa `core/smc_context.py::detect_liquidity_sweeps`: candidate canonical dựng **chỉ** từ `liquidity_pools["records"]` (không fallback numeric level/swing_id); `source_pool_id` = `records.pool_id`, evidence mang thêm `source_ids`/`source_pool_usable_at`; cổng **A-D01** `usable_at <= reclaimed_at` tại từng nến sweep với max-usable của mọi source (equality được phép, nguồn tương lai/provisional/timestamp không parse được bị loại). **Boundary canonical/legacy tường minh:** payload có `records` ⇒ canonical (chỉ records); payload **không** có `records` ⇒ route numeric legacy, chỉ giữ **equal** level do caller cấp (contract task67), numeric **swing** level không cấp sweep vì không thể truy lineage từ level. Excursion/reclaim P8, BUY/SELL, numeric projection và pool-selection giữ nguyên; không thêm priority/dedupe/cardinality. Bằng chứng: §A3.145.

**BLOCKER (đã khép tại F07/r1 — §A3.147):** route production `_smc_for_timeframe` dùng producer legacy `swing_points` (không có `confirmed`/`usable`/`usable_at`) ⇒ pool payload có `records` rỗng ⇒ sweep pool-driven fail closed. **F07/r1** cho caller này chọn **nhánh legacy tường minh** (`pool_provenance="legacy"`) và khôi phục đúng `liquidity_sweeps`/`zone_link_sweeps` của route legacy, có regression qua `_smc_for_timeframe` thật (fixture 12 nến + control equal-pool). Không migrate `swing_points`, không tạo ID/`usable_at` giả, canonical vẫn fail closed.

**Owner:** sweep detector, caller cung cấp pool/cutoff.

- Kiểm source usable time tại từng sweep candle theo boundary đã duyệt A, không chỉ so pivot index. Reclaim/excursion vẫn theo P8, không thay ngưỡng.
- Tách canonical vs legacy explicit; thiếu provenance không trở thành nguồn hợp lệ qua numeric pool fallback.
- Test `r72_01` và genuine positive sau usable, equal pool đủ source, sweep trước source confirmation, closed cutoff, BUY/SELL, prefix/batch. Dùng actual swing/pool producer trong ít nhất một positive end-to-end.

**Đạt khi:** `r72_01` hai chiều pass nhưng không mất positive hợp lệ; F06 source records giữ qua sweep evidence.

### F08 — Đưa mọi eligible claim vào ownership (R72-02)

**Trạng thái:** **IMPLEMENTED 2026-09-11.** Sửa `core/smc_sweep_linking.py` (`associate_sweeps_to_zones`, `assign_sweep_ownership`) + `core/smc_context.py::_attach_zone_sweep_links`: (1) rank owner theo **causal claim time** `max(reclaimed_at, setup_available_at)` sớm nhất, tie-break **setup ID ổn định**, bỏ distance/departure/time_delta khỏi thứ tự chọn owner; (2) claim **canonical** (khai pool lineage) **buộc đủ cả hai mốc** — thiếu mốc ⇒ không assignment + reason `SWEEP_CLAIM_TIME_MISSING`, không lấy mốc còn lại/alias thay thế; (3) availability của **setup** được chốt một lần cho cả owner (`setup_availability_by_owner`), không lấy theo child tình cờ đứng đầu; (4) boundary tường minh: claim gắn setup đi theo claim time, zone **không có setup metadata** giữ nguyên thứ hạng legacy một-một (task67/task72 contract). R72-02: **9 passed / 0 failed**, probe R72-02 xanh. Bằng chứng: §A3.148.

**Owner:** linker + `_attach_zone_sweep_links`; không sửa policy claim time.

- Tách ba bước: enumerate/filter eligible links → chọn owner bằng claim time → project link tới children owner. Không khóa độc quyền setup bằng distance rank ở bước enumerate.
- Owner theo `max(reclaimed_at, setup_available_at)` sớm nhất; chỉ tie-break stable setup ID khi cùng thời điểm. Missing time trên canonical claim không tự fallback sang time còn lại.
- Ghi rõ field source/caller nào cung cấp setup availability. Không dùng availability của child tình cờ đứng đầu để thay meaning setup đã thống nhất.
- Test `r72_02`, early-far/late-near đều trong gate, same-time tie, permutation, unrelated distance/time/side bị loại, các child cùng owner chia sẻ link qua context.

**Đạt khi:** context—not only assignment helper—trả early13:00 dù late15:00 gần hơn; ownership không đổi khi đảo input.

### F09 — Contribution chỉ trong owner (R72-03)

**Trạng thái:** **IMPLEMENTED 2026-09-11.** Sửa `core/smc_sweep_linking.py::assign_sweep_ownership`: `contribution_winner[sweep_id]` nay chỉ chọn trong **claims thuộc owner hiện tại** (`claim.setup_id == assignment.owner_setup_id`) với **đúng tie-break đã duyệt** `(zone_id, visit_id, input order)` — không thêm priority mới; non-owner có `zone_id` nhỏ hơn **không** chiếm được slot. Owner-present ⇒ tổng `contribution_applied` trên claims = **1**; non-owner = 0; owner history-only (không child hiện tại) = 0. Không hạ cờ assignment để che tổng 0, không thêm bất biến assignment-flag↔tổng-claim. R72-03: **3 passed / 1 failed** (node còn lại là F10, không phải F09), probe R72-03 xanh. Bằng chứng: §A3.149.

**Owner:** contribution selection trong assignment helper.

- Chỉ chọn representative trong các child thuộc owner. Non-owner có zone ID nhỏ hơn không được chiếm slot.
- Owner hiện diện với nhiều/duplicate child: tổng contribution1. Owner chỉ còn historical, không có child hiện tại: không trao contribution cho setup muộn.
- Test `r72_03`, same-owner control, non-owner ID trước/sau, permutation/duplicate, nhiều sweep độc lập.

**Đạt khi:** thêm child/setup không sở hữu sweep không làm contribution owner mất hoặc tăng; F08 giữ xanh.

### F10 — History/consumption phải có hiệu lực tại caller (R72-04)

**Trạng thái:** **IMPLEMENTED 2026-09-11.** Sửa `core/smc_sweep_linking.py` + `core/smc_context.py::_attach_zone_sweep_links`: (1) `SweepAssignment` mang `pool_id`/`source_ids` (lineage của lần cấp owner); (2) `assign_sweep_ownership` tra consumption theo **sweep ID trước, rồi theo causal pool lineage** (không theo index/thời điểm/level) ⇒ observation mới của cùng pool không né được consumption, pool mới vẫn xử lý độc lập; (3) history record **không thể tôn trọng** ⇒ fail closed + reason `SWEEP_OWNER_HISTORY_CONFLICT`, không reset history rồi cấp owner mới; (4) owner chỉ còn trong history vẫn giữ assignment đã biết, current contribution **0**; (5) caller dựng `assignment_history` từ chính payload sweep đã consumed và truyền `history_complete=True` tường minh; (6) sweep đã consumed chỉ được link lại bởi **chính setup sở hữu**, và giữ `linked_zone_id` đã biết khi cửa sổ hiện tại không còn child. R72-03 + R72-04 **xanh toàn bộ**; **acceptance 125/125 và 16/16 reviewer probes GREEN**. Bằng chứng + bản trình C: §A3.150.

**Owner:** context → assignment history → projection; không chỉ helper test.

- Dùng record owner/assignment đã có làm authority, kể cả owner không còn trong current zone list. Truyền history/completeness thực tế qua caller; không mặc định complete khi biết window thiếu owner.
- Sweep đã consumed không thành claim mới cho setup khác. Pool source identity nối observation mới với consumption có sẵn; không chỉ thêm boolean trên một sweep rồi detector phát evidence mới từ cùng nguồn.
- Missing/incomplete/conflicting history theo matrix A phải reason/fail closed; không sáng tạo owner mới để tránh null.
- Test `r72_04`: context lần1 → serialize record/history → restore với late-only zones; owner/assignment cũ giữ nguyên. Thêm same-snapshot repeat, owner thiếu khỏi window, history missing, cùng pool khác observation và legitimate new pool identity.

**Đạt khi:** owner/contribution không tái phân phối theo snapshot window; F08/F09 xanh qua integrated path.

**DỪNG CHECKPOINT C:** trình actual pool → sweep → context owner/consumed, không chỉ mocks. Toàn bộ16 reviewer probes phải pass; thêm tests theo matrix A cũng phải pass. Chưa F11 nếu C chưa PASS.

> **Đã trình lại CHECKPOINT C — `WAITING_REVIEW` (2026-09-12, lần 3): sửa nốt phần còn lại của C-R1, bản trình tại [fix-progress §A3.154](smc-task-72-fix-progress.md).** Caller canonical **chỉ** truyền `reclaimed_at` thật (không dùng `time`/`sweep_time` hay mốc còn lại thay thế) ⇒ sweep canonical thiếu reclaim close **không** được cấp owner/consumption/contribution nữa; alias chỉ sống ở nhánh legacy tường minh. Thêm node `test_r72_04_caller_canonical_claim_without_reclaim_time_gets_no_owner` (collection **127 → 128**) với control đủ mốc và control legacy. **acceptance 128/128, probes 16/16**, task57–71 108, retained 854, full §5 **982 passed / 0 failed**; `git diff --check` sạch; probe + 5 R56 vẫn khớp ledger F00. **G1/G2 đã được TL chốt tại §A3.153** (ghi đúng semantics ở matrix: rank = completeness/claim time trước, class sau; setup-scoped chỉ ưu tiên khi đồng hạng). Chưa F11/task73; **không tự PASS C/CLOSED/APPROVED gate72**.

> **Đã trình lại CHECKPOINT C (lần 2) — `WAITING_REVIEW` (2026-09-12): C-R1/C-R2 đã sửa, bản trình tại [fix-progress §A3.152](smc-task-72-fix-progress.md); TL còn C-R1 (reclaim-time fallback) tại §A3.153.** C-R1: caller truyền canonical `pool_id`/`source_ids` + `reclaimed_at`; C-R2: kiểm consistency mọi history cùng lineage ⇒ fail closed + `SWEEP_OWNER_HISTORY_CONFLICT`. Thêm 2 node regression (125 → 127); acceptance 127/127, probes 16/16, full §5 981/0.

> **Đã trình CHECKPOINT C (lần 1) — `WAITING_REVIEW` (2026-09-11), bản trình tại [fix-progress §A3.150](smc-task-72-fix-progress.md); TL trả CHANGES_REQUESTED (C-R1/C-R2) tại §A3.151.** F06→F10 IMPLEMENTED; acceptance 125/125, probes 16/16, task57–71 108, retained 854, full §5 979 passed / 0 failed.

### F11 — Nghiệm thu end-to-end và bản bàn giao duy nhất — **IMPLEMENTED 2026-09-12, CHECKPOINT D `WAITING_REVIEW`**

**Trạng thái:** IMPLEMENTED. Bản trình cuối duy nhất: [smc-task-72-response.md](smc-task-72-response.md) — mapping R72-01…09 → nguyên nhân gốc → sửa → node/evidence; hai chuỗi end-to-end (a) actual swing → pool → sweep → linked children → assignment → repeat/JSON restore (**node mới `test_r72_chain_actual_producer_pool_sweep_reaches_owner_and_survives_restore`**, collection 128 → **129**) và (b) confirmed zone → visit → exit/reaction → invalidation/expiry → D1 (đủ bằng chứng A3-056…A3-062, không thêm node); đối chiếu inventory node (không node cũ bị xoá/đổi tên; retained 854 không đổi; `-k "not chain_actual_producer"` ⇒ đúng 128 node cũ); commands/counts chạy mới trên cùng snapshot; manifest/hash + fingerprint bảo vệ; giới hạn chưa kiểm (gồm **legacy production route chưa nâng cấp**). Kết quả: acceptance **129P/0F**, probes **16P/0F**, task57–71 108P, retained 854P, full §5 **983P/0F**; `git diff --check` sạch; 6/6 artifact bảo vệ khớp ledger F00. Bằng chứng: §A3.156.

- Chạy actual producers theo hai chuỗi dữ liệu hợp lệ: (a) swing → pool → sweep → linked setup children → assignment → repeat/restore; (b) confirmed detector zone → visit → exit/reaction → invalidation/expiry → D1 evidence. Không cần ép hai chuỗi thành cùng một symbol/timeframe nếu làm mất nghĩa testcase.
- Matrix BUY/SELL và cutoff có expected source IDs, availability/reclaim/assignment/visit/terminal times, contribution count và status. Ghép tests thực tế, không dựng mọi intermediate output bằng dict để tự chứng minh.
- Giữ full/partial-fill original bounds/ID, age/lifetime và R56 acceptance xanh. Kiểm bad data bị reject/unknown đúng boundary; fixtures valid được validator xác nhận.
- Chạy toàn bộ lệnh §5, đối chiếu test node IDs baseline. Số lượng test tăng phải có danh sách; không coi tổng count lớn hơn854 là bằng chứng không xóa tests cũ.
- Viết `docs/plans/smc-task-72-response.md`: mapping9 findings, checkpoint A/B/C decisions, command/count, manifest code/tests và phạm vi chưa test. Ghi WAITING_REVIEW, chưa CLOSED/APPROVED.

**DỪNG CHECKPOINT D:** TL re-review code, chạy lại và đồng bộ progress/response. Chỉ khi gate72 APPROVED mới bỏ chặn task73.

> **Đã trình CHECKPOINT D — `WAITING_REVIEW` (2026-09-12), bản trình cuối tại [smc-task-72-response.md](smc-task-72-response.md) + evidence [fix-progress §A3.156](smc-task-72-fix-progress.md).** F00…F11 IMPLEMENTED; A/B/C **PASS**. Acceptance **129P/0F**, 16 probes **16P/0F**, task57–71 108P, retained 854P, full §5 **983P/0F** trên cùng snapshot; Δcollection +1 (chuỗi (a)), không mất/đổi tên node cũ; 6/6 fingerprint bảo vệ khớp; `git diff --check` sạch. Không blocker kỹ thuật; giới hạn ở response mục 7 (legacy production route chưa nâng cấp + 7 điểm khác). **Chưa CLOSED 9 finding / APPROVED gate72 / task73** — TL quyết định cuối.

## 4. Acceptance obligations phải có trước checkpoint A

Không chỉ khóa14 fail hiện có. Đây là nội dung đã yêu cầu ở review lần1, tập hợp lại để Coder không bỏ sót:

| Cụm | Positive/control | Negative/boundary | Integration/parity |
|---|---|---|---|
| Metadata | Tick argument = tick item; đủ ATR/tick | Missing/invalid, explicit override, đúng/vượt buffer | Context → lifecycle; nguồn causal, không latest fallback |
| Terminal/reaction | Exit+reaction hợp lệ, trước terminal | Invalidation/expiry trước/đúng/sau reaction | Prefix, repeat, typed round-trip, D1 consumer |
| Pool/sweep | Confirmed usable source; valid excursion/reclaim | Before confirmation, provisional/missing source, equal-source time, threshold equality | Actual producer; pool ID/provenance qua prefix/batch |
| Owner | Earliest claim, same-time tie | Nearer-late, unrelated side/distance/time, missing claim time | Context enumerate→assign→project, input permutation |
| Contribution | Nhiều child cùng owner tổng1 | Non-owner ID nhỏ hơn, duplicate, owner absent | Thêm metadata/child không tăng/mất evidence |
| Consumed | Restore đúng assignment | Late-only window, missing/inconsistent history, repeated same-pool observation | First run→serialize→restore→repeat, không đổi owner/ID |
| Fixtures/fill | Valid BUY/SELL, partial/full fill bounds | Invalid data có chủ đích tách khỏi positive; no-break/full-fill | Detector→lifecycle; original IDs/bounds và R56 invariant |

## 5. Lệnh kiểm chứng

PowerShell từ `D:/Projects/AIMarketAnalyst`:

```powershell
# Chạy toàn bộ reviewer probes để giữ rõ phần còn RED.
python -m pytest docs/plans/probes/test_smc_gate72_review.py -q --tb=short

# Ví dụ chọn một finding khi làm subtask; không thay full cumulative check.
python -m pytest docs/plans/probes/test_smc_gate72_review.py -q -k r72_07

$gate72Tests = @(rg --files tests -g '*task5[7-9].py' -g '*task6[0-9].py' -g '*task7[01].py')
python -m pytest @gate72Tests -q

# Sau F01 dùng tên file acceptance đã chốt tại checkpoint A.
python -m pytest tests/test_smc_gate72_fix_acceptance.py -q

$smcTests = @(rg --files tests -g 'test_smc*.py')
python -m pytest @smcTests tests/test_technical_signal_scorer.py tests/test_scanner_features.py tests/test_scanner_live_producers.py tests/test_analysis_pipeline_integration.py tests/test_scanner_scenario_producers.py tests/test_scanner_replay.py -q

git diff --check
```

F00 chưa có acceptance file mới thì không chạy lệnh của file đó và không gọi là skipped test. Sau mỗi bước ghi cụ thể node IDs pass/fail và reasons; các bước sau luôn chạy lại nhóm đã sửa trước. Trước B/C/D chạy full baseline, không chỉ targeted. Tại A ghi riêng các fail lộ ra vì đính chính fixture để TL phê duyệt oracle trước core fix.

## 6. Ledger và mẫu báo cáo

Coder tạo `smc-task-72-fix-progress.md` với bảng:

```text
Subtask | TODO/DOING/IMPLEMENTED/WAITING_CHECKPOINT | files changed
        | RED node IDs before fix | GREEN after fix | previous tests retained
        | contract reference | remaining known RED | blocker/decision needed
```

Mỗi checkpoint có riêng: `status=WAITING_REVIEW`, ngày, hash/code scope, tests, link matrix, quyết định TL (chỉ TL điền). `IMPLEMENTED` là Coder báo hoàn thành bước; không được dùng thay CLOSED finding. Một finding nhiều bước như01/09 chỉ đề nghị đóng khi toàn bộ obligations đạt.

Mẫu gửi TL sau mỗi checkpoint:

```text
Checkpoint [A/B/C/D] — WAITING_REVIEW
Đã làm Fxx…Fyy; chưa làm bước kế tiếp.
Files + root causes + contract/test mapping: ...
Expected đã giữ nguyên / đề nghị quyết định cụ thể: ...
Targeted: ...; cumulative: ...; baseline: ...; known RED còn lại: ...
Manifest + diff + timeline: ...
Không task73, không production rollout; chờ TL duyệt checkpoint.
```

## 7. Lệnh giao CODER2 đợt đầu

> Đọc `docs/plans/smc-task-72-fix-plan.md` và `docs/plans/smc-task-72-review.md`. Chỉ thực hiện **F00 và F01**: tái lập baseline, tạo fix ledger, sửa OHLC fixtures sai và trình acceptance matrix/tests cho đủ9 findings. **Không sửa core implementation.** Giữ nguyên reviewer probes và R56 golden. Ghi rõ old→new fixture, expected theo contract, các interface/boundary cần TL chốt và actual test results. Trình **checkpoint A — WAITING_REVIEW**, báo TECHLEAD/ADMIN rồi dừng. Không tự F02, task73 hoặc APPROVED gate72.

ADMIN chỉ giao cụm tiếp sau khi TL có quyết định checkpoint tương ứng. Không tạo task song song ghi cùng core files. Kế hoạch này thay cách giao “fix cả9 finding rồi báo xong”, không thay review contract hoặc phạm vi quyền của các gate.

## 8. Checklist rất nhỏ cho checkpoint A lần 3

### 8.1. Phạm vi, cách giao và cách xác nhận xong

Nguồn yêu cầu là [review A lần2, F01.1–F01.4](smc-task-72-checkpoint-a-review-round2.md), các quyết định [A-D01…07](smc-task-72-checkpoint-a-review.md) và ma trận §4 của kế hoạch này. Checklist chỉ chia nhỏ các yêu cầu đó, không bổ sung công thức, ngưỡng hoặc tính năng. A3-001…090 là mã việc con của F01, không thay số 1–144 hoặc F00–F11.

Trạng thái hiện tại: **A lần3 CHANGES_REQUESTED** theo [review A lần3 §2–4](smc-task-72-checkpoint-a-review-round3.md). A3-039/040/041/043/061/066/067/071 mở lại đúng phần được nêu; A3-090 cần trình lại A lần4. **Cập nhật 2026-09-11 (lượt A3-071/a, /b, /c; A3-067/a, /b, /c; A3-061/a, /b):** A3-071 đã sửa xong A3R3-01 — fixture context dùng ATR thật, assert eligibility từng zone đứng riêng qua chính caller ở cả tầng payload lẫn event, và kiểm hai thứ tự zone (outside available sớm hơn vẫn không lấy owner) với pair dựng mới mỗi lượt; **A3-067** đã sửa xong A3R3-02 — oracle thiếu metadata khóa `None`/`unknown`/reason thay cho `0.0`, kèm cross-control chứng minh mỗi override chỉ đổi đúng rule của nó và đồng bộ mọi mô tả “fail-closed” còn lại; **A3-061** đã sửa xong A3R3-03 — positive control thay bằng **prefix thật** enrich riêng (không rewind `as_of` trên payload terminal), kèm kiểm identity/history khớp bản append, trạng thái chưa terminal, và khóa bản append kết thúc đúng tại invalidation close (bằng chứng [§A3.95](smc-task-72-fix-progress.md)…[§A3.102](smc-task-72-fix-progress.md)) ⇒ cả ba **IMPLEMENTED**; còn lại A3-039/040/041/043/066 vẫn mở. Các phần khác giữ IMPLEMENTED, không phải làm lại. A3-081…089 refresh kết quả sau sửa, chưa F02. Tìm theo tên hàm và hành vi, không áp dụng mù số dòng cũ; phần đã đúng giữ nguyên.

Mỗi lần giao nên chọn **một mã A3 hoặc một đoạn liên tiếp ngắn**, bắt đầu từ mã chưa hoàn tất đầu tiên. Làm theo thứ tự; khi một việc cần đầu ra trước đó còn thiếu thì ghi BLOCKED với mã phụ thuộc, không tự tạo dữ liệu thay thế. Không cần review Tech Lead riêng cho từng mã; sau A3-090 mới trình A lần3. Giới hạn lượt giao là cách chia việc, không phải cổng approval mới.

Quy tắc cho mọi mã:

- Mỗi mã xử lý một hành vi hoặc một sản phẩm kiểm tra được. Các biến thể BUY/SELL hoặc trước/đúng/sau ngưỡng có thể dùng parameterization khi cùng một quy tắc; không cần một file hay một test mới cho mỗi mã.
- Chỉ sửa acceptance tests, fixture/factory cần thiết thuộc task57–71 và hồ sơ fix. Không sửa `core/`, reviewer probes, R56 contract/golden, ngưỡng đã duyệt hoặc review lịch sử. Có thể đọc chúng để xác định input/output đúng.
- Test quan trọng có precondition được assert rõ, positive control và negative/boundary tương ứng. Expected lấy từ quy tắc/fixture tính độc lập, không gọi hàm production để tự sinh expected.
- Thiếu interface tương lai thì ghi tên/data contract đã duyệt và assertion mô tả thiếu interface; giữ collection chạy được. Không stub/mock kết quả nghiệp vụ để gọi test là GREEN; không sửa core trong checkpoint A.
- Sau mỗi sửa test, chạy node liên quan và các node đã đạt bị ảnh hưởng. Thay factory/helper dùng chung thì chạy mọi consumer của helper. Giữ cumulative checks theo §5; không chạy full regression cho một thay đổi câu chữ đơn thuần.
- Test RED do core chưa sửa có thể được ghi là đã hoàn tất phần chuẩn bị test nếu fixture, preconditions và oracle đúng. RED do dữ liệu test sai, assertion sai nhánh, import/collection lỗi thì việc vẫn chưa xong. Không skip/xfail để che RED.
- Ghi kết quả vào [fix progress](smc-task-72-fix-progress.md): mã A3, file/hàm, command/node, actual, loại RED và việc tiếp theo. Không tạo một báo cáo riêng cho mỗi mã. Không đóng R72 finding hoặc tự ghi PASS/APPROVED khi mới hoàn thành checklist.

Tên ngắn trong các bảng:

| Tên | File / vị trí |
|---|---|
| `T` | `tests/test_smc_gate72_fix_acceptance.py` |
| `M` | [smc-task-72-acceptance-matrix.md](smc-task-72-acceptance-matrix.md) |
| `L` | [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) |
| `P` | [probes/test_smc_gate72_review.py](probes/test_smc_gate72_review.py), chỉ đọc/chạy |

Trong T, tiền tố `test_r72_` giúp tìm đúng finding. Các tên rút gọn dưới đây là chuỗi tìm trong tên hàm hiện có; ghi full node ID thực tế vào M sau khi collect. Tên test bổ sung không phải API production mới.

### 8.2. Chuẩn bị — A3-001…004

| Mã | Trạng thái | Vị trí | Một việc cần làm | Đầu ra / kiểm tra xong |
|---|---|---|---|---|
| A3-001 | IMPLEMENTED | L / Git | Ghi mốc nhận việc hiện tại và các thay đổi có trước trong phạm vi F01. | Có trạng thái Git, revision và fingerprint core/probe/R56 theo danh sách F00 để so cuối lượt; giữ baseline lịch sử, không chạy lại toàn F00. |
| A3-002 | IMPLEMENTED | T / L | Lập danh sách node acceptance đang collect được trước khi sửa. | Ghi command collection, danh sách node và lỗi collection nếu có; không dùng con số 49 cũ làm kết quả mới. |
| A3-003 | IMPLEMENTED | T / M | Đối chiếu từng điểm F01.1…4 với code hiện tại. | Mỗi điểm ghi đúng rồi/còn thiếu/chưa rõ và tên hàm; nhận diện fixture đã sửa để không ghi đè. |
| A3-004 | IMPLEMENTED | M | Chuẩn hóa cột theo dõi một ca nghiệm thu. | Mỗi ca có rule reference, input/timeframe/cutoff, precondition, expected, full node ID, actual, loại RED; để trống actual khi chưa chạy. |

### 8.3. F01.1 — Representation và interface, A3-005…022

| Mã | Trạng thái | Vị trí | Một việc cần làm | Đầu ra / kiểm tra xong |
|---|---|---|---|---|
| A3-005 | IMPLEMENTED | M / interface pool | Ghi lại schema pool theo quyết định review lần2. | Numeric keys giữ numeric; canonical `records` có `pool_id/kind/level/source_ids/sources/usable_at`; mỗi source giữ thời điểm, ID. Không để cùng key vừa float vừa dict. |
| A3-006 | IMPLEMENTED | M / interface history | Sửa đề xuất `history_complete` cho khớp quyết định mới. | Giữ compatibility default đang có; canonical caller truyền True/False tường minh. Không đề xuất đổi default ngầm. |
| A3-007 | IMPLEMENTED | M / interface metadata | Ghi chính xác biểu diễn thiếu dữ liệu. | `metadata_state=unknown`, reason không rỗng, `usable=False`, threshold không tính được là None; lifecycle enum không thêm unknown. |
| A3-008 | IMPLEMENTED | M / claim→assignment | Ghi đường truyền nguồn pool và thời gian claim sang history. | Có caller→owner→output; cả reclaimed/setup-available time và pool/source lineage được giữ. Không tự quyết định thêm một thuật toán ownership. |
| A3-009 | IMPLEMENTED | T / `positive_pool_keeps_source_lineage` | Chuyển assertion provenance sang canonical records. | Kiểm đúng record theo kind/source, giữ kiểm numeric projection; max source time được tính từ fixture. |
| A3-010 | IMPLEMENTED | T / pool→sweep | Viết/đối chiếu ca canonical thiếu records. | Numeric levels còn tồn tại cũng không đủ để cấp sweep canonical; không reject do fixture không có excursion. |
| A3-011 | IMPLEMENTED | T / claim factories/callers | Làm rõ completeness của các positive ownership/history fixtures. | Mọi fixture đủ history truyền `history_complete=True`; không dựa default để chứng minh canonical path. |
| A3-012 | IMPLEMENTED | T / `incomplete_history` | Làm rõ completeness của ca history thiếu/unknown. | Truyền False tường minh; no assignment và reason theo contract; không dùng giá trị thiếu của field khác để làm test fail. |
| A3-013 | IMPLEMENTED | T / `missing_canonical_claim_time` | Khóa trường hợp thiếu riêng `reclaimed_at`. | Setup time và lineage còn hợp lệ; không assignment, có reason thiếu claim time. |
| A3-014 | IMPLEMENTED | T / `missing_canonical_claim_time` | Khóa trường hợp thiếu riêng `setup_available_at`. | Reclaimed time còn hợp lệ; không fallback sang timestamp còn lại. Có thể parameterize cùng test A3-013. |
| A3-015 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / metadata | Khóa ca thiếu riêng ATR trên vùng chưa terminal. | Tick hợp lệ; metadata unknown, reason, unusable, threshold phụ thuộc ATR=None; status confirmed/broken=False được giữ. |
| A3-016 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / metadata | Khóa ca thiếu riêng tick trên vùng chưa terminal. | ATR hợp lệ; cùng semantics unknown, không tự lấy digits hoặc điền threshold0. |
| A3-017 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / metadata | Tách ca metadata nonfinite khỏi missing. | Parameterize ATR/tick không finite, trường còn lại hợp lệ; assert state/reason/None thay vì chỉ `buffer != 0`. |
| A3-018 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / `conflicting_same_scope_tick` | Sửa expected của ca hai nguồn tick mâu thuẫn. | Unknown là data-quality, không phải lifecycle status; không tự chọn nguồn thuận lợi. |
| A3-019 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / `item_and_argument_tick_sources_have_parity` | Kiểm đủ ba cách cấp tick hợp lệ. | Item-only, argument-only, equal-both cho cùng buffer/state với BUY/SELL; không chỉ so hai kết quả cùng sai. |
| A3-020 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / metadata với terminal | Khóa trường hợp thiếu metadata nhưng đã có terminal evidence. | Giữ invalid/expired và lịch sử tương ứng; không đổi thành confirmed hoặc cứu usable. |
| A3-021 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / metadata serialization | Kiểm metadata đi qua lifecycle→context→typed round-trip. | Unknown/reason/None/usability được giữ đúng nguồn; nếu field chưa có, RED nêu đúng phần thiếu. |
| A3-022 | IMPLEMENTED | M / interface và các node 005–021 | Rà hết mô tả schema cũ còn mâu thuẫn. | Không còn numeric-as-dict, default False mới hoặc unknown lifecycle; docs/tests cùng interface. Record actual targeted/collection, chưa đổi core. |

### 8.4. F01.2 — Fixture và timeline, A3-023…038

| Mã | Trạng thái | Vị trí | Một việc cần làm | Đầu ra / kiểm tra xong |
|---|---|---|---|---|
| A3-023 | IMPLEMENTED | T / các candle factories | Liệt kê mọi positive factory và local `run(rows)`. | Có caller, timeframe, bước timestamp và nơi validate; phân biệt negative invalid-data có chủ đích. |
| A3-024 | IMPLEMENTED | T / `_terminal_candles` | Xác minh/sửa BUY exit candle. | Dùng `(110.2,110.22,110.15,110.2)` theo review; OHLC hợp lệ, outside tolerance .1, chưa đạt reaction .25 với zone[100,110]/ATR1/tick.1. |
| A3-025 | IMPLEMENTED | T / `_terminal_candles` | Xác minh/sửa BUY reaction candle. | `(112,114,111,113)` hợp lệ và đủ reaction; kiểm độc lập điều kiện, không chỉ validator pass. |
| A3-026 | IMPLEMENTED | T / `_terminal_candles` | Kiểm SELL mirror của exit/reaction. | Mirror quanh210, high/low đảo đúng; cùng ý nghĩa exit và reaction đối xứng BUY. |
| A3-027 | IMPLEMENTED | T / positive factories | Bổ sung validator ở những đường tạo nến còn thiếu. | Mọi positive factory gọi `validate_smc_candles` đúng timeframe trước evaluator; không nới validator hoặc validate negative cố ý như positive. |
| A3-028 | IMPLEMENTED | T / `equal_and_outside_buffer` | Đồng bộ timestamp cadence với timeframe của fixture. | H1 dùng bước giờ; D1 dùng bước ngày. Không để test H1 tạo chuỗi ngày rồi vô tình kiểm nhánh gap khác. |
| A3-029 | IMPLEMENTED | M / D1 terminal fixture | Ghi timeline D1 độc lập từ availability đến expiry. | Lifetime20; age20 trước expiry, age21 terminal, age22 sau terminal; ghi index và candle close theo origin thực tế, không mặc định index=age khi origin khác. |
| A3-030 | IMPLEMENTED | T / `terminal_order_before_reaction` | Khóa D1 reaction trước terminal. | Reaction ở age20 có đúng reacted_at; sau append candle terminal vẫn giữ lịch sử, không đổi thời điểm. |
| A3-031 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / cùng nhóm | Khóa D1 reaction tại candle terminal. | Age21 không tạo reaction mới; terminal timestamp/index đúng, không pass vì chưa từng vào vùng. |
| A3-032 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / cùng nhóm | Khóa D1 reaction sau candle terminal. | Age22 không tạo reaction; visit/history trước terminal còn nguyên. |
| A3-033 | IMPLEMENTED | M / H4 terminal fixture | Ghi timeline H4 đối ứng. | Lifetime30, trước/đúng/sau terminal theo age30/31/32; đúng bước4h, close cutoff và follow-through window. |
| A3-034 | IMPLEMENTED | T / H4 terminal | Khóa H4 reaction trước terminal. | Có visit exit và reaction hợp lệ ở age30; expected time tính từ fixture, BUY/SELL. |
| A3-035 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / H4 terminal | Khóa H4 reaction đúng terminal. | Không reaction mới ở age31; terminal evidence rõ, BUY/SELL. |
| A3-036 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / H4 terminal | Khóa H4 reaction sau terminal. | Không reaction mới ở age32; không mất lịch sử hợp lệ trước đó. |
| A3-037 | IMPLEMENTED | T / `invalidation_precedes_expiry_and_reaction` | Đặt invalidation vào đúng candle hết lifetime. | D1 age21, không phải row20 cũ; invalidation ưu tiên expiry, assert timestamp/index và không reaction. Không dựng close vừa phá distal vừa phản ứng hướng đối lập bất khả thi. |
| A3-038 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED` cho nhánh `invalid`) | T / terminal repeat | Khóa trạng thái terminal qua enrich→serialize→restore→enrich lại. | Cùng zone ID giữ invalid/expired, unusable và reaction history trước terminal; không tạo reaction mới, D1 vẫn invalid/0. Parameterize terminal kind; fixture consumers bị ảnh hưởng phải chạy lại theo §8.1. |

### 8.5. F01.3 — Các test chưa chứng minh đúng nhánh, A3-039…062

| Mã | Trạng thái | Vị trí | Một việc cần làm | Đầu ra / kiểm tra xong |
|---|---|---|---|---|
| A3-039 | **IMPLEMENTED** (A3R3-05; kèm `EXPECTED_IMPLEMENTATION_RED` **F06** — `records` chưa có, temporal seam PASS) | T / `equal_usable_at_reclaimed` | Dựng fixture excursion/reclaim thực cho temporal tests. | Fix candle close đang xét; giá xuyên đủ và reclaim hợp lệ; test gọi sweep detector, không chỉ pool detector. → **A3-039 (2026-09-11)** — bỏ khóa toàn bộ `swept_lows` và phần tử `[0]`; nay **lọc target event theo level equal pool + candle `index 1`**, đòi **tồn tại** target event và có `reclaimed_at == pool_usable_at == stamp(2)`. Không dùng vị trí list, không assert tổng số `swept_lows`, không pool priority/dedupe. Block canonical `records` chuyển xuống **sau** temporal seam để A3-039/041 thực sự chạy; node cuối cùng RED **F06** (thiếu `records`), không phải lỗi temporal. Bằng chứng: §A3.112. |
| A3-040 | **IMPLEMENTED** (A3R3-05; **GREEN** — oracle không còn khóa priority/cardinality) | T / temporal sweep | Khóa source usable trước sweep close. | Với các điều kiện khác hợp lệ, nhận đúng sweep; assert source/event time để positive không chỉ là list nonempty. → **A3-040 (2026-09-11)** — bỏ `assert [s["index"] for s in sweeps["swept_lows"]] == [2]` và `sweeps["swept_lows"][0]`; nay lọc **target event theo level `100.025` + candle `index 2`**, đòi tồn tại event khớp `kind "swept_low"`, `side "buy"`, `time stamp(2)` (open), `reclaimed_at stamp(3)` (close), `excursion_buffer ≈ 0.2`. Thêm precondition equal pool tồn tại ở `expected_level` và `expected_level` khác cả hai level single-source. **Không** dùng vị trí list, **không** assert tổng số `swept_lows`/pool priority/dedupe; giữ `swept_highs == []`; **không** thêm `records`/`source_pool_id` fallback. **GREEN.** Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.113. |
| A3-041 | **IMPLEMENTED** (A3R3-05; kèm `EXPECTED_IMPLEMENTATION_RED` **F06** — `records` chưa có, temporal seam PASS) | T / temporal sweep | Khóa source usable đúng sweep close. | Equality được chấp nhận theo A-D01; ghi đây là temporal seam synthetic, không giả là chứng minh pivot producer. → **A3-041 (2026-09-11)** — equality `usable_at == reclaimed_at` được chấp nhận, nay khóa bằng **target event** (lọc theo level + candle `index 1`) chứ không qua toàn list/`[0]`; `pool_usable_at` tính **từ fixture** và có precondition `pool_usable_at == usable_at == stamp(2)`. Vẫn ghi rõ đây là **temporal seam synthetic**. Bằng chứng: §A3.112. |
| A3-042 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / temporal sweep | Khóa source usable sau sweep close. | Cùng excursion/reclaim như control nhưng không được dùng nguồn tương lai; BUY/SELL. |
| A3-043 | **IMPLEMENTED** (A3R3-05; lượt A3-043/a + /b — /a `EXPECTED_IMPLEMENTATION_RED` **F07**, /b **GREEN**) | T / equal pool temporal | Kiểm usable time từ cả hai source. | Hai source khác confirmation time; lấy max, thiếu/chưa usable một source thì không nhận. Không chỉ so thời gian của source đầu. → **A3-043/a (2026-09-11)** — cô lập negative equal-pool temporal: bỏ `assert sweeps["swept_lows"] == []` (quá rộng, cấm oan single-source sweep hợp lệ) và thay bằng lọc theo **đúng level equal pool** `[s for s in sweeps["swept_lows"] if s["level"] == expected_level]` rồi assert danh sách đó rỗng; thêm precondition `expected_level not in {source_a.level, source_b.level}` để bộ lọc không thể khớp nhầm single-source. Không assert tổng số sweep, không pool priority/dedupe, **không** thêm `records` canonical (F06 sẽ che RED F07) và **không** thêm single-source control (đó là A3-043/b). Node vẫn **RED đúng F07** — lỗi nằm ở assertion target equal pool. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.110. → **A3-043/b (2026-09-11)** — **single-source control**: node mới `test_r72_01_single_source_pool_usable_before_sweep_close_is_accepted`, **cùng geometry H1 của /a** (nến idx1 `(100.3,100.5,99.5,100.3)`) nhưng **chỉ khai một low source** `source-a` level `100.0`, `confirmed_at = usable_at = stamp(1)`, non-provisional. `equal_lows == []`, `swing_lows == [100.0]`; lọc event theo **level `100.0`** (không theo vị trí list) rồi đòi tồn tại event khớp `index 1`, `reclaimed_at stamp(2)`, `source_swing_id "source-a"`. Không assert tổng số `swept_lows`, pool priority hay dedupe; **không** dùng/đòi canonical `records`. **GREEN** ⇒ chứng minh single-source sweep hợp lệ và negative /a **không** được cấm toàn bộ `swept_lows`. Bằng chứng: §A3.111. |
| A3-044 | IMPLEMENTED | T / actual swing→pool→sweep | Thêm/đối chiếu positive bằng actual swing producer. | OHLC thật của fixture tạo confirmed swing rồi pool và sweep sau usable; expected source/pivot/close tính độc lập. Tách khỏi fixture temporal synthetic 039–043. |
| A3-045 | IMPLEMENTED | T / `same_pool_observation_cannot_bypass_consumption` | Tạo và xác minh assignment ban đầu. | Có pool/source lineage, early owner, assignment ID; completeness True; JSON serialize/restore được history. |
| A3-046 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / cùng nhóm | Tạo observation mới của cùng causal pool. | Thay sweep ID, observation time và rolling index, giữ nguồn pool; old owner/assignment giữ, late contribution0. Không chỉ replay cùng sweep ID/time như test cũ. |
| A3-047 | IMPLEMENTED | T / cùng nhóm | Thêm control thực sự có nguồn pool mới. | Source IDs và causal lineage đổi có bằng chứng; không bị khóa nhầm bởi pool cũ. Các điều kiện source/claim/history khác vẫn hợp lệ. |
| A3-048 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / history conflict | Kiểm assignment history mâu thuẫn. | History/pool/owner conflict không tự cấp lại owner, có reason; không fail do thiếu fixture timestamp. |
| A3-049 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / `cutoff_equal_invalidated_at` | Tạo positive D1 trước invalidation. | Outside→touch→exit+reaction, cùng zone/visit ID; reacted_at có thật, close cutoff trước terminal cho valid/score>0. |
| A3-050 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / cùng nhóm | Kiểm cutoff đúng invalidated_at. | Cùng chuỗi A3-049 thêm invalidation; tại close đó invalid/score0 nhưng reacted_at trước đó giữ nguyên; reject đúng terminal. |
| A3-051 | IMPLEMENTED | T / D1 expiry cutoff | Kiểm cutoff đúng expired_at đối ứng. | Trước expiry có reaction còn hợp lệ; tại expiry invalid/score0, giữ lịch sử. Không pass vì stale/no-reaction hoặc cutoff trước reaction. |
| A3-052 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / `typed_terminal_projection_reaches_d1_consumer` | Chuẩn hóa payload đủ điều kiện typed. | Zone/visit IDs khớp, timeframe D1, cutoff sau reaction; required fields hợp lệ theo model. Không dùng `dict()` thay typed conversion. |
| A3-053 | IMPLEMENTED | T / typed D1 control | Kiểm positive qua `SmcZone.from_dict(...).to_dict()`. | D1 còn valid/positive sau round-trip, ID/bounds/times/visits đúng. |
| A3-054 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / typed D1 invalid | Kiểm invalidation qua typed restore tới D1. | Cùng nguồn A3-053 tại terminal trả invalid/0; trạng thái canonical thắng legacy flags trái nhau. |
| A3-055 | IMPLEMENTED | T / typed D1 expired | Kiểm expiry qua typed restore tới D1. | Expired không usable và D1 invalid/0; không mất reacted history, không pass bằng mismatched ID. |
| A3-056 | IMPLEMENTED | T / `h1_candidate_chain_reaches_d1_consumer_smoke` (tên cũ `actual_detector_lifecycle_d1_context_chain`) | Ghi đúng phạm vi ca H1 candidate hiện có. | Nếu giữ, tên/mapping thể hiện candidate/H1 smoke; giữ assertions còn đúng, không dùng ca này làm bằng chứng confirmed D1 end-to-end. → **Xong 2026-09-11:** node đổi tên thành `test_r72_09_h1_candidate_chain_reaches_d1_consumer_smoke`, thêm assert candidate (`lifecycle_status == "candidate"`, `confirmed is not True`), gỡ assert tautology `to_dict()["visits"]`, ghi rõ `atr_current=5.0` do ca smoke tự gán; node PASSED, toàn file 83 collected / 56 failed / 27 passed (RED do core chưa sửa). Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.59. |
| A3-057 | IMPLEMENTED | T / confirmed D1 source fixture | Tạo/reuse fixture qua detector và bước confirmation hiện có để có vùng D1 confirmed. | `detect_order_block_candidates` chỉ tạo candidate; đi tiếp `confirm_order_block_candidate(s)` với BOS confirmed/source/break hợp lệ từ chuỗi fixture. Có warm-up, metadata, departure; assert confirmed/available_at/source IDs/bounds trước lifecycle. Không tự gán confirmed. → **Xong 2026-09-11:** fixture D1 37 nến (`_D1_SOURCE_ROWS`) 0..32 warm-up zigzag để `replay_smc_structure` tự bootstrap bullish (HH 102.0/103.0, HL 96.4/97.8), nến 32 base bearish + 33 departure bullish + 34 nến phá đỉnh swing; node `test_r72_09_d1_source_is_confirmed_by_fixture_break` khẳng định precondition (validator, warm-up ≥15, candidate chưa confirmed, `departure_measurement.status == "ok"`) và contract sau `confirm_order_block_candidate`: `confirmed`/`available_at` = close nến 34 (tính từ fixture), source/break IDs khớp BOS thật của replay, bounds 99.2/100.2 giữ nguyên. **GREEN**. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.60. |
| A3-058 | IMPLEMENTED | T / D1 detector→enrich | Cho vùng A3-057 đi qua canonical lifecycle từ `enrich_zones`. | Expected entered/exited/reacted times từ fixture; không gọi lifecycle thứ hai với ATR tự gán để thay output cần kiểm. → **Xong 2026-09-11:** `_D1_RETEST_ROWS` = chuỗi A3-057 + 5 nến retest (bar 38 vào vùng, 39 trong vùng, 40 thoát lên, 41 follow-through) + `_d1_retest_candles()` assert hình học theo metadata fixture (tolerance/tolerance buffer/threshold); node `test_r72_09_d1_confirmed_zone_timeline_comes_from_fixture_retest` đưa **chính** vùng confirmed của A3-057 (kèm `tick_size`/`atr_current` là metadata fixture) qua `enrich_zones` và khẳng định timeline = close nến 38/40/41 tính từ fixture; **không** gọi `analyze_zone_lifecycle` lần hai. **GREEN**. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.61. |
| A3-059 | IMPLEMENTED | T / D1 enriched→typed restore | Kiểm identity/bounds/timeline sau typed restore. | So với expected độc lập của fixture; không chỉ so hai serialization tạo từ cùng object. → **Xong 2026-09-11:** helper `_d1_enriched_source()`/`_d1_close_at()` + node `test_r72_09_d1_enriched_zone_survives_typed_restore` cho vùng enriched của A3-058 qua `SmcZone.from_dict(enriched)` rồi `SmcZone.from_dict(typed.to_dict())`; **mỗi** model (typed và restored) được so với giá trị **tính từ fixture** (bounds 99.2/100.2 theo nến base; close nến 34/38/40/41 cho `available_at`/`entered_at`/`exited_at`/`reacted_at`), không so hai serialization của cùng object. **GREEN**. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.62. |
| A3-060 | IMPLEMENTED | T / D1 integration positive | Kiểm D1 reaction trước terminal bằng chính lifecycle canonical. | Cùng confirmed zone, sau availability, trước terminal: valid/score>0; loại khả năng pass từ legacy flag. → **Xong 2026-09-11:** node `test_r72_09_d1_reaction_positive_reads_canonical_lifecycle` dùng payload typed đã restore (A3-059) + **chính** lifecycle của `enrich_zones` (A3-058, không gọi lifecycle lần hai), cutoff = close nến cuối fixture (sau `available_at` 2026-02-05, không có invalidation/expiry); evidence **valid True / score 1.0**, `source_visit_id` = visit canonical, `reacted_at` = close nến 41, reason `D1_REACTION_COMPLETED_REACTED`. Control: bỏ visit canonical + bật legacy `d1_reaction`/`proximity` = True ⇒ **invalid/0** (`D1_REACTION_NOT_COMPLETED_REACTED`) ⇒ không pass nhờ legacy flag. **GREEN**. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.63. |
| A3-061 | IMPLEMENTED (A3R3-03; lượt A3-061/a + A3-061/b) | T / D1 integration invalid | Kiểm chuỗi A3-060 khi vùng bị invalidated. | Tại close terminal: canonical status invalid/unusable, D1 invalid/0; lịch sử reaction trước đó còn. → **Xong 2026-09-11:** fixture `_D1_INVALIDATION_ROWS` = chuỗi retest + 2 nến (42 ngoài vùng, 43 đóng `98.6 < 99.0` = zone_low − buffer ⇒ terminal) + `_d1_invalidated_candles()` assert hình học; node `test_r72_09_d1_invalidated_source_is_terminal_for_the_consumer`: precondition PASS (invalidation_index 43, `invalidated_at` = close nến 43, `lifecycle_broken`/`broken` True, không expiry, `available_at` giữ nguyên, history 2 visit với visit-1 `completed_reacted` còn nguyên `reacted_at`), control trước terminal **valid True** (→ **sửa A3R3-03, lượt A3-061/a:** control đổi thành **prefix thật** `_D1_RETEST_ROWS` enrich riêng, `as_of` = close cuối của chính prefix — assert prefix chưa terminal + identity/history khớp bản append; bỏ cách rewind `as_of` trên payload terminal; → **A3-061/b:** khóa bản append tại invalidation close — `_D1_INVALIDATION_INDEX == len(candles) − 1` và `terminal_at` = close cuối của chính snapshot, nên không có bản terminal nào bị hồi dựng quá khứ); RED đúng loại `implementation` tại contract: `lifecycle_status == "invalid"` (thực tế `"confirmed"` — R72-08), `usable is False` (thực tế **thiếu key**), D1 `valid is False`/`score == 0` (thực tế `True`/`1.0`, reason `D1_REACTION_COMPLETED_REACTED` — F05/A-D04). Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.64 (lượt đầu), **§A3.101** (A3-061/a) và **§A3.102** (A3-061/b). |
| A3-062 | IMPLEMENTED | T / D1 integration expired | Kiểm chuỗi A3-060 khi vùng hết lifetime. | Tại expiry: expired/unusable và D1 invalid/0; expected expiry từ fixture, không do thiếu metadata/no-reaction. → **Xong 2026-09-11:** fixture `_D1_EXPIRY_ROWS` = chuỗi retest + 14 nến ngoài vùng (low > tolerance, không đóng qua buffer) để tuổi vùng đạt lifetime D1 (20 nến kể từ anchor = nến xác nhận 34) tại nến 55; `_d1_expired_candles()` assert hình học + `_D1_EXPIRY_INDEX == _D1_BREAK_INDEX + 21` = nến cuối. Node `test_r72_09_d1_expired_source_is_terminal_for_the_consumer`: `expiry_index 55`, `expired_at` = close nến 55 (tính từ fixture), `lifecycle_expired`/`lifecycle_stale` True, không broken/invalidation, metadata fixture còn nguyên; history 1 visit `completed_reacted` giữ nguyên; contract **GREEN**: `lifecycle_status "expired"`, `usable False`, reason có `ZONE_EXPIRED`, và D1 tại expiry `valid False`/`score 0` với reason `["D1_REACTION_STALE"]` (không phải thiếu reaction). Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.65. |

### 8.6. F01.4 — Bù coverage và bàn giao, A3-063…090

Các mã 064–080 là **audit một ô coverage rồi bù đúng chỗ còn thiếu**. Nếu test ở phần trước hoặc baseline đã chứng minh đủ thì chỉ map/reuse và chạy node đó; không tạo bản sao. Một test có thể làm bằng chứng cho nhiều mã nếu assertions thực sự đủ.

| Mã | Trạng thái | Vị trí | Một việc cần làm | Đầu ra / kiểm tra xong |
|---|---|---|---|---|
| A3-063 | IMPLEMENTED | M / §4 coverage | Lập mapping đủ từng ô của bảy cụm §4. | Metadata, terminal, pool, owner, contribution, consumed, fixture/fill đều có positive/negative/integration; ghi missing nếu chưa có node. → **Xong 2026-09-11:** thêm mục **"Mapping coverage §4 — bảy cụm (A3-063)"** vào M, phủ đủ **21 ô** (7 cụm × 3) với node thật lấy từ `--collect-only` ngày 2026-09-11 (**89 node**): **13 ĐỦ** (3 ô kèm phần REFERENCED: fill bounds, no-break/full-fill, R56 invariant), **6 MỘT PHẦN** (Metadata integration; Pool negative; Pool integration; Owner negative; Owner integration; Consumed integration), **2 THIẾU** (Contribution integration; Fixtures negative phần invalid-data cố ý). Phần thiếu đã giao đúng mã: A3-065, A3-066, A3-068, A3-069, A3-070/071/072, A3-074, A3-078, A3-079, A3-080 — **chưa làm** ở bước này. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.66. |
| A3-064 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / pool source | Audit provisional và missing source/provenance. | Reuse A3-010/043 và ca provisional; đủ từng nguồn thiếu riêng, không lấy tên finding làm coverage. → **Xong 2026-09-11:** audit theo luật 3 của interface pool (A3-005): 5 ca **nguồn/provenance thiếu riêng** chưa có node (record sai level; record không `sources`; `source_ids` trỏ nguồn vắng mặt; source thiếu ID; source thiếu `usable_at`) ⇒ thêm node `test_r72_01_missing_pool_provenance_fails_closed[5 param]` + helper `_pool_record_fixture()`; các ca đã có được **reuse** (`canonical_sweep_requires_pool_records_not_numeric_levels` = thiếu `records`; `provisional_source_cannot_create_a_sweep` = provisional; `equal_pool_without_usable_source_does_not_exist` = source usable=False). **RED 5/5** đúng loại `implementation` (core chưa đọc `records`, còn fallback numeric) — precondition + control legacy đều PASS. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.67. |
| A3-065 | IMPLEMENTED | T / sweep threshold | Audit equality và vượt excursion threshold. | Theo strict excursion rule đã duyệt: đúng biên chưa đủ, vượt mới có thể nhận khi reclaim/source hợp lệ; BUY/SELL. → **Xong 2026-09-11:** node `test_r72_01_excursion_threshold_is_strict[distance × side]` (6 param): nến sweep có penetration **đúng** `excursion + slack` với slack `-0.05`/`0`/`+0.05`, reclaim trong cùng nến, source confirmed/usable/non-provisional; `below` và `equal` ⇒ **không** sweep (đúng biên chưa đủ), `beyond` ⇒ sweep `index 2` với `depth == excursion + 0.05`, `excursion_buffer` đúng rule, `source_swing_id` giữ. **GREEN 6/6** — rule strict đã đúng trong code, không cần sửa. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.68. |
| A3-066 | IMPLEMENTED (A3R3-04; lượt A3-066/a + /b + /c + /d + /e + /e-r1, kèm `EXPECTED_IMPLEMENTATION_RED`) | T / pool→sweep prefix/batch | Audit kết quả causal qua prefix/batch tại cùng close cutoff. | Pool ID/source/usable time và sweep evidence/time khớp; thêm nến tương lai không đổi evidence đã biết trước cutoff. Permutation/rolling index không đổi causal identity hoặc backdate confirmation. → **Xong 2026-09-11:** 2 node mới trên **cùng chuỗi thật** (fixture A3-044 12 nến + 4 nến đuôi): (1) `test_r72_01_pool_sweep_evidence_survives_future_bars` — prefix 12 nến vs batch 16 nến, evidence của sweep trước cutoff **giống hệt** (sweep_id/level/time/reclaimed_at/depth/source IDs), swing pivot giữ nguyên `swing_id`/`confirmed_at`; (2) `test_r72_01_pool_sweep_identity_survives_rolling_index` — bỏ nến cũ nhất (giữ timestamp) ⇒ pivot/sweep **giữ nguyên identity** (`swing_id`, `sweep_id`, `reclaimed_at`) chỉ `index` dịch 1. **GREEN 2/2** — tính causal đã đúng. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.69. → **Sửa A3R3-04 (2026-09-11), đủ năm phần:** **(a)** §A3.103 — lọc evidence trước cutoff theo `reclaimed_at` (close) qua helper `_reclaimed_by`, không dùng `time` (open nến), assert mọi event đã biết có `reclaimed_at <= cutoff` và assert toán tử **bao gồm** tại đúng cutoff; **(b)** §A3.104 — helper `_pool_record_for` (chọn record theo **kind + lineage**, không theo vị trí list hay level numeric) + `_assert_pool_record_matches_source` (`pool_id` chuỗi không rỗng; `sources` khớp `swing_id`/`confirmed_at`/`provisional` của swing fixture; `usable_at` là **max** các source của chính record và `>= confirmed_at`). Cố ý **không** khoá quy tắc fallback `confirmed_at → usable_at` vì chưa có quyết định; **(c)** §A3.105 — nối `sweep["source_pool_id"] == record["pool_id"]` cho **cả** prefix và batch, kèm pool thứ hai cùng kind (`swing_low` idx10 level `99.0`) phân biệt bằng lineage; **(d)** §A3.106 — rolling index 4 → 3 giữ **cùng** `pool_id`/`source_ids`/`usable_at`/`kind`/`level` và nối sweep↔record ở cả hai lượt; **(e)** §A3.107 + **§A3.109 (/e-r1)** — **node mới** `test_r72_01_pool_identity_survives_source_permutation`: đảo thứ tự 2 nguồn (precondition khẳng định thứ tự **thật sự** đổi) giữ `pool_id`/`usable_at`, không assert số lượng sweep/pool priority/dedupe. Sau finding **A3-066/e-r1**, node dùng 2 source cố ý đặt **thứ tự causal NGƯỢC thứ tự sort ID** (`source-z` pivot sớm/sort cuối, `source-a` pivot muộn/sort đầu) và khóa **`source_ids` = danh sách ĐÃ SORT ổn định** `["source-a","source-z"]` (không theo input order, không theo causal order); provenance từng source tra **theo `swing_id`**, không theo vị trí list `sources`. **Kết quả:** hai node A3-066 chuyển **GREEN → RED chủ ý** đúng loại `implementation` **F06/R72-01** (`records` canonical chưa được `core/` phát ra; `source_pool_id` hiện = `source_swing_id` do fallback `core/smc_context.py:4408-4415`). File acceptance **116 → 117 node**, **66 failed / 50 passed → 69 failed / 48 passed** (đầu lô → cuối lô); hash T cuối `67E47D6E5CFEDE9236BA684230AC44D80A76A2245AD2A30D792C331FA93BC2F2` (sau đính chính A3-066/e-r1 §A3.109; hash trước đó `21768F94…`). |
| A3-067 | IMPLEMENTED (A3R3-02; lượt A3-067/a + A3-067/b + A3-067/c) | T / explicit override | Audit override hợp lệ của rule metadata tương ứng. | Ghi đúng parameter/rule đang hỗ trợ override; chỉ thay requirement của rule đó, không cứu thiếu nguồn/metadata khác. Tick argument không được tự thắng tick item mâu thuẫn (A-D07). Không thêm override mới. → **Xong 2026-09-11:** bảng audit override vào M + 3 node mới: `test_r72_07_excursion_override_only_replaces_its_own_rule` (`excursion_buffer` cho sweep sweep được khi dưới ngưỡng tính toán, nhưng **không** cứu source provisional/unusable/unconfirmed), `test_r72_07_equal_tolerance_override_only_replaces_its_own_rule` (`equal_tolerance` bỏ/giữ cặp equal, không cứu source unusable; `swing_lows` vẫn còn), `test_r72_07_lifecycle_threshold_overrides_only_replace_their_own_rule` (`zone_tolerance` chỉ đổi rule overlap — buffer giữ nguyên; `break_buffer` chỉ đổi ngưỡng invalidation). Phần A-D07 **reuse** `test_r72_07_conflicting_same_scope_tick_sources_fail_closed` (tick argument không thắng tick item mâu thuẫn). **GREEN 3/3**; không thêm override mới. → **Sửa A3R3-02 (2026-09-11, lượt A3-067/a):** bỏ oracle “không tick/ATR ⇒ buffer fail-closed `0.0`”; ca thiếu metadata nay khóa theo contract A3-007 — `metadata_state == "unknown"`, `metadata_reason` không rỗng, `invalidation_buffer is None`, `lifecycle_broken is False` (kể cả nến đóng `99.93`), **không** coi `0` là fail-closed; 3 controls đủ metadata vẫn PASS, phần thiếu-metadata là **RED F02**. → **A3-067/b:** thêm **2 cross-control** (mỗi override chạy trên fixture của rule kia) — `break_buffer=0.05` trên fixture chạm ⇒ buffer `0.05` nhưng `visits == ()`; `zone_tolerance=0.5` trên fixture phá ⇒ buffer vẫn `0.1` (không phải `0.5`), `broken False`; nhóm controls đặt trước khối thiếu-metadata để chạy thật. → **A3-067/c:** quét đồng bộ mọi mô tả/docstring còn gọi thiếu metadata là “fail-closed” — sửa docstring A3-021 sang `unknown ⇒ unusable, threshold None`; giữ nguyên lịch sử (interface proposal đã bị §A3-007 thay, §A3.70 đã có đính chính) và không sửa báo cáo review. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.70 (lượt đầu), **§A3.98** (A3-067/a), **§A3.99** (A3-067/b) và **§A3.100** (A3-067/c). |
| A3-068 | IMPLEMENTED | T / ATR/tick source | Audit nguồn metadata tại thời điểm đánh giá. | Timeframe/cutoff đúng, không dùng nến tương lai/latest fallback khi thiếu nguồn; phân biệt ATR formation với ATR current theo đúng rule được kiểm. → **Xong 2026-09-11:** 2 node mới: `test_r72_07_formation_atr_is_causal_and_never_latest_fallback` (reference nằm ở **close trước** nến sự kiện, 5 nến biến động sau không đổi giá trị, trước warm-up ⇒ `None` + `DEPARTURE_ATR_UNAVAILABLE` + candidate `unavailable` + BOS hợp lệ vẫn **không** promote được — kèm control cùng event shape promote được candidate có ATR) và `test_r72_07_formation_and_current_atr_keep_their_own_source` (fixture đuôi biến động để ATR formation `2.0357` ≠ ATR current `9.666`; detector dùng formation, lifecycle dùng current, buffer mỗi bên theo nguồn của mình; timeframe lạ và `event_time` không khớp candle close đều `ValueError`, không default). **GREEN 2/2**. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.71. |
| A3-069 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / context owner permutation | Audit early-far thắng late-near qua context với thứ tự đảo. | `_attach_zone_sweep_links` thực sự enumerate→assign→project; owner theo claim time, không chỉ helper độc lập pass. → **Xong 2026-09-11:** node `test_r72_02_context_owner_follows_claim_time_under_input_permutation` chạy **cả hai thứ tự** zone (`[early, late]` và `[late, early]`) qua `_attach_zone_sweep_links` và đọc **payload sweep được chiếu** (owner_setup_id/assignment_id/consumed/contribution_applied/linked_zone_id/claim_eligible_at) ⇒ chứng minh caller thực hiện enumerate→assign→project. Precondition + invariance **PASS** (cùng owner/assignment/linked zone/claim time ở cả hai thứ tự); RED đúng loại `implementation` (**F08**) tại contract: owner là `"late"` (zone gần) thay vì `"early"` (claim sớm) — kèm `linked_zone_id`/`claim_eligible_at` lệch theo. **Reuse** `test_r72_02_acceptance_context_ranks_all_eligible_claims_by_causal_time` (một thứ tự) làm ca gốc. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.72. |
| A3-070 | IMPLEMENTED | T / owner eligibility | Audit claim sai chiều. | Opposite-side claim bị loại dù gần/hay sớm hơn; eligible positive còn được chọn. → **Xong 2026-09-11:** node `test_r72_02_opposite_side_claim_cannot_own_a_sweep`: zone sai chiều (`supply_zone`/`sell`) **nằm sát** level sweep (khe giá `0.05`, gần hơn zone hợp lệ `0.2`) + zone hợp lệ (`demand_zone`/`buy`) ⇒ tầng **link** chỉ liên kết zone hợp lệ (`distance_atr == 0.2`), và **caller context** chiếu owner `"right-side"` ở **cả hai thứ tự**, zone sai chiều `liquidity_sweep_linked False`/`linked_sweep_id None`. **GREEN 1/1**. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.73. |
| A3-071 | IMPLEMENTED (A3R3-01; lượt A3-071/a + A3-071/b + A3-071/c) | T / owner eligibility | Audit claim ngoài khoảng cách cho phép. | Trong/đúng/ngoài boundary theo rule đã chốt; claim không đủ không thắng bằng timestamp sớm. → **Xong 2026-09-11:** node `test_r72_02_claim_outside_distance_boundary_cannot_own_a_sweep`: level `105`, ATR `1.0`, tolerance `0.25` — **trong** band `[104,106]` ⇒ link với `distance_atr 0.0`; **đúng biên** `[105.25,106]` (khe `0.25`) ⇒ **vẫn link** (`distance_atr 0.25`, biên bao gồm); **ngoài** `[105.26,106]` (khe `0.26`) ⇒ **không** link, không claim. Tầng context: zone **ngoài khoảng cách nhưng available sớm hơn** (`hour 13`) **không** thắng; zone trong khoảng (`hour 15`) giữ owner ở **cả hai thứ tự**. **GREEN 1/1**. → **Sửa A3R3-01 (2026-09-11, lượt A3-071/a):** tầng **link direct** đã đúng nhưng tầng **context** dùng `_probe.attach` với ATR thật `2.0` ⇒ zone “ngoài” chỉ cách `0.13` ATR ⇒ vẫn eligible (xanh nhờ nearest-rank sai). Nay context dùng **candle input local** `(120,120.5,119.5,120) × 20` ⇒ `TR = 1.0` mọi nến ⇒ ATR caller **1.0**, và **từng zone** được assert eligibility/`linked_sweep_distance_atr` **một mình qua chính caller** trước khi ghép cặp; zone “ngoài” không link kể cả đứng một mình. Ba boundary controls ở protected helper giữ nguyên. → **A3-071/b:** thêm vế **event-level** cho từng zone đứng riêng — trong/đúng biên có `linked_zone_id`/`owner_setup_id`/`consumed True` của chính zone, ngoài biên giữ `linked_zone_id None`/`consumed False`/không `owner_setup_id`. → **A3-071/c:** vế **hai thứ tự** — mỗi thứ tự dựng **pair mới** (không dùng lại dict đã bị caller ghi link payload); outside-early `hour 13` **sớm hơn** vẫn **không** có link/owner/assignment (`sweep_owner_setup_id`/`sweep_assignment_id` vắng), owner/`assignment_id`/`linked_zone_id`/`claim_eligible_at` **không đổi** khi đảo input. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.74 (lượt đầu), **§A3.95** (A3-071/a), **§A3.96** (A3-071/b) và **§A3.97** (A3-071/c). |
| A3-072 | IMPLEMENTED | T / owner eligibility | Audit claim ngoài cửa sổ thời gian. | Valid positive và boundary rõ; không đổi time window để test đạt. → **Xong 2026-09-11:** node `test_r72_02_claim_outside_time_window_cannot_own_a_sweep` với sweep idx `10` và `max_time_bars=3` (đúng tham số đã có, **không** chỉnh window): delta formation→sweep `2` ⇒ link; **đúng biên** delta `3 == window` ⇒ **vẫn** link; delta `4 > window` ⇒ **không** link; cửa sổ formation/departure kết thúc **trước** sweep (`departure_end_index 9 < 10`) ⇒ không link. Tầng context (window mặc định): zone ngoài cửa sổ available **sớm hơn** không thắng; zone trong cửa sổ giữ owner ở **cả hai thứ tự**. **GREEN 1/1**. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.75. |
| A3-073 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / same-time owner tie | Audit tie-break khi claim time bằng nhau. | Stable setup ID quyết định dưới permutation; nếu helper đã đủ thì reuse, ghi riêng coverage context từ 069. → **Xong 2026-09-11:** **reuse** `test_r72_02_same_time_tie_is_stable_under_claim_permutation` (helper: cùng claim time, owner theo setup ID, đúng cả hai thứ tự — chạy lại xác nhận, không viết lại). **Bù phần context** (chưa có node riêng cho tie): `test_r72_02_context_same_time_tie_follows_stable_setup_id` — hai zone cùng `available_at stamp(13)`, cùng link được sweep, `alpha-setup` có setup ID nhỏ hơn còn `a-loser-child` có zone ID nhỏ hơn (nên phân biệt được rule); precondition + invariance **PASS** (link one-to-one đúng 1 zone; đảo thứ tự không đổi owner/assignment/linked zone), RED đúng loại `implementation` (**F08**) tại contract: owner `"beta-setup"` (zone ID nhỏ) thay vì `"alpha-setup"` (setup ID ổn định). Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.76. |
| A3-074 | IMPLEMENTED | T / owner children | Audit contribution theo từng sweep khi owner có nhiều child. | Duplicate/multi-family/permutation/metadata không tăng hoặc làm mất contribution: mỗi sweep có owner hiện diện tổng1. Có control hai sweep độc lập, mỗi sweep tổng1; không cap tổng toàn danh sách về1. → **Xong 2026-09-11:** node `test_r72_03_contribution_is_counted_per_sweep_not_per_list`: 2 sweep độc lập cùng owner `owner-1` (5 claim: multi-family `fvg`/`ob`, 1 dòng metadata, 2 sweep khác nhau) ⇒ mỗi sweep **đúng 1** contribution, tổng danh sách **2** (không cap về 1), owner mỗi sweep `owner-1`; claim được credit là con **đầu tiên theo ID** (`child-a`/`child-d`); đảo thứ tự input ⇒ per-sweep count và con được credit **không đổi**. **GREEN 1/1**. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.77. |
| A3-075 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / non-owner child | Audit child non-owner có ID nhỏ hơn. | Không chiếm contribution slot; owner vẫn tổng1, non-owner0. → **Xong 2026-09-11:** audit trên node sẵn có `test_r72_03_acceptance_contribution_is_selected_within_owner_children` (fixture `early/z-owner` 13:00 vs `late/a-nonowner` 15:00 — non-owner có zone ID **nhỏ hơn**) và **bổ sung tối thiểu** đúng hai vế của hàng: precondition `"a-nonowner" < "z-owner"`, `non_owner_claim["contribution_applied"] is False` (PASS), `owner_claim["contribution_applied"] is True` (**RED**). Probe cùng fixture: assignment owner `"early"` ✓ nhưng **cả hai** claim đều `contribution_applied False` ⇒ tổng `0` ⇒ slot đang được chọn trên **toàn bộ** children nên non-owner ID nhỏ "chiếm" khoá và owner không được credit (F09). Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.78. |
| A3-076 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / owner absent | Audit owner chỉ còn trong history. | Owner/assignment giữ, current contribution0; không chuyển cho late child. Reuse A3-045/046 nếu đủ. → **Xong 2026-09-11:** audit → **reuse** hai node sẵn có + **bổ sung tối thiểu** vế "assignment giữ" vào node history-only: `test_r72_03_historical_owner_without_current_child_gets_zero_contribution` thêm `assert restored["assignments"]["sweep"] == first["assignments"]["sweep"]` (owner **và** assignment_id/claim_eligible_at/assigned_at phải giữ). Kết quả: `restored["claims"] == []` PASS (current contribution 0), nhưng **RED** đúng loại `implementation` (**F10**): `restored["assignments"] == {}` ⇒ assignment của owner history-only bị mất (KeyError `'sweep'`). Vế "không chuyển cho late child" **đã đủ** ở node reused `test_r72_04_assignment_survives_json_restore_with_late_only_window` (GREEN: owner `"original"` giữ, claim late có `contribution_applied False`). Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.79. |
| A3-077 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / missing/conflicting history | Audit coverage thiếu và mâu thuẫn history riêng biệt. | Reuse A3-012/048; có reason đúng từng nhóm, không reset history rồi cấp owner mới. → **Xong 2026-09-11:** audit hai nhóm **riêng biệt** bằng hai node sẵn có: (a) **thiếu coverage** `test_r72_04_incomplete_history_returns_explicit_reason` — GREEN: `history_complete=False` ⇒ `assignments == {}` + reason **riêng** `["SWEEP_OWNER_HISTORY_INCOMPLETE"]`; (b) **mâu thuẫn history** `test_r72_04_conflicting_assignment_history_fails_closed` — thêm 1 assert phân nhóm (`"SWEEP_OWNER_HISTORY_INCOMPLETE" not in reason_codes` vì fixture khai `history_complete=True` và có record ⇒ không được báo nhầm nhóm thiếu) rồi giữ hai assert fail-closed. Kết quả (b): **RED** — helper **cấp lại owner mới** cho setup hiện tại (`owner_setup_id "late"`, `assignment_id` mới, `contribution_applied True`) và `reason_codes == []` ⇒ đúng điều hàng cấm ("reset history rồi cấp owner mới"). Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.80. |
| A3-078 | IMPLEMENTED (kèm `EXPECTED_IMPLEMENTATION_RED`) | T / repeat/restore | Audit first run→JSON restore→repeat trên context. | Owner/assignment/consumed giữ đúng qua caller thực tế; đối chiếu A3-046 để không chỉ kiểm cùng dict trong RAM. → **Xong 2026-09-11:** node `test_r72_04_context_repeat_after_json_restore_keeps_assignment`: lượt 1 `_attach_zone_sweep_links` với `old-child/original-owner` 13:00 ⇒ owner/`assignment_id`/`consumed True`; **JSON restore** payload (`json.loads(json.dumps(sweeps))`, assert `restored is not sweeps` và giá trị giữ nguyên trước khi lượt 2 chạy — khác ca in-RAM của A3-004); lượt 2 với `new-child/later-owner` 15:00 ⇒ **RED**: owner bị đổi thành `later-owner` cùng `assignment_id` mới (`smca-eb0c85c4…` thay `smca-81d0ce9b…`) và `linked_zone_id` mới ⇒ caller **không** tôn trọng assignment đã ghi sau restore (F10). Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.81. |
| A3-079 | IMPLEMENTED | T / tests fill hiện có | Audit partial/full fill và invariant R56 liên quan. | Original bounds/IDs giữ; full fill không tự là break; reuse tests đã có và fixture hợp lệ, không sửa protected golden. → **Xong 2026-09-11:** **reuse** `tests/test_smc_fvg_fill_task62.py` (partial/full fill, `original_bounds`/`zone_id` giữ, `fill_ratio`, tolerance, tích hợp `enrich_zones`, typed round-trip) + `tests/test_smc_r56_01_session_acceptance.py` — chạy chung **121 passed**. **Bù vế còn thiếu** ("full fill không tự là break"): node mới `test_r72_09_full_fvg_fill_is_not_a_break` (fixture task62 full-fill, reuse rows): `fill_status "filled"`/`fill_ratio 1.0`/`remaining_bounds {100,100}`; `zone_id`/`setup_id`/`original_bounds`/`low`/`high` **không đổi**; `broken is False` và `lifecycle_status "confirmed"` ⇒ fill **không** nâng terminal state. **GREEN 1/1**; không sửa golden. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.82. |
| A3-080 | IMPLEMENTED | T / fixture audit | Audit positive và negative invalid-data đã tách. | Mọi positive qua validator; invalid-data intentional kiểm reason riêng. Giữ năm fixture fixes cũ đã PASS. → **Xong 2026-09-11:** **reuse** `test_r72_09_acceptance_corrected_task57_71_positive_fixtures_are_valid` (năm fixture fixes F01-T59/T60-BUY/T60-SELL/T62-BUY/T63-SELL, PASS — đã chạy lại). **Bù vế negative** (A3-023 từng ghi "chưa có fixture invalid-data cố ý"): node mới `test_r72_09_intentional_invalid_data_reports_its_own_reason` dựng nến **đúng ví dụ R72-09** (`O112 H113 L111.2 C110.2` — close dưới low) ở bar 4/5 nến và khẳng định **reason riêng** `SMC_OHLC_INVALID` với `index 4`/`field "ohlc"`; control là **cùng bar đã sửa theo F01-T60-BUY** (low `110.2`) ⇒ validator sạch (`== ()`), và 4 nến đầu giống nhau ở cả hai series ⇒ reason do **dữ liệu**, không do họ fixture. **GREEN 1/1**; positive/negative tách bằng **cùng một validator**; không sửa golden. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.83. |
| A3-081 | IMPLEMENTED | M | Hoàn thiện mapping case-level sau các sửa/bù. | Mỗi obligation có full test path + parameter ID thật, preconditions, expected, nguồn quy tắc; ca thiếu vẫn ghi thiếu. Không dùng `[buy/sell]` như node chạy được. → **Xong 2026-09-11:** audit cơ học trên M: **35** lần xuất hiện dạng gộp `[buy/sell]` và các dạng gộp khác (`[20/21/22-*]`, `[20-buy/sell]`, `[21/22-buy/sell]`, `[20-*]`, `[expired\|invalid-buy/sell]`, `[False/True]`, `[atr_current\|tick_size]`, `[reclaimed_at\|setup_available_at]`, `[record_for_other_level\|…]`, `[below\|equal\|beyond × buy\|sell]`) đã **thay bằng parameter ID thật** lấy từ `--collect-only`; hai tên bị cắt bằng dấu ba chấm trong bảng factory (A3-023) đã mở rộng thành ID đầy đủ. Thêm **quy ước ghi node (A3-081)** ở đầu mục mapping: ô nêu node chạy được phải kèm param thật; tên không ngoặc là **tên họ** (param thật ở bảng theo dõi R72-0X); tên lịch sử/probe ghi chú tại ô. Kết quả kiểm chứng: **0** mention dạng ngoặc không tồn tại trong collection; 3 tên còn lại (1 tên lịch sử đã tách, 1 tên đã đổi tại A3-056, 1 reviewer probe) đều đã **được ghi chú**; ca thiếu vẫn ghi thiếu (không đổi). Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.84. → **Refresh r1a:** bằng chứng **§A3.115** — đối chiếu collection hiện hành (**118 node / 76 hàm**) với bảng R72-01 trong `M`: **29/29 node khớp**, không node matrix nào không collect được, không node collect bị thiếu, không ID trùng; các param đều là ID thật. → **Khép A3-081 (2026-09-11, lượt trình A lần4):** đối chiếu mapping với collection thật ⇒ **118/118 node khớp** (không thiếu/thừa/trùng); một tên trong `M` là **ghi chú đổi tên lịch sử** (`test_r72_09_actual_detector_lifecycle_d1_context_chain_uses_valid_source_fixture` → A3-056 đổi tên thành `test_r72_09_h1_candidate_chain_reaches_d1_consumer_smoke`), **không** phải node thừa. Đã điền **77 ô `Actual`/`Loại RED` còn trống** trên **9 bảng tracking** (109 hàng) từ evidence đã xác minh ⇒ **0 ô trống, 0 hàng lệch cột**; đồng bộ header `M` và kết luận A3-063 (§4) để phân biệt **ảnh chụp lịch sử** với **bản trình A lần4**. Bằng chứng: §A3.126. |
| A3-082 | IMPLEMENTED | T / L | Collect toàn bộ acceptance bản cuối. | Collection thành công; so node inventory A3-002, giải thích renamed/parameterized/new node, không làm mất coverage cũ. → **Xong 2026-09-11:** `--collect-only -q` ⇒ **116 node / 74 hàm test**, 0 lỗi collection. So inventory A3-002 (49 node): **46 giữ nguyên**, **1 đổi tên** (A3-056), **1 tách node** (A3-016/017), **1 thêm param** (A3-013/014), **68 node mới** (45 họ) thuộc A3-010…A3-081 — phân bố theo finding: R72-01 +21, R72-02 +6, R72-03 +1, R72-04 +1, R72-05 +2, R72-06 +11, R72-07 +9, R72-08 +5, R72-09 +8. **Không mất coverage cũ**. Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.85. → **Refresh r1 (2026-09-11, A3-082/r1)** — chạy lại **đúng một lệnh** `python -m pytest tests/test_smc_gate72_fix_acceptance.py --collect-only -q` sau các thay đổi A3-066/e và A3-043/b: **actual 118 node / 76 test functions** (snapshot **116 / 74** giữ làm lịch sử). Hai node mới chính là phần tăng: `test_r72_01_pool_identity_survives_source_permutation` (**A3-066/e**) và `test_r72_01_single_source_pool_usable_before_sweep_close_is_accepted` (**A3-043/b**) — mỗi node +1 node và +1 function, khớp đúng Δ+2/+2. Mọi node cũ **vẫn collect được** (kể cả các họ parametrized: `excursion_threshold_is_strict` 6, `missing_pool_provenance_fails_closed` 5, `source_usable_after_sweep_close_is_rejected` 2); không tên rỗng, không trùng. SHA256 `T` **không đổi**. Bằng chứng: §A3.114. |
| A3-083 | IMPLEMENTED | T / fixture consumers / L | Chạy targeted cho các factory/test vừa chỉnh và consumer liên quan. | Ghi actual; fixture/collection/test-defect cần sửa trước bàn giao, core RED có expected rõ. → **Xong 2026-09-11:** năm file fixture vừa sửa (task59/60/62/63/65) **32 passed**; 15 file task57–71 **108 passed**; consumer duy nhất của các factory đó là file acceptance (116 node, **0 lỗi collection**). Phân loại **65 RED** theo finding (R72-01 15, 02 5, 03 2, 04 4, 05 6, 06 12, 07 12, 08 8, 09 1) — **tất cả là implementation gap có expected rõ**, **không** có fixture/collection/test-defect: hai dạng lỗi không-phải-assert đều đúng chủ đích (KeyError trên field canonical đã duyệt nhưng chưa có `metadata_state`/`metadata_reason`/`usable`; `ValueError` từ `core/smc_lifecycle.py:674` khi metadata non-finite ⇒ gap A3-017). Bằng chứng: [smc-task-72-fix-progress.md](smc-task-72-fix-progress.md) §A3.86. → **Targeted evidence r1a (2026-09-11, A3-083/r1a)** — chạy **một** lệnh pytest gồm **14 node** đã mở lại ở A lần4: **9 failed / 5 passed**, khớp **toàn bộ** expected hiện hành. RED theo F-code: **F02 ×3** (`missing_canonical_atr`, `missing_canonical_tick`, `lifecycle_threshold_overrides` — cả ba fail ở `metadata_state`, riêng node lifecycle fail **sau** khi 3 override control đủ-metadata đã PASS nên vẫn đúng "controls GREEN, thiếu-metadata RED F02"), **F03 ×1** (`d1_invalidated_source`: `'confirmed' == 'invalid'`), **F06 ×4** (`pool_sweep_evidence`, `pool_sweep_identity`, `pool_identity_permutation` — `_pool_record_for` `assert 0 == 1`; `equal_usable_at_reclaimed` — `len(records) == 1`), **F07 ×1** (`equal_pool_usable_time_is_max`: `equal_pool_events == []`). GREEN 5: A3-071, hai override control r72_07, A3-040, A3-043/b. Đây là **targeted evidence**, **chưa** phải full classification A3-084/r1; snapshot full acceptance **69 failed / 49 passed** giữ nguyên. Bằng chứng: §A3.116. Ghi actual; fixture/collection/test-defect cần sửa trước bàn giao, core RED có expected rõ. |
| A3-084 | IMPLEMENTED | T / L | Chạy full acceptance và phân loại từng failure. | Mỗi RED có node, expected, actual, precondition và implementation gap/test defect; GREEN không tự là finding CLOSED. → **Xong 2026-09-11:** full acceptance `65 failed, 51 passed`; `--tb=line` cho 63 frame acceptance + 2 frame `core/smc_lifecycle.py:674` (ValueError nan) = 65; bảng **65 dòng** (node/expected/actual/gap F02…F10 + precondition PASS kèm §A3.NN) ở §A3.87. Không có test/fixture-defect (0 lỗi collection; mọi RED dừng ở assert contract). Ghi rõ **GREEN 51 node không tự là finding CLOSED** (thuộc Tech Lead). Mỗi RED có node, expected, actual, precondition và implementation gap/test defect; GREEN không tự là finding CLOSED. → **Refresh r1a (2026-09-11):** chạy `pytest tests/test_smc_gate72_fix_acceptance.py -v --tb=short -ra` ⇒ **118 collected / 49 passed / 69 failed / 0 skip / 0 xfail / 0 error** — **khớp hoàn toàn** snapshot 69/49, **không node nào đổi trạng thái**; bảng PASS/FAIL theo R72-01…09 và **69 dòng** full node ID + frame + lỗi đầu tiên ở **§A3.117**. Đây là **r1a (chạy + inventory)**; **r1b phân loại RED còn chờ**. |
| A3-085 | IMPLEMENTED | P / L | Chạy toàn bộ reviewer probes nguyên bản. | Ghi actual pass/fail/node; không sửa probe, không buộc tất cả GREEN trước khi sửa core B/C. → **Xong 2026-09-11:** `pytest docs/plans/probes/test_smc_gate72_review.py -v --tb=no` ⇒ **13 failed, 3 passed** (16 node). FAILED thuộc R72-01×2, 02×1, 03×1, 04×1, 05×2, 06×2, 07×2, 08×2 (mỗi node gắn cụm F02…F10); PASSED gồm fixture probe R72-09 + **2 control** (D1 reaction live, owner children một contribution) — hai control này phải giữ xanh khi fix. Probe hash khớp F00 (`5B040D6A…`) ⇒ không sửa. Ghi rõ: 13 RED là trạng thái trước B/C, không phải việc của checkpoint A; 16/16 phải xanh trước checkpoint C. |
| A3-086 | IMPLEMENTED | tests task57–71 / L | Chạy nhóm task tests theo §5. | Ghi command/nodes/count thật, so baseline liên quan; không lấy con số 108 làm kết quả chưa chạy. → **Xong 2026-09-11:** theo đúng §5 (`rg --files tests -g '*task5[7-9].py' -g '*task6[0-9].py' -g '*task7[01].py'`) ⇒ **15 file**, **108 passed in 1.26s**, collect **108** (0 skip/xfail/collection error); số node mỗi file ở §A3.89. So baseline F00 15/108 (1.27s) và reviewer 15/108 (1.26s) ⇒ **không đổi**; đây là chạy thật lượt này. Ghi command/nodes/count thật, so baseline liên quan; không lấy con số108 làm kết quả chưa chạy. |
| A3-087 | IMPLEMENTED | retained SMC/scanner / L | Chạy riêng retained baseline, loại acceptance mới khỏi nhóm này. | So inventory đã có và từng lỗi; không dùng tổng count tăng để che test mất, không lấy854 làm số đích. → **Xong 2026-09-11:** `$retainedSmcTests` (rg `test_smc*.py` trừ acceptance) = **66 file** + 6 file integration ⇒ **854 passed in 8.96s**, collect **854** (0 skip/xfail). Kiểm inventory: 67 file `test_smc*.py` − 1 acceptance = 66 (không loại nhầm); so F00 **66/854 (9.08s)** ⇒ trùng khít, không mất test. 854 là số **đối chiếu**, không phải mục tiêu. |
| A3-088 | IMPLEMENTED | full SMC/scanner / L | Chạy lệnh full §5 có cả acceptance. | Kết quả tổng tách được known acceptance RED khỏi regression khác; ghi command và actual, không gọi toàn hệ thống xanh. → **Xong 2026-09-11:** 73 file (67 `test_smc*.py` + 6 integration) ⇒ **65 failed, 905 passed in 9.48s**, collect **970**; kiểm máy móc `-rf` ⇒ **cả 65 FAILED đều thuộc file acceptance**, 0 failure ở 72 file còn lại; 854 (retained, 0 fail) + 116 (acceptance: 51 pass/65 fail) = 970/905/65 khớp tuyệt đối. 65 RED là gap F02…F10 (§A3.87) — không gọi toàn hệ thống xanh. |
| A3-089 | IMPLEMENTED | Git / L | Kiểm phạm vi diff và fingerprints trước trình. | `git diff --check`, status và inspection cả file untracked; đối chiếu core/probe/R56 với 001. Thay đổi của người khác ghi rõ, không reset/ghi đè. → **Xong 2026-09-11:** `git diff --check` **exit=0** (chỉ cảnh báo LF→CRLF có trước); status **135 = 7 M + 5 D (có trước) + 123 ??**; fingerprint **11/11 khớp A3-001**; lượt này chỉ sửa **4 file untracked**, không thêm/xoá file. `git diff --check`, status và inspection cả file untracked; đối chiếu core/probe/R56 với001. Thay đổi của người khác ghi rõ, không reset/ghi đè. → **Chạy mới trên snapshot trình A lần4 (2026-09-11):** `git status` **136 entry** (khớp baseline ~135 có trước), `git diff --stat` 12 file tracked, `git diff --check` **exit=0** (chỉ cảnh báo LF→CRLF có trước); **11/11 fingerprint KHỚP**; SHA256 `T` **không đổi** `766EC04E…`; `L`/`P` **0** hàng lệch cột, `M` **3** hàng cũ (DEFERRED_OBSERVATION); **0** section trùng số ở cả ba file. Chi tiết: **§A3.127**. |
| A3-090 | **IMPLEMENTED** (TL: checkpoint A lần4 PASS, §A3.129) | M / L / progress tổng | Ghép bản trình A lần3 trong hồ sơ hiện có. | Mapping F01.1…4→A3→file/node/results, các quyết định còn mở, manifest và known RED; chỉ khi đủ mới ghi WAITING_REVIEW cho A3. Quyết định A lần2 giữ lịch sử; không tự PASS/CLOSED/F02. → **Xong 2026-09-11:** bản trình ở **§A3.93** của fix-progress (không tạo file mới): 90/90 mã IMPLEMENTED; manifest 4 file + hash, fingerprint 11/11 khớp A3-001, git 135 (diff --check exit=0); acceptance 116 node (65 failed/51 passed), retained 854 passed, full §5 970/905/65, probe 13 failed/3 passed; known RED phân loại đầy đủ (§A3.87/§A3.88); **6 quyết định còn mở** liệt kê cho TL. Trạng thái A3 ghi **WAITING_REVIEW** — Coder không tự PASS/CLOSED, chưa sang F02. → **Bản trình A lần4 ghép tại §A3.128 (2026-09-11) — trạng thái `WAITING_REVIEW`.** Gồm: mapping 5 finding A3R3 → sửa đổi → evidence; manifest + hash; bảng kết quả 6 nhóm kiểm chứng (acceptance 118/69/49 · probes 13/3 · task57–71 108 · retained 854 · full 69/903); phân loại **69/69 RED** (F02×13, F03×7, F04×10, F05×10, F06×13, F06/F07×2, F07×3, F08×5, F09×1, F10×5 — tất cả `implementation`, **không** regression); 5 giới hạn còn lại; bảng trạng thái refresh A3-081…090. **Quyết định A lần3 giữ lịch sử**; không tự PASS/CLOSED/APPROVED, chưa F02/task73. |

**Refresh A3-081/r1a (2026-09-11, docs-only):** chỉ audit mapping R72-01 sau sửa A lần4. `--collect-only` cho **118 node / 76 hàm**, trong đó **29/29 R72-01** có hàng matrix tương ứng; header R72-01 đã đổi `18` → `29`. Bảy node đổi/thêm A3R3 giữ Expected/Actual/F06/F07 hiện hành. Không đổi R72-02…09 và không refresh snapshot full acceptance `69 failed / 49 passed` (thuộc A3-084/r1). A3-081 giữ **IMPLEMENTED**.

### 8.7. Cách chạy và ghi kết quả cho CODER

Chạy từ root repository. `python` phải là môi trường dự án đã chạy được baseline; ghi interpreter thật vào L, không tự cài/đổi dependency để che lỗi test. Node dưới là ví dụ có sẵn để kiểm một nhóm, không phải một lần chạy đã thực hiện:

```powershell
python -m pytest tests/test_smc_gate72_fix_acceptance.py --collect-only -q
python -m pytest tests/test_smc_gate72_fix_acceptance.py::test_r72_01_positive_pool_keeps_source_lineage_and_usable_time -q --tb=short
```

Dùng full node ID lấy từ collection cho bước đang làm; nếu tên thay đổi, cập nhật M ngay. Acceptance, probes, task57–71 và full integration dùng commands §5. Riêng A3-087 cần tách retained baseline để không nhầm RED mới với regression:

```powershell
$retainedSmcTests = @(rg --files tests -g 'test_smc*.py' | Where-Object { ($_ -replace '\\','/') -ne 'tests/test_smc_gate72_fix_acceptance.py' })
python -m pytest @retainedSmcTests tests/test_technical_signal_scorer.py tests/test_scanner_features.py tests/test_scanner_live_producers.py tests/test_analysis_pipeline_integration.py tests/test_scanner_scenario_producers.py tests/test_scanner_replay.py -q --tb=short
```

Template một dòng L, không thêm file báo cáo:

```text
A3-xxx | TODO/DOING/IMPLEMENTED/BLOCKED | file::function
       | rule reference + command/full node ID | actual + loại RED
       | giữ nguyên vì đã đúng / thay đổi đã làm | việc còn lại/phụ thuộc
```

`IMPLEMENTED` ở checklist này chỉ nghĩa đã hoàn tất việc docs/test/verification của mã đó; có thể kèm `EXPECTED_IMPLEMENTATION_RED`. Trạng thái nghiệm thu A vẫn do Tech Lead quyết định. Nếu chỉ inspect chưa run thì ghi rõ, không đánh xong bước đòi execution evidence.

Khi giao một lượt, dùng nội dung cụ thể, ví dụ:

> Thực hiện A3-001…004 trong §8 của `smc-task-72-fix-plan.md`. Giữ mọi thay đổi có trước; ghi actual vào fix-progress hiện có. Đây là chuẩn bị F01 cho checkpoint A lần3. Chỉ làm dải mã được giao; báo mã đã xong, bằng chứng, blocker và mã kế tiếp. Không sửa core/probe/R56 hoặc tự chuyển F02. Không làm lại F00 đã PASS.

Khi tất cả mã có bằng chứng phù hợp và không còn fixture/interface/test defect chặn A, trình A lần3 một lần. Lỗi hành vi production còn RED được bàn giao cho B/C đúng mapping; yêu cầu mới ngoài review lần2/§4 cần được phân loại và quyết định phạm vi, không tự thêm làm tiêu chí chặn mới.
