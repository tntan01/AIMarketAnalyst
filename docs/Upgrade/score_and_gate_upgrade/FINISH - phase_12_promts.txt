PROMPT 1 — PHASE 12.1: KIỂM TRA HIỆN TRẠNG SAU PHASE 11 VÀ KHÓA PHẠM VI PHASE 12

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Chỉ kiểm tra hiện trạng code sau Phase 11, xác định file được phép sửa trong Phase 12, và bổ sung reason code còn thiếu nếu cần. Chưa tạo logic detector đầy đủ.

Yêu cầu rất quan trọng:
- Chỉ được đọc code và bổ sung constant reason code nếu thiếu.
- Không sửa signal_engine.py.
- Không sửa analysis_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa account_guard.py.
- Không sửa statistical_edge_engine.py.
- Không sửa execution_quality_engine.py trong prompt này, trừ khi chỉ cần xem để hiểu output Phase 11.
- Không sửa services/journal_service.py trong prompt này.
- Không sửa migration/database trong prompt này.
- Không sửa UI/scanner.

Việc cần làm:
1. Kiểm tra các file sau có tồn tại không:
   - core/reason_codes.py
   - core/execution_quality_engine.py
   - services/journal_service.py
   - tests/

2. Mở core/execution_quality_engine.py để hiểu style output hiện tại:
   - execution_quality_score
   - reason_codes
   - penalty_codes
   - warning_codes
   - score_breakdown
   - reason
   Không sửa logic file này.

3. Mở core/reason_codes.py để kiểm tra đã có các code lỗi hành vi chưa. Nếu thiếu thì chỉ bổ sung đúng các constant sau:
   - MISTAKE_ENTERED_TOO_EARLY
   - MISTAKE_CHASED_PRICE
   - MISTAKE_IGNORED_M15
   - MISTAKE_IGNORED_NEWS
   - MISTAKE_MOVED_STOP_LOSS
   - MISTAKE_CLOSED_TOO_EARLY
   - MISTAKE_OVERSIZED_POSITION
   - MISTAKE_REVENGE_TRADE_WARNING
   - MISTAKE_REVENGE_TRADE_CONFIRMED
   - MISTAKE_DATA_INCOMPLETE
   - MISTAKE_DETECTOR_OK

4. Nếu core/reason_codes.py có mapping tiếng Việt như REASON_CODE_MESSAGES thì bổ sung message ngắn cho các code trên, ví dụ:
   - MISTAKE_CHASED_PRICE: "Vào lệnh bị đuổi giá so với kế hoạch."
   - MISTAKE_OVERSIZED_POSITION: "Khối lượng thực tế lớn hơn kế hoạch."
   - MISTAKE_REVENGE_TRADE_CONFIRMED: "Có dấu hiệu revenge trade rõ ràng sau lệnh thua."

5. Không đổi tên code Phase 9/10/11 đã có.
6. Không xóa code cũ.
7. Không refactor lớn.

Test bắt buộc:
- Chạy các test reason code hiện có nếu có:
  python -m pytest tests/test_reason_codes_phase9.py -q
- Nếu vừa thêm code mới thì tạo hoặc cập nhật một test nhỏ, ví dụ:
  tests/test_reason_codes_mistake_detector_phase12.py
  Test chỉ cần import được các constant MISTAKE_* mới.
- Chạy:
  python -m pytest tests/test_reason_codes_mistake_detector_phase12.py -q

Tiêu chí hoàn thành:
- Các constant MISTAKE_* import được.
- Không ảnh hưởng behavior hiện tại.
- Không có lỗi import.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 2 — PHASE 12.2: TẠO MODULE RỖNG core/trade_mistake_detector.py VÀ TEST IMPORT

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Tạo module core/trade_mistake_detector.py có cấu trúc output mặc định an toàn, chưa tự phát hiện lỗi phức tạp.

Yêu cầu rất quan trọng:
- Chỉ được tạo/sửa 2 file:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_import_phase12.py
- Không sửa journal_service.py.
- Không sửa execution_quality_engine.py.
- Không sửa signal_engine.py, analysis_engine.py, trade_gate_engine.py, account_guard.py.
- Không sửa UI/scanner.
- Không tạo migration.
- Không thay đổi quyết định giao dịch.

Việc cần làm:
1. Tạo file mới:
   core/trade_mistake_detector.py

2. Trong file, dùng style import tương thích project. Nếu core/reason_codes.py có constant thì import các code tối thiểu:
   - MISTAKE_DETECTOR_OK
   - MISTAKE_DATA_INCOMPLETE

3. Nếu import trực tiếp từ reason_codes gây lỗi vì tên chưa tồn tại, không hard-code bừa. Quay lại kiểm tra prompt 1 hoặc bổ sung đúng constant ở reason_codes.py. Không tạo duplicate constant trong nhiều file.

4. Tạo hàm:

   def default_mistake_detection_result(reason: str = "no_mistake_detected") -> dict:
       return {
           "auto_mistake_tags": [],
           "manual_mistake_tags": [],
           "mistake_codes": [],
           "warning_codes": [],
           "reason_codes": [MISTAKE_DETECTOR_OK],
           "score_breakdown": {},
           "reason": reason,
       }

5. Tạo hàm chính nhưng hiện tại chỉ trả default:

   def detect_trade_mistakes(trade: dict | None = None, previous_trades: list[dict] | None = None, settings: dict | None = None) -> dict:
       ...

   Yêu cầu:
   - Không raise exception khi trade=None.
   - Không raise exception khi previous_trades=None.
   - Nếu trade không phải dict thì trả default với warning/data incomplete phù hợp.

6. Tạo test:
   tests/test_trade_mistake_detector_import_phase12.py

7. Test cần kiểm tra:
   - import được core.trade_mistake_detector
   - default_mistake_detection_result() có auto_mistake_tags là list rỗng
   - detect_trade_mistakes({}) trả dict
   - detect_trade_mistakes(None) không crash
   - output có đủ key: auto_mistake_tags, manual_mistake_tags, mistake_codes, warning_codes, reason_codes

Test bắt buộc:
python -m pytest tests/test_trade_mistake_detector_import_phase12.py -q

Tiêu chí hoàn thành:
- Module import sạch.
- Không ảnh hưởng module khác.
- Test mới pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 3 — PHASE 12.3: THÊM HELPER ĐỌC SỐ, THỜI GIAN, BOOLEAN VÀ TAG AN TOÀN

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Thêm helper nhỏ trong trade_mistake_detector để đọc dữ liệu journal/trade an toàn trước khi detect lỗi. Chưa viết rule detect cụ thể.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_helpers_phase12.py
- Không sửa journal_service.py.
- Không sửa execution_quality_engine.py.
- Không sửa migration.
- Không sửa UI/scanner.
- Không thay đổi quyết định giao dịch.

Việc cần làm:
1. Trong core/trade_mistake_detector.py, thêm helper:

   def safe_float(value, default: float = 0.0) -> float:
       ...

   Yêu cầu:
   - Nhận int, float, string số như "1.5", " 2.0 ".
   - None, "", "abc", NaN, inf thì trả default.
   - Không raise exception.

2. Thêm helper:

   def safe_datetime(value):
       ...

   Yêu cầu:
   - Nhận datetime object thì trả lại datetime đó.
   - Nhận ISO string như "2026-06-04T09:30:00Z" hoặc "2026-06-04T09:30:00+00:00" thì parse được.
   - Nếu string không timezone thì coi là UTC hoặc xử lý theo style hiện có của project, nhưng phải nhất quán.
   - Value không hợp lệ thì trả None.
   - Không raise exception.

3. Thêm helper:

   def truthy(value) -> bool:
       ...

   Yêu cầu:
   - True, 1, "1", "true", "yes", "y", "có", "co", "đúng", "dung" => True.
   - False, 0, "0", "false", "no", "n", "không", "khong", "sai", None, "" => False.
   - Không raise exception.

4. Thêm helper:

   def normalize_tags(value) -> list[str]:
       ...

   Yêu cầu:
   - list/tuple/set => list string lowercase, strip, bỏ rỗng.
   - JSON string list như '["ignored_m15", "oversized_position"]' => parse được.
   - String phân tách bằng dấu phẩy: "ignored_m15, oversized_position" => tách được.
   - None hoặc dữ liệu bẩn => [].
   - Không raise exception.

5. Thêm helper:

   def add_unique(items: list[str], value: str) -> None:
       ...

   Yêu cầu:
   - Thêm value vào list nếu chưa tồn tại.
   - Bỏ qua value rỗng.
   - Không làm thay đổi thứ tự các phần tử đã có.

6. Tạo test:
   tests/test_trade_mistake_detector_helpers_phase12.py

Test bắt buộc:
- safe_float("1.25") == 1.25
- safe_float("abc", default=-1) == -1
- safe_datetime("2026-06-04T09:30:00Z") không None
- safe_datetime("bad") is None
- truthy("có") is True
- truthy("không") is False
- normalize_tags('["ignored_m15", "oversized_position"]') == ["ignored_m15", "oversized_position"]
- normalize_tags("ignored_m15, oversized_position") == ["ignored_m15", "oversized_position"]
- add_unique không thêm trùng.

Chạy:
python -m pytest tests/test_trade_mistake_detector_helpers_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 4 — PHASE 12.4: DETECT OVERSIZED POSITION

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Thêm rule tự phát hiện lỗi oversized_position: actual_lot lớn hơn planned_lot quá ngưỡng.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_oversized_phase12.py
- Không sửa execution_quality_engine.py.
- Không sửa journal_service.py.
- Không sửa migration.
- Không sửa decision/gate/scoring.
- Không sửa UI/scanner.

Việc cần làm:
1. Trong core/trade_mistake_detector.py, thêm constant setting mặc định nếu chưa có:
   DEFAULT_SETTINGS = {
       "oversized_lot_multiplier": 1.2,
       ...
   }
   Nếu đã có DEFAULT_SETTINGS thì chỉ bổ sung key này, không đổi các key khác.

2. Thêm helper hoặc rule:

   def detect_oversized_position(trade: dict, settings: dict | None = None) -> dict:
       ...

3. Logic:
   - planned_lot = safe_float(trade.get("planned_lot"), default=0.0)
   - actual_lot = safe_float(trade.get("actual_lot"), default=0.0)
   - threshold = settings.get("oversized_lot_multiplier", 1.2)
   - Nếu planned_lot <= 0 hoặc actual_lot <= 0 thì không gắn lỗi, nhưng có thể warning data incomplete nếu cần.
   - Nếu actual_lot > planned_lot * threshold thì gắn:
     auto_mistake_tags: ["oversized_position"]
     mistake_codes: [MISTAKE_OVERSIZED_POSITION]
   - Nếu actual_lot <= planned_lot * threshold thì không gắn lỗi.

4. Tích hợp rule này vào detect_trade_mistakes().

5. Output vẫn giữ key chuẩn:
   - auto_mistake_tags
   - manual_mistake_tags
   - mistake_codes
   - warning_codes
   - reason_codes
   - score_breakdown
   - reason

6. score_breakdown nên có thông tin tối thiểu:
   "oversized_position": {
       "planned_lot": ...,
       "actual_lot": ...,
       "threshold_multiplier": ...,
       "triggered": True/False
   }

7. Test với dữ liệu giống thật:
   - planned_lot=0.10, actual_lot=0.13 => oversized_position.
   - planned_lot=0.10, actual_lot=0.12 => không trigger nếu dùng điều kiện > 1.2, không dùng >=.
   - planned_lot=0 hoặc None => không crash.

Chạy:
python -m pytest tests/test_trade_mistake_detector_oversized_phase12.py -q

Tiêu chí hoàn thành:
- Detect đúng oversized.
- Không gắn lỗi khi thiếu dữ liệu.
- Không ảnh hưởng module khác.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 5 — PHASE 12.5: DETECT MOVED STOP LOSS FURTHER

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Thêm rule tự phát hiện lỗi moved_stop_loss khi trader dời SL xa hơn kế hoạch, làm tăng rủi ro.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_moved_sl_phase12.py
- Không sửa risk_engine.py.
- Không sửa account_guard.py.
- Không sửa trade_gate_engine.py.
- Không sửa execution_quality_engine.py.
- Không sửa journal_service.py/migration.
- Không sửa UI/scanner.

Việc cần làm:
1. Thêm rule:

   def detect_moved_stop_loss(trade: dict, settings: dict | None = None) -> dict:
       ...

2. Dữ liệu đầu vào dự kiến:
   - direction: "buy" hoặc "sell" hoặc selected_scenario tương tự.
   - planned_sl hoặc stop_loss kế hoạch.
   - actual_sl.
   - Có thể có planned_entry/actual_entry nhưng prompt này chưa cần.

3. Logic theo yêu cầu:
   - Với BUY: nếu actual_sl < planned_sl thì SL bị dời xa hơn xuống dưới => lỗi moved_stop_loss.
   - Với SELL: nếu actual_sl > planned_sl thì SL bị dời xa hơn lên trên => lỗi moved_stop_loss.
   - Nếu thiếu direction hoặc thiếu planned_sl/actual_sl thì không crash, không trigger.
   - Chấp nhận direction viết hoa/thường: "BUY", "buy", "Sell", "sell".
   - Nếu direction là "long" coi như buy; "short" coi như sell.

4. Khi trigger, gắn:
   - auto_mistake_tags: "moved_stop_loss"
   - mistake_codes: MISTAKE_MOVED_STOP_LOSS

5. score_breakdown thêm:
   "moved_stop_loss": {
       "direction": ...,
       "planned_sl": ...,
       "actual_sl": ...,
       "triggered": True/False
   }

6. Tích hợp vào detect_trade_mistakes().

7. Test bắt buộc:
   - Buy planned_sl=1.0800, actual_sl=1.0780 => trigger.
   - Buy planned_sl=1.0800, actual_sl=1.0810 => không trigger.
   - Sell planned_sl=1.0950, actual_sl=1.0980 => trigger.
   - Sell planned_sl=1.0950, actual_sl=1.0930 => không trigger.
   - Thiếu actual_sl => không crash.

Chạy:
python -m pytest tests/test_trade_mistake_detector_moved_sl_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 6 — PHASE 12.6: DETECT REVENGE TRADE WARNING DỰA TRÊN THỜI GIAN SAU LỆNH THUA

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Thêm phát hiện revenge_trade_warning khi lệnh mới được mở quá gần sau một lệnh thua trước đó.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_revenge_time_phase12.py
- Không sửa account_guard.py.
- Không sửa journal_service.py.
- Không sửa execution_quality_engine.py.
- Không sửa scoring/gate/decision.
- Không sửa UI/scanner.

Việc cần làm:
1. Bổ sung DEFAULT_SETTINGS nếu chưa có:
   - revenge_trade_minutes: 5

2. Thêm helper:

   def is_loss_trade(trade: dict) -> bool:
       ...

   Logic:
   - Ưu tiên result_r nếu có: result_r < 0 là lệnh thua.
   - Nếu result_r không có, dùng result_pct < 0.
   - Nếu result/result_label có text "loss", "thua" thì có thể coi là thua.
   - Dữ liệu thiếu => False.

3. Thêm helper:

   def find_previous_closed_trade(current_trade: dict, previous_trades: list[dict] | None) -> dict | None:
       ...

   Yêu cầu:
   - Lọc previous_trades là dict có closed_at hợp lệ.
   - Chọn lệnh có closed_at gần nhất trước current_trade.opened_at hoặc entry_time.
   - Nếu current_trade không có opened_at/entry_time/timestamp thì có thể lấy trade.get("opened_at") rồi fallback trade.get("timestamp").
   - Không chọn lệnh đóng sau thời điểm mở lệnh hiện tại.
   - Không crash với dữ liệu bẩn.

4. Thêm rule:

   def detect_revenge_trade_time(trade: dict, previous_trades: list[dict] | None = None, settings: dict | None = None) -> dict:
       ...

5. Logic:
   - Nếu previous_trade là lệnh thua.
   - Và current_open_time - previous_closed_at <= revenge_trade_minutes.
   - Thì gắn:
     auto_mistake_tags: "revenge_trade_warning"
     mistake_codes hoặc warning_codes: MISTAKE_REVENGE_TRADE_WARNING
   - Đây mới là warning, chưa confirmed.

6. Tích hợp vào detect_trade_mistakes().

7. Test bằng dữ liệu giống thật:
   - Previous EURUSD loss closed_at 09:00, current opened_at 09:03 => warning.
   - Previous loss closed_at 09:00, current opened_at 09:10 => không warning nếu threshold 5 phút.
   - Previous win closed_at 09:00, current opened_at 09:03 => không warning.
   - previous_trades rỗng => không crash.

Chạy:
python -m pytest tests/test_trade_mistake_detector_revenge_time_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 7 — PHASE 12.7: DETECT REVENGE TRADE WARNING DỰA TRÊN TĂNG LOT SAU LỆNH THUA

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Thêm phát hiện revenge_trade_warning khi sau lệnh thua, trader mở lệnh mới với actual_lot lớn hơn nhiều so với lệnh trước.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_revenge_lot_phase12.py
- Không sửa account_guard.py.
- Không sửa execution_quality_engine.py.
- Không sửa journal_service.py.
- Không sửa scoring/gate/decision.
- Không sửa UI/scanner.

Việc cần làm:
1. Bổ sung DEFAULT_SETTINGS nếu chưa có:
   - revenge_lot_multiplier: 1.5

2. Thêm rule:

   def detect_revenge_trade_lot(trade: dict, previous_trades: list[dict] | None = None, settings: dict | None = None) -> dict:
       ...

3. Logic:
   - Dùng find_previous_closed_trade() đã có.
   - previous_trade phải là lệnh thua theo is_loss_trade().
   - previous_lot lấy theo thứ tự: actual_lot, planned_lot, suggested_lot.
   - current_lot lấy theo thứ tự: actual_lot, planned_lot, suggested_lot.
   - Nếu previous_lot > 0 và current_lot > previous_lot * revenge_lot_multiplier thì gắn warning:
     auto_mistake_tags: "revenge_trade_warning"
     warning_codes hoặc mistake_codes: MISTAKE_REVENGE_TRADE_WARNING
   - Đây chỉ là warning nếu chỉ có tăng lot, chưa confirmed nếu chưa kết hợp thời gian.

4. score_breakdown thêm:
   "revenge_trade_lot": {
       "previous_lot": ...,
       "current_lot": ...,
       "threshold_multiplier": ...,
       "triggered": True/False
   }

5. Không tạo tag trùng nếu prompt 6 đã tạo warning theo thời gian.

6. Test:
   - Previous loss actual_lot=0.10, current actual_lot=0.20 => warning với threshold 1.5.
   - Previous loss 0.10, current 0.14 => không warning.
   - Previous win 0.10, current 0.20 => không warning.
   - previous_lot thiếu hoặc 0 => không crash.

Chạy:
python -m pytest tests/test_trade_mistake_detector_revenge_lot_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 8 — PHASE 12.8: DETECT REVENGE TRADE CONFIRMED KHI VỪA GẦN THỜI GIAN VỪA TĂNG LOT

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Nâng rule revenge trade: nếu sau lệnh thua, lệnh mới vừa mở quá sớm vừa tăng lot mạnh thì gắn revenge_trade_confirmed.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_revenge_confirmed_phase12.py
- Không sửa account_guard.py.
- Không sửa execution_quality_engine.py.
- Không sửa journal_service.py.
- Không sửa scoring/gate/decision.
- Không sửa UI/scanner.

Việc cần làm:
1. Tận dụng logic của prompt 6 và prompt 7.
2. Thêm rule tổng hợp:

   def detect_revenge_trade_confirmed(trade: dict, previous_trades: list[dict] | None = None, settings: dict | None = None) -> dict:
       ...

3. Logic:
   - previous_trade là lệnh thua.
   - current_open_time - previous_closed_at <= revenge_trade_minutes.
   - current_lot > previous_lot * revenge_lot_multiplier.
   - Nếu cả 2 điều kiện đúng thì:
     auto_mistake_tags thêm "revenge_trade_confirmed".
     mistake_codes thêm MISTAKE_REVENGE_TRADE_CONFIRMED.
   - Có thể vẫn giữ "revenge_trade_warning" nếu đã có, nhưng không được duplicate.

4. Nếu chỉ có 1 điều kiện đúng thì chỉ warning, không confirmed.
5. Nếu previous_trade không phải loss thì không trigger.

6. score_breakdown thêm:
   "revenge_trade_confirmed": {
       "time_condition": True/False,
       "lot_condition": True/False,
       "triggered": True/False
   }

7. Test:
   - Previous loss closed_at 09:00 lot 0.10, current opened_at 09:03 lot 0.20 => confirmed.
   - Previous loss 09:00 lot 0.10, current 09:03 lot 0.12 => warning time, không confirmed.
   - Previous loss 09:00 lot 0.10, current 09:10 lot 0.20 => warning lot, không confirmed.
   - Previous win 09:00 lot 0.10, current 09:03 lot 0.20 => không warning/confirmed.

Chạy:
python -m pytest tests/test_trade_mistake_detector_revenge_confirmed_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 9 — PHASE 12.9: DETECT IGNORED M15

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Tự phát hiện lỗi ignored_m15 khi trader vẫn vào lệnh trong khi M15 chưa xác nhận hoặc entry_status không phải confirmed_entry.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_ignored_m15_phase12.py
- Không sửa entry_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa analysis_engine.py.
- Không sửa execution_quality_engine.py.
- Không sửa journal_service.py/migration.
- Không sửa UI/scanner.

Việc cần làm:
1. Thêm rule:

   def detect_ignored_m15(trade: dict, settings: dict | None = None) -> dict:
       ...

2. Dữ liệu đầu vào có thể nằm ở trade hoặc analysis_json đã parse:
   - m15_quality
   - entry_status
   - user_action
   - action
   - order_status
   - opened_at hoặc actual_entry

3. Logic:
   - Chỉ detect nếu trader thật sự đã vào lệnh. Xác định bằng một trong các dấu hiệu:
     user_action in {"opened", "open", "entered", "trade_opened", "vào lệnh", "da_vao_lenh"}
     hoặc actual_entry có giá trị > 0
     hoặc opened_at có timestamp.
   - Nếu không có dấu hiệu vào lệnh thì không gắn ignored_m15.
   - Nếu m15_quality == "none" => ignored_m15.
   - Nếu entry_status in {"watch_zone", "waiting_confirmation"} mà vẫn vào lệnh => ignored_m15.
   - Nếu m15_quality == "strict" hoặc entry_status == "confirmed_entry" => không trigger.

4. Khi trigger:
   - auto_mistake_tags: "ignored_m15"
   - mistake_codes: MISTAKE_IGNORED_M15

5. score_breakdown thêm:
   "ignored_m15": {
       "m15_quality": ...,
       "entry_status": ...,
       "has_real_entry": True/False,
       "triggered": True/False
   }

6. Test:
   - actual_entry=1.0850, m15_quality="none" => trigger.
   - user_action="opened", entry_status="waiting_confirmation" => trigger.
   - m15_quality="strict", entry_status="confirmed_entry" => không trigger.
   - m15 none nhưng chưa vào lệnh thật => không trigger.

Chạy:
python -m pytest tests/test_trade_mistake_detector_ignored_m15_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 10 — PHASE 12.10: DETECT IGNORED NEWS

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Tự phát hiện lỗi ignored_news khi trader vào lệnh trong lúc có tin mạnh gần hoặc gate/news đã cảnh báo/chặn.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_ignored_news_phase12.py
- Không sửa news_service.py.
- Không sửa trade_gate_engine.py.
- Không sửa analysis_engine.py.
- Không sửa scoring/gate/decision.
- Không sửa UI/scanner.

Việc cần làm:
1. Thêm rule:

   def detect_ignored_news(trade: dict, settings: dict | None = None) -> dict:
       ...

2. Rule này không gọi internet, không gọi news API. Chỉ dùng dữ liệu đã có trong trade/journal/analysis.

3. Dữ liệu đầu vào có thể là:
   - high_impact_news_within_30m: True/False
   - high_impact_event_within_30m: True/False
   - news_risk: "high" / "blocked" / "nearby"
   - gate_result.block_codes chứa HIGH_IMPACT_NEWS_NEARBY
   - block_codes chứa HIGH_IMPACT_NEWS_NEARBY
   - warning_codes chứa HIGH_IMPACT_NEWS_NEARBY

4. Chỉ detect nếu trader thật sự đã vào lệnh, dùng helper has_real_entry nếu đã có hoặc tạo helper nhỏ:
   - actual_entry > 0
   - opened_at có timestamp
   - user_action/action thể hiện đã mở lệnh.

5. Nếu đã vào lệnh và có news warning/block thì trigger:
   - auto_mistake_tags: "ignored_news"
   - mistake_codes: MISTAKE_IGNORED_NEWS

6. Nếu chưa vào lệnh thật thì không trigger, dù có news warning.

7. score_breakdown thêm:
   "ignored_news": {
       "has_real_entry": True/False,
       "news_risk_detected": True/False,
       "triggered": True/False
   }

8. Test:
   - actual_entry có giá trị, block_codes=["HIGH_IMPACT_NEWS_NEARBY"] => trigger.
   - user_action="opened", high_impact_news_within_30m=True => trigger.
   - high_impact_news=True nhưng chưa vào lệnh => không trigger.
   - vào lệnh nhưng news_risk bình thường => không trigger.

Chạy:
python -m pytest tests/test_trade_mistake_detector_ignored_news_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 11 — PHASE 12.11: DETECT CHASED PRICE

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Tự phát hiện lỗi chased_price khi actual_entry xấu hơn planned_entry/entry_zone quá ngưỡng cho phép.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_chased_price_phase12.py
- Không sửa entry_engine.py.
- Không sửa risk_engine.py.
- Không sửa execution_quality_engine.py.
- Không sửa journal_service.py/migration.
- Không sửa scoring/gate/decision.
- Không sửa UI/scanner.

Việc cần làm:
1. Bổ sung DEFAULT_SETTINGS nếu chưa có:
   - chased_price_tolerance_pct: 0.10
   Nghĩa là lệch quá 0.10% so với planned_entry thì coi là đuổi giá trong MVP.

2. Thêm helper:

   def normalize_direction(value: object) -> str:
       ...

   Logic:
   - "buy", "BUY", "long" => "buy"
   - "sell", "SELL", "short" => "sell"
   - Không rõ => ""

3. Thêm rule:

   def detect_chased_price(trade: dict, settings: dict | None = None) -> dict:
       ...

4. Logic:
   - planned_entry lấy từ planned_entry, entry, suggested_entry nếu có.
   - actual_entry lấy từ actual_entry.
   - direction lấy từ direction hoặc selected_scenario.
   - Nếu thiếu planned_entry/actual_entry/direction thì không trigger.
   - Với BUY: actual_entry > planned_entry * (1 + tolerance_pct/100) => chased_price.
   - Với SELL: actual_entry < planned_entry * (1 - tolerance_pct/100) => chased_price.
   - Nếu actual_entry tốt hơn kế hoạch thì không trigger.

5. Khi trigger:
   - auto_mistake_tags: "chased_price"
   - mistake_codes: MISTAKE_CHASED_PRICE

6. score_breakdown thêm:
   "chased_price": {
       "direction": ...,
       "planned_entry": ...,
       "actual_entry": ...,
       "tolerance_pct": ...,
       "triggered": True/False
   }

7. Test:
   - Buy planned_entry=1.1000, actual_entry=1.1020, tolerance 0.10% => trigger.
   - Buy planned_entry=1.1000, actual_entry=1.1005 => không trigger.
   - Sell planned_entry=1.1000, actual_entry=1.0980, tolerance 0.10% => trigger.
   - Sell planned_entry=1.1000, actual_entry=1.0995 => không trigger.
   - Thiếu dữ liệu => không crash.

Chạy:
python -m pytest tests/test_trade_mistake_detector_chased_price_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 12 — PHASE 12.12: DETECT ENTERED TOO EARLY

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Tự phát hiện lỗi entered_too_early khi trader vào lệnh trước khi entry_status cho phép hoặc giá chưa tới entry zone.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_entered_too_early_phase12.py
- Không sửa entry_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa analysis_engine.py.
- Không sửa UI/scanner.
- Không thay đổi decision/gate.

Việc cần làm:
1. Thêm rule:

   def detect_entered_too_early(trade: dict, settings: dict | None = None) -> dict:
       ...

2. Dữ liệu có thể có:
   - entry_status
   - in_entry_zone
   - price_in_entry_zone
   - actual_entry
   - opened_at
   - user_action/action

3. Logic:
   - Chỉ detect nếu trader đã vào lệnh thật.
   - Nếu entry_status in {"watch_zone", "waiting_confirmation"} mà vẫn vào lệnh => entered_too_early.
   - Nếu in_entry_zone hoặc price_in_entry_zone là False mà vẫn vào lệnh => entered_too_early.
   - Nếu entry_status == "confirmed_entry" và in_entry_zone không False => không trigger.
   - Nếu thiếu dữ liệu entry_status và zone thì không trigger, chỉ có thể warning data incomplete nếu cần.

4. Khi trigger:
   - auto_mistake_tags: "entered_too_early"
   - mistake_codes: MISTAKE_ENTERED_TOO_EARLY

5. score_breakdown thêm:
   "entered_too_early": {
       "has_real_entry": True/False,
       "entry_status": ...,
       "in_entry_zone": ...,
       "triggered": True/False
   }

6. Test:
   - user_action="opened", entry_status="waiting_confirmation" => trigger.
   - actual_entry có giá trị, in_entry_zone=False => trigger.
   - confirmed_entry + in_entry_zone=True => không trigger.
   - chưa vào lệnh => không trigger.

Chạy:
python -m pytest tests/test_trade_mistake_detector_entered_too_early_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 13 — PHASE 12.13: DETECT CLOSED TOO EARLY

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Tự phát hiện lỗi closed_too_early khi trader đóng non trước TP/plan mà không có lý do hợp lệ.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_closed_too_early_phase12.py
- Không sửa journal_service.py/migration.
- Không sửa execution_quality_engine.py.
- Không sửa risk_engine.py.
- Không sửa scoring/gate/decision.
- Không sửa UI/scanner.

Việc cần làm:
1. Bổ sung DEFAULT_SETTINGS nếu chưa có:
   - closed_too_early_min_planned_r_multiple: 1.0
   - closed_too_early_actual_r_threshold: 0.5

2. Thêm rule:

   def detect_closed_too_early(trade: dict, settings: dict | None = None) -> dict:
       ...

3. Logic MVP:
   - Chỉ detect với lệnh đã đóng: closed_at có giá trị hoặc exit_reason có giá trị hoặc actual_exit có giá trị.
   - planned_rr hoặc risk_reward nếu parse được >= 1.0.
   - result_r thực tế > 0 nhưng < 0.5R thì coi là có khả năng chốt non.
   - Nếu exit_reason thuộc nhóm hợp lệ thì không trigger:
     "news_exit", "manual_risk_reduce", "breakeven", "system_exit", "tp_hit", "sl_hit"
   - Nếu exit_reason chứa "closed_early", "manual_close", "chot_non", "chốt non" thì trigger mạnh hơn.

4. Khi trigger:
   - auto_mistake_tags: "closed_too_early"
   - mistake_codes: MISTAKE_CLOSED_TOO_EARLY

5. score_breakdown thêm:
   "closed_too_early": {
       "result_r": ...,
       "risk_reward": ...,
       "exit_reason": ...,
       "triggered": True/False
   }

6. Test:
   - result_r=0.25, risk_reward=2.0, exit_reason="manual_close" => trigger.
   - result_r=0.25, risk_reward=2.0, exit_reason="news_exit" => không trigger.
   - result_r=1.2, risk_reward=2.0 => không trigger.
   - lệnh chưa đóng => không trigger.
   - dữ liệu bẩn => không crash.

Chạy:
python -m pytest tests/test_trade_mistake_detector_closed_too_early_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 14 — PHASE 12.14: GỘP TAG THỦ CÔNG VÀ TAG TỰ ĐỘNG, KHÔNG TRÙNG LẶP

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Chuẩn hóa output detect_trade_mistakes để luôn trả cả auto_mistake_tags và manual_mistake_tags, không bị trùng tag, phục vụ journal sau này.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_tag_merge_phase12.py
- Không sửa journal_service.py.
- Không sửa execution_quality_engine.py.
- Không sửa database/migration.
- Không sửa UI/scanner.
- Không thay đổi scoring/gate/decision.

Việc cần làm:
1. Trong detect_trade_mistakes(), đọc manual tags từ các key có thể có:
   - manual_mistake_tags
   - mistake_tags
   - user_mistake_tags

2. Dùng normalize_tags() để chuẩn hóa:
   - lowercase
   - strip
   - bỏ rỗng
   - không duplicate.

3. Output cần có:
   - auto_mistake_tags: chỉ tag do detector tự phát hiện.
   - manual_mistake_tags: tag do người dùng nhập/journal có sẵn.
   - all_mistake_tags: hợp nhất auto + manual, không trùng.

4. Không để manual tag tự động tạo mistake_codes nếu chưa có rule mapping rõ. Prompt này chỉ merge tag.

5. Nếu auto_mistake_tags có "oversized_position" và manual_mistake_tags cũng có "oversized_position" thì all_mistake_tags chỉ có 1 lần.

6. Test:
   - trade có manual_mistake_tags='["ignored_m15", "oversized_position"]'
   - detector tự phát hiện oversized_position.
   - output manual_mistake_tags chứa 2 tag.
   - output auto_mistake_tags chứa oversized_position.
   - output all_mistake_tags không trùng.

Chạy:
python -m pytest tests/test_trade_mistake_detector_tag_merge_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 15 — PHASE 12.15: THÊM SUMMARY TEXT NGẮN CHO DETECTOR OUTPUT

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Thêm summary tiếng Việt ngắn trong output để UI/log dễ hiển thị, nhưng không sửa UI ở prompt này.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_summary_phase12.py
- Không sửa UI.
- Không sửa journal_service.py.
- Không sửa execution_quality_engine.py.
- Không sửa scoring/gate/decision.

Việc cần làm:
1. Thêm helper:

   def build_mistake_summary(result: dict) -> str:
       ...

2. Logic:
   - Nếu không có auto_mistake_tags và không có manual_mistake_tags:
     trả "Không phát hiện lỗi hành vi rõ ràng."
   - Nếu có auto_mistake_tags:
     trả câu ngắn liệt kê tag chính bằng tiếng Việt dễ hiểu.
   - Nếu có manual_mistake_tags:
     có thể thêm "Có tag thủ công từ journal."
   - Không dài quá 2 câu.
   - Không bịa thông tin ngoài tag/code.

3. Mapping tiếng Việt nội bộ đơn giản:
   - oversized_position => "vào khối lượng lớn hơn kế hoạch"
   - moved_stop_loss => "dời stop loss làm tăng rủi ro"
   - revenge_trade_warning => "có dấu hiệu revenge trade"
   - revenge_trade_confirmed => "revenge trade rõ ràng"
   - ignored_m15 => "bỏ qua xác nhận M15"
   - ignored_news => "bỏ qua cảnh báo tin tức"
   - chased_price => "đuổi giá"
   - entered_too_early => "vào lệnh quá sớm"
   - closed_too_early => "chốt lệnh quá sớm"

4. detect_trade_mistakes() cần thêm key:
   - summary

5. Test:
   - Không có tag => summary đúng câu mặc định.
   - Có oversized_position và ignored_m15 => summary chứa ý nghĩa tiếng Việt.
   - Có manual tag nhưng không auto tag => summary không rỗng.

Chạy:
python -m pytest tests/test_trade_mistake_detector_summary_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 16 — PHASE 12.16: TÍCH HỢP NHẸ VỚI execution_quality_engine QUA INPUT TAG, KHÔNG ĐỔI CÔNG THỨC ĐIỂM

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Đảm bảo output của trade_mistake_detector có thể đưa vào execution_quality_engine ở Phase 11 mà không cần sửa scoring/decision. Chỉ tích hợp ở mức helper/test, không thay đổi công thức điểm nếu không cần.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_execution_quality_compat_phase12.py
- Chỉ được sửa core/execution_quality_engine.py nếu thực sự cần để nó đọc auto_mistake_tags/all_mistake_tags đã có, và phải sửa tối thiểu.
- Không sửa signal_engine.py.
- Không sửa analysis_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa journal_service.py/migration.
- Không sửa UI/scanner.
- Không làm Phase 13 final_score.

Việc cần làm:
1. Tạo helper trong trade_mistake_detector:

   def apply_detected_mistakes_to_trade(trade: dict, detection_result: dict) -> dict:
       ...

2. Logic:
   - Trả về bản copy của trade, không mutate input gốc.
   - Gắn:
     auto_mistake_tags
     manual_mistake_tags
     all_mistake_tags
     mistake_codes
   - Không xóa field cũ của trade.

3. Nếu execution_quality_engine Phase 11 đã có logic đọc các tag như chased_price/oversized_position/moved_sl/revenge_trade thì test compat chỉ cần:
   - detect_trade_mistakes tạo tag.
   - apply_detected_mistakes_to_trade gắn tag vào trade copy.
   - calculate_execution_quality(trade_copy) có thể đọc được và trả score <= 100.

4. Nếu execution_quality_engine chưa đọc all_mistake_tags mà chỉ đọc boolean flag, KHÔNG refactor lớn. Chỉ test helper output trong prompt này và ghi chú để prompt sau xử lý.

5. Test:
   - Input trade có planned_lot=0.10, actual_lot=0.20.
   - detect_trade_mistakes() ra oversized_position.
   - apply_detected_mistakes_to_trade() trả trade mới có all_mistake_tags chứa oversized_position.
   - Trade gốc không bị mutate.

Chạy:
python -m pytest tests/test_trade_mistake_detector_execution_quality_compat_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 17 — PHASE 12.17: TÙY CHỌN TÍCH HỢP VÀO JOURNAL SERVICE Ở MỨC READ/UTILITY, KHÔNG MIGRATION

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Nếu cần, thêm utility đọc trade từ journal và chạy detector trên dict trade, nhưng không thay đổi schema database. Prompt này không bắt buộc nếu hệ thống chưa có chỗ dùng.

Yêu cầu rất quan trọng:
- Chỉ được sửa nếu thật sự cần:
  1. services/journal_service.py
  2. tests/test_journal_trade_mistake_detector_utility_phase12.py
- Ưu tiên KHÔNG sửa journal_service.py nếu có thể test trực tiếp bằng dict trade.
- Không tạo migration.
- Không thêm cột auto_mistake_tags/manual_mistake_tags vào DB trong prompt này.
- Không sửa UI.
- Không sửa scoring/gate/decision.

Việc cần làm nếu quyết định cần tích hợp nhẹ:
1. Thêm method không phá vỡ API cũ, ví dụ:

   def detect_mistakes_for_entry(self, entry_id: int) -> dict | None:
       ...

2. Method này:
   - get_entry(entry_id)
   - chuyển JournalEntry sang dict bằng asdict hoặc helper có sẵn.
   - nếu analysis_json có thể parse JSON thì merge thêm dữ liệu cần thiết, nhưng không bắt buộc.
   - gọi core.trade_mistake_detector.detect_trade_mistakes(trade_dict, previous_trades=...)
   - trả result, không ghi database.

3. Nếu cần previous_trades:
   - dùng list_closed_trades_for_account_guard(limit=50) nếu đã có.
   - Không sửa logic account_guard.

4. Nếu không tích hợp journal_service, tạo test utility độc lập trong tests dùng dict trade trực tiếp và bỏ qua sửa file service.

Test nếu có sửa journal_service:
- Tạo sqlite temp db như các test journal hiện có.
- Insert 1 JournalEntry mẫu có planned_lot/actual_lot nếu dataclass hỗ trợ.
- Gọi detect_mistakes_for_entry().
- Không ghi DB.

Chạy:
python -m pytest tests/test_journal_trade_mistake_detector_utility_phase12.py -q

Tiêu chí hoàn thành:
- Không thay đổi schema.
- Không làm vỡ create/list journal cũ.
- Detector chạy được ở mức utility nếu cần.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 18 — PHASE 12.18: TEST REALISTIC TỔNG HỢP CHO TRADE MISTAKE DETECTOR

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Tạo test tổng hợp bằng dữ liệu giao dịch giả nhưng giống thật để đảm bảo các rule Phase 12 phối hợp đúng và không duplicate tag/code.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_trade_mistake_detector_realistic_phase12.py
  2. core/trade_mistake_detector.py nếu cần sửa bug nhỏ phát hiện từ test.
- Không sửa engine khác.
- Không sửa journal_service.py/migration.
- Không sửa UI/scanner.
- Không thay đổi decision/gate/scoring.

Việc cần làm:
1. Tạo test file:
   tests/test_trade_mistake_detector_realistic_phase12.py

2. Case 1 — Trade xấu nhiều lỗi:
   current_trade = {
       "symbol": "EURUSD",
       "direction": "buy",
       "planned_lot": 0.10,
       "actual_lot": 0.20,
       "planned_sl": 1.0800,
       "actual_sl": 1.0780,
       "planned_entry": 1.0850,
       "actual_entry": 1.0880,
       "opened_at": "2026-06-04T09:03:00Z",
       "entry_status": "waiting_confirmation",
       "m15_quality": "none",
       "high_impact_news_within_30m": True,
       "closed_at": "2026-06-04T09:25:00Z",
       "result_r": 0.25,
       "risk_reward": "2.0",
       "exit_reason": "manual_close",
   }
   previous_trades = [
       {
           "symbol": "GBPJPY",
           "result_r": -1.0,
           "result_pct": -1.2,
           "closed_at": "2026-06-04T09:00:00Z",
           "actual_lot": 0.10,
       }
   ]

   Kỳ vọng output có các tag:
   - oversized_position
   - moved_stop_loss
   - chased_price
   - ignored_m15
   - ignored_news
   - entered_too_early
   - closed_too_early
   - revenge_trade_warning
   - revenge_trade_confirmed

3. Case 2 — Trade tốt, không lỗi:
   - direction buy
   - planned_lot=actual_lot=0.10
   - actual_sl không xa hơn planned_sl
   - actual_entry gần planned_entry
   - entry_status="confirmed_entry"
   - m15_quality="strict"
   - không có news
   - previous_trade là win hoặc quá xa thời gian
   Kỳ vọng không có auto_mistake_tags.

4. Case 3 — Dữ liệu thiếu/bẩn:
   - trade có string bẩn, None, missing keys.
   Kỳ vọng không crash.

5. Kiểm tra không duplicate:
   - len(all_mistake_tags) == len(set(all_mistake_tags))
   - len(mistake_codes) == len(set(mistake_codes)) nếu logic dùng unique.

Chạy:
python -m pytest tests/test_trade_mistake_detector_realistic_phase12.py -q

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 19 — PHASE 12.19: CHẠY FULL TEST LIÊN QUAN PHASE 12 VÀ KIỂM TRA KHÔNG ẢNH HƯỞNG PHASE CŨ

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Chạy test tổng hợp sau khi đã hoàn thành các rule Phase 12, sửa bug nhỏ nếu có, và đảm bảo không làm hỏng Phase 7-11.

Yêu cầu rất quan trọng:
- Không thêm feature mới trong prompt này.
- Chỉ được sửa bug nhỏ trong:
  1. core/trade_mistake_detector.py
  2. tests/test_trade_mistake_detector_*.py
  3. core/reason_codes.py nếu lỗi import constant rõ ràng.
- Không sửa signal_engine.py.
- Không sửa analysis_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa account_guard.py.
- Không sửa statistical_edge_engine.py.
- Không sửa execution_quality_engine.py trừ khi test compat phát hiện bug nhỏ liên quan đọc tag và cần sửa tối thiểu.
- Không sửa UI/scanner.
- Không sửa database/migration.

Việc cần làm:
1. Chạy toàn bộ test Phase 12:
   python -m pytest tests/test_trade_mistake_detector_*phase12.py -q

2. Chạy test reason code Phase 12:
   python -m pytest tests/test_reason_codes_mistake_detector_phase12.py -q
   nếu file tồn tại.

3. Chạy lại các test Phase 11 quan trọng:
   python -m pytest tests/test_execution_quality_engine_*phase11.py -q
   nếu các file tồn tại.

4. Chạy lại các test account guard/gate quan trọng nếu không quá nặng:
   python -m pytest tests/test_account_guard_phase7.py tests/test_trade_gate_engine.py -q
   nếu các file tồn tại.

5. Nếu test fail:
   - Đọc kỹ lỗi.
   - Chỉ sửa đúng bug liên quan Phase 12.
   - Không refactor lớn.
   - Không thay đổi expected behavior của Phase cũ chỉ để test pass.

6. Tạo/ghi chú nhanh trong comment hoặc docstring đầu file core/trade_mistake_detector.py:
   - Module này chỉ phát hiện mistake tag.
   - Không quyết định trade.
   - Không tự thay đổi score/gate.

Tiêu chí hoàn thành:
- Test Phase 12 pass.
- Các test Phase 11/7 quan trọng không bị hỏng do Phase 12.
- Không có sửa đổi ngoài phạm vi.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 20 — PHASE 12.20: RÀ SOÁT CUỐI PHASE 12 VÀ BÁO CÁO HOÀN TẤT

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 11: có core/execution_quality_engine.py để tính execution_quality_score dựa trên dữ liệu lỗi thực thi đã có. Bây giờ bắt đầu Phase 12: TRADE MISTAKE DETECTOR.

Mục tiêu Phase 12:
Tạo module tự phát hiện lỗi hành vi giao dịch từ dữ liệu journal/trade, ví dụ:
- entered_too_early
- chased_price
- ignored_m15
- ignored_news
- moved_stop_loss
- closed_too_early
- oversized_position
- revenge_trade

Nguyên tắc cực kỳ quan trọng:
- Phase 12 chỉ phát hiện và gắn tag lỗi hành vi. Không được thay đổi scoring chính, không được thay đổi final_score, không được thay đổi decision/gate.
- Không được làm Phase 13: không tính final_score mới.
- Không được làm Phase 14: không tạo decision_engine.py.
- Không được sửa UI/scanner nếu prompt không yêu cầu rõ.
- Không được chỉnh sửa lan sang các phần không liên quan.
- Mỗi prompt chỉ làm đúng phạm vi được yêu cầu.

Mục tiêu riêng của prompt này:
Rà soát cuối Phase 12 để chắc chắn trade_mistake_detector đã hoàn chỉnh ở mức MVP, không sửa lan sang Phase 13, và báo cáo đúng yêu cầu.

Yêu cầu rất quan trọng:
- Không viết thêm feature mới.
- Không sửa scoring/final_score/decision.
- Không tạo decision_engine.py.
- Không sửa UI/scanner.
- Không tạo migration mới.
- Chỉ được sửa lỗi nhỏ nếu phát hiện import/test fail rõ ràng.

Checklist rà soát:
1. core/trade_mistake_detector.py tồn tại và có các hàm chính:
   - detect_trade_mistakes
   - default_mistake_detection_result
   - detect_oversized_position
   - detect_moved_stop_loss
   - detect_revenge_trade_time
   - detect_revenge_trade_lot
   - detect_revenge_trade_confirmed
   - detect_ignored_m15
   - detect_ignored_news
   - detect_chased_price
   - detect_entered_too_early
   - detect_closed_too_early

2. Output detect_trade_mistakes có đủ key:
   - auto_mistake_tags
   - manual_mistake_tags
   - all_mistake_tags
   - mistake_codes
   - warning_codes
   - reason_codes
   - score_breakdown
   - summary
   - reason

3. Không duplicate tag/code.
4. Dữ liệu bẩn không làm crash.
5. Không có import vòng tròn.
6. Không thay đổi behavior của:
   - signal_engine.py
   - analysis_engine.py
   - trade_gate_engine.py
   - account_guard.py
   - statistical_edge_engine.py
   - execution_quality_engine.py

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_trade_mistake_detector_*phase12.py -q
- Nếu đủ thời gian và môi trường cho phép, chạy thêm:
  python -m pytest -q
  Nếu full test fail do môi trường thiếu MT5/PyQt6 thì ghi rõ test nào fail do môi trường, không sửa code bừa.

Báo cáo cuối cần nêu ngắn gọn:
- Đã tạo module nào.
- Đã có các nhóm detect nào.
- Test nào đã chạy.
- Có lỗi môi trường nào không.

Sau khi hoàn thành prompt cuối cùng, bắt buộc báo đúng 2 câu sau:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT
ĐÃ HOÀN THÀNH PHASE SỐ 12
