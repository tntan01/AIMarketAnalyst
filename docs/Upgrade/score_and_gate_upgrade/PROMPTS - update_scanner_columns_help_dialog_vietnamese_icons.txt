PROMPTS UI: Cập nhật dialog “Giải thích ý nghĩa” cho các cột dữ liệu trên Bảng kết quả quét

Mục tiêu tổng thể:
- Cập nhật tính năng mô tả các cột dữ liệu trên “Bảng kết quả quét” trong màn hình “Quét thị trường”.
- Dialog cần có kích thước vừa đủ, không quá to, không quá nhỏ, đọc thoải mái.
- Nội dung giải thích phải đầy đủ cho tất cả cột hiện có trong bảng.
- Ưu tiên tiếng Việt, ngắn gọn nhưng rõ nghĩa.
- Mỗi cột cần giải thích các trường hợp giá trị thường gặp.
- Có icon sinh động để người dùng dễ quét thông tin, nhưng không làm UI rối.
- Chỉ sửa UI/dialog giải thích cột, không sửa scoring, scanner, ranking, decision, gate, dữ liệu core.

===============================================================================================================================
PROMPT 1: Khảo sát dialog “Giải thích ý nghĩa” hiện tại và danh sách cột thật trong bảng

Bạn là Deepseek_v4_pro đang lập trình trong VSCode tại repo ai-market-analyst-v2.0.

Vấn đề cần xử lý:
- Màn hình “Quét thị trường” có nút “Giải thích ý nghĩa” cạnh tiêu đề “Bảng kết quả quét”.
- Dialog hiện tại cần được cập nhật để mô tả đầy đủ các cột dữ liệu trong “Bảng kết quả quét”.
- Nội dung phải dễ hiểu, ưu tiên tiếng Việt, có icon sinh động, kích thước dialog vừa đủ.

Hãy khảo sát thật kỹ trước khi sửa:
1. Mở và đọc file:
   - ui/screens/scanner_screen.py
2. Tìm và đọc kỹ:
   - ScannerTableModel.COLUMNS
   - ScannerColumnsHelpDialog
   - COLUMN_HELP
   - _show_columns_help()
   - help_button
   - mọi chỗ liên quan tới QDialog, QTableWidget, QDialogButtonBox trong dialog này.
3. Liệt kê danh sách cột thật theo ScannerTableModel.COLUMNS, không đoán bằng mắt.
   Hiện các cột dự kiến gồm:
   - Xếp hạng
   - Mã
   - Hành động
   - Xu hướng
   - Entry
   - Quyền
   - Điểm tốt nhất
   - Final
   - Cơ hội
   - Nhóm
   - Trạng thái entry
   - M15
   - Gap
   - Điểm mua
   - Điểm bán
   - Vĩ mô (0-30)
   - Thuận vĩ mô
   - R:R
   - Lý do chính
   - Chi tiết
4. Xác định dialog hiện tại đang thiếu cột nào hoặc mô tả nào chưa đúng với tên cột mới:
   - “Xu hướng” thay cho “Thiên hướng”.
   - “Trạng thái entry” thay cho “Entry status”.
   - Cột mới như Final, Cơ hội, Nhóm, M15, Gap nếu chưa được giải thích đầy đủ.
5. Kiểm tra có style/objectName nào đang dùng:
   - ScannerHelpDialog
   - DataTable
   - HelperText
   - SecondaryButton
6. Không sửa code ở prompt này, chỉ khảo sát và ghi kế hoạch.

Phạm vi được phép sửa ở các prompt sau:
- ui/screens/scanner_screen.py
- tests hoặc scripts mới liên quan trực tiếp đến ScannerColumnsHelpDialog.

Phạm vi cấm sửa:
- Không sửa core/analysis_engine.py.
- Không sửa core/scanner.py.
- Không sửa core/scanner_ranking_engine.py.
- Không sửa core/decision_engine.py.
- Không sửa core/signal_engine.py.
- Không sửa thuật toán score/gate/ranking/decision.
- Không đổi dữ liệu row.
- Không đổi thứ tự cột bảng.
- Không sửa progress bar, symbol grid, nút Quét thị trường, nút Xem chi tiết, nút Lưu snapshot.
- Không làm lại toàn bộ màn hình Quét thị trường.

Kết quả cần báo:
- Danh sách cột thật lấy từ ScannerTableModel.COLUMNS.
- Dialog hiện tại thiếu gì.
- Kế hoạch sửa UI/dialog.
- Test/script sẽ thêm để kiểm tra.

Sau khi hoàn thành prompt này, bắt buộc báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT

===============================================================================================================================
PROMPT 2: Thiết kế lại nội dung COLUMN_HELP đầy đủ, tiếng Việt rõ ràng, có icon cho từng cột

Bạn là Deepseek_v4_pro đang lập trình trong VSCode tại repo ai-market-analyst-v2.0.

Dựa trên khảo sát ở prompt trước, hãy cập nhật dữ liệu nội dung giúp người dùng hiểu “Bảng kết quả quét”.

Yêu cầu nội dung:
1. Mỗi cột trong ScannerTableModel.COLUMNS phải có một mục giải thích tương ứng trong dialog.
2. Tên cột trong dialog phải khớp với header hiển thị trên bảng.
3. Nội dung ưu tiên tiếng Việt, ngắn gọn, dễ hiểu.
4. Mỗi mục nên có icon sinh động ở đầu tên cột hoặc trong cột icon riêng.
5. Icon có thể dùng Unicode/icon ký hiệu đơn giản, ví dụ:
   - Xếp hạng: 🏁 hoặc #
   - Mã: 💱 hoặc FX
   - Hành động: ▶
   - Xu hướng: 🧭
   - Entry: 🎯
   - Quyền: 🛡
   - Điểm tốt nhất: ⭐
   - Final: ✅
   - Cơ hội: 🔎
   - Nhóm: 🗂
   - Trạng thái entry: 🚦
   - M15: ⏱
   - Gap: ↔
   - Điểm mua: ▲
   - Điểm bán: ▼
   - Vĩ mô: 🌐
   - Thuận vĩ mô: 🧲
   - R:R: ⚖
   - Lý do chính: 💬
   - Chi tiết: 🔍
6. Nếu font/Windows/PyQt hiển thị emoji không ổn, fallback sang icon ký hiệu ngắn như “[Rank]”, “[FX]”, “[Entry]”, “▲”, “▼”, “✓”. Không được để app crash vì icon.
7. Nội dung cần giải thích các trường hợp thường gặp của từng cột.

Nội dung gợi ý bắt buộc phải bao phủ:
- Xếp hạng:
  “Thứ tự ưu tiên sau khi sắp xếp. Số nhỏ hơn nghĩa là đáng xem trước hơn.”
- Mã:
  “Cặp tiền hoặc sản phẩm được quét, ví dụ EUR/USD, GBP/CAD, XAU/USD.”
- Hành động:
  “Sẵn sàng = có thể xem xét vào lệnh; Theo dõi = đáng chú ý; Chờ = cần thêm xác nhận; Bỏ qua = chưa đạt hoặc bị chặn.”
- Xu hướng:
  “BUY rõ/SELL rõ = nghiêng rõ một phía; BUY yếu/SELL yếu = có nghiêng nhưng chưa mạnh; Trung lập = chưa đủ lệch.”
- Entry:
  “Trong vùng = giá đang ở vùng entry; Gần vùng = sắp tới vùng; Còn xa = chưa nên vội; -- = thiếu dữ liệu.”
- Quyền:
  “Được phép = dữ liệu ổn; Cẩn trọng = có rủi ro phụ; Bị chặn = không nên giao dịch do lỗi/dữ liệu/rủi ro.”
- Điểm tốt nhất:
  “Điểm cao nhất giữa kịch bản mua và bán, thang 0-100.”
- Final:
  “Điểm cuối sau gate/điều kiện xác nhận. Có thể khác điểm tốt nhất nếu bị cap hoặc bị phạt.”
- Cơ hội:
  “Điểm xếp hạng cơ hội trong scanner, ưu tiên setup gần vùng, rõ hướng, ít bị chặn.”
- Nhóm:
  “Sẵn sàng ngay = ưu tiên cao; Chờ xác nhận = cần tín hiệu thêm; Theo dõi = chưa đủ điều kiện; Bị chặn = không xét vào lệnh.”
- Trạng thái entry:
  “Sẵn sàng = có xác nhận entry; Chờ xác nhận = cần trigger; Theo dõi vùng = chờ giá/tín hiệu; Vô hiệu/Thiếu dữ liệu = không dùng.”
- M15:
  “strict = xác nhận tốt; loose = xác nhận yếu; none/-- = chưa có xác nhận M15.”
- Gap:
  “Khoảng cách điểm giữa BUY và SELL. Gap càng lớn thì xu hướng càng rõ.”
- Điểm mua:
  “Điểm riêng của kịch bản BUY, thang 0-100.”
- Điểm bán:
  “Điểm riêng của kịch bản SELL, thang 0-100.”
- Vĩ mô (0-30):
  “Điểm macro theo tin tức/dữ liệu nền. Chấm tròn nếu có thể hiện độ tin cậy dữ liệu.”
- Thuận vĩ mô:
  “Thuận = macro cùng chiều setup; Trung tính = chưa rõ; Ngược = macro mâu thuẫn.”
- R:R:
  “Tỷ lệ rủi ro/lợi nhuận dự kiến, ví dụ 1:2.0.”
- Lý do chính:
  “Tóm tắt ngắn vì sao mã được xếp như vậy. Tiền tố AI nghĩa là nhận định do AI viết.”
- Chi tiết:
  “Bấm Xem hoặc dùng nút Xem chi tiết để mở màn hình chi tiết của mã.”

Gợi ý cấu trúc dữ liệu:
- Có thể đổi COLUMN_HELP từ list[tuple[str, str]] thành list[dict[str, str]], ví dụ:
  {
      "icon": "🧭",
      "column": "Xu hướng",
      "meaning": "...",
      "cases": "BUY rõ/SELL rõ = ...; Trung lập = ..."
  }
- Hoặc giữ tuple nhưng thêm icon vào tên cột. Nếu muốn dễ test và dễ đọc, khuyến nghị dùng dict có các key rõ ràng.

Ràng buộc:
- Không đổi ScannerTableModel.COLUMNS.
- Không đổi logic hiển thị dữ liệu trong bảng.
- Không đổi formatter của các cột nếu không cần.
- Không sửa core.
- Không dùng ảnh/icon asset ngoài, tránh phụ thuộc file mới không cần thiết.
- Không dùng HTML phức tạp nếu QTableWidgetItem không render tốt.

Sau khi cập nhật, tự kiểm tra:
- Số mục trong COLUMN_HELP bằng số cột trong ScannerTableModel.COLUMNS.
- Không còn tên cột cũ “Thiên hướng” hoặc “Entry status”.
- Nội dung không quá dài dòng.
- Mỗi mục có icon hoặc ký hiệu.

Sau khi hoàn thành prompt này, bắt buộc báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT

===============================================================================================================================
PROMPT 3: Cải thiện layout dialog kích thước vừa đủ, dễ đọc, không rối

Bạn là Deepseek_v4_pro đang lập trình trong VSCode tại repo ai-market-analyst-v2.0.

Sau khi cập nhật nội dung, hãy cải thiện layout của ScannerColumnsHelpDialog.

Mục tiêu UI:
- Dialog có kích thước vừa đủ để đọc thoải mái.
- Không full-screen.
- Không quá nhỏ khiến phải scroll ngang hoặc chữ bị cắt.
- Có thể scroll dọc nếu nhiều cột.
- Không có scroll ngang nếu tránh được.
- Nhìn gọn, dễ quét bằng mắt.
- Có icon sinh động nhưng không quá lòe loẹt.

Yêu cầu layout:
1. Window title tiếng Việt:
   - “Giải thích Bảng kết quả quét”
2. Kích thước đề xuất:
   - resize khoảng 860x620 hoặc 900x640.
   - setMinimumSize khoảng 760x520.
   - Nếu muốn setMaximumSize thì chọn hợp lý, không khóa quá chặt.
3. Intro ngắn:
   - “Các cột dưới đây giúp đọc nhanh kết quả quét. Giá trị trong bảng đã được rút gọn để dễ so sánh.”
4. Bảng trong dialog nên có 3 hoặc 4 cột:
   - Icon
   - Cột
   - Ý nghĩa
   - Trường hợp thường gặp
5. Nếu giữ 2 cột cũ thì phải đảm bảo icon và cases vẫn dễ đọc, nhưng khuyến nghị 4 cột để rõ ràng.
6. Header table tiếng Việt:
   - “”
   - “Cột”
   - “Ý nghĩa”
   - “Trường hợp thường gặp”
7. Cấu hình bảng:
   - verticalHeader hidden.
   - NoEditTriggers.
   - NoSelection hoặc SingleSelection không bắt buộc, ưu tiên NoSelection.
   - WordWrap True.
   - TextElideMode ElideNone.
   - HorizontalScrollBarAlwaysOff nếu width đủ.
   - VerticalScrollBarAsNeeded.
   - ScrollPerPixel.
   - AlternatingRowColors True nếu style đang hỗ trợ.
8. Cột Icon:
   - Width cố định khoảng 42-52.
   - Align center.
9. Cột “Cột”:
   - Width khoảng 150-190.
   - Không bị cắt header/cột.
10. Cột “Ý nghĩa” và “Trường hợp thường gặp”:
   - Stretch chia hợp lý.
   - Word wrap.
11. Sau khi set item, gọi:
   - resizeRowsToContents()
   - có thể set row min height hợp lý nếu cần.
12. Nút đóng:
   - Text “Đóng”.
   - Giữ objectName SecondaryButton nếu đang dùng style.

Ràng buộc quan trọng:
- Không làm dialog thành landing page/marketing.
- Không thêm animation.
- Không gọi network.
- Không dùng ảnh/icon bên ngoài.
- Không sửa màn hình chính ngoài dialog help.
- Không sửa table chính.
- Không sửa logic scan.

Tự kiểm tra bằng mắt:
- Dialog không bị quá to.
- Không có dòng chữ bị cắt.
- Không có horizontal scrollbar.
- Nội dung đọc được trên màn hình 1366x768 và 1440x900.
- Icon không làm layout lệch.

Sau khi hoàn thành prompt này, bắt buộc báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT

===============================================================================================================================
PROMPT 4: Thêm test/script kiểm tra dialog có đủ cột, nội dung tiếng Việt và icon

Bạn là Deepseek_v4_pro đang lập trình trong VSCode tại repo ai-market-analyst-v2.0.

Hãy thêm test hoặc script Python chuyên biệt để đảm bảo dialog “Giải thích ý nghĩa” đúng yêu cầu.

Ưu tiên:
- Nếu phù hợp, thêm test vào tests/test_scanner.py hoặc tests/test_scanner_screen_columns_phase15.py.
- Nếu muốn script độc lập, tạo:
  scripts/check_scanner_columns_help_dialog.py

Yêu cầu script/test:
1. Set:
   os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")
2. Import:
   - QApplication
   - ScannerTableModel
   - ScannerColumnsHelpDialog
3. Tạo QApplication.
4. Tạo dialog.
5. Kiểm tra dữ liệu help:
   - Số mục help bằng số cột trong ScannerTableModel.COLUMNS.
   - Mỗi header hiển thị trong ScannerTableModel.COLUMNS có mục trong dialog.
   - Không còn text cũ:
     - “Thiên hướng”
     - “Entry status”
   - Có text mới:
     - “Xu hướng”
     - “Trạng thái entry”
     - “Cơ hội”
     - “Nhóm”
     - “Final”
     - “M15”
     - “Gap”
6. Kiểm tra nội dung các case quan trọng:
   - Hành động có “Sẵn sàng”, “Theo dõi”, “Chờ”, “Bỏ qua”.
   - Xu hướng có “BUY rõ”, “SELL rõ”, “Trung lập”.
   - Entry có “Trong vùng”, “Gần vùng”, “Còn xa”.
   - Quyền có “Được phép”, “Cẩn trọng”, “Bị chặn”.
   - Nhóm có “Sẵn sàng ngay”, “Chờ xác nhận”, “Theo dõi”, “Bị chặn”.
   - Trạng thái entry có “Sẵn sàng”, “Chờ xác nhận”, “Theo dõi vùng”, “Thiếu dữ liệu”.
   - M15 có “strict”, “loose”.
   - Thuận vĩ mô có “Thuận”, “Trung tính”, “Ngược”.
7. Kiểm tra icon:
   - Mỗi mục có icon/ký hiệu không rỗng.
   - Không yêu cầu icon phải là emoji cụ thể, chỉ cần không rỗng và không quá dài.
8. Nếu dialog có QTableWidget:
   - Kiểm tra số row bằng số mục help.
   - Kiểm tra số column là 3 hoặc 4 theo thiết kế.
   - Kiểm tra horizontalScrollBarPolicy là ScrollBarAlwaysOff nếu đã đặt.
9. In kết quả rõ:
   - Help items: OK
   - Vietnamese labels: OK
   - Icons: OK
   - Dialog size: OK

Ràng buộc:
- Không gọi MT5.
- Không gọi scanner thật.
- Không gọi network.
- Không gọi AI.
- Không tạo dữ liệu giao dịch thật.
- Không phụ thuộc screenshot để pass test này.

Sau khi hoàn thành prompt này, bắt buộc báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT

===============================================================================================================================
PROMPT 5: Render preview dialog offscreen để kiểm tra kích thước và trình bày

Bạn là Deepseek_v4_pro đang lập trình trong VSCode tại repo ai-market-analyst-v2.0.

Hãy tạo script render preview để kiểm tra bằng mắt dialog sau khi nâng cấp.

Script đề xuất:
- scripts/render_scanner_columns_help_dialog_preview.py

Yêu cầu script:
1. Set:
   os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")
2. Import QApplication và ScannerColumnsHelpDialog.
3. Tạo dialog.
4. Gọi:
   - dialog.resize hoặc dùng size mặc định đã cấu hình.
   - dialog.show()
   - app.processEvents()
5. Nếu dialog có help_table:
   - gọi resizeRowsToContents()
   - app.processEvents()
6. Render ra ảnh:
   - data/scanner_columns_help_dialog_preview.png
7. Trước khi render, assert:
   - dialog.width nằm trong khoảng hợp lý, ví dụ 760-980.
   - dialog.height nằm trong khoảng hợp lý, ví dụ 520-720.
   - không có horizontal scrollbar active nếu có thể kiểm tra.
   - số row help bằng số cột bảng.
8. In đường dẫn ảnh output.

Kiểm tra bằng mắt sau khi render:
- Dialog có kích thước vừa đủ.
- Header table rõ.
- Mỗi dòng có icon.
- Nội dung tiếng Việt dễ đọc.
- Không có text bị cắt.
- Không có scroll ngang.
- Nút “Đóng” hiển thị rõ.

Ràng buộc:
- Không sửa logic scanner.
- Không cần mở app thật.
- Không cần MT5/network/AI.
- Không ghi đè ảnh khác nếu không cần.

Sau khi hoàn thành prompt này, bắt buộc báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT

===============================================================================================================================
PROMPT 6: Chạy test cuối, rà soát không ảnh hưởng phần khác, báo cáo hoàn tất

Bạn là Deepseek_v4_pro đang lập trình trong VSCode tại repo ai-market-analyst-v2.0.

Đây là prompt cuối. Hãy chạy kiểm tra tổng hợp thật cẩn thận.

Các lệnh cần chạy:
1. Chạy script/check dialog:
   - python scripts/check_scanner_columns_help_dialog.py
2. Chạy render preview nếu có:
   - python scripts/render_scanner_columns_help_dialog_preview.py
3. Chạy test scanner/UI liên quan:
   - python -m pytest tests/test_scanner.py -q
   - python -m pytest tests/test_scanner_screen_columns_phase15.py -q
4. Nếu thời gian cho phép, chạy thêm:
   - python -m pytest tests -q
5. Rà lại bằng ripgrep:
   - rg -n "ScannerColumnsHelpDialog|COLUMN_HELP|Giải thích|Thiên hướng|Entry status|Trạng thái entry|Xu hướng" ui tests scripts

Điều cần xác nhận:
- Dialog mở được.
- Dialog có kích thước vừa đủ.
- Có đủ mô tả cho tất cả cột của ScannerTableModel.COLUMNS.
- Nội dung ưu tiên tiếng Việt, dễ hiểu, không quá dài.
- Có icon/ký hiệu cho từng dòng.
- Không còn dùng tên cột cũ “Thiên hướng” hoặc “Entry status”.
- Không có horizontal scrollbar nếu thiết kế đặt off.
- Không thay đổi logic scanner/scoring/ranking/decision/gate.
- Không sửa thứ tự cột bảng.
- Không làm hỏng các test scanner/UI hiện có.

Nếu có lỗi:
- Đọc kỹ stacktrace.
- Chỉ sửa đúng phần dialog/help UI.
- Không sửa core để làm test pass.
- Không xóa assert quan trọng.
- Chạy lại test sau khi sửa.

Báo cáo cuối cùng cần nêu rõ:
- File đã sửa.
- Script/test đã thêm hoặc cập nhật.
- Ảnh preview nếu có.
- Các lệnh đã chạy và kết quả pass/fail.
- Nếu còn test fail không liên quan, ghi rõ tên test và lý do.
- Xác nhận không thay đổi core scoring/gate/decision/ranking.

Sau khi hoàn thành prompt này, bắt buộc báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT

Vì đây là prompt cuối cùng, sau khi chạy test lại toàn bộ và xác nhận công việc đã hoàn tất, bắt buộc báo thêm đúng câu:
ĐÃ HOÀN THÀNH FIX lỗi
