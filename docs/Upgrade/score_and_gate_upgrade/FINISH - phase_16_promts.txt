PROMPT 1 — PHASE 16.1: KIỂM TRA HIỆN TRẠNG TEST SAU PHASE 15 VÀ KHÓA PHẠM VI PHASE 16

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 15: scanner ranking mới có scanner_group/opportunity_score và các metadata liên quan. Bây giờ bắt đầu Phase 16: TEST PLAN.

Mục tiêu Phase 16:
Không thêm feature mới. Phase 16 chỉ bổ sung, rà soát và chạy test để khóa hành vi đã nâng cấp từ Phase 1 đến Phase 15.

Các nhóm test bắt buộc theo plan:
- score rename: signal_score tồn tại, total vẫn backward-compatible.
- gate: spread abnormal, daily loss, M15 none/loose, expected effective R:R thấp, score gap thấp.
- score gap: Buy/Sell sát điểm không được READY.
- effective R:R: spread lớn làm expected_effective_rr thấp.
- account guard: daily loss block open_new_trade nhưng không block close_trade.
- revenge trade: loss trước + trade mới gần thời gian/tăng lot.
- final_score: công thức signal/evidence/execution.
- decision_engine: gate > decision_cap > score_gap > entry_status > final_score.
- scanner ranking: blocked không leo top, ready/wait/watch group đúng.

Nguyên tắc cực kỳ quan trọng:
- Phase 16 ưu tiên tạo test mới và chạy regression.
- KHÔNG thêm feature mới.
- KHÔNG refactor.
- KHÔNG sửa UI trừ khi test UI hiện có fail do bug rõ ràng.
- KHÔNG sửa database/migration.
- KHÔNG sửa services.
- KHÔNG sửa logic scoring/gate/decision/scanner nếu test chưa chứng minh bug rõ ràng.
- Nếu test fail do môi trường thiếu PyQt6/MT5/network thì ghi rõ, không sửa bừa.

Mục tiêu riêng của prompt này:
Kiểm tra hiện trạng test hiện có, lập danh sách file test liên quan Phase 1-15, và tạo một test import/smoke nhỏ cho Phase 16 nếu cần. Chưa sửa engine.

Yêu cầu rất quan trọng:
- Chỉ được đọc code/test.
- Chỉ được tạo file test nhỏ nếu cần:
  tests/test_phase16_test_plan_smoke.py
- Không sửa core/.
- Không sửa services/.
- Không sửa ui/.
- Không sửa controllers/.

Việc cần làm:
1. Liệt kê các test hiện có liên quan:
   - signal_score/score_gap/gate
   - account_guard
   - entry_engine
   - final_score_engine
   - decision_engine
   - scanner_ranking
   - trade_mistake_detector

2. Mở nhanh các file test đại diện để hiểu style:
   - tests/test_score_gap.py nếu có.
   - tests/test_trade_gate_engine.py.
   - tests/test_account_guard_phase7.py.
   - tests/test_final_score_engine_basic_phase13.py.
   - tests/test_decision_engine_basic_phase14.py.
   - tests/test_phase15_scanner_ranking_realistic.py.

3. Tạo tests/test_phase16_test_plan_smoke.py nếu chưa có.

4. Test smoke chỉ cần import được các module trọng tâm:
   - core.signal_engine
   - core.trade_gate_engine
   - core.account_guard
   - core.entry_engine
   - core.final_score_engine
   - core.decision_engine
   - core.scanner
   - core.scanner_ranking_engine
   - core.trade_mistake_detector

5. Test smoke không gọi MT5, không gọi network, không import PyQt UI.

6. Chạy:
   python -m pytest tests/test_phase16_test_plan_smoke.py -q

Tiêu chí hoàn thành:
- Có cái nhìn rõ test hiện có.
- Smoke import pass.
- Không sửa engine/app.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 2 — PHASE 16.2: TEST SCORE RENAME signal_score VÀ BACKWARD COMPAT total

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 1 đã đổi total thành signal_score nhưng giữ total để backward compatibility. Phase 16 cần test rõ rằng score_scenario() luôn trả signal_score và total vẫn còn.

Mục tiêu riêng của prompt này:
Tạo test Phase 16 khóa hành vi signal_score/total.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase16_score_rename_signal_score.py
  2. core/signal_engine.py chỉ nếu test phát hiện bug rõ ràng liên quan signal_score/total.
- Không sửa analysis_engine.py.
- Không sửa scanner.py.
- Không sửa UI.
- Không sửa gate/decision/final_score.

Việc cần làm:
1. Tạo file:
   tests/test_phase16_score_rename_signal_score.py

2. Tạo dữ liệu technical/smc tối thiểu giống realistic nhưng không cần gọi MT5:
   - technical có các key cần cho score_scenario:
     price, ema50_d1, ema200_d1, ema50_h4, structure_h4, structure_d1,
     rsi_h4, rsi_h4_previous, macd_histogram_h4,
     support_zones, resistance_zones, atr_h4, atr_d1.
   - smc có H4/H1 tối thiểu, demand_zones/supply_zones, premium_discount.

3. Gọi:
   from core.signal_engine import score_scenario
   result = score_scenario("buy", technical, smc, risk_score=12, macro_score=15, market_regime={"primary": "trend_up"})

4. Assert:
   - "signal_score" in result.
   - "total" in result.
   - result["signal_score"] == result["total"].
   - signal_score là int trong 0-100.
   - result có component keys: trend_alignment, momentum_alignment, location_quality, smc_quality, risk_condition, macro_alignment.

5. Test sell tương tự để đảm bảo cả hai side.

6. Test backward-compatible access:
   - score = result.get("signal_score", result.get("total", 0))
   - score == result["signal_score"].

Chạy:
python -m pytest tests/test_phase16_score_rename_signal_score.py -q
python -m pytest tests/test_score_gap.py tests/test_signal_engine.py -q
nếu các file này tồn tại.

Tiêu chí hoàn thành:
- signal_score và total cùng tồn tại.
- total vẫn backward-compatible.
- Không thay đổi behavior khác.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 3 — PHASE 16.3: TEST TRADE GATE MATRIX CHO SPREAD, NEWS, M15, R:R, SCORE GAP

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 2-6 đã tách score/gate và thêm gate cho spread, news, M15, expected effective R:R, score gap. Phase 16 cần test ma trận đầy đủ để tránh regression.

Mục tiêu riêng của prompt này:
Tạo test matrix cho core.trade_gate_engine.check_trade_gates().

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase16_trade_gate_matrix.py
  2. core/trade_gate_engine.py chỉ nếu test phát hiện bug rõ ràng trong gate.
- Không sửa analysis_engine.py.
- Không sửa signal_engine.py.
- Không sửa scanner.py.
- Không sửa decision_engine.py.
- Không sửa UI.

Việc cần làm:
1. Tạo file:
   tests/test_phase16_trade_gate_matrix.py

2. Import:
   from core.trade_gate_engine import check_trade_gates
   from core.reason_codes import các code liên quan nếu có:
   SPREAD_ABNORMAL, HIGH_IMPACT_NEWS_NEARBY, M15_NOT_CONFIRMED, M15_LOOSE_CONFIRMATION, EXPECTED_RR_TOO_LOW, BUY_SELL_SCORE_GAP_LOW

3. Viết helper base_context():
   return {
       "terminal_connected": True,
       "broker_logged_in": True,
       "spread_status": "normal",
       "high_impact_event_within_30m": False,
       "m15_quality": "strict",
       "expected_effective_rr": 1.8,
       "score_gap": 20,
       "min_buy_sell_score_gap": 10,
       "zone_broken": False,
       "daily_loss_limit_reached": False,
       "weekly_loss_limit_reached": False,
   }

4. Test cases:
   - spread abnormal:
     context["spread_status"] = "abnormal"
     => allowed False, decision_cap TRADE_BLOCKED, block_codes có SPREAD_ABNORMAL hoặc equivalent.

   - high impact news within 30m:
     => allowed False, decision_cap TRADE_BLOCKED.

   - M15 none:
     context["m15_quality"] = "none"
     => allowed True hoặc allowed theo implementation hiện tại, nhưng decision_cap phải WATCH_ONLY.
     warning_codes có M15_NOT_CONFIRMED nếu code tồn tại.

   - M15 loose:
     => decision_cap WAITING_CONFIRMATION.

   - expected_effective_rr thấp:
     context["expected_effective_rr"] = 1.1
     => decision_cap WATCH_ONLY.

   - score_gap thấp:
     context["score_gap"] = 3
     => decision_cap WAITING_CONFIRMATION.

   - clean context:
     => allowed True, decision_cap None hoặc không block.

5. Nếu exact code names khác implementation, test theo constants hiện có. Không hard-code sai.

Chạy:
python -m pytest tests/test_phase16_trade_gate_matrix.py -q
python -m pytest tests/test_trade_gate_engine.py tests/test_phase2_trade_gate_realistic.py -q

Tiêu chí hoàn thành:
- Gate matrix đúng thứ tự an toàn.
- Không có setup score cao vượt qua hard block.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 4 — PHASE 16.4: TEST SCORE GAP BUY/SELL KHÔNG ĐƯỢC READY KHI ĐIỂM SÁT NHAU

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Plan yêu cầu: Buy=80, Sell=77 thì không được READY vì score_gap thấp; Buy=82, Sell=65 thì bias rõ. Phase 16 cần khóa behavior này ở signal/decision/gate level.

Mục tiêu riêng của prompt này:
Tạo test score gap rõ ràng cho calculate_direction_bias và decision_engine/gate.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase16_score_gap_decision.py
  2. core/signal_engine.py hoặc core/decision_engine.py chỉ nếu test phát hiện bug rõ ràng.
- Không sửa analysis_engine.py.
- Không sửa scanner.py.
- Không sửa UI.

Việc cần làm:
1. Tạo file:
   tests/test_phase16_score_gap_decision.py

2. Test calculate_direction_bias:
   from core.signal_engine import calculate_direction_bias

   Case A:
   buy_result={"signal_score": 80}
   sell_result={"signal_score": 77}
   min_gap=10
   => score_gap=3, is_clear_bias False.

   Case B:
   buy_result={"signal_score": 82}
   sell_result={"signal_score": 65}
   => score_gap=17, is_clear_bias True, best_side buy.

3. Test decision_engine:
   from core.decision_engine import make_final_decision

   Case score gap thấp:
   final_score=90
   gate_result={"allowed": True}
   entry_status="confirmed_entry"
   score_gap=3
   => decision WAITING_CONFIRMATION, không READY_TO_TRADE.

   Case score gap rõ:
   final_score=90
   gate_result={"allowed": True}
   entry_status="confirmed_entry"
   score_gap=17
   => READY_TO_TRADE.

4. Test gate nếu trade_gate_engine có rule score_gap:
   from core.trade_gate_engine import check_trade_gates
   context score_gap=3, min_buy_sell_score_gap=10
   => decision_cap WAITING_CONFIRMATION.

5. Không cần tạo candle data.

Chạy:
python -m pytest tests/test_phase16_score_gap_decision.py -q
python -m pytest tests/test_score_gap.py tests/test_decision_engine_basic_phase14.py -q

Tiêu chí hoàn thành:
- Score gap thấp không READY.
- Score gap rõ có thể READY nếu điều kiện khác đạt.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 5 — PHASE 16.5: TEST EXPECTED EFFECTIVE R:R VỚI SPREAD LỚN

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 6 thêm expected_effective_rr để tránh R:R lý tưởng đẹp nhưng sau spread không đáng vào. Phase 16 cần test dữ liệu giống thật.

Mục tiêu riêng của prompt này:
Tạo test expected effective R:R cho core.risk_engine và gate.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase16_expected_effective_rr_gate.py
  2. core/risk_engine.py hoặc core/trade_gate_engine.py chỉ nếu test phát hiện bug rõ ràng.
- Không sửa analysis_engine.py.
- Không sửa scanner.py.
- Không sửa UI.

Việc cần làm:
1. Tạo file:
   tests/test_phase16_expected_effective_rr_gate.py

2. Import:
   from core.risk_engine import calculate_expected_effective_rr
   from core.trade_gate_engine import check_trade_gates

3. Test ideal R:R 1.5 nhưng spread lớn:
   direction="buy"
   entry=1.1000
   stop_loss=1.0900
   take_profit=1.1150
   ideal reward/risk = 0.015 / 0.010 = 1.5
   spread_price=0.0030
   effective_risk=0.013
   effective_reward=0.012
   expected_effective_rr khoảng 0.923
   Assert < 1.3.

4. Test spread nhỏ:
   spread_price=0.0002
   expected_effective_rr > 1.3.

5. Test sell tương tự:
   entry=1.1000
   stop_loss=1.1100
   take_profit=1.0850
   spread lớn => rr thấp.

6. Gate test:
   context clean nhưng expected_effective_rr=1.1
   => check_trade_gates decision_cap WATCH_ONLY, warning_codes có EXPECTED_RR_TOO_LOW nếu code tồn tại.

7. Dữ liệu bẩn:
   calculate_expected_effective_rr("buy", None, 1.0, 1.1, 0.001) == 0.0 hoặc không crash.

Chạy:
python -m pytest tests/test_phase16_expected_effective_rr_gate.py -q
python -m pytest tests/test_expected_effective_rr_phase6.py tests/test_expected_rr_gate_phase6.py -q

Tiêu chí hoàn thành:
- Spread lớn làm expected RR giảm.
- Gate không cho READY khi expected RR thấp.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 6 — PHASE 16.6: TEST ACCOUNT GUARD DAILY LOSS BLOCK OPEN_NEW_TRADE NHƯNG CHO CLOSE_TRADE

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Plan yêu cầu daily loss -2.1%, max 2% => block open_new_trade, nhưng close_trade vẫn allowed. Phase 16 cần test hành vi này rõ ràng.

Mục tiêu riêng của prompt này:
Tạo test account guard cho action-specific block.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase16_account_guard_action_permissions.py
  2. core/account_guard.py chỉ nếu test phát hiện bug rõ ràng.
- Không sửa journal_service.py.
- Không sửa database/migration.
- Không sửa trade_gate_engine.py trừ khi test integration sau yêu cầu.
- Không sửa UI.

Việc cần làm:
1. Tạo file:
   tests/test_phase16_account_guard_action_permissions.py

2. Import:
   from core.account_guard import check_account_guard

3. Tạo closed_trades giống thật:
   [
     {
       "symbol": "EUR/USD",
       "direction": "buy",
       "result_pct": -1.1,
       "result_r": -1.0,
       "closed_at": "2026-06-04T09:00:00+07:00",
     },
     {
       "symbol": "GBP/JPY",
       "direction": "sell",
       "result_pct": -1.0,
       "result_r": -1.0,
       "closed_at": "2026-06-04T11:00:00+07:00",
     },
   ]
   Tổng daily loss = -2.1%.

4. Settings:
   {
     "max_daily_loss_pct": 2.0,
     "max_weekly_loss_pct": 5.0,
     "max_consecutive_losses": 3,
     "max_open_risk_pct": 3.0,
     "trader_timezone": "Asia/Ho_Chi_Minh",
   }

5. Test open_new_trade:
   result = check_account_guard(..., action="open_new_trade", now="2026-06-04T12:00:00+07:00")
   => blocked True hoặc allowed False tùy API hiện tại.
   block_codes có DAILY_LOSS_LIMIT_REACHED.

6. Test close_trade:
   result = check_account_guard(..., action="close_trade", same now)
   => không blocked hoặc allowed True.
   Nếu API trả action_allowed, assert đúng theo implementation.

7. Test move_sl_to_breakeven hoặc move_sl_closer nếu supported:
   => không bị hard block.

8. Test increase_position/open_new_trade vẫn blocked nếu supported.

Chạy:
python -m pytest tests/test_phase16_account_guard_action_permissions.py -q
python -m pytest tests/test_account_guard_phase7.py tests/test_phase7_account_guard_realistic.py -q

Tiêu chí hoàn thành:
- Account guard phân biệt action tăng rủi ro và giảm rủi ro.
- Daily loss block mở lệnh mới.
- Close trade vẫn allowed.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 7 — PHASE 16.7: TEST REVENGE TRADE WARNING VÀ CONFIRMED THEO PHASE 12

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Plan yêu cầu test revenge trade:
- Lệnh trước thua, lệnh mới trong 3 phút => revenge warning.
- Lệnh mới tăng lot 2x => revenge confirmed.

Mục tiêu riêng của prompt này:
Tạo test revenge trade tổng hợp cho core.trade_mistake_detector.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase16_revenge_trade_detector.py
  2. core/trade_mistake_detector.py chỉ nếu test phát hiện bug rõ ràng.
- Không sửa execution_quality_engine.py.
- Không sửa journal_service.py.
- Không sửa account_guard.py.
- Không sửa UI.

Việc cần làm:
1. Tạo file:
   tests/test_phase16_revenge_trade_detector.py

2. Import:
   from core.trade_mistake_detector import detect_trade_mistakes
   from core.reason_codes import MISTAKE_REVENGE_TRADE_WARNING, MISTAKE_REVENGE_TRADE_CONFIRMED

3. Case warning do thời gian:
   previous_trades = [
     {
       "symbol": "EUR/USD",
       "direction": "buy",
       "result_r": -1.0,
       "closed_at": "2026-06-04T09:00:00Z",
       "actual_lot": 0.10,
     }
   ]
   current_trade = {
       "symbol": "EUR/USD",
       "direction": "sell",
       "opened_at": "2026-06-04T09:03:00Z",
       "actual_lot": 0.10,
       "planned_lot": 0.10,
   }
   => auto_mistake_tags có revenge_trade_warning hoặc mistake_codes có MISTAKE_REVENGE_TRADE_WARNING.

4. Case warning do lot:
   previous loss actual_lot=0.10.
   current opened sau 30 phút nhưng actual_lot=0.20.
   => warning code/tag.

5. Case confirmed:
   previous loss close 09:00.
   current opened 09:03 và actual_lot=0.20.
   => MISTAKE_REVENGE_TRADE_CONFIRMED.

6. Case không revenge:
   previous trade win hoặc trade mới sau nhiều giờ và lot bình thường.
   => không có revenge tag/code.

7. Test không duplicate:
   - len(auto_mistake_tags) == len(set(auto_mistake_tags)).
   - len(mistake_codes) == len(set(mistake_codes)).

Chạy:
python -m pytest tests/test_phase16_revenge_trade_detector.py -q
python -m pytest tests/test_trade_mistake_detector_revenge_*phase12.py tests/test_trade_mistake_detector_realistic_phase12.py -q

Tiêu chí hoàn thành:
- Revenge warning/confirmed đúng.
- Không duplicate tag/code.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 8 — PHASE 16.8: TEST FINAL_SCORE VÀ DECISION_ENGINE PHỐI HỢP ĐÚNG THỨ TỰ ƯU TIÊN

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 13 tạo final_score. Phase 14 tạo decision_engine. Phase 16 cần test chúng phối hợp đúng, đặc biệt gate vẫn thắng final_score cao.

Mục tiêu riêng của prompt này:
Tạo test tích hợp final_score_engine + decision_engine, không gọi analysis_engine.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase16_final_score_decision_priority.py
  2. core/final_score_engine.py hoặc core/decision_engine.py chỉ nếu test phát hiện bug rõ ràng.
- Không sửa analysis_engine.py.
- Không sửa scanner.py.
- Không sửa UI.

Việc cần làm:
1. Tạo file:
   tests/test_phase16_final_score_decision_priority.py

2. Import:
   from core.final_score_engine import calculate_final_score
   from core.decision_engine import make_final_decision

3. Case final_score cao nhưng gate block:
   final = calculate_final_score(signal_score=95, evidence_score=80, execution_quality_score=100)
   decision = make_final_decision(final_score=final["final_score"], gate_result={"allowed": False, "decision_cap": "TRADE_BLOCKED"}, entry_status="confirmed_entry", score_gap=30)
   => TRADE_BLOCKED.

4. Case final_score cao nhưng score_gap thấp:
   gate allowed True, entry confirmed, score_gap=4
   => WAITING_CONFIRMATION, không READY.

5. Case final_score cao nhưng entry waiting:
   => WAITING_CONFIRMATION hoặc AGGRESSIVE_SETUP only nếu allow_aggressive_setup=True. Với default False phải WAITING_CONFIRMATION.

6. Case final_score vừa:
   signal=70, evidence=50, execution=100 => final khoảng 70.
   gate allowed, entry confirmed, score_gap=20
   => WATCH_ONLY.

7. Case final_score yếu:
   => STAND_ASIDE.

8. Case mọi điều kiện đẹp:
   signal=90, evidence=80, execution=95, gate allowed, entry confirmed, score_gap=20
   => READY_TO_TRADE.

Chạy:
python -m pytest tests/test_phase16_final_score_decision_priority.py -q
python -m pytest tests/test_final_score_engine_*_phase13.py tests/test_decision_engine_*_phase14.py -q

Tiêu chí hoàn thành:
- Gate/entry/score_gap vẫn ưu tiên hơn final_score.
- Final_score chỉ là input cho decision.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 9 — PHASE 16.9: TEST SCANNER RANKING KHÔNG CHO BLOCKED ROW LEO TOP

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 15 đổi scanner ranking. Phase 16 cần test kỹ tình huống nguy hiểm: blocked row có final_score rất cao nhưng không được leo top.

Mục tiêu riêng của prompt này:
Tạo test scanner ranking edge cases với blocked/high-score rows.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase16_scanner_blocked_ranking_guard.py
  2. core/scanner.py hoặc core/scanner_ranking_engine.py chỉ nếu test phát hiện bug rõ ràng.
- Không sửa controller/UI.
- Không sửa decision/final_score engine.

Việc cần làm:
1. Tạo file:
   tests/test_phase16_scanner_blocked_ranking_guard.py

2. Import:
   from core.scanner import sort_scanner_rows, scanner_summary, ai_targets

3. Tạo rows giống thật:
   - blocked_gold:
     symbol XAU/USD, final_score 98, opportunity_score 20, scanner_group blocked, trade_permission blocked, scanner_decision TRADE_BLOCKED.
   - ready_eurusd:
     final_score 82, opportunity_score 88, scanner_group ready_now, trade_permission allowed.
   - waiting_gbpjpy:
     final_score 90, opportunity_score 86, scanner_group waiting_confirmation.
   - watch_usdjpy:
     final_score 75, opportunity_score 78, scanner_group watch_zone.

4. sort_scanner_rows(rows):
   - ready_eurusd rank 1.
   - waiting_gbpjpy rank sau ready.
   - watch_usdjpy sau waiting.
   - blocked_gold cuối dù final_score 98.

5. scanner_summary(sorted_rows):
   - blocked_count = 1.
   - ready_now_count = 1.

6. ai_targets(sorted_rows, limit=3):
   - Không chứa blocked_gold.
   - Không vượt limit.

7. Test fallback legacy rows:
   - row chỉ có scanner_action/best_score/risk_reward không crash.

Chạy:
python -m pytest tests/test_phase16_scanner_blocked_ranking_guard.py -q
python -m pytest tests/test_phase15_scanner_ranking_realistic.py tests/test_scanner.py -q

Tiêu chí hoàn thành:
- Blocked row không leo top.
- AI targets không chọn blocked.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 10 — PHASE 16.10: TEST ANALYSIS_ENGINE OUTPUT CÓ ĐỦ FINAL_SCORE, DECISION_ENGINE, GATE, SCENARIO

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Sau Phase 13-15, analyze_symbol output cần đủ thông tin cho scanner và UI: final_score, decision_engine, trade_gate, scenarios, score_gap. Phase 16 cần test output shape với candle fake giống thật.

Mục tiêu riêng của prompt này:
Tạo test integration nhẹ cho core.analysis_engine.analyze_symbol, không gọi MT5/AI/news.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase16_analysis_output_contract.py
  2. core/analysis_engine.py chỉ nếu test phát hiện bug output contract rõ ràng.
- Không sửa services.
- Không sửa controller/UI.
- Không sửa signal/gate/decision/final_score nếu không cần.

Việc cần làm:
1. Tạo file:
   tests/test_phase16_analysis_output_contract.py

2. Tạo helper build_candles(count, start_price, drift):
   - Dùng core.market_models.Candle.
   - D1 >= 80 nến.
   - H4 >= 80 nến.
   - H1 >= 50 nến.
   - Giá có drift nhẹ, high/low khác open/close để ATR > 0.
   - time tăng đều, timezone UTC.

3. Tạo AnalysisInput:
   from core.risk_engine import AnalysisInput
   request = AnalysisInput(symbol="EUR/USD", broker_symbol="EURUSD", account_balance=10000, risk_percent=1.0)

4. Gọi analyze_symbol với:
   data_quality clean:
   {
     "terminal_connected": True,
     "broker_logged_in": True,
     "spread_status": "normal",
     "news_in_3h": False,
     "high_impact_event_within_30m": False,
   }
   macro_alignment={"buy": 15, "sell": 15}
   macro_confidence=1.0

5. Assert output có:
   - symbol
   - scenario_scores
   - decision_summary
   - trade_permission
   - trade_gate
   - scenarios
   - final_score
   - final_score_detail
   - decision_engine
   - chart_payload

6. Assert:
   - final_score là int 0-100 hoặc 0-120 nếu implementation khác? Theo Phase 13 final_score phải 0-100.
   - decision_engine["decision"] tồn tại.
   - decision_summary["score_gap"] tồn tại.
   - scenarios là list không crash.

7. Không assert decision cụ thể vì candle fake có thể thay đổi score.

Chạy:
python -m pytest tests/test_phase16_analysis_output_contract.py -q
python -m pytest tests/test_analysis_final_score_*_phase13.py tests/test_analysis_decision_engine_*_phase14.py -q

Tiêu chí hoàn thành:
- analyze_symbol output contract đủ cho scanner.
- Không gọi external service.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 11 — PHASE 16.11: TẠO TEST REALISTIC END-TO-END CORE TỪ ANALYSIS RESULT SANG SCANNER ROW

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 15 scanner_row_from_analysis dùng analysis_result để tạo row ranking. Phase 16 cần test end-to-end core: analyze_symbol -> scanner_row_from_analysis -> sort_scanner_rows.

Mục tiêu riêng của prompt này:
Tạo test realistic core không dùng MT5/AI/UI.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase16_analysis_to_scanner_contract.py
  2. core/analysis_engine.py/core/scanner.py chỉ nếu test phát hiện bug contract rõ ràng.
- Không sửa services.
- Không sửa UI/controller.
- Không sửa database.

Việc cần làm:
1. Tạo file:
   tests/test_phase16_analysis_to_scanner_contract.py

2. Có thể reuse helper candle từ test_phase16_analysis_output_contract hoặc copy nhỏ trong file test.

3. Tạo 2 analysis_result fake hoặc thật:
   Option A khuyến nghị để nhanh và ổn định:
   - Tạo result fake giống output analyze_symbol, với đầy đủ:
     + symbol
     + scenario_scores
     + final_score
     + decision_engine
     + decision_summary
     + trade_permission
     + trade_gate
     + technical
     + scenarios
     + market_regime
   - Dùng scanner_row_from_analysis(result).

   Option B nếu muốn realistic hơn:
   - Gọi analyze_symbol với candle fake cho 2 symbol.
   - Sau đó scanner_row_from_analysis.

4. Assert row có:
   - symbol
   - best_score
   - final_score
   - scanner_decision
   - opportunity_score
   - scanner_group
   - score_gap
   - entry_status
   - analysis_result

5. Tạo list rows gồm:
   - one ready-like row.
   - one blocked-like row.
   sort_scanner_rows(rows).
   Assert blocked không rank 1 nếu ready-like tồn tại.

6. build_scanner_output(rows, request, ai_called=0):
   - summary có key cũ và mới.
   - rows giữ analysis_result.

Chạy:
python -m pytest tests/test_phase16_analysis_to_scanner_contract.py -q
python -m pytest tests/test_scanner.py tests/test_phase15_scanner_ranking_realistic.py -q

Tiêu chí hoàn thành:
- Contract analysis -> scanner row ổn.
- Row có đủ metadata Phase 13-15.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 12 — PHASE 16.12: TẠO SCRIPT/TEST REGRESSION MANIFEST CHẠY CÁC TEST PHASE 16 QUAN TRỌNG

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 16 có nhiều nhóm test. Cần một manifest hoặc test file nhỏ ghi danh sách nhóm test quan trọng để developer không bỏ sót khi chạy regression.

Mục tiêu riêng của prompt này:
Tạo tài liệu test manifest dạng .txt hoặc test kiểm tra danh sách file. Không sửa engine.

Yêu cầu rất quan trọng:
- Chỉ được tạo/sửa:
  1. docs/Upgrade/score_and_gate_upgrade/phase16_test_manifest.txt
  2. tests/test_phase16_test_manifest_exists.py
- Không sửa core/.
- Không sửa services/.
- Không sửa UI.
- Không sửa controller.

Việc cần làm:
1. Tạo file:
   docs/Upgrade/score_and_gate_upgrade/phase16_test_manifest.txt

2. Nội dung tiếng Việt đầy đủ:
   - Mục tiêu Phase 16.
   - Các nhóm test phải chạy.
   - Command gợi ý:
     python -m pytest tests/test_phase16_*.py -q
     python -m pytest tests/test_trade_gate_engine.py tests/test_account_guard_phase7.py -q
     python -m pytest tests/test_final_score_engine_*_phase13.py tests/test_decision_engine_*_phase14.py -q
     python -m pytest tests/test_scanner.py tests/test_phase15_scanner_ranking_realistic.py -q
   - Ghi chú: lỗi môi trường PyQt6/MT5/network phải phân loại riêng, không sửa code bừa.

3. Tạo test:
   tests/test_phase16_test_manifest_exists.py

4. Test:
   - File manifest tồn tại.
   - Nội dung có "Phase 16".
   - Nội dung có "python -m pytest tests/test_phase16_*.py -q".
   - Nội dung có "MT5" hoặc "PyQt6" để nhắc lỗi môi trường.

Chạy:
python -m pytest tests/test_phase16_test_manifest_exists.py -q

Tiêu chí hoàn thành:
- Có manifest dễ chạy regression.
- Không sửa app code.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 13 — PHASE 16.13: CHẠY TOÀN BỘ TEST PHASE 16 VÀ SỬA BUG NHỎ ĐÚNG PHẠM VI

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Các prompt trước đã tạo test Phase 16. Bây giờ chạy toàn bộ test Phase 16 và sửa bug nhỏ nếu bug rõ ràng do implementation trước đó.

Mục tiêu riêng của prompt này:
Chạy test Phase 16 tổng hợp, không thêm feature mới.

Yêu cầu rất quan trọng:
- Không thêm feature mới.
- Không refactor.
- Không sửa UI trừ khi test Phase 16 UI contract fail rõ ràng.
- Không sửa database.
- Không sửa services.
- Chỉ được sửa bug nhỏ trong module liên quan nếu test chứng minh rõ:
  - core/signal_engine.py
  - core/trade_gate_engine.py
  - core/risk_engine.py
  - core/account_guard.py
  - core/trade_mistake_detector.py
  - core/final_score_engine.py
  - core/decision_engine.py
  - core/scanner.py
  - core/scanner_ranking_engine.py
  - core/analysis_engine.py
- Không thay đổi expected behavior của phase cũ chỉ để test pass.

Việc cần làm:
1. Chạy:
   python -m pytest tests/test_phase16_*.py -q

2. Nếu fail:
   - Đọc stacktrace.
   - Xác định lỗi test sai hay bug code.
   - Nếu test sai vì không khớp API hiện tại nhưng behavior đúng theo plan, sửa test cho đúng API.
   - Nếu code bug rõ, sửa tối thiểu.

3. Chạy lại:
   python -m pytest tests/test_phase16_*.py -q

4. Chạy thêm nhóm regression trọng tâm:
   python -m pytest tests/test_trade_gate_engine.py tests/test_account_guard_phase7.py tests/test_entry_engine.py -q
   python -m pytest tests/test_final_score_engine_*_phase13.py tests/test_decision_engine_*_phase14.py -q
   python -m pytest tests/test_scanner.py tests/test_phase15_scanner_ranking_realistic.py -q

5. Nếu regression fail do môi trường thiếu PyQt6/MT5/network, ghi rõ. Không sửa code bừa.

Tiêu chí hoàn thành:
- tests/test_phase16_*.py pass.
- Regression trọng tâm pass hoặc lỗi môi trường được ghi rõ.
- Không có sửa ngoài phạm vi.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 14 — PHASE 16.14: CHẠY FULL REGRESSION NẾU MÔI TRƯỜNG CHO PHÉP VÀ PHÂN LOẠI LỖI

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 16 là phase test plan. Sau khi test Phase 16 pass, cần thử full regression nếu môi trường cho phép.

Mục tiêu riêng của prompt này:
Chạy full pytest hoặc gần full pytest, phân loại lỗi môi trường và lỗi code. Chỉ sửa bug rất nhỏ nếu chắc chắn do code test plan phát hiện.

Yêu cầu rất quan trọng:
- Không thêm feature mới.
- Không refactor.
- Không sửa code nếu lỗi do:
  - thiếu PyQt6
  - thiếu MT5
  - network không ổn định
  - test integration cần môi trường broker/news thật
- Không sửa database/migration.
- Không sửa UI nếu không cần.

Việc cần làm:
1. Chạy:
   python -m pytest -q

2. Nếu full test quá lâu hoặc fail nhiều do môi trường:
   - Ghi lại nhóm lỗi.
   - Chạy fallback focused suite:
     python -m pytest tests/test_phase16_*.py -q
     python -m pytest tests/test_trade_gate_engine.py tests/test_account_guard_phase7.py tests/test_entry_engine.py tests/test_scanner.py -q
     python -m pytest tests/test_final_score_engine_*_phase13.py tests/test_decision_engine_*_phase14.py tests/test_scanner_ranking_engine_*_phase15.py -q

3. Nếu có lỗi code rõ ràng do Phase 16 test phát hiện:
   - Sửa tối thiểu.
   - Chạy lại test liên quan.

4. Không tạo feature mới.
5. Không chỉnh expected test cũ bừa.

Tiêu chí hoàn thành:
- Có kết quả full regression hoặc focused regression.
- Lỗi môi trường được phân loại rõ.
- Không sửa ngoài phạm vi.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 15 — PHASE 16.15: RÀ SOÁT CUỐI PHASE 16 VÀ BÁO HOÀN THÀNH

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Đây là prompt cuối Phase 16. Không thêm test mới lớn nữa, chỉ rà soát cuối và xác nhận phase test plan đã hoàn thành.

Mục tiêu riêng của prompt này:
Xác nhận Phase 16 đã hoàn thành đúng phạm vi: bổ sung test plan, test coverage quan trọng, regression, không thêm feature mới.

Yêu cầu rất quan trọng:
- Không thêm feature mới.
- Không refactor code.
- Không sửa database/migration.
- Không sửa services.
- Chỉ sửa lỗi import/test fail nhỏ nếu phát hiện rõ ràng.

Checklist cần kiểm tra:
1. Có các test Phase 16 tối thiểu:
   - tests/test_phase16_test_plan_smoke.py
   - tests/test_phase16_score_rename_signal_score.py
   - tests/test_phase16_trade_gate_matrix.py
   - tests/test_phase16_score_gap_decision.py
   - tests/test_phase16_expected_effective_rr_gate.py
   - tests/test_phase16_account_guard_action_permissions.py
   - tests/test_phase16_revenge_trade_detector.py
   - tests/test_phase16_final_score_decision_priority.py
   - tests/test_phase16_scanner_blocked_ranking_guard.py
   - tests/test_phase16_analysis_output_contract.py
   - tests/test_phase16_analysis_to_scanner_contract.py
   - tests/test_phase16_test_manifest_exists.py

2. Có manifest:
   - docs/Upgrade/score_and_gate_upgrade/phase16_test_manifest.txt

3. Các nhóm behavior đã được test:
   - signal_score/total.
   - gate matrix.
   - score_gap.
   - expected_effective_rr.
   - account_guard action permissions.
   - revenge trade.
   - final_score + decision priority.
   - scanner blocked ranking guard.
   - analysis output contract.
   - analysis -> scanner contract.

4. Không có thay đổi feature không cần thiết ở:
   - services/
   - data/migrations/
   - ui/
   - controllers/

Test bắt buộc:
1. Chạy:
   python -m pytest tests/test_phase16_*.py -q

2. Chạy focused regression:
   python -m pytest tests/test_trade_gate_engine.py tests/test_account_guard_phase7.py tests/test_entry_engine.py tests/test_scanner.py -q
   python -m pytest tests/test_final_score_engine_*_phase13.py tests/test_decision_engine_*_phase14.py tests/test_scanner_ranking_engine_*_phase15.py -q

3. Nếu đủ thời gian:
   python -m pytest -q

4. Nếu full test fail do môi trường, báo rõ:
   - thiếu PyQt6?
   - thiếu MT5?
   - network?
   - test integration external?

Báo cáo cuối cần nêu ngắn gọn:
- Đã tạo các nhóm test nào.
- Test nào đã chạy.
- Có lỗi môi trường nào không.
- Có sửa bug nhỏ nào không.

Sau khi hoàn thành prompt cuối cùng, bắt buộc báo đúng 2 câu sau:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT
ĐÃ HOÀN THÀNH PHASE SỐ 16
