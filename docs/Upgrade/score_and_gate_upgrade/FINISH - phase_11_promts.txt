PROMPT 1 — PHASE 11.1: KIỂM TRA HIỆN TRẠNG SAU PHASE 10 VÀ KHÓA PHẠM VI PHASE 11

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Hệ thống đã hoàn thành Phase 10: có evidence_score/statistical_edge_engine bản đơn giản. Bây giờ bắt đầu Phase 11: EXECUTION QUALITY SCORE.

Mục tiêu Phase 11:
Tạo lớp tính điểm chất lượng thực thi lệnh sau khi lệnh đã đóng hoặc sau khi có dữ liệu thực thi từ journal. Điểm này dùng để phân biệt hệ thống phân tích sai với trader thực thi sai. Phase này KHÔNG được thay đổi quyết định vào lệnh hiện tại, KHÔNG được thay đổi gate, KHÔNG được tự động phát hiện lỗi hành vi nâng cao của Phase 12.

Mục tiêu riêng của prompt này:
Chỉ kiểm tra hiện trạng code và xác định file được phép sửa trong Phase 11. Chưa viết logic mới nếu chưa cần.

Yêu cầu rất quan trọng:
- Chỉ được đọc code, không sửa logic nghiệp vụ trong prompt này.
- Không sửa signal_engine.py.
- Không sửa analysis_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa entry_engine.py.
- Không sửa account_guard.py.
- Không sửa statistical_edge_engine.py.
- Không sửa scanner/UI.
- Không sửa database migration trong prompt này.
- Không làm Phase 12: không tạo trade_mistake_detector.py.
- Không làm Phase 13: không tính final_score mới.
- Không làm Phase 14: không tạo decision_engine.py.

Việc cần làm:
1. Kiểm tra các file/thư mục sau có tồn tại không:
   - core/reason_codes.py
   - core/statistical_edge_engine.py
   - services/journal_service.py
   - tests/

2. Mở services/journal_service.py để xem JournalEntry hiện có các field Phase 7 chưa:
   - result_r
   - result_pct
   - closed_at
   - exit_reason
   - actual_lot
   - planned_lot

3. Mở core/reason_codes.py để kiểm tra đã có các reason/penalty code liên quan execution chưa. Tối thiểu cần chuẩn bị các code sau cho Phase 11:
   - EXECUTION_QUALITY_OK
   - EXECUTION_CHASED_PRICE
   - EXECUTION_OVERSIZED
   - EXECUTION_MOVED_SL_FURTHER
   - EXECUTION_REVENGE_CONFIRMED
   - EXECUTION_MANUAL_PENALTY
   - EXECUTION_DATA_INCOMPLETE

4. Nếu thiếu các code trên thì CHỈ bổ sung đúng các constant còn thiếu vào core/reason_codes.py.
   - Nếu file có REASON_CODE_MESSAGES hoặc mapping tiếng Việt tương tự thì bổ sung message tiếng Việt ngắn, dễ hiểu.
   - Không đổi tên code cũ.
   - Không xóa code cũ.
   - Không chỉnh các code STAT_EDGE_* của Phase 10.

5. Ghi nhớ phạm vi Phase 11:
   - Được tạo core/execution_quality_engine.py.
   - Được tạo tests/test_execution_quality_engine_*.py.
   - Chỉ tích hợp vào journal nếu prompt sau yêu cầu rõ.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_reason_codes_phase9.py -q
  nếu file này tồn tại.
- Chạy:
  python -m pytest tests/test_statistical_edge_engine_import_phase10.py -q
  nếu file này tồn tại.
- Nếu chưa có test reason code cho Phase 11 thì chưa cần tạo trong prompt này, trừ khi bạn vừa thêm constant mới và muốn tạo test import cực nhỏ.

Tiêu chí hoàn thành:
- Không thay đổi hành vi phân tích/gate/decision hiện tại.
- Nếu có sửa core/reason_codes.py thì chỉ sửa đúng phần constant/message còn thiếu.
- Không phát sinh lỗi import.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 2 — PHASE 11.2: TẠO MODULE RỖNG core/execution_quality_engine.py VÀ TEST IMPORT

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 11 cần module riêng để tính execution_quality_score sau khi có dữ liệu thực thi. Prompt này chỉ tạo khung module và test import, chưa viết logic phạt điểm đầy đủ.

Mục tiêu riêng của prompt này:
Tạo file core/execution_quality_engine.py có output mặc định an toàn.

Yêu cầu rất quan trọng:
- Chỉ được tạo/sửa 2 file:
  1. core/execution_quality_engine.py
  2. tests/test_execution_quality_engine_import_phase11.py
- Không sửa journal_service.py.
- Không sửa database migration.
- Không sửa signal_engine.py.
- Không sửa analysis_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa statistical_edge_engine.py.
- Không sửa UI/scanner.
- Không thay đổi decision hiện tại.

Việc cần làm:
1. Tạo file mới:
   core/execution_quality_engine.py

2. Trong file này, import các reason code từ core.reason_codes nếu đã có:
   - EXECUTION_QUALITY_OK
   - EXECUTION_DATA_INCOMPLETE

3. Nếu import từ core.reason_codes gây lỗi do project đang dùng style khác, hãy theo đúng style import hiện tại của project. Không phá cấu trúc package.

4. Định nghĩa constant:
   - DEFAULT_EXECUTION_QUALITY_SCORE = 100
   - MIN_EXECUTION_QUALITY_SCORE = 0
   - MAX_EXECUTION_QUALITY_SCORE = 100

5. Tạo helper:

   def clamp_score(value: float | int, minimum: int = 0, maximum: int = 100) -> int:
       ...

   Yêu cầu clamp_score:
   - Convert được int/float/string số về int gần nhất bằng round hoặc int hợp lý.
   - Nếu input không hợp lệ thì trả minimum.
   - Không raise exception với None, string bẩn, NaN, inf.
   - Kết quả luôn nằm trong [minimum, maximum].

6. Tạo hàm mặc định:

   def default_execution_quality_result(reason: str = "no_execution_issue_detected") -> dict:
       return {
           "execution_quality_score": 100,
           "reason_codes": [EXECUTION_QUALITY_OK],
           "penalty_codes": [],
           "warning_codes": [],
           "score_breakdown": {},
           "reason": reason,
       }

7. Chưa tạo calculate_execution_quality trong prompt này nếu chưa cần. Nếu cần tạo thì chỉ cho return default, chưa phạt điểm.

8. Tạo test mới:
   tests/test_execution_quality_engine_import_phase11.py

9. Test cần kiểm tra:
   - import được core.execution_quality_engine
   - DEFAULT_EXECUTION_QUALITY_SCORE == 100
   - clamp_score(120) == 100
   - clamp_score(-5) == 0
   - clamp_score("80") == 80
   - default_execution_quality_result() trả execution_quality_score = 100
   - penalty_codes là list rỗng

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_execution_quality_engine_import_phase11.py -q

Tiêu chí hoàn thành:
- Module import sạch.
- Không ảnh hưởng hệ thống hiện tại.
- Test mới pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 3 — PHASE 11.3: THÊM HELPER ĐỌC BOOLEAN/TAG LỖI THỰC THI TỪ TRADE DICT

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Journal thực tế có thể lưu lỗi thực thi dưới nhiều dạng: boolean, string, list tag thủ công, list tag tự động. Cần helper nhỏ để đọc an toàn trước khi tính điểm.

Mục tiêu riêng của prompt này:
Thêm helper đọc dữ liệu lỗi thực thi từ dict, chưa tính điểm chính.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/execution_quality_engine.py
  2. tests/test_execution_quality_engine_helpers_phase11.py
- Không sửa journal_service.py.
- Không sửa migration.
- Không sửa UI.
- Không sửa các engine khác.
- Không tạo trade_mistake_detector.py.
- Không tự động phát hiện revenge/chased/oversized ở prompt này. Chỉ đọc flag/tag nếu dữ liệu đã có.

Việc cần làm:
1. Trong core/execution_quality_engine.py, thêm helper:

   def truthy(value) -> bool:
       ...

   Logic yêu cầu:
   - True, 1, "1", "true", "yes", "y", "có", "co", "đúng", "dung" => True.
   - False, 0, "0", "false", "no", "n", "không", "khong", "sai", None, "" => False.
   - Không raise exception.

2. Thêm helper:

   def normalize_tags(value) -> list[str]:
       ...

   Logic yêu cầu:
   - Nếu value là list/tuple/set => trả list string đã strip, lower, bỏ rỗng.
   - Nếu value là string JSON list như '["chased_price", "oversized_position"]' => parse được thì trả list.
   - Nếu value là string phân tách bằng dấu phẩy như "chased_price, oversized_position" => tách thành list.
   - Nếu value là None hoặc không hợp lệ => trả [].
   - Không raise exception.

3. Thêm helper:

   def has_tag(trade: dict, tag: str) -> bool:
       ...

   Logic yêu cầu:
   - Kiểm tra tag trong các field nếu có:
     - auto_mistake_tags
     - manual_mistake_tags
     - mistake_tags
     - execution_tags
   - So sánh không phân biệt hoa thường.
   - Nếu trade không phải dict thì trả False.

4. Tạo test mới:
   tests/test_execution_quality_engine_helpers_phase11.py

5. Test với dữ liệu giống thật:
   - {"manual_mistake_tags": ["chased_price", "oversized_position"]}
   - {"auto_mistake_tags": "moved_sl_further, revenge_trade"}
   - {"execution_tags": '["chased_price", "moved_sl_further"]'}
   - dữ liệu None/string bẩn không làm crash.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_execution_quality_engine_helpers_phase11.py -q
- Chạy lại:
  python -m pytest tests/test_execution_quality_engine_import_phase11.py -q

Tiêu chí hoàn thành:
- Helper ổn định với dữ liệu bẩn.
- Không có side effect.
- Test mới pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 4 — PHASE 11.4: THÊM HÀM TẠO PENALTY ITEM CHUẨN HÓA

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Execution quality cần trả breakdown rõ ràng để UI/debug/journal có thể hiểu vì sao bị trừ điểm.

Mục tiêu riêng của prompt này:
Chuẩn hóa cấu trúc penalty item, chưa áp dụng công thức tính điểm chính.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/execution_quality_engine.py
  2. tests/test_execution_quality_engine_penalty_item_phase11.py
- Không sửa UI/journal/database.
- Không sửa decision/gate/analysis.
- Không tạo auto detector.

Việc cần làm:
1. Trong core/execution_quality_engine.py, thêm helper:

   def make_penalty_item(code: str, points: int, description: str = "") -> dict:
       ...

2. Output yêu cầu:

   {
       "code": code,
       "points": positive_integer_points,
       "description": description,
   }

3. Logic yêu cầu:
   - points luôn là số nguyên dương hoặc 0.
   - Nếu truyền points âm thì chuyển thành abs hoặc max hợp lý, nhưng phải nhất quán.
   - Nếu code rỗng thì dùng "UNKNOWN_EXECUTION_PENALTY".
   - description luôn là string.
   - Không raise exception với dữ liệu bẩn.

4. Thêm helper:

   def sum_penalty_points(items: list[dict]) -> int:
       ...

   Logic:
   - Cộng field points của từng item.
   - Bỏ qua item bẩn.
   - Không raise exception.

5. Tạo test mới:
   tests/test_execution_quality_engine_penalty_item_phase11.py

6. Test cần kiểm tra:
   - make_penalty_item("EXECUTION_CHASED_PRICE", 25) trả points = 25.
   - make_penalty_item("", -30) không crash và code fallback.
   - sum_penalty_points với list mixed dữ liệu bẩn vẫn trả đúng.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_execution_quality_engine_penalty_item_phase11.py -q
- Chạy lại test helper Phase 11 trước đó.

Tiêu chí hoàn thành:
- Penalty item chuẩn hóa.
- Chưa thay đổi behavior app.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 5 — PHASE 11.5: THÊM calculate_execution_quality() BẢN CƠ BẢN THEO 4 LỖI CHÍNH

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Theo plan Phase 11, score ban đầu:
- bắt đầu từ 100
- chased_price: trừ 25
- oversized_position: trừ 30
- moved_sl_further: trừ 30
- revenge_trade/revenge_trade_confirmed: trừ 35
- score không thấp hơn 0

Mục tiêu riêng của prompt này:
Triển khai calculate_execution_quality(trade: dict) bản cơ bản, chỉ đọc flag/tag có sẵn, không tự phát hiện lỗi.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/execution_quality_engine.py
  2. tests/test_execution_quality_engine_basic_phase11.py
- Không sửa journal_service.py.
- Không sửa migration.
- Không sửa analysis_engine.py.
- Không sửa decision_engine.py.
- Không sửa trade_gate_engine.py.
- Không tạo trade_mistake_detector.py.
- Không tự suy luận lỗi từ giá/lot/thời gian trong prompt này. Chỉ đọc field boolean/tag đã có.

Việc cần làm:
1. Trong core/execution_quality_engine.py, thêm hoặc hoàn thiện hàm:

   def calculate_execution_quality(trade: dict | None) -> dict:
       ...

2. Logic bắt buộc:
   - Nếu trade không phải dict hoặc None:
     trả execution_quality_score = 100 hoặc 50 tùy bạn thấy an toàn hơn, nhưng phải có warning_codes chứa EXECUTION_DATA_INCOMPLETE. Khuyến nghị: 100 nhưng warning data incomplete, vì thiếu dữ liệu không được tự phạt trader.
   - score ban đầu = 100.
   - Nếu trade có flag truthy hoặc tag tương ứng:
     a. chased_price hoặc tag "chased_price" => trừ 25, penalty code EXECUTION_CHASED_PRICE.
     b. oversized_position hoặc tag "oversized_position" => trừ 30, penalty code EXECUTION_OVERSIZED.
     c. moved_sl_further hoặc moved_stop_loss hoặc tag "moved_sl_further"/"moved_stop_loss" => trừ 30, penalty code EXECUTION_MOVED_SL_FURTHER.
     d. revenge_trade hoặc revenge_trade_confirmed hoặc tag "revenge_trade"/"revenge_trade_confirmed" => trừ 35, penalty code EXECUTION_REVENGE_CONFIRMED.
   - Không trừ 2 lần cho cùng một lỗi nếu vừa có boolean vừa có tag.
   - score cuối = max(100 - tổng penalty, 0).

3. Output bắt buộc có cấu trúc:

   {
       "execution_quality_score": int,
       "reason_codes": list[str],
       "penalty_codes": list[str],
       "warning_codes": list[str],
       "score_breakdown": {
           "base_score": 100,
           "total_penalty": int,
           "penalties": list[dict],
       },
   }

4. Nếu không có lỗi nào:
   - execution_quality_score = 100
   - reason_codes chứa EXECUTION_QUALITY_OK
   - penalty_codes rỗng

5. Tạo test mới:
   tests/test_execution_quality_engine_basic_phase11.py

6. Test bắt buộc:
   - trade sạch => score 100.
   - chased_price True => score 75.
   - oversized_position True => score 70.
   - moved_sl_further True => score 70.
   - revenge_trade True => score 65.
   - cả 4 lỗi => score 0, không âm.
   - vừa boolean chased_price True vừa tag chased_price => chỉ trừ 25 một lần.
   - trade None không crash.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_execution_quality_engine_basic_phase11.py -q
- Chạy toàn bộ test Phase 11 đã tạo:
  python -m pytest tests/test_execution_quality_engine_*_phase11.py -q

Tiêu chí hoàn thành:
- Công thức đúng plan.
- Không tự động detect lỗi ngoài dữ liệu đã có.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 6 — PHASE 11.6: BỔ SUNG TRƯỜNG HỢP LỖI THỦ CÔNG manual_penalty NHƯNG KHÔNG LÀM AUTO DETECTOR

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Trader hoặc UI sau này có thể nhập lỗi thủ công. Phase 11 cần hỗ trợ manual_mistake_tags/manual_penalty một cách an toàn, nhưng chưa tạo UI và chưa tự phát hiện lỗi.

Mục tiêu riêng của prompt này:
Cho calculate_execution_quality đọc manual_mistake_tags và manual_penalty_points nếu có.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/execution_quality_engine.py
  2. tests/test_execution_quality_engine_manual_phase11.py
- Không sửa UI.
- Không sửa journal schema.
- Không sửa journal_service.py.
- Không tạo trade_mistake_detector.py.
- Không tự động tạo manual tags.

Việc cần làm:
1. Cập nhật calculate_execution_quality để hỗ trợ các field tùy chọn:
   - manual_penalty_points
   - manual_execution_penalty
   - manual_mistake_tags

2. Logic manual_penalty_points:
   - Nếu trade có manual_penalty_points hợp lệ > 0 thì trừ thêm số điểm đó.
   - Cap manual penalty tối đa 50 điểm để tránh nhập nhầm làm score âm quá mạnh trước khi clamp.
   - Thêm penalty code EXECUTION_MANUAL_PENALTY.
   - Thêm penalty item description: "Điểm phạt thủ công từ journal/trader."

3. Logic manual_mistake_tags:
   - Nếu manual_mistake_tags chứa tag đã hỗ trợ như chased_price/oversized_position/moved_sl_further/revenge_trade thì vẫn áp dụng penalty tương ứng như Prompt 5.
   - Không trừ trùng nếu tag đó cũng nằm trong auto_mistake_tags.

4. Không được tạo tag mới trong prompt này.

5. Tạo test mới:
   tests/test_execution_quality_engine_manual_phase11.py

6. Test bắt buộc:
   - manual_penalty_points = 10 => score 90, penalty_codes có EXECUTION_MANUAL_PENALTY.
   - manual_penalty_points = 999 => chỉ trừ tối đa 50.
   - manual_mistake_tags = ["chased_price"] => score 75.
   - manual_mistake_tags và auto_mistake_tags cùng có chased_price => chỉ trừ 25 một lần.
   - manual_penalty_points là string "15" => xử lý được.
   - manual_penalty_points bẩn => không crash, không trừ.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_execution_quality_engine_manual_phase11.py -q
- Chạy toàn bộ test Phase 11:
  python -m pytest tests/test_execution_quality_engine_*_phase11.py -q

Tiêu chí hoàn thành:
- Hỗ trợ manual penalty an toàn.
- Không thêm UI/schema.
- Không làm Phase 12.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 7 — PHASE 11.7: THÊM LÝ DO DATA INCOMPLETE NHƯNG KHÔNG PHẠT NẶNG KHI THIẾU DỮ LIỆU

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Dữ liệu journal cũ có thể chưa có actual_lot/planned_lot/auto_mistake_tags/manual_mistake_tags. Thiếu dữ liệu không có nghĩa trader thực thi sai.

Mục tiêu riêng của prompt này:
Chuẩn hóa warning khi dữ liệu thực thi thiếu, nhưng giữ score trung lập/an toàn.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/execution_quality_engine.py
  2. tests/test_execution_quality_engine_incomplete_data_phase11.py
- Không sửa journal_service.py.
- Không sửa migration.
- Không sửa UI.
- Không sửa analysis/decision/gate.

Việc cần làm:
1. Trong calculate_execution_quality, xác định dữ liệu thực thi có đủ để đánh giá hay không.

2. Các dấu hiệu có dữ liệu thực thi:
   - Có một trong các field boolean: chased_price, oversized_position, moved_sl_further, revenge_trade.
   - Hoặc có auto_mistake_tags/manual_mistake_tags/mistake_tags/execution_tags.
   - Hoặc có manual_penalty_points.

3. Nếu trade là dict nhưng không có bất kỳ dữ liệu thực thi nào ở trên:
   - execution_quality_score vẫn = 100.
   - warning_codes có EXECUTION_DATA_INCOMPLETE.
   - reason_codes có EXECUTION_QUALITY_OK hoặc không tùy thiết kế, nhưng không được thêm penalty.
   - score_breakdown phải ghi data_complete = False.

4. Nếu có dữ liệu thực thi thì score_breakdown ghi data_complete = True.

5. Không được trừ điểm chỉ vì thiếu dữ liệu.

6. Tạo test mới:
   tests/test_execution_quality_engine_incomplete_data_phase11.py

7. Test bắt buộc:
   - trade chỉ có symbol/direction/result_r/closed_at => score 100 + warning EXECUTION_DATA_INCOMPLETE.
   - trade có chased_price False rõ ràng => data_complete True, không warning incomplete.
   - trade có manual_mistake_tags [] rõ ràng => data_complete True, không warning incomplete.
   - trade None => không crash, warning incomplete.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_execution_quality_engine_incomplete_data_phase11.py -q
- Chạy toàn bộ test Phase 11.

Tiêu chí hoàn thành:
- Journal cũ không bị phạt oan.
- Output có warning rõ.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 8 — PHASE 11.8: THÊM HÀM TÍNH EXECUTION QUALITY CHO NHIỀU TRADE VÀ SUMMARY THỐNG KÊ

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Sau này dashboard/journal có thể cần xem chất lượng thực thi trung bình. Prompt này chỉ thêm helper tổng hợp trong module, chưa tích hợp UI.

Mục tiêu riêng của prompt này:
Thêm calculate_execution_quality_batch(trades) và summarize_execution_quality(results).

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/execution_quality_engine.py
  2. tests/test_execution_quality_engine_batch_phase11.py
- Không sửa UI/dashboard.
- Không sửa journal_service.py.
- Không sửa database.
- Không sửa statistical_edge_engine.py.
- Không dùng batch result để thay đổi final_score/decision.

Việc cần làm:
1. Thêm hàm:

   def calculate_execution_quality_batch(trades: list[dict]) -> list[dict]:
       ...

   Logic:
   - Nếu trades không phải list thì trả [].
   - Với mỗi trade dict, gọi calculate_execution_quality.
   - Mỗi result nên giữ lại một số field định danh nếu có:
     - symbol
     - direction
     - closed_at
     - result_r
   - Không mutate trade gốc.

2. Thêm hàm:

   def summarize_execution_quality(results: list[dict]) -> dict:
       ...

   Output đề xuất:
   {
       "sample_size": int,
       "average_execution_quality_score": float | None,
       "min_execution_quality_score": int | None,
       "max_execution_quality_score": int | None,
       "penalty_code_counts": dict[str, int],
       "warning_code_counts": dict[str, int],
   }

3. Logic summary:
   - Bỏ qua result bẩn không có score hợp lệ.
   - average làm tròn 2 chữ số.
   - Không raise exception với dữ liệu bẩn.

4. Tạo test mới:
   tests/test_execution_quality_engine_batch_phase11.py

5. Test với dữ liệu giống thật:
   - EURUSD buy sạch score 100.
   - GBPJPY sell chased_price score 75.
   - XAUUSD buy oversized + moved_sl_further score 40.
   - Summary average = 71.67 hoặc tương ứng theo rounding.
   - penalty_code_counts đếm đúng.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_execution_quality_engine_batch_phase11.py -q
- Chạy toàn bộ test Phase 11.

Tiêu chí hoàn thành:
- Batch helper hoạt động ổn.
- Không tích hợp UI/decision.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 9 — PHASE 11.9: THÊM HÀM CHUẨN BỊ PAYLOAD LƯU JOURNAL NHƯNG CHƯA MIGRATION DB

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Plan Phase 11 nói sau mỗi lệnh đóng, journal nên có execution_quality_score, auto_mistake_tags, manual_mistake_tags. Tuy nhiên migration nâng cao nằm ở Phase 17 đợt 2. Prompt này chỉ tạo helper payload để sau này dùng, không sửa DB.

Mục tiêu riêng của prompt này:
Thêm helper tạo payload kết quả execution quality để lưu vào journal/export sau này.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/execution_quality_engine.py
  2. tests/test_execution_quality_engine_payload_phase11.py
- Không sửa data/migrations.
- Không sửa services/journal_service.py.
- Không sửa JournalEntry dataclass.
- Không sửa UI.
- Không tạo cột DB mới trong prompt này.
- Không làm Phase 17 migration.

Việc cần làm:
1. Thêm hàm:

   def build_execution_quality_journal_payload(trade: dict | None) -> dict:
       ...

2. Hàm này gọi calculate_execution_quality(trade) và trả payload phẳng, dễ lưu:

   {
       "execution_quality_score": int,
       "execution_quality_penalty_codes": list[str],
       "execution_quality_warning_codes": list[str],
       "execution_quality_breakdown": dict,
   }

3. Nếu trade có sẵn auto_mistake_tags/manual_mistake_tags thì payload giữ lại bản normalize:

   {
       "auto_mistake_tags": list[str],
       "manual_mistake_tags": list[str],
       ...
   }

4. Không ghi file, không gọi database.

5. Tạo test mới:
   tests/test_execution_quality_engine_payload_phase11.py

6. Test bắt buộc:
   - trade có chased_price True => payload score 75, penalty code đúng.
   - trade có auto_mistake_tags string => payload normalize thành list.
   - trade None không crash.
   - payload có đủ key bắt buộc.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_execution_quality_engine_payload_phase11.py -q
- Chạy toàn bộ test Phase 11.

Tiêu chí hoàn thành:
- Có payload chuẩn cho migration/tích hợp sau này.
- Không sửa database/journal_service.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 10 — PHASE 11.10: THÊM TÙY CHỌN LẤY EXECUTION QUALITY TỪ TRADE ĐÃ CÓ SCORE SẴN

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Một số journal/export tương lai có thể đã lưu execution_quality_score từ trước. Hàm tính nên ưu tiên giá trị đã lưu nếu người dùng yêu cầu, tránh tính lại khác lịch sử.

Mục tiêu riêng của prompt này:
Thêm tham số use_existing_score an toàn cho calculate_execution_quality.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/execution_quality_engine.py
  2. tests/test_execution_quality_engine_existing_score_phase11.py
- Không sửa journal_service.py.
- Không sửa migration.
- Không sửa analysis/decision/gate.

Việc cần làm:
1. Cập nhật chữ ký hàm:

   def calculate_execution_quality(trade: dict | None, *, use_existing_score: bool = True) -> dict:
       ...

2. Logic:
   - Nếu use_existing_score=True và trade có execution_quality_score hợp lệ:
     + Trả score đó sau khi clamp 0–100.
     + reason_codes nên có EXECUTION_QUALITY_OK nếu không có penalty.
     + score_breakdown ghi "used_existing_score": True.
   - Nếu use_existing_score=False thì bỏ qua score sẵn và tính lại từ tags/flags.
   - Nếu execution_quality_score bẩn thì tính lại bình thường, không crash.

3. Đảm bảo các test cũ vẫn pass.

4. Tạo test mới:
   tests/test_execution_quality_engine_existing_score_phase11.py

5. Test bắt buộc:
   - trade {execution_quality_score: 88} => trả 88 khi use_existing_score=True.
   - trade {execution_quality_score: 88, chased_price: True} => use_existing_score=True trả 88, use_existing_score=False trả 75.
   - score 999 clamp về 100.
   - score bẩn không crash.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_execution_quality_engine_existing_score_phase11.py -q
- Chạy toàn bộ test Phase 11.

Tiêu chí hoàn thành:
- Tương thích dữ liệu đã lưu.
- Không phá test cũ.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 11 — PHASE 11.11: KIỂM TRA TƯƠNG THÍCH VỚI JournalEntry HIỆN TẠI, KHÔNG SỬA SCHEMA

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Execution quality score sẽ được dùng sau khi journal có thêm field ở migration đợt 2. Nhưng hiện tại Phase 11 không được sửa schema nếu chưa được yêu cầu riêng.

Mục tiêu riêng của prompt này:
Viết test chứng minh engine có thể nhận dict từ list_closed_trades_for_account_guard() hiện tại mà không crash.

Yêu cầu rất quan trọng:
- Chỉ được tạo/sửa:
  1. tests/test_execution_quality_engine_journal_compat_phase11.py
  2. core/execution_quality_engine.py nếu thật sự cần sửa để test pass
- Không sửa services/journal_service.py.
- Không sửa JournalEntry.
- Không sửa data/migrations.
- Không sửa controllers/UI.

Việc cần làm:
1. Tạo test mới:
   tests/test_execution_quality_engine_journal_compat_phase11.py

2. Dữ liệu test mô phỏng output hiện tại của JournalService.list_closed_trades_for_account_guard():

   trades = [
       {
           "symbol": "EUR/USD",
           "direction": "buy",
           "result_r": 1.4,
           "result_pct": 1.2,
           "closed_at": "2026-06-04T10:30:00+07:00",
           "exit_reason": "take_profit",
           "actual_lot": 0.10,
           "planned_lot": 0.10,
       },
       {
           "symbol": "GBP/JPY",
           "direction": "sell",
           "result_r": -1.0,
           "result_pct": -0.9,
           "closed_at": "2026-06-04T11:15:00+07:00",
           "exit_reason": "stop_loss",
           "actual_lot": 0.20,
           "planned_lot": 0.10,
       },
   ]

3. Gọi calculate_execution_quality_batch(trades).

4. Kỳ vọng:
   - Không crash.
   - Mỗi result có execution_quality_score.
   - Vì dữ liệu chưa có mistake tags/flags nên score = 100 và có warning EXECUTION_DATA_INCOMPLETE.
   - Không tự động detect oversized từ actual_lot/planned_lot trong Phase 11 này, vì đó là Phase 12.

5. Chỉ sửa engine nếu hiện tại không xử lý được dict dạng này.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_execution_quality_engine_journal_compat_phase11.py -q
- Chạy toàn bộ test Phase 11.

Tiêu chí hoàn thành:
- Engine tương thích journal hiện tại.
- Không sửa schema.
- Không tự động detect lỗi hành vi.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 12 — PHASE 11.12: THÊM REALISTIC TEST VỚI 6 LỆNH ĐÓNG GIỐNG JOURNAL THẬT

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Cần test bằng dữ liệu gần thực tế để tránh engine chỉ đúng với toy data.

Mục tiêu riêng của prompt này:
Tạo test realistic cho execution_quality_engine với nhiều symbol, nhiều lỗi, dữ liệu bẩn, và dữ liệu thiếu.

Yêu cầu rất quan trọng:
- Chỉ được tạo/sửa:
  1. tests/test_phase11_execution_quality_realistic.py
  2. core/execution_quality_engine.py nếu thật sự cần sửa bug do test phát hiện
- Không sửa UI/journal/schema/decision/gate.
- Không làm Phase 12 auto detector.

Việc cần làm:
1. Tạo test mới:
   tests/test_phase11_execution_quality_realistic.py

2. Tạo dataset 6 lệnh đóng:
   a. EUR/USD buy thắng, không lỗi, manual_mistake_tags=[] => score 100.
   b. GBP/JPY sell thua, chased_price=True => score 75.
   c. XAU/USD buy thua, auto_mistake_tags="oversized_position, moved_sl_further" => score 40.
   d. USD/JPY sell thua, manual_mistake_tags=["revenge_trade_confirmed"], manual_penalty_points=10 => score 55 nếu 100-35-10.
   e. AUD/USD buy hòa, dữ liệu thiếu mistake tags => score 100 + warning incomplete.
   f. EUR/USD sell có execution_quality_score=82 sẵn => trả 82 nếu use_existing_score=True.

3. Test batch:
   - calculate_execution_quality_batch trả 6 result.
   - summarize_execution_quality tính average đúng theo scores: [100,75,40,55,100,82] => average 75.33.
   - penalty_code_counts có:
     + EXECUTION_CHASED_PRICE: 1
     + EXECUTION_OVERSIZED: 1
     + EXECUTION_MOVED_SL_FURTHER: 1
     + EXECUTION_REVENGE_CONFIRMED: 1
     + EXECUTION_MANUAL_PENALTY: 1

4. Test dữ liệu bẩn:
   - manual_mistake_tags=None
   - auto_mistake_tags="not-json ["
   - manual_penalty_points="abc"
   Phải không crash.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_phase11_execution_quality_realistic.py -q
- Chạy toàn bộ test Phase 11:
  python -m pytest tests/test_execution_quality_engine_*_phase11.py tests/test_phase11_execution_quality_realistic.py -q

Tiêu chí hoàn thành:
- Realistic test pass.
- Engine chống dữ liệu bẩn tốt.
- Không có thay đổi ngoài phạm vi.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 13 — PHASE 11.13: TÀI LIỆU NGẮN TRONG DOCSTRING VÀ KHÔNG TẠO FILE DOC RIÊNG

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 11 cần người sau đọc code hiểu rõ: execution quality score không phải signal score, không phải gate, không phải auto mistake detector.

Mục tiêu riêng của prompt này:
Bổ sung docstring/comment ngắn trong core/execution_quality_engine.py để tránh developer dùng sai.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/execution_quality_engine.py
- Không tạo file docs mới.
- Không sửa logic nếu không cần.
- Không sửa test nếu test đang pass.
- Không sửa UI/journal/decision/gate.

Việc cần làm:
1. Thêm docstring đầu file giải thích:
   - Module này tính execution_quality_score sau khi có dữ liệu thực thi.
   - Không quyết định vào lệnh.
   - Không block trade.
   - Không tự động phát hiện lỗi hành vi nâng cao; phần đó thuộc Phase 12.
   - Thiếu dữ liệu không bị phạt, chỉ warning EXECUTION_DATA_INCOMPLETE.

2. Thêm docstring cho calculate_execution_quality:
   - Input là trade dict từ journal/export hoặc payload tương lai.
   - Có thể đọc flag/tag/manual_penalty/existing_score.
   - Output luôn là dict ổn định cho UI/journal/debug.

3. Thêm comment gần phần manual_penalty:
   - Manual penalty là dữ liệu do trader/UI nhập, không phải engine tự suy luận.

Test bắt buộc:
- Chạy toàn bộ test Phase 11:
  python -m pytest tests/test_execution_quality_engine_*_phase11.py tests/test_phase11_execution_quality_realistic.py -q

Tiêu chí hoàn thành:
- Code dễ hiểu hơn.
- Không đổi behavior.
- Test vẫn pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 14 — PHASE 11.14: CHẠY REGRESSION TEST CÁC PHASE TRƯỚC ĐỂ ĐẢM BẢO KHÔNG PHÁ HỆ THỐNG

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 11 chỉ thêm execution_quality_engine độc lập. Cần chạy regression để chắc chắn không phá Phase 7/8/9/10.

Mục tiêu riêng của prompt này:
Chạy test kiểm tra không ảnh hưởng hệ thống hiện tại. Chỉ sửa bug nhỏ nếu bug nằm trong code Phase 11 vừa viết.

Yêu cầu rất quan trọng:
- Không thêm tính năng mới.
- Không sửa engine khác nếu lỗi không do Phase 11 gây ra.
- Không sửa UI.
- Không sửa database.
- Không refactor diện rộng.
- Nếu test cũ fail do môi trường thiếu MT5/PyQt6 thì ghi rõ, không cố sửa lung tung.

Việc cần làm:
1. Chạy toàn bộ test Phase 11:
   python -m pytest tests/test_execution_quality_engine_*_phase11.py tests/test_phase11_execution_quality_realistic.py -q

2. Chạy test Phase 10 nếu tồn tại:
   python -m pytest tests/test_statistical_edge_engine_*_phase10.py tests/test_phase10_statistical_edge_realistic.py -q

3. Chạy test Phase 7 nếu tồn tại:
   python -m pytest tests/test_account_guard_phase7.py tests/test_phase7_account_guard_realistic.py -q

4. Chạy test gate/account guard integration nếu tồn tại:
   python -m pytest tests/test_trade_gate_account_guard_phase7.py tests/test_analysis_account_guard_integration_phase7.py -q

5. Chạy test entry engine nếu tồn tại:
   python -m pytest tests/test_entry_engine.py -q

6. Nếu tất cả ổn, không sửa gì thêm.

7. Nếu có lỗi do Phase 11:
   - Chỉ sửa core/execution_quality_engine.py hoặc test Phase 11 tương ứng.
   - Không sửa file khác.

Tiêu chí hoàn thành:
- Test Phase 11 pass.
- Không làm hỏng Phase 7/8/9/10.
- Không sửa lan sang phần không liên quan.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 15 — PHASE 11.15: KIỂM TRA LẦN CUỐI VÀ BÁO HOÀN THÀNH PHASE 11

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Đây là prompt cuối của Phase 11. Không thêm tính năng mới nữa, chỉ rà soát cuối.

Mục tiêu riêng của prompt này:
Xác nhận Phase 11 đã hoàn thành đúng phạm vi, không lấn sang Phase 12/13/14/17.

Yêu cầu rất quan trọng:
- Không thêm feature mới.
- Không refactor code.
- Không sửa UI.
- Không sửa database migration.
- Không tạo trade_mistake_detector.py.
- Không tạo decision_engine.py.
- Không thay đổi final_score/decision/gate.

Checklist cần kiểm tra:
1. Có file:
   - core/execution_quality_engine.py

2. Module có tối thiểu các hàm/helper:
   - clamp_score
   - truthy
   - normalize_tags
   - has_tag
   - make_penalty_item
   - sum_penalty_points
   - calculate_execution_quality
   - calculate_execution_quality_batch
   - summarize_execution_quality
   - build_execution_quality_journal_payload

3. calculate_execution_quality có các behavior:
   - trade sạch => score 100.
   - chased_price => trừ 25.
   - oversized_position => trừ 30.
   - moved_sl_further/moved_stop_loss => trừ 30.
   - revenge_trade/revenge_trade_confirmed => trừ 35.
   - manual_penalty_points hợp lệ => trừ thêm nhưng cap tối đa 50.
   - score không âm, không vượt 100.
   - thiếu dữ liệu => warning EXECUTION_DATA_INCOMPLETE, không phạt.
   - existing execution_quality_score được dùng nếu use_existing_score=True.

4. Không có thay đổi ở các file ngoài phạm vi trừ reason_codes nếu cần bổ sung constant.

5. Chạy test cuối:
   python -m pytest tests/test_execution_quality_engine_*_phase11.py tests/test_phase11_execution_quality_realistic.py -q

6. Chạy import nhanh bằng python:
   python - <<'PY'
   from core.execution_quality_engine import calculate_execution_quality
   trade = {"symbol": "EUR/USD", "direction": "buy", "chased_price": True, "closed_at": "2026-06-04T10:00:00+07:00"}
   result = calculate_execution_quality(trade)
   print(result)
   assert result["execution_quality_score"] == 75
   assert "penalty_codes" in result
   PY

Tiêu chí hoàn thành:
- Phase 11 hoàn thành độc lập.
- Test pass.
- Không động vào Phase 12/13/14/17.

Sau khi hoàn thành, báo đúng 2 câu sau:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT
ĐÃ HOÀN THÀNH PHASE SỐ 11
