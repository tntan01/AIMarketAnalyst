PROMPT 1 — PHASE 17.1: KIỂM TRA HIỆN TRẠNG MIGRATION/JOURNAL SAU PHASE 16 VÀ KHÓA PHẠM VI PHASE 17

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 16: test plan và regression. Bây giờ bắt đầu Phase 17: MIGRATION PLAN cho Journal.

Mục tiêu Phase 17:
Cập nhật journal database/schema để lưu đủ dữ liệu phục vụ:
- Account Guard.
- Statistical Edge.
- Execution Quality.
- Trade Mistake Detector.
- Final Score / Decision / Scanner ranking export sau này.

Theo plan, Phase 17 có 2 đợt:

Đợt 1 — field tối thiểu:
- result_r
- result_pct
- closed_at
- exit_reason
- actual_lot
- planned_lot

Đợt 2 — field nâng cao:
- planned_entry
- actual_entry
- planned_sl
- actual_sl
- planned_tp
- actual_tp
- actual_exit
- setup_type
- regime
- session
- m15_quality
- spread_at_entry
- expected_effective_rr
- realized_effective_rr
- manual_mistake_tags
- auto_mistake_tags
- execution_quality_score

Nguyên tắc cực kỳ quan trọng:
- Phase 17 được phép sửa migration và journal_service.
- KHÔNG sửa scoring/gate/decision/scanner logic.
- KHÔNG sửa UI trừ khi prompt riêng yêu cầu, và mặc định không sửa UI.
- KHÔNG gọi MT5, AI, news, network.
- KHÔNG làm mất dữ liệu journal cũ.
- Migration phải idempotent: chạy lại không phá DB.
- App cũ có DB 001/002 phải nâng cấp an toàn.
- Database mới từ đầu phải tạo đủ schema sau migration.

Mục tiêu riêng của prompt này:
Chỉ kiểm tra hiện trạng migration và JournalService. Chưa sửa schema, chưa thêm migration mới nếu chưa cần. Nếu đợt 1 đã có đủ thì ghi nhận rõ.

Yêu cầu rất quan trọng:
- Chỉ được đọc code và tạo test audit nhỏ nếu cần.
- Không sửa services/journal_service.py trong prompt này.
- Không sửa data/migrations trong prompt này.
- Không sửa UI/controller/core engines.

Việc cần làm:
1. Kiểm tra thư mục:
   data/migrations/

2. Ghi nhận các migration hiện có:
   - 001_create_journal.sql
   - 002_add_account_guard_fields.sql
   - các file khác nếu có.

3. Mở 001_create_journal.sql và 002_add_account_guard_fields.sql:
   - Xem bảng journal_entries hiện có cột gì.
   - Kiểm tra đợt 1 đã có đủ result_r/result_pct/closed_at/exit_reason/actual_lot/planned_lot chưa.

4. Mở services/journal_service.py:
   - JournalEntry dataclass có field nào.
   - entry_to_record() có ghi field nào.
   - entry_from_row() có đọc field nào.
   - journal_entry_from_analysis() lấy field nào từ analysis.
   - journal_entry_from_scanner_row() lấy field nào từ row.
   - list_closed_trades_for_account_guard() đang trả gì.

5. Tạo file test audit nếu cần:
   tests/test_phase17_journal_migration_audit.py

6. Test audit nên:
   - Tạo temp sqlite db.
   - Khởi tạo JournalService với migrations_dir hiện tại.
   - Query PRAGMA table_info(journal_entries).
   - Assert các field đợt 1 tồn tại nếu migration hiện tại đã có.
   - Không yêu cầu field đợt 2 trong prompt này.

7. Chạy:
   python -m pytest tests/test_phase17_journal_migration_audit.py -q
   nếu đã tạo test audit.

Tiêu chí hoàn thành:
- Biết rõ đợt 1 đã có hay chưa.
- Biết rõ cần migration đợt 2 bắt đầu từ version nào.
- Không thay đổi app code.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 2 — PHASE 17.2: TẠO TEST KHÓA MIGRATION ĐỢT 1 TỐI THIỂU, KHÔNG SỬA NẾU ĐÃ ĐỦ

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 17 đợt 1 yêu cầu các field tối thiểu phục vụ account guard và revenge/oversized cơ bản:
- result_r
- result_pct
- closed_at
- exit_reason
- actual_lot
- planned_lot

Hiện project có thể đã có 002_add_account_guard_fields.sql. Prompt này khóa hành vi bằng test, chỉ sửa migration nếu thật sự thiếu field.

Mục tiêu riêng của prompt này:
Đảm bảo migration đợt 1 tồn tại và idempotent.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase17_journal_migration_phase1_fields.py
  2. data/migrations/002_add_account_guard_fields.sql chỉ nếu thiếu field đợt 1 rõ ràng.
- Không sửa journal_service.py trong prompt này.
- Không sửa migration khác nếu 002 đã đủ.
- Không sửa core engines/UI/controller.

Việc cần làm:
1. Tạo test:
   tests/test_phase17_journal_migration_phase1_fields.py

2. Test database mới:
   - Dùng tmp_path tạo sqlite db mới.
   - Khởi tạo JournalService(db_path=temp_db, migrations_dir=PROJECT_ROOT/data/migrations).
   - Query PRAGMA table_info(journal_entries).
   - Assert có đủ field đợt 1.

3. Test migration idempotent:
   - Gọi service.migrate() thêm lần nữa.
   - Không crash.
   - schema_migrations không duplicate version.
   - Field vẫn tồn tại.

4. Test insert/read tối thiểu:
   - Tạo JournalEntry có result_r=-1.0, result_pct=-0.8, closed_at ISO, exit_reason="stop_loss", actual_lot=0.20, planned_lot=0.10.
   - service.create(entry).
   - service.get_entry(id).
   - Assert các field đợt 1 đọc lại đúng.

5. Nếu 002 thiếu field:
   - Sửa 002_add_account_guard_fields.sql tối thiểu.
   - Dùng ALTER TABLE ADD COLUMN IF NOT EXISTS nếu SQLite version hỗ trợ? SQLite không luôn hỗ trợ IF NOT EXISTS cho ADD COLUMN.
   - Nếu migration hiện tại dùng cách đơn giản, giữ pattern hiện có.
   - Không tạo migration mới cho đợt 1 nếu 002 là migration đợt 1 hiện tại và chưa release.
   - Nếu DB thực tế có thể đã apply 002 thiếu field thì tốt hơn tạo 003 bổ sung thiếu field. Chọn an toàn theo hiện trạng.

6. Không làm đợt 2 trong prompt này.

Chạy:
python -m pytest tests/test_phase17_journal_migration_phase1_fields.py -q
python -m pytest tests/test_journal_account_guard_fields_phase7.py tests/test_account_guard_phase7.py -q

Tiêu chí hoàn thành:
- Field đợt 1 tồn tại.
- Migration idempotent.
- Journal create/get giữ field đợt 1.
- Không sửa ngoài phạm vi.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 3 — PHASE 17.3: TẠO MIGRATION ĐỢT 2 CHO FIELD JOURNAL NÂNG CAO

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 17 đợt 2 yêu cầu thêm field nâng cao phục vụ execution quality, mistake detector, statistical edge và phân tích journal sau này.

Field đợt 2:
- planned_entry
- actual_entry
- planned_sl
- actual_sl
- planned_tp
- actual_tp
- actual_exit
- setup_type
- regime
- session
- m15_quality
- spread_at_entry
- expected_effective_rr
- realized_effective_rr
- manual_mistake_tags
- auto_mistake_tags
- execution_quality_score

Mục tiêu riêng của prompt này:
Tạo migration SQL mới cho đợt 2 và test schema. Chưa sửa JournalEntry dataclass trong prompt này.

Yêu cầu rất quan trọng:
- Chỉ được tạo/sửa:
  1. data/migrations/003_add_journal_execution_fields.sql
  2. tests/test_phase17_journal_migration_phase2_schema.py
- Không sửa services/journal_service.py trong prompt này.
- Không sửa core engines.
- Không sửa UI/controller.
- Không tạo migration khác.

Việc cần làm:
1. Tạo migration mới:
   data/migrations/003_add_journal_execution_fields.sql

2. Nội dung migration:
   - ALTER TABLE journal_entries ADD COLUMN planned_entry REAL;
   - ALTER TABLE journal_entries ADD COLUMN actual_entry REAL;
   - ALTER TABLE journal_entries ADD COLUMN planned_sl REAL;
   - ALTER TABLE journal_entries ADD COLUMN actual_sl REAL;
   - ALTER TABLE journal_entries ADD COLUMN planned_tp REAL;
   - ALTER TABLE journal_entries ADD COLUMN actual_tp REAL;
   - ALTER TABLE journal_entries ADD COLUMN actual_exit REAL;
   - ALTER TABLE journal_entries ADD COLUMN setup_type TEXT;
   - ALTER TABLE journal_entries ADD COLUMN regime TEXT;
   - ALTER TABLE journal_entries ADD COLUMN session TEXT;
   - ALTER TABLE journal_entries ADD COLUMN m15_quality TEXT;
   - ALTER TABLE journal_entries ADD COLUMN spread_at_entry REAL;
   - ALTER TABLE journal_entries ADD COLUMN expected_effective_rr REAL;
   - ALTER TABLE journal_entries ADD COLUMN realized_effective_rr REAL;
   - ALTER TABLE journal_entries ADD COLUMN manual_mistake_tags TEXT;
   - ALTER TABLE journal_entries ADD COLUMN auto_mistake_tags TEXT;
   - ALTER TABLE journal_entries ADD COLUMN execution_quality_score INTEGER;

3. Lưu ý SQLite:
   - Không dùng syntax không chắc chắn như ADD COLUMN IF NOT EXISTS nếu project chưa dùng.
   - Vì JournalService chỉ chạy migration chưa apply theo schema_migrations, migration idempotent ở cấp version.
   - Không thêm NOT NULL cho field mới để không phá dữ liệu cũ.
   - Không thêm default phức tạp.

4. Tạo test:
   tests/test_phase17_journal_migration_phase2_schema.py

5. Test database mới:
   - Init JournalService temp db.
   - Assert các field đợt 2 tồn tại.

6. Test migration trên DB cũ đã có 001/002:
   - Có thể tạo temp db, chạy JournalService với migrations_dir đầy đủ.
   - Hoặc mô phỏng DB cũ bằng cách apply 001/002 trước rồi 003.
   - Assert sau migrate có đủ field 1 và 2.

7. Test schema_migrations:
   - version "003_add_journal_execution_fields" xuất hiện.
   - migrate lần 2 không crash.

Chạy:
python -m pytest tests/test_phase17_journal_migration_phase2_schema.py -q
python -m pytest tests/test_phase17_journal_migration_phase1_fields.py -q

Tiêu chí hoàn thành:
- Migration 003 tồn tại.
- Field đợt 2 tồn tại.
- Không phá DB mới/cũ.
- Chưa sửa journal_service.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 4 — PHASE 17.4: MỞ RỘNG JournalEntry DATACLASS VỚI FIELD ĐỢT 2

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Migration 003 đã thêm field đợt 2 vào DB. Bây giờ JournalEntry dataclass cần có các field tương ứng để create/get/list hoạt động.

Mục tiêu riêng của prompt này:
Cập nhật JournalEntry dataclass và mapping đọc/ghi cơ bản, nhưng chưa tự populate từ analysis.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. services/journal_service.py
  2. tests/test_phase17_journal_entry_phase2_fields.py
- Không sửa migration trong prompt này trừ bug schema rõ ràng.
- Không sửa core engines.
- Không sửa UI/controller.

Việc cần làm:
1. Mở services/journal_service.py.

2. Cập nhật JournalEntry dataclass, thêm field đợt 2 ở cuối với default None hoặc "":
   - planned_entry: float | None = None
   - actual_entry: float | None = None
   - planned_sl: float | None = None
   - actual_sl: float | None = None
   - planned_tp: float | None = None
   - actual_tp: float | None = None
   - actual_exit: float | None = None
   - setup_type: str | None = None
   - regime: str | None = None
   - session: str | None = None
   - m15_quality: str | None = None
   - spread_at_entry: float | None = None
   - expected_effective_rr: float | None = None
   - realized_effective_rr: float | None = None
   - manual_mistake_tags: str | None = None
   - auto_mistake_tags: str | None = None
   - execution_quality_score: int | None = None

3. Kiểm tra entry_to_record(entry):
   - Nếu dùng asdict(entry), có thể tự thêm field mới.
   - Nếu có mapping thủ công, thêm đủ field mới.

4. Kiểm tra entry_from_row(row):
   - Thêm đọc field mới.
   - Phải backward-compatible nếu DB cũ chưa có field? Vì service.migrate() chạy trước nên field có. Nhưng entry_from_row nên dùng helper an toàn nếu đã có pattern.

5. Không tự parse JSON tags trong prompt này; manual_mistake_tags/auto_mistake_tags lưu TEXT JSON/string.

6. Tạo test:
   tests/test_phase17_journal_entry_phase2_fields.py

7. Test:
   - Init JournalService temp db.
   - Tạo JournalEntry với đầy đủ field đợt 2:
     planned_entry=1.0850, actual_entry=1.0860, planned_sl=1.0800, actual_sl=1.0790,
     planned_tp=1.0950, actual_tp=1.0950, actual_exit=1.0920,
     setup_type="pullback", regime="trend_up", session="London",
     m15_quality="strict", spread_at_entry=0.0002,
     expected_effective_rr=1.6, realized_effective_rr=1.2,
     manual_mistake_tags='["closed_too_early"]',
     auto_mistake_tags='["chased_price"]',
     execution_quality_score=75.
   - create(entry), get_entry(id).
   - Assert các field đọc lại đúng.
   - list_entries() cũng trả field đúng.

Chạy:
python -m pytest tests/test_phase17_journal_entry_phase2_fields.py -q
python -m pytest tests/test_journal.py tests/test_journal_account_guard_fields_phase7.py -q

Tiêu chí hoàn thành:
- JournalEntry có field đợt 2.
- Create/get/list giữ field mới.
- Field cũ vẫn hoạt động.
- Không sửa logic core khác.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 5 — PHASE 17.5: THÊM HELPER SERIALIZE/ NORMALIZE TAGS CHO JOURNAL, KHÔNG ĐỔI DETECTOR

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Journal field manual_mistake_tags và auto_mistake_tags là TEXT. Cần helper lưu tags an toàn dưới dạng JSON string để sau này đọc/export ổn định.

Mục tiêu riêng của prompt này:
Thêm helper nhỏ trong journal_service để normalize tag list thành JSON string và parse ngược khi cần.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. services/journal_service.py
  2. tests/test_phase17_journal_tag_serialization.py
- Không sửa core/trade_mistake_detector.py.
- Không sửa execution_quality_engine.py.
- Không sửa migration.
- Không sửa UI/controller.

Việc cần làm:
1. Trong services/journal_service.py, thêm helper module-level:
   def normalize_tag_list(value: object) -> list[str]:
       ...

   Yêu cầu:
   - list/tuple/set => list string lowercase/strip, bỏ rỗng.
   - JSON string list => parse được.
   - comma string => tách được.
   - None/bẩn => [].
   - Deduplicate giữ thứ tự.
   - Không raise exception.

2. Thêm:
   def tags_to_json(value: object) -> str:
       ...

   Yêu cầu:
   - Gọi normalize_tag_list.
   - Trả json.dumps(list, ensure_ascii=False).
   - Nếu empty => "[]".

3. Thêm:
   def tags_from_json(value: object) -> list[str]:
       ...

   Có thể gọi normalize_tag_list.

4. Không bắt buộc đổi JournalEntry type trong prompt này.

5. Tạo test:
   tests/test_phase17_journal_tag_serialization.py

6. Test:
   - normalize_tag_list(["Chased_Price", " chased_price ", ""]) == ["chased_price"].
   - normalize_tag_list('["ignored_m15", "oversized_position"]') đúng.
   - normalize_tag_list("ignored_m15, oversized_position") đúng.
   - tags_to_json(["ignored_m15"]) == '["ignored_m15"]' hoặc parse JSON ra đúng.
   - tags_from_json(tags_to_json([...])) roundtrip.
   - dữ liệu bẩn không crash.

Chạy:
python -m pytest tests/test_phase17_journal_tag_serialization.py -q
python -m pytest tests/test_trade_mistake_detector_helpers_phase12.py -q

Tiêu chí hoàn thành:
- Tags journal serialize ổn định.
- Không sửa detector.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 6 — PHASE 17.6: POPULATE FIELD ĐỢT 2 TỪ analysis_result KHI SAVE JOURNAL

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
JournalEntry đã có field đợt 2. Bây giờ journal_entry_from_analysis() cần lấy dữ liệu từ analysis_result để lưu: planned entry/sl/tp, m15_quality, expected_effective_rr, final_score/decision không bắt buộc nhưng các field execution/mistake cần chuẩn bị.

Mục tiêu riêng của prompt này:
Cập nhật journal_entry_from_analysis() để populate field đợt 2 từ analysis dict an toàn.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. services/journal_service.py
  2. tests/test_phase17_journal_from_analysis_phase2_fields.py
- Không sửa core analysis/scoring.
- Không sửa migration.
- Không sửa UI/controller.

Việc cần làm:
1. Trong journal_entry_from_analysis(analysis,...), xác định scenario đang dùng như hiện tại.

2. Populate các field:
   - planned_entry:
     + Nếu scenario["entry_zone"] là list 2 số, lấy trung bình.
     + Nếu analysis có planned_entry thì ưu tiên nếu đã có pattern.
   - planned_sl: scenario["stop_loss"] nếu parse float được.
   - planned_tp:
     + Nếu scenario["take_profit"] là list, lấy target đầu tiên.
     + Nếu là số/string parse được, dùng.
   - setup_type:
     + scenario["trigger_type"] hoặc scenario["type"] hoặc selected scenario.
   - regime:
     + market_regime["primary"].
   - m15_quality:
     + scenario["m15_quality"].
   - spread_at_entry:
     + data_quality["spread_points"] hoặc scenario["spread_at_entry"] nếu có.
   - expected_effective_rr:
     + scenario["expected_effective_rr"].
   - execution_quality_score:
     + analysis["execution_quality"]["execution_quality_score"] nếu có.
     + hoặc analysis["execution_quality_score"].
   - manual_mistake_tags:
     + analysis hoặc scenario có manual_mistake_tags thì tags_to_json.
     + fallback "[]".
   - auto_mistake_tags:
     + analysis hoặc scenario có auto_mistake_tags thì tags_to_json.
     + fallback "[]".

3. Các field actual_* và realized_effective_rr:
   - actual_entry, actual_sl, actual_tp, actual_exit, realized_effective_rr không có trong analysis live thì để None.

4. Thêm helper parse_float an toàn nếu chưa có:
   - None/string bẩn => None.
   - Không raise exception.

5. Không thay đổi các field cũ.
6. Không làm create_from_analysis gọi execution_quality_engine hay trade_mistake_detector.

7. Test:
   tests/test_phase17_journal_from_analysis_phase2_fields.py

8. Test analysis fake realistic:
   - symbol EUR/USD
   - scenario_scores buy/sell
   - data_quality spread_points=12, broker_symbol EURUSD
   - market_regime primary trend_up
   - decision_summary action watch, best_scenario buy
   - trade_permission allowed
   - scenarios[0]:
     {
       "type": "buy",
       "entry_zone": [1.0840, 1.0860],
       "stop_loss": 1.0800,
       "take_profit": [1.0950, 1.1000],
       "risk_reward": "1:2.0",
       "expected_effective_rr": 1.75,
       "m15_quality": "strict",
       "trigger_type": "h1_bullish_break",
       "position_sizing": {"suggested_lot": 0.1}
     }
   - execution_quality={"execution_quality_score": 88}
   - auto_mistake_tags=["ignored_news"]

9. Gọi journal_entry_from_analysis().
   Assert:
   - planned_entry == 1.0850.
   - planned_sl == 1.0800.
   - planned_tp == 1.0950.
   - setup_type == "h1_bullish_break".
   - regime == "trend_up".
   - m15_quality == "strict".
   - expected_effective_rr == 1.75.
   - execution_quality_score == 88.
   - auto_mistake_tags parse ra có ignored_news.

Chạy:
python -m pytest tests/test_phase17_journal_from_analysis_phase2_fields.py -q
python -m pytest tests/test_journal.py -q

Tiêu chí hoàn thành:
- Save từ analysis tự populate field đợt 2 an toàn.
- Không gọi core detector/scoring mới.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 7 — PHASE 17.7: POPULATE FIELD ĐỢT 2 TỪ scanner_row KHI SAVE JOURNAL

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
create_from_scanner_row() lưu scanner row vào journal. Sau Phase 15 row có final_score/opportunity/entry/m15/expected_effective_rr. Nếu row không có analysis_result, journal_entry_from_scanner_row() vẫn cần lưu được các field phase 2 cơ bản.

Mục tiêu riêng của prompt này:
Cập nhật journal_entry_from_scanner_row() để populate field đợt 2 từ scanner row trực tiếp.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. services/journal_service.py
  2. tests/test_phase17_journal_from_scanner_row_phase2_fields.py
- Không sửa core/scanner.py.
- Không sửa controller/UI.
- Không sửa migration.

Việc cần làm:
1. Trong journal_entry_from_scanner_row(row,...), populate:
   - regime = row["market_regime"].
   - m15_quality = row["m15_quality"].
   - expected_effective_rr = row["expected_effective_rr"].
   - setup_type = row["entry_status"] hoặc row["scanner_group"] nếu không có.
   - execution_quality_score nếu row có.
   - manual_mistake_tags/auto_mistake_tags nếu row có.
   - planned_lot = row["planned_lot"] nếu có, fallback None.
   - actual_lot = row["actual_lot"] nếu có, fallback None.
   - Nếu row có entry_zone list 2 số hoặc string JSON list, planned_entry là trung bình.
   - planned_sl từ row["stop_loss"] nếu có.
   - planned_tp từ row["take_profit"] nếu có.

2. Nếu row có analysis_result dict, create_from_scanner_row hiện ưu tiên create_from_analysis(). Không thay đổi behavior đó.
   Prompt này chủ yếu cho fallback row không có analysis_result.

3. Không thay đổi payload analysis_json đang lưu row trừ khi hiện tại đã có pattern.

4. Test:
   tests/test_phase17_journal_from_scanner_row_phase2_fields.py

5. Test row fake không có analysis_result:
   {
     "symbol": "EUR/USD",
     "broker_symbol": "EURUSD",
     "market_regime": "trend_up",
     "scanner_action": "watch",
     "direction_bias": "buy",
     "trade_permission": "allowed",
     "buy_score": 82,
     "sell_score": 60,
     "best_side": "buy",
     "entry_status": "waiting_confirmation",
     "m15_quality": "loose",
     "expected_effective_rr": 1.45,
     "risk_reward": "1:1.8",
     "entry_zone": [1.0840, 1.0860],
     "stop_loss": 1.0800,
     "take_profit": [1.0950],
     "planned_lot": 0.10,
     "auto_mistake_tags": ["ignored_m15"],
   }

6. Gọi journal_entry_from_scanner_row(row).
   Assert:
   - regime == trend_up.
   - m15_quality == loose.
   - expected_effective_rr == 1.45.
   - planned_entry == 1.0850.
   - planned_sl == 1.0800.
   - planned_tp == 1.0950.
   - planned_lot == 0.10.
   - auto_mistake_tags parse có ignored_m15.

7. Test create_from_scanner_row với temp DB nếu dễ:
   - service.create(entry), get_entry.
   - Assert persisted.

Chạy:
python -m pytest tests/test_phase17_journal_from_scanner_row_phase2_fields.py -q
python -m pytest tests/test_journal.py -q

Tiêu chí hoàn thành:
- Scanner row fallback lưu field đợt 2.
- Không ảnh hưởng path có analysis_result.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 8 — PHASE 17.8: MỞ RỘNG list_closed_trades_for_account_guard() TRẢ FIELD PHỤC VỤ EDGE/MISTAKE

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
list_closed_trades_for_account_guard() hiện phục vụ Account Guard. Sau Phase 17, closed trade dict nên có thêm field hữu ích cho statistical edge và mistake detector, nhưng không được phá account_guard cũ.

Mục tiêu riêng của prompt này:
Mở rộng output list_closed_trades_for_account_guard() với field đợt 2, giữ key cũ.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. services/journal_service.py
  2. tests/test_phase17_closed_trades_extended_fields.py
- Không sửa account_guard.py.
- Không sửa statistical_edge_engine.py.
- Không sửa trade_mistake_detector.py.
- Không sửa database/migration.
- Không đổi tên method.

Việc cần làm:
1. Trong list_closed_trades_for_account_guard(), SELECT thêm các cột:
   - planned_entry
   - actual_entry
   - planned_sl
   - actual_sl
   - planned_tp
   - actual_tp
   - actual_exit
   - setup_type
   - regime
   - session
   - m15_quality
   - spread_at_entry
   - expected_effective_rr
   - realized_effective_rr
   - manual_mistake_tags
   - auto_mistake_tags
   - execution_quality_score

2. Output trade dict phải vẫn có key cũ:
   - result_r
   - result_pct
   - closed_at
   - exit_reason
   - actual_lot
   - planned_lot
   - symbol
   - direction

3. Thêm key mới tương ứng field đợt 2.

4. Với tags:
   - Có thể trả raw TEXT hoặc list?
   - Khuyến nghị trả cả:
     + manual_mistake_tags: list[str] đã parse bằng tags_from_json
     + auto_mistake_tags: list[str] đã parse
   - Nếu muốn giữ raw thì thêm manual_mistake_tags_raw. Nhưng tránh phức tạp nếu không cần.
   - Đảm bảo execution_quality_engine/trade_mistake_detector có thể dùng list.

5. Không đổi filter: chỉ closed_at not null/empty.

6. Test:
   tests/test_phase17_closed_trades_extended_fields.py

7. Test:
   - Tạo temp DB.
   - Insert JournalEntry có closed_at và field đợt 2.
   - Gọi list_closed_trades_for_account_guard().
   - Assert output có key cũ và key mới.
   - Assert direction lấy từ selected_scenario vẫn đúng.
   - Assert manual_mistake_tags/auto_mistake_tags parse list đúng.
   - Assert limit hoạt động nếu có thể.

Chạy:
python -m pytest tests/test_phase17_closed_trades_extended_fields.py -q
python -m pytest tests/test_account_guard_phase7.py tests/test_statistical_edge_engine_*_phase10.py -q

Tiêu chí hoàn thành:
- Closed trades output giàu dữ liệu hơn.
- Account Guard cũ không vỡ.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 9 — PHASE 17.9: THÊM METHOD update_trade_outcome() ĐỂ CẬP NHẬT KẾT QUẢ ĐÓNG LỆNH

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Journal cần cách cập nhật kết quả sau khi trade đóng: result_r, result_pct, closed_at, exit_reason, actual lot/entry/sl/tp/exit, realized_effective_rr, mistake tags, execution_quality_score. Không cần UI trong prompt này.

Mục tiêu riêng của prompt này:
Thêm method JournalService.update_trade_outcome(entry_id, updates) an toàn.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. services/journal_service.py
  2. tests/test_phase17_update_trade_outcome.py
- Không sửa UI/controller.
- Không sửa core detector/engines.
- Không sửa migration.
- Không cho update arbitrary column nguy hiểm.

Việc cần làm:
1. Trong JournalService, thêm method:
   def update_trade_outcome(self, entry_id: int, updates: dict[str, object]) -> None:
       ...

2. Chỉ cho phép update whitelist field:
   - result
   - result_r
   - result_pct
   - closed_at
   - exit_reason
   - actual_lot
   - planned_lot
   - actual_entry
   - actual_sl
   - actual_tp
   - actual_exit
   - realized_effective_rr
   - manual_mistake_tags
   - auto_mistake_tags
   - execution_quality_score
   - note

3. Không cho update:
   - id
   - analysis_json
   - symbol
   - timestamp_utc
   - saved_at_utc
   - bất kỳ key không nằm whitelist.

4. Normalize:
   - manual_mistake_tags/auto_mistake_tags dùng tags_to_json.
   - numeric field parse an toàn nếu có helper, nhưng không bắt buộc ép quá mạnh. Nếu string "1.2" thì nên lưu float.
   - closed_at normalize UTC nếu project có normalize_utc_timestamp, hoặc giữ ISO string an toàn.

5. Nếu updates rỗng hoặc không có key hợp lệ:
   - Không crash.
   - Không update gì.

6. Test:
   tests/test_phase17_update_trade_outcome.py

7. Test:
   - Tạo entry basic.
   - update_trade_outcome(id, {"result_r": 1.5, "result_pct": 1.2, "closed_at": "...", "exit_reason": "take_profit", "actual_exit": 1.0950, "manual_mistake_tags": ["closed_too_early"], "execution_quality_score": 80})
   - get_entry.
   - Assert fields updated.
   - Assert tags serialized/parse đúng.

8. Test unknown field ignored:
   - updates {"symbol": "HACK", "analysis_json": "bad", "unknown": 1}
   - symbol/analysis_json không đổi.

9. Test empty updates no crash.

Chạy:
python -m pytest tests/test_phase17_update_trade_outcome.py -q
python -m pytest tests/test_journal.py -q

Tiêu chí hoàn thành:
- Có method update outcome an toàn.
- Whitelist bảo vệ field nhạy cảm.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 10 — PHASE 17.10: THÊM METHOD apply_execution_analysis_to_entry() Ở MỨC UTILITY, KHÔNG TỰ QUYẾT ĐỊNH TRADE

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 11/12 có execution_quality_engine và trade_mistake_detector. Phase 17 có DB fields để lưu kết quả. Cần utility áp dụng detection/quality vào journal entry, nhưng không thay đổi scoring/gate/live decision.

Mục tiêu riêng của prompt này:
Thêm method utility trong JournalService để chạy detector trên entry dict và lưu tags/score nếu caller yêu cầu.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. services/journal_service.py
  2. tests/test_phase17_apply_execution_analysis_to_entry.py
- Không sửa core/trade_mistake_detector.py.
- Không sửa core/execution_quality_engine.py.
- Không sửa scoring/gate/decision.
- Không sửa UI/controller.
- Không tự gọi method này ở create/update trong prompt này.

Việc cần làm:
1. Trong JournalService, thêm method:
   def apply_execution_analysis_to_entry(self, entry_id: int, *, previous_limit: int = 50) -> dict | None:
       ...

2. Logic:
   - get_entry(entry_id). Nếu None => return None.
   - Convert JournalEntry sang dict bằng asdict.
   - Nếu analysis_json parse được, merge thêm data cần thiết nhưng không overwrite field journal đã có nếu field journal có giá trị.
   - Lấy previous_trades = list_closed_trades_for_account_guard(limit=previous_limit).
   - Gọi core.trade_mistake_detector.detect_trade_mistakes(trade_dict, previous_trades=previous_trades).
   - Gọi core.trade_mistake_detector.apply_detected_mistakes_to_trade nếu có, hoặc tự merge tags từ detection_result.
   - Gọi core.execution_quality_engine.calculate_execution_quality(trade_with_tags, use_existing_score=False) nếu function có param đó.
   - Lưu vào journal bằng update_trade_outcome:
     + auto_mistake_tags
     + manual_mistake_tags nếu có trong entry/trade
     + execution_quality_score
   - Return dict:
     {
       "detection": detection_result,
       "execution_quality": execution_quality_result,
       "updated_entry_id": entry_id,
     }

3. Không thay đổi decision/gate/live analysis.
4. Không tự động chạy khi create entry.

5. Test:
   tests/test_phase17_apply_execution_analysis_to_entry.py

6. Test:
   - Tạo previous closed losing trade trong temp DB.
   - Tạo current entry có planned_lot=0.10, actual_lot=0.20, opened_at/timestamp_utc gần previous closed_at nếu fields hỗ trợ, hoặc dùng timestamp_utc.
   - Gọi apply_execution_analysis_to_entry(current_id).
   - Assert result không None.
   - get_entry current:
     + auto_mistake_tags không empty hoặc có oversized_position/revenge nếu data đủ.
     + execution_quality_score không None.

7. Nếu detector cần fields opened_at mà JournalEntry chưa có:
   - Dùng timestamp_utc như opened_at trong trade_dict nếu chưa có.
   - Không thêm migration mới ngoài Phase 17 field list.

Chạy:
python -m pytest tests/test_phase17_apply_execution_analysis_to_entry.py -q
python -m pytest tests/test_journal_trade_mistake_detector_utility_phase12.py tests/test_execution_quality_engine_*phase11.py -q

Tiêu chí hoàn thành:
- Utility chạy detector/quality và lưu tags/score.
- Không tự động thay đổi live decision.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 11 — PHASE 17.11: TEST MIGRATION TỪ DATABASE CŨ 001/002 LÊN 003 KHÔNG MẤT DỮ LIỆU

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Người dùng có thể đã có journal DB cũ chỉ apply 001/002. Migration 003 phải nâng cấp mà không mất dữ liệu.

Mục tiêu riêng của prompt này:
Tạo test mô phỏng DB cũ có dữ liệu, sau đó chạy migrate đầy đủ lên 003.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase17_migrate_existing_db_preserves_data.py
  2. data/migrations/003_add_journal_execution_fields.sql chỉ nếu migration bug rõ ràng.
  3. services/journal_service.py chỉ nếu migrate bug rõ ràng.
- Không sửa core engines/UI.

Việc cần làm:
1. Tạo test:
   tests/test_phase17_migrate_existing_db_preserves_data.py

2. Mô phỏng DB cũ:
   - Dùng sqlite3 tạo temp db.
   - Tạo schema_migrations.
   - Apply nội dung 001_create_journal.sql và 002_add_account_guard_fields.sql thủ công.
   - Insert 1 journal_entries row với field cũ:
     symbol EUR/USD, buy_score/sell_score, analysis_json, result_r, closed_at...
   - Insert schema_migrations version 001 và 002.

3. Sau đó khởi tạo JournalService(db_path=temp_db, migrations_dir=full_migrations_dir).
   - migrate() sẽ chỉ apply 003.

4. Assert:
   - Row cũ vẫn tồn tại.
   - Field cũ còn đúng.
   - Field mới tồn tại và default None.
   - schema_migrations có 003.

5. Gọi service.list_entries() và get_entry(id):
   - Không crash.
   - Entry có symbol cũ.

6. Gọi service.create(new_entry) với field đợt 2:
   - Insert mới được.

Chạy:
python -m pytest tests/test_phase17_migrate_existing_db_preserves_data.py -q
python -m pytest tests/test_phase17_journal_migration_phase2_schema.py tests/test_phase17_journal_entry_phase2_fields.py -q

Tiêu chí hoàn thành:
- DB cũ nâng cấp không mất data.
- Field mới nullable.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 12 — PHASE 17.12: TEST REALISTIC JOURNAL WORKFLOW SAU MIGRATION

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Sau migration và JournalService update, cần test workflow giống thật: save analysis, update trade outcome, apply execution analysis, list closed trades.

Mục tiêu riêng của prompt này:
Tạo test realistic tổng hợp cho journal sau Phase 17.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase17_journal_realistic_workflow.py
  2. services/journal_service.py nếu phát hiện bug nhỏ rõ ràng.
- Không sửa core engines trừ bug import rất nhỏ.
- Không sửa UI/controller.
- Không sửa migration nếu không cần.

Việc cần làm:
1. Tạo test:
   tests/test_phase17_journal_realistic_workflow.py

2. Workflow:
   - Init JournalService temp db.
   - Tạo analysis fake realistic có:
     + symbol EUR/USD
     + scenario_scores
     + final_score/final_score_detail
     + decision_engine
     + decision_summary
     + trade_permission
     + data_quality spread_points
     + market_regime
     + scenarios[0] có entry_zone/sl/tp/expected_effective_rr/m15_quality/position_sizing
   - create_from_analysis(analysis).
   - get_entry.
   - Assert field đợt 2 từ analysis đã được lưu.

3. Update outcome:
   - update_trade_outcome(entry_id, {
       "actual_entry": 1.0865,
       "actual_sl": 1.0790,
       "actual_exit": 1.0920,
       "result_r": 1.2,
       "result_pct": 1.0,
       "closed_at": "2026-06-04T12:00:00Z",
       "exit_reason": "manual_close",
       "auto_mistake_tags": ["closed_too_early"],
       "execution_quality_score": 80,
     })
   - get_entry assert updated.

4. list_closed_trades_for_account_guard:
   - Assert returns one trade.
   - Trade has result_r, actual_entry, auto_mistake_tags list, execution_quality_score.

5. Nếu apply_execution_analysis_to_entry tồn tại:
   - Có thể gọi và assert không crash.
   - Không assert quá cụ thể nếu detector tùy dữ liệu.

6. stats/list_entries vẫn không crash.

Chạy:
python -m pytest tests/test_phase17_journal_realistic_workflow.py -q
python -m pytest tests/test_phase17_*.py -q

Tiêu chí hoàn thành:
- Workflow journal thực tế hoạt động.
- Field mới lưu/đọc/update/list được.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 13 — PHASE 17.13: CẬP NHẬT PACKAGING/NOTES NẾU CẦN ĐỂ INCLUDE MIGRATION 003

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Migration mới phải được đóng gói cùng app. Project có packaging/pyinstaller.spec hoặc build script có thể include data/migrations.

Mục tiêu riêng của prompt này:
Rà soát packaging để đảm bảo data/migrations/*.sql được include. Chỉ sửa nếu migration 003 chưa được include theo pattern hiện tại.

Yêu cầu rất quan trọng:
- Ưu tiên chỉ đọc.
- Chỉ được sửa nếu thật sự cần:
  1. packaging/pyinstaller.spec
  2. packaging/build_windows.ps1
  3. packaging/installer_notes.md
  4. tests/test_phase17_packaging_migrations.py
- Không sửa app core/service nếu không cần.
- Không chạy build package thật nếu mất thời gian.

Việc cần làm:
1. Mở:
   - packaging/pyinstaller.spec
   - packaging/build_windows.ps1
   - packaging/installer_notes.md

2. Kiểm tra migrations có được include bằng glob data/migrations hoặc từng file không.

3. Nếu đã include cả folder/glob:
   - Không sửa packaging.
   - Tạo test kiểm tra file migration tồn tại và packaging notes/glob nếu dễ.

4. Nếu chưa include migration 003:
   - Sửa tối thiểu để include data/migrations/*.sql hoặc folder data/migrations.
   - Không đổi cấu trúc build lớn.

5. Tạo test:
   tests/test_phase17_packaging_migrations.py

6. Test:
   - Assert data/migrations/003_add_journal_execution_fields.sql tồn tại.
   - Nếu pyinstaller.spec có nội dung include migrations, assert có "data/migrations" hoặc "migrations".
   - Test không cần chạy pyinstaller.

Chạy:
python -m pytest tests/test_phase17_packaging_migrations.py -q

Tiêu chí hoàn thành:
- Migration 003 không bị quên khi packaging.
- Không chạy build nặng.
- Không sửa ngoài phạm vi.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 14 — PHASE 17.14: REGRESSION TEST JOURNAL, ACCOUNT GUARD, EDGE, EXECUTION, MISTAKE

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 17 sửa migration/journal_service. Cần regression để chắc chắn account_guard/statistical_edge/execution_quality/trade_mistake_detector không bị phá.

Mục tiêu riêng của prompt này:
Chạy test tổng hợp và sửa bug nhỏ đúng phạm vi nếu có.

Yêu cầu rất quan trọng:
- Không thêm feature mới.
- Không refactor.
- Không sửa UI.
- Không sửa core engines nếu lỗi không do Phase 17.
- Chỉ được sửa nếu bug rõ ràng do Phase 17:
  1. services/journal_service.py
  2. data/migrations/003_add_journal_execution_fields.sql
  3. tests/test_phase17_*.py

Việc cần làm:
1. Chạy toàn bộ test Phase 17:
   python -m pytest tests/test_phase17_*.py -q

2. Chạy journal tests cũ:
   python -m pytest tests/test_journal.py tests/test_journal_account_guard_fields_phase7.py tests/test_journal_trade_mistake_detector_utility_phase12.py -q

3. Chạy account guard:
   python -m pytest tests/test_account_guard_phase7.py tests/test_phase7_account_guard_realistic.py -q

4. Chạy statistical edge:
   python -m pytest tests/test_statistical_edge_engine_*_phase10.py -q

5. Chạy execution quality:
   python -m pytest tests/test_execution_quality_engine_*phase11.py tests/test_phase11_execution_quality_realistic.py -q

6. Chạy trade mistake detector:
   python -m pytest tests/test_trade_mistake_detector_*phase12.py tests/test_reason_codes_mistake_detector_phase12.py -q

7. Nếu fail:
   - Đọc kỹ lỗi.
   - Nếu lỗi do JournalEntry mới thiếu default/mapping, sửa journal_service tối thiểu.
   - Nếu lỗi do migration, sửa migration 003 hoặc test setup.
   - Không sửa engine khác bừa.

8. Chạy lại test liên quan sau khi sửa.

Tiêu chí hoàn thành:
- Phase 17 tests pass.
- Journal/account guard/edge/execution/mistake không bị phá.
- Không sửa ngoài phạm vi.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 15 — PHASE 17.15: RÀ SOÁT CUỐI PHASE 17 VÀ BÁO HOÀN THÀNH

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Đây là prompt cuối Phase 17. Không thêm tính năng mới nữa, chỉ rà soát migration/journal và test.

Mục tiêu riêng của prompt này:
Xác nhận Phase 17 đã hoàn thành đúng phạm vi: migration đợt 1/2, JournalEntry field mới, create/get/list/update workflow, closed trades extended, không phá hệ thống.

Yêu cầu rất quan trọng:
- Không thêm feature mới.
- Không refactor code.
- Không sửa UI.
- Không sửa scoring/gate/decision/scanner.
- Không sửa database ngoài migration đã có.
- Chỉ sửa lỗi import/test fail nhỏ nếu phát hiện rõ ràng.

Checklist cần kiểm tra:
1. Có migration:
   - data/migrations/003_add_journal_execution_fields.sql

2. Migration 003 có đủ field:
   - planned_entry
   - actual_entry
   - planned_sl
   - actual_sl
   - planned_tp
   - actual_tp
   - actual_exit
   - setup_type
   - regime
   - session
   - m15_quality
   - spread_at_entry
   - expected_effective_rr
   - realized_effective_rr
   - manual_mistake_tags
   - auto_mistake_tags
   - execution_quality_score

3. JournalEntry có đủ field đợt 1 và đợt 2.

4. JournalService hỗ trợ:
   - create/get/list với field mới.
   - journal_entry_from_analysis populate field đợt 2.
   - journal_entry_from_scanner_row populate field đợt 2 khi không có analysis_result.
   - list_closed_trades_for_account_guard trả field cũ và mới.
   - update_trade_outcome nếu đã thêm.
   - apply_execution_analysis_to_entry nếu đã thêm.

5. Migration an toàn:
   - DB mới có đủ schema.
   - DB cũ 001/002 nâng lên 003 không mất dữ liệu.
   - migrate chạy lại không crash.

6. Không có thay đổi không cần thiết ở:
   - core/signal_engine.py
   - core/trade_gate_engine.py
   - core/final_score_engine.py
   - core/decision_engine.py
   - core/scanner.py
   - ui/

Test bắt buộc:
1. Chạy:
   python -m pytest tests/test_phase17_*.py -q

2. Chạy focused regression:
   python -m pytest tests/test_journal.py tests/test_journal_account_guard_fields_phase7.py tests/test_account_guard_phase7.py -q
   python -m pytest tests/test_statistical_edge_engine_*_phase10.py tests/test_execution_quality_engine_*phase11.py tests/test_trade_mistake_detector_*phase12.py -q

3. Nếu đủ thời gian:
   python -m pytest -q
   Nếu full test fail do môi trường thiếu PyQt6/MT5/network thì ghi rõ, không sửa code bừa.

Báo cáo cuối cần nêu ngắn gọn:
- Migration nào đã tạo.
- Field mới nào đã thêm.
- Journal workflow nào đã test.
- Test nào đã chạy.
- Có lỗi môi trường nào không.

Sau khi hoàn thành prompt cuối cùng, bắt buộc báo đúng 2 câu sau:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT
ĐÃ HOÀN THÀNH PHASE SỐ 17
