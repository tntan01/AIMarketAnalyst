PROMPTS CHO DEEPSEEK_V4_PRO — PHASE 4
FOREX SCORING SYSTEM UPGRADE
============================================================

GHI CHÚ CHUNG
============================================================

File này chứa các prompt nhỏ để thực hiện PHASE 4 — GIẢM QUYỀN LỰC CỦA MACRO SCORE.

Ngôn ngữ: Tiếng Việt có dấu đầy đủ.

Bối cảnh hiện tại:
- Phase 1 đã xong: score đã có `signal_score`, có thể vẫn giữ `total` để backward compatibility.
- Phase 2 đã xong: đã có `core/trade_gate_engine.py`, output `trade_gate`, gate có quyền block/cap.
- Phase 3 đã xong: đã có `score_gap`, `direction_bias`, và score_gap đã được truyền vào trade gate.
- Phase 4 chỉ tập trung vào macro:
  1. Giảm trọng số macro trong scoring.
  2. Chuyển macro từ “điểm cộng lớn” thành “bias modifier + risk gate”.
  3. Macro aligned chỉ cộng nhẹ.
  4. Macro conflict trừ rõ.
  5. Macro unclear không cộng điểm, chỉ ghi warning/penalty.
  6. Tin mạnh gần phải là gate/block, không chỉ là điểm trừ.

Triết lý Phase 4:
- Macro quan trọng, nhưng không được kéo một setup kỹ thuật yếu thành READY.
- Macro không nên chiếm 30–40% điểm nếu dữ liệu macro chưa được chuẩn hóa rất kỹ.
- Macro nên đóng vai trò:
  - Bias modifier: điều chỉnh nhẹ thiên hướng.
  - Risk gate: chặn giao dịch khi có tin mạnh gần.
- Không được để `macro_score` một mình làm score tăng quá mạnh.
- Không implement các phase khác trong Phase 4.

Yêu cầu bắt buộc cho tất cả prompt:
- Prompt phải làm từng bước nhỏ, hạn chế sửa lan man.
- Deepseek_v4_pro phải đọc code hiện tại trước khi sửa.
- Sau khi code xong phải tự kiểm tra lại code thật cẩn thận.
- Sau khi code xong phải chạy test.
- Phải dùng Python/pytest để test với dữ liệu giống thật hoặc dữ liệu mock hợp lý.
- Không được chỉnh sửa, thay đổi hoặc refactor các phần không liên quan.
- Sau khi hoàn thành hết các công việc trong MỖI prompt, phải báo đúng dòng:

ĐÃ HOÀN THÀNH YÊU CẦU

- Sau khi hoàn thành TOÀN BỘ Phase 4, báo cáo cuối cùng phải có dòng:

ĐÃ HOÀN THÀNH TASK

Khuyến nghị sử dụng:
- Chạy lần lượt Prompt 1 → Prompt 2 → Prompt 3 → Prompt 4.
- Không nên đưa cả 4 prompt vào Deepseek cùng lúc.
- Nếu bắt buộc đưa một lần, yêu cầu Deepseek làm tuần tự từng prompt, xong prompt nào phải test prompt đó.

============================================================
PROMPT 1 — RÀ SOÁT MACRO SCORING HIỆN TẠI, GIẢM TRỌNG SỐ MACRO TRONG DYNAMIC_WEIGHTS
============================================================

Bạn đang làm việc trên project Python/PyQt6 AI Market Analyst v2.0 trong VSCode.

Nhiệm vụ lần này CHỈ làm phần đầu của Phase 4:
- Rà soát cách macro đang được dùng trong `core/signal_engine.py`.
- Giảm trọng số macro trong `DYNAMIC_WEIGHTS`.
- Không thay đổi thuật toán macro modifier ở prompt này.
- Không sửa gate ở prompt này.
- Không sửa UI/scanner ở prompt này.

Yêu cầu cực kỳ quan trọng:
- CHỈ sửa file liên quan trực tiếp đến trọng số scoring, ưu tiên chỉ sửa:
  - `core/signal_engine.py`
  - test liên quan nếu cần.
- KHÔNG sửa UI.
- KHÔNG sửa scanner.
- KHÔNG sửa `core/analysis_engine.py` nếu không thật sự bắt buộc.
- KHÔNG sửa `core/trade_gate_engine.py` trong prompt này.
- KHÔNG sửa `core/entry_engine.py`.
- KHÔNG sửa `core/risk_engine.py`.
- KHÔNG sửa SMC.
- KHÔNG thêm evidence_score.
- KHÔNG thêm execution_quality_score.
- KHÔNG implement Phase 5, 6, 7 hoặc các phase sau.
- KHÔNG refactor lan rộng.
- Sau khi code xong phải tự kiểm tra lại code thật cẩn thận để tránh bug.

Bước 1 — Đọc code hiện tại:
Hãy mở và đọc kỹ:
- `core/signal_engine.py`
- các test hiện có liên quan đến signal/scoring, ví dụ:
  - `tests/test_signal_engine.py`
  - nếu có test khác liên quan scoring thì đọc thêm.

Tìm:
- `DYNAMIC_WEIGHTS`
- nơi tính `macro_alignment`
- nơi dùng weight `"macro"`
- nơi scale điểm thành `signal_score` hoặc `total`

Bước 2 — Giảm trọng số macro trong `DYNAMIC_WEIGHTS`:

Nếu hiện tại macro đang là:
- trending: khoảng 30
- ranging: khoảng 30
- volatile: khoảng 40

Hãy chỉnh theo hướng tạm thời:

```python
DYNAMIC_WEIGHTS = {
    "trending_up": {
        "trend": 25,
        "momentum": 15,
        "location": 15,
        "smc": 15,
        "risk": 15,
        "macro": 15,
    },
    "trending_down": {
        "trend": 25,
        "momentum": 15,
        "location": 15,
        "smc": 15,
        "risk": 15,
        "macro": 15,
    },
    "ranging": {
        "trend": 10,
        "momentum": 10,
        "location": 25,
        "smc": 25,
        "risk": 15,
        "macro": 15,
    },
    "volatile": {
        "trend": 10,
        "momentum": 5,
        "location": 15,
        "smc": 10,
        "risk": 40,
        "macro": 20,
    },
}
```

Lưu ý:
- Nếu code hiện tại có thêm regime khác, KHÔNG xóa regime đó.
- Với regime khác, giữ cấu trúc hiện tại nhưng đảm bảo macro không vượt quá 20 nếu không có lý do rõ ràng.
- Tổng trọng số mỗi regime nên vẫn hợp lý, ưu tiên tổng khoảng 100.
- Nếu code có hàm normalize weight riêng, phải đọc hiểu trước khi sửa.
- Không đổi tên key.
- Không thay đổi ý nghĩa các nhóm điểm khác nếu không bắt buộc.

Bước 3 — Backward compatibility:
- Không đổi tên `macro_alignment`.
- Không đổi output `score_scenario()`.
- Không bỏ `total`.
- Không bỏ `signal_score`.
- Không sửa decision/action.

Bước 4 — Test:
Cập nhật hoặc tạo test trong:

`tests/test_macro_weights_phase4.py`

Test tối thiểu:

1. Kiểm tra macro weight không còn quá lớn:
```python
from core.signal_engine import DYNAMIC_WEIGHTS

def test_macro_weight_reduced():
    for regime, weights in DYNAMIC_WEIGHTS.items():
        assert weights.get("macro", 0) <= 20
```

2. Kiểm tra tổng trọng số hợp lý:
```python
def test_dynamic_weights_total_reasonable():
    for regime, weights in DYNAMIC_WEIGHTS.items():
        total = sum(weights.values())
        assert 95 <= total <= 105
```

Nếu code hiện tại cố tình không dùng tổng 100, hãy điều chỉnh test theo thực tế, nhưng phải đảm bảo macro không còn chiếm 30–40% tổng điểm.

3. Kiểm tra các key chính vẫn tồn tại:
```python
required = {"trend", "momentum", "location", "smc", "risk", "macro"}
for regime, weights in DYNAMIC_WEIGHTS.items():
    assert required.issubset(weights.keys())
```

Chạy test:

```bash
pytest -q tests/test_macro_weights_phase4.py
pytest -q tests/test_signal_engine.py
pytest -q tests/test_score_gap.py
```

Nếu test cũ lỗi do môi trường thiếu MT5/PyQt6 thì không sửa lan man.
Nếu lỗi do thay đổi Phase 4 Prompt 1 thì sửa đúng chỗ.

Bước 5 — Báo cáo:
Sau khi hoàn thành Prompt 1, báo cáo ngắn:
1. File đã sửa/tạo.
2. Trọng số macro mới theo từng regime.
3. Test đã chạy.
4. Test pass/fail.
5. Xác nhận không sửa UI/scanner/gate/risk/entry/SMC.
6. Cuối báo cáo ghi đúng dòng:

ĐÃ HOÀN THÀNH YÊU CẦU

Chưa được báo “ĐÃ HOÀN THÀNH TASK” sau Prompt 1, vì còn các prompt tiếp theo.

============================================================
PROMPT 2 — THÊM MACRO MODIFIER: aligned CỘNG NHẸ, conflict TRỪ MẠNH, unclear KHÔNG CỘNG
============================================================

Bạn đang tiếp tục Phase 4 của project AI Market Analyst v2.0.

Điều kiện:
- Prompt 1 đã xong: macro weight trong `DYNAMIC_WEIGHTS` đã được giảm.
- Phase 1, 2, 3 đã hoàn thành trước đó.

Nhiệm vụ lần này:
- Thêm logic macro modifier sau khi tính điểm tín hiệu.
- Macro aligned chỉ cộng nhẹ.
- Macro conflict trừ rõ.
- Macro unclear không cộng điểm.
- Thêm reason/penalty code dạng string đơn giản vào output score.
- Không tạo module reason_codes.py ở prompt này, vì đó là Phase 9.
- Không sửa gate ở prompt này, trừ khi có lỗi trực tiếp.

Yêu cầu cực kỳ quan trọng:
- Ưu tiên CHỈ sửa:
  - `core/signal_engine.py`
  - test liên quan.
- KHÔNG sửa UI.
- KHÔNG sửa scanner.
- KHÔNG sửa `core/analysis_engine.py` nếu không thật sự bắt buộc.
- KHÔNG sửa `core/trade_gate_engine.py` trong prompt này.
- KHÔNG sửa entry engine.
- KHÔNG sửa risk engine.
- KHÔNG sửa SMC.
- KHÔNG tạo `core/reason_codes.py` vì đó là Phase 9.
- KHÔNG implement Phase 5, 6, 7, 8, 9 hoặc các phase sau.
- KHÔNG refactor lan rộng.
- Sau khi code xong phải tự kiểm tra thật cẩn thận để tránh bug.

Bước 1 — Đọc code hiện tại:
Mở:
- `core/signal_engine.py`
- test liên quan signal.

Tìm:
- hàm `score_scenario(...)`
- nơi lấy `macro_alignment`
- nơi tạo dict output score
- nơi tính `signal_score` hoặc `total`
- nơi có `reason`, `reasons`, `reason_codes`, `penalty_codes` nếu đã tồn tại.

Bước 2 — Xác định macro_status an toàn:
Trong `score_scenario(...)`, dựa vào dữ liệu hiện có để xác định trạng thái macro.

Ưu tiên lấy từ các key có sẵn, ví dụ:
- `macro_status`
- `macro_alignment_status`
- `macro_bias`
- `macro_context`
- `macro_alignment`
- hoặc cấu trúc hiện tại của project.

Không được tự đoán bừa nếu code đã có chuẩn riêng.
Hãy đọc code để dùng đúng key hiện có.

Nếu không có field rõ ràng, có thể tạo helper nội bộ trong `signal_engine.py`:

```python
def _detect_macro_status(macro_context: dict | None, direction: str) -> str:
    ...
```

Output:
- `"aligned"`
- `"conflict"`
- `"unclear"`

Yêu cầu helper:
- Không crash khi `macro_context` None.
- Không crash khi thiếu key.
- Nếu không đủ dữ liệu thì trả `"unclear"`.
- Nếu macro bias cùng hướng với direction thì `"aligned"`.
- Nếu macro bias ngược direction thì `"conflict"`.
- Nếu neutral/mixed/unknown thì `"unclear"`.

Gợi ý logic nếu macro_context có dạng đơn giản:
```python
bias = str(macro_context.get("bias", "")).lower()
direction = direction.lower()

if bias in ("buy", "bullish", "long") and direction == "buy":
    return "aligned"
if bias in ("sell", "bearish", "short") and direction == "sell":
    return "aligned"
if bias in ("buy", "bullish", "long") and direction == "sell":
    return "conflict"
if bias in ("sell", "bearish", "short") and direction == "buy":
    return "conflict"
return "unclear"
```

Nhưng phải ưu tiên cấu trúc thật trong code hiện tại.

Bước 3 — Thêm macro modifier sau khi tính score gốc:

Sau khi có `signal_score` hoặc `total` gốc, áp dụng:

```python
if macro_status == "conflict":
    signal_score -= 15
    penalty_codes.append("MACRO_CONFLICT")

elif macro_status == "unclear":
    penalty_codes.append("MACRO_UNCLEAR")

elif macro_status == "aligned":
    signal_score += 5
    reason_codes.append("MACRO_ALIGNED")
```

Yêu cầu quan trọng:
- `signal_score` sau modifier phải clamp trong khoảng 0–100.
- `total` nếu còn để backward compatibility thì nên bằng `signal_score` sau modifier, hoặc nếu project đang giữ `total` là alias của signal_score thì phải đồng bộ.
- Không để macro aligned cộng quá 5.
- Không để macro unclear cộng điểm.
- Macro conflict phải trừ rõ 15 điểm.
- Nếu code hiện tại đã có macro raw score trong weighted score, vẫn giữ weighted score sau khi đã giảm weight ở Prompt 1, rồi mới áp dụng modifier nhẹ/penalty theo status.
- Không làm thay đổi các nhóm điểm khác.

Bước 4 — Reason/Penalty code:
Nếu output hiện tại đã có `reason_codes` và `penalty_codes`, hãy append vào đó.
Nếu chưa có, thêm vào output:

```python
"reason_codes": reason_codes,
"penalty_codes": penalty_codes,
```

Lưu ý:
- Đây chỉ là string list đơn giản.
- Không tạo module reason code riêng ở Phase 4.
- Không đổi text UI.

Bước 5 — Output bổ sung:
Trong output của `score_scenario(...)`, thêm:

```python
"macro_status": macro_status,
"macro_modifier": 5 hoặc -15 hoặc 0,
```

Yêu cầu:
- Không phá output cũ.
- Không xóa `macro_alignment`.
- Không xóa `components`.
- Không xóa `signal_score`.
- Không xóa `total`.

Bước 6 — Test:
Tạo file:

`tests/test_macro_modifier_phase4.py`

Test nên gọi trực tiếp helper hoặc `score_scenario(...)` tùy cấu trúc code.

Test tối thiểu:

1. Macro aligned cộng nhẹ:
- Dựng input sao cho macro status aligned.
- So sánh score có macro aligned với macro unclear, các thành phần khác giống nhau.
- Kỳ vọng chênh lệch không quá 5 điểm cộng.
- Kỳ vọng:
```python
"MACRO_ALIGNED" in result["reason_codes"]
result["macro_modifier"] == 5
result["macro_status"] == "aligned"
```

2. Macro conflict trừ mạnh:
- Dựng input sao cho macro status conflict.
- Kỳ vọng:
```python
"MACRO_CONFLICT" in result["penalty_codes"]
result["macro_modifier"] == -15
result["macro_status"] == "conflict"
```

3. Macro unclear không cộng:
- Dựng input thiếu macro hoặc neutral/mixed.
- Kỳ vọng:
```python
"MACRO_UNCLEAR" in result["penalty_codes"]
result["macro_modifier"] == 0
result["macro_status"] == "unclear"
```

4. Clamp 0–100:
- Nếu score cao và macro aligned, không vượt 100.
- Nếu score thấp và macro conflict, không thấp hơn 0.

5. Backward compatibility:
```python
assert "signal_score" in result
assert "total" in result
assert result["signal_score"] == result["total"]
```
Nếu project đang có chủ đích khác giữa `signal_score` và `total`, không ép bằng nhau, nhưng phải đảm bảo UI cũ không vỡ.

Chạy test:

```bash
pytest -q tests/test_macro_modifier_phase4.py
pytest -q tests/test_macro_weights_phase4.py
pytest -q tests/test_signal_engine.py
```

Chạy thêm regression:

```bash
pytest -q tests/test_score_gap.py tests/test_trade_gate_engine.py
```

Nếu lỗi do thiếu PyQt6/MT5 thì không sửa lan man.
Nếu lỗi do thay đổi Phase 4 Prompt 2 thì sửa đúng chỗ.

Bước 7 — Báo cáo:
Sau khi hoàn thành Prompt 2, báo cáo ngắn:
1. File đã sửa/tạo.
2. Macro modifier đã thêm như thế nào.
3. Test đã chạy.
4. Test pass/fail.
5. Xác nhận không sửa ngoài phạm vi Prompt 2.
6. Cuối báo cáo ghi đúng dòng:

ĐÃ HOÀN THÀNH YÊU CẦU

Chưa được báo “ĐÃ HOÀN THÀNH TASK” sau Prompt 2, vì còn các prompt tiếp theo.

============================================================
PROMPT 3 — ĐẢM BẢO TIN MẠNH GẦN LÀ GATE/BLOCK, KHÔNG CHỈ LÀ ĐIỂM TRỪ
============================================================

Bạn đang tiếp tục Phase 4 của project AI Market Analyst v2.0.

Điều kiện:
- Phase 2 đã có `core/trade_gate_engine.py`.
- Phase 4 Prompt 1 đã giảm macro weight.
- Phase 4 Prompt 2 đã thêm macro modifier.
- Prompt này chỉ kiểm tra và hoàn thiện phần “macro news gần phải là gate”.

Nhiệm vụ:
- Đảm bảo nếu có tin mạnh trong 30 phút thì trade gate block giao dịch.
- Không để setup có score cao vẫn thành ready khi có tin mạnh gần.
- Đảm bảo `analysis_engine.py` truyền đúng field tin mạnh gần vào `gate_context`.
- Không sửa thuật toán scoring trong prompt này, trừ khi phát hiện bug trực tiếp liên quan đến field news gate.

Yêu cầu cực kỳ quan trọng:
- Ưu tiên CHỈ sửa:
  - `core/trade_gate_engine.py` nếu gate chưa đúng.
  - `core/analysis_engine.py` nếu chưa truyền field đúng.
  - test liên quan.
- KHÔNG sửa UI.
- KHÔNG sửa scanner.
- KHÔNG sửa `core/signal_engine.py` trong prompt này, trừ khi có lỗi import/test do prompt trước.
- KHÔNG sửa entry engine.
- KHÔNG sửa risk engine.
- KHÔNG sửa SMC.
- KHÔNG implement Phase 5, 6, 7, 8, 9 hoặc các phase sau.
- KHÔNG refactor lan rộng.
- Sau khi code xong phải tự kiểm tra thật cẩn thận.

Bước 1 — Đọc code:
Mở:
- `core/trade_gate_engine.py`
- `core/analysis_engine.py`
- các test Phase 2 liên quan gate.

Kiểm tra trong `trade_gate_engine.py` đã có logic:

```python
if high_impact_event_within_30m:
    allowed = False
    decision_cap = "TRADE_BLOCKED"
    block_codes.append("HIGH_IMPACT_NEWS_NEARBY")
```

Nếu đã có và test pass, không sửa logic này.

Bước 2 — Đảm bảo field truyền vào gate:
Trong `analysis_engine.py`, kiểm tra `gate_context` có:

```python
"high_impact_event_within_30m": data_quality.get("high_impact_event_within_30m")
```

Nếu dữ liệu tin nằm trong macro/news context chứ không nằm trong `data_quality`, hãy đọc code hiện tại và truyền đúng nguồn.

Có thể hỗ trợ thêm fallback an toàn, ví dụ:

```python
high_impact_event_within_30m = (
    data_quality.get("high_impact_event_within_30m")
    or macro_context.get("high_impact_event_within_30m")
    or news_context.get("high_impact_event_within_30m")
)
```

Nhưng chỉ làm nếu các biến đó thực sự tồn tại trong code.
Không tự tạo biến không có thật.
Không refactor lớn.

Bước 3 — Đảm bảo cap decision:
Nếu gate block bởi `"HIGH_IMPACT_NEWS_NEARBY"`, kết quả cuối trong output phân tích phải đảm bảo:
```python
result["trade_gate"]["allowed"] is False
result["trade_gate"]["decision_cap"] == "TRADE_BLOCKED"
result["trade_permission"]["status"] == "blocked"
result["decision_summary"]["action"] == "stand_aside"
```

Nếu Phase 2 đã có logic này thì chỉ test lại, không viết trùng.

Bước 4 — Test:
Tạo hoặc cập nhật file:

`tests/test_macro_news_gate_phase4.py`

Test 1 — gate trực tiếp:
Gọi trực tiếp:

```python
from core.trade_gate_engine import check_trade_gates

context = {
    "terminal_connected": True,
    "broker_logged_in": True,
    "spread_status": "normal",
    "high_impact_event_within_30m": True,
    "m15_quality": "strict",
    "score_gap": 30,
}
result = check_trade_gates(context)
```

Kỳ vọng:
```python
result["allowed"] is False
result["decision_cap"] == "TRADE_BLOCKED"
"HIGH_IMPACT_NEWS_NEARBY" in result["block_codes"]
```

Test 2 — analyze_symbol bị block khi có tin mạnh gần:
Dùng dữ liệu nến giả giống thật hoặc monkeypatch score cao.
Mục tiêu là dù score cao, nếu có:
```python
"high_impact_event_within_30m": True
```
thì:
```python
result["trade_gate"]["allowed"] is False
result["trade_gate"]["decision_cap"] == "TRADE_BLOCKED"
"HIGH_IMPACT_NEWS_NEARBY" in result["trade_gate"]["block_codes"]
result["decision_summary"]["action"] == "stand_aside"
```

Test 3 — không có tin mạnh gần thì không block bởi news:
```python
"high_impact_event_within_30m": False
```
Kỳ vọng:
```python
"HIGH_IMPACT_NEWS_NEARBY" not in result["trade_gate"]["block_codes"]
```

Chạy test:

```bash
pytest -q tests/test_macro_news_gate_phase4.py
pytest -q tests/test_trade_gate_engine.py
pytest -q tests/test_analysis_trade_gate_integration.py
```

Chạy regression:

```bash
pytest -q tests/test_macro_modifier_phase4.py tests/test_macro_weights_phase4.py tests/test_score_gap.py tests/test_signal_engine.py
```

Nếu lỗi do thiếu PyQt6/MT5 thì không sửa lan man.
Nếu lỗi do thay đổi Phase 4 Prompt 3 thì sửa đúng chỗ.

Bước 5 — Báo cáo:
Sau khi hoàn thành Prompt 3, báo cáo ngắn:
1. File đã sửa/tạo.
2. Tin mạnh gần đã được gate/block thế nào.
3. Test đã chạy.
4. Test pass/fail.
5. Xác nhận không sửa ngoài phạm vi Prompt 3.
6. Cuối báo cáo ghi đúng dòng:

ĐÃ HOÀN THÀNH YÊU CẦU

Chưa được báo “ĐÃ HOÀN THÀNH TASK” sau Prompt 3, vì còn Prompt 4 rà soát cuối.

============================================================
PROMPT 4 — TEST DỮ LIỆU GIỐNG THẬT, RÀ SOÁT TOÀN BỘ PHASE 4 VÀ BÁO CÁO CUỐI
============================================================

Bạn đang hoàn thiện Phase 4 của project AI Market Analyst v2.0.

Nhiệm vụ lần này:
- Không thêm tính năng mới.
- Không refactor.
- Không sửa UI.
- Không sửa scanner.
- Không sửa SMC.
- Không sửa risk.
- Không sửa entry engine.
- Chỉ kiểm tra, test dữ liệu giống thật, và sửa bug nhỏ nếu bug do Phase 4 gây ra.

Mục tiêu:
Rà soát toàn bộ Phase 4:
1. Macro weight đã giảm, macro không còn chiếm 30–40% điểm.
2. Macro aligned chỉ cộng nhẹ.
3. Macro conflict trừ rõ.
4. Macro unclear không cộng điểm.
5. Tin mạnh gần là gate/block.
6. Score/gate/decision không bị phá.
7. Không làm thay đổi các phase trước.

Yêu cầu kiểm tra code thủ công:

1. Mở `core/signal_engine.py`, kiểm tra:
- `DYNAMIC_WEIGHTS` macro <= 20 ở các regime.
- Tổng trọng số từng regime vẫn hợp lý.
- `macro_status` có thể là:
  - `"aligned"`
  - `"conflict"`
  - `"unclear"`
- `macro_modifier`:
  - aligned: +5
  - conflict: -15
  - unclear: 0
- `signal_score` được clamp 0–100.
- Không mất `total`.
- Không mất `signal_score`.
- Không sửa logic scoring khác ngoài macro.

2. Mở `core/trade_gate_engine.py`, kiểm tra:
- `HIGH_IMPACT_NEWS_NEARBY` là block cứng.
- Nếu có block cứng thì `allowed=False`.
- Nếu có block cứng thì `decision_cap="TRADE_BLOCKED"`.

3. Mở `core/analysis_engine.py`, kiểm tra:
- `high_impact_event_within_30m` được truyền vào `gate_context`.
- Nếu gate block thì `decision_summary["action"] == "stand_aside"`.
- `trade_permission` cũ vẫn tồn tại.
- `trade_gate` vẫn tồn tại.
- `direction_bias` Phase 3 không bị mất.

Viết hoặc cập nhật test dữ liệu giống thật:

Tạo file:
`tests/test_phase4_macro_realistic.py`

Mục tiêu test:
Không cần kiểm tra chính xác toàn bộ indicator.
Chỉ cần đảm bảo macro không kéo setup yếu thành ready, macro conflict phạt rõ, và news gần block.

Gợi ý tạo dữ liệu nến giả giống thật:

```python
from datetime import datetime, timedelta, timezone
from core.market_models import Candle

def make_trending_candles(count=150, start_price=1.0800, step=0.00025):
    candles = []
    t = datetime(2026, 1, 1, tzinfo=timezone.utc)
    price = start_price

    for i in range(count):
        open_ = price
        close = price + step
        high = max(open_, close) + 0.00015
        low = min(open_, close) - 0.00015
        candles.append(Candle(
            time=t + timedelta(hours=i),
            open=open_,
            high=high,
            low=low,
            close=close,
            volume=1000 + i,
        ))
        price = close

    return candles
```

Nếu constructor `Candle` khác field, hãy kiểm tra `core/market_models.py` và sửa test cho đúng.
Không sửa model chỉ để test.

Test 1 — macro aligned chỉ cộng nhẹ:
- Dùng cùng bộ dữ liệu/score.
- So sánh case macro unclear và macro aligned.
- Kỳ vọng:
```python
aligned_score - unclear_score <= 5
```
hoặc nếu score bị clamp 100 thì kiểm tra:
```python
result_aligned["macro_modifier"] == 5
"MACRO_ALIGNED" in result_aligned["reason_codes"]
```

Test 2 — macro conflict trừ rõ:
- So sánh case macro unclear và macro conflict.
- Kỳ vọng:
```python
unclear_score - conflict_score >= 10
```
hoặc:
```python
result_conflict["macro_modifier"] == -15
"MACRO_CONFLICT" in result_conflict["penalty_codes"]
```

Test 3 — macro unclear không cộng:
- Dữ liệu macro thiếu hoặc neutral.
- Kỳ vọng:
```python
result_unclear["macro_modifier"] == 0
"MACRO_UNCLEAR" in result_unclear["penalty_codes"]
```

Test 4 — high impact news nearby block:
- Dùng score cao hoặc monkeypatch score cao.
- Set:
```python
"high_impact_event_within_30m": True
```
Kỳ vọng:
```python
result["trade_gate"]["allowed"] is False
result["trade_gate"]["decision_cap"] == "TRADE_BLOCKED"
"HIGH_IMPACT_NEWS_NEARBY" in result["trade_gate"]["block_codes"]
result["decision_summary"]["action"] == "stand_aside"
```

Test 5 — output Phase 1/2/3 vẫn còn:
Kỳ vọng result vẫn có:
```python
"signal_score" hoặc scores có "signal_score"
"trade_gate"
"direction_bias"
"decision_summary"
```

Chạy toàn bộ test Phase 4:

```bash
pytest -q tests/test_macro_weights_phase4.py
pytest -q tests/test_macro_modifier_phase4.py
pytest -q tests/test_macro_news_gate_phase4.py
pytest -q tests/test_phase4_macro_realistic.py
```

Chạy regression các phase trước:

```bash
pytest -q tests/test_signal_engine.py
pytest -q tests/test_score_gap.py
pytest -q tests/test_trade_gate_engine.py
pytest -q tests/test_analysis_score_gap_integration.py
pytest -q tests/test_analysis_trade_gate_integration.py
```

Chạy regression core nếu môi trường cho phép:

```bash
pytest -q tests/test_risk_engine.py tests/test_entry_engine.py tests/test_single_analysis.py tests/test_scanner.py
```

Nếu lỗi do thiếu PyQt6/MT5:
- Không sửa code project để né lỗi.
- Ghi rõ test nào không chạy được do thiếu dependency.

Nếu lỗi do Phase 4:
- Sửa đúng chỗ gây lỗi.
- Không sửa lan sang module khác.

Yêu cầu báo cáo cuối cùng sau khi hoàn thành toàn bộ Phase 4:
Báo cáo ngắn gồm:
1. File đã sửa/tạo.
2. Macro weight mới.
3. Macro modifier đã hoạt động thế nào.
4. News gate đã hoạt động thế nào.
5. Test đã chạy.
6. Test pass/fail.
7. Nếu fail thì lý do.
8. Xác nhận không sửa phần ngoài phạm vi Phase 4.
9. Cuối báo cáo bắt buộc ghi đúng 2 dòng sau:

ĐÃ HOÀN THÀNH YÊU CẦU

ĐÃ HOÀN THÀNH TASK

============================================================
KẾT THÚC FILE PROMPT PHASE 4
============================================================
