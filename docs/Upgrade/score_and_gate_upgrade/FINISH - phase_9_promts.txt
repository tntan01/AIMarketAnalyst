PROMPT 1 — PHASE 9.1: TẠO MODULE CHUẨN HÓA REASON CODES

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Hệ thống đã hoàn thành Phase 8. Bây giờ chỉ làm Phase 9 theo plan: REASON CODES CHUẨN HÓA.

Mục tiêu Phase 9:
Mọi điểm cộng, điểm trừ, warning và block quan trọng phải có mã lý do chuẩn để UI, AI commentary, journal và test có thể đọc ổn định. Không chỉ dùng text tự do.

Mục tiêu riêng của prompt này:
Tạo module trung tâm core/reason_codes.py, chỉ định nghĩa constant, nhóm code, bản dịch tiếng Việt và helper nhỏ để chuẩn hóa/deduplicate danh sách code.

Yêu cầu rất quan trọng:
- Chỉ được tạo file mới core/reason_codes.py.
- Chỉ được tạo test mới tests/test_reason_codes_phase9.py.
- Không sửa signal_engine.py, trade_gate_engine.py, account_guard.py, entry_engine.py, analysis_engine.py, risk_engine.py, scanner.py hoặc UI trong prompt này.
- Không đổi output hiện tại của bất kỳ engine nào trong prompt này.
- Không làm Phase 10, không tạo statistical_edge_engine.py, không tạo execution_quality_engine.py, không tạo decision_engine.py.
- Không refactor code ngoài phạm vi prompt này.

Việc cần làm:
1. Tạo file:
   core/reason_codes.py

2. Trong core/reason_codes.py, định nghĩa các constant dạng string. Tối thiểu phải có các nhóm sau:

   Nhóm trend/location/SMC:
   - TREND_D1_H4_ALIGNED
   - PRICE_NEAR_SUPPORT
   - PRICE_NEAR_RESISTANCE
   - CHOCH_AGAINST_DIRECTION
   - ZONE_BROKEN
   - SWEEP_DISPLACEMENT_M15_ALIGNED

   Nhóm M15:
   - M15_STRICT_CONFIRMED
   - M15_LOOSE_CONFIRMATION
   - M15_NOT_CONFIRMED
   - M15_DATA_UNAVAILABLE

   Nhóm spread/news/data/MT5:
   - SPREAD_NORMAL
   - SPREAD_CAUTION
   - SPREAD_ABNORMAL
   - HIGH_IMPACT_NEWS_NEARBY
   - DATA_QUALITY_WARNING
   - MT5_NOT_READY

   Nhóm R:R:
   - EXPECTED_RR_OK
   - EXPECTED_RR_TOO_LOW

   Nhóm account guard:
   - DAILY_LOSS_LIMIT_REACHED
   - WEEKLY_LOSS_LIMIT_REACHED
   - MAX_CONSECUTIVE_LOSSES_REACHED
   - MAX_OPEN_RISK_REACHED

   Nhóm macro:
   - MACRO_ALIGNED
   - MACRO_UNCLEAR
   - MACRO_CONFLICT

   Nhóm score gap:
   - BUY_SELL_SCORE_GAP_LOW

   Nhóm statistical edge để chuẩn bị cho phase sau, chỉ định nghĩa constant, chưa dùng:
   - STAT_EDGE_NOT_ENOUGH_DATA
   - STAT_EDGE_POSITIVE
   - STAT_EDGE_NEGATIVE

3. Tạo dictionary REASON_CODE_MESSAGES để map code sang tiếng Việt ngắn gọn, dễ hiển thị UI.
   Ví dụ:
   REASON_CODE_MESSAGES = {
       SPREAD_ABNORMAL: "Spread đang bất thường, không nên mở lệnh mới.",
       M15_NOT_CONFIRMED: "M15 chưa xác nhận tín hiệu vào lệnh.",
   }

4. Tạo helper:

   def normalize_codes(codes: list[str] | tuple[str, ...] | None) -> list[str]:
       """Trả về list code đã bỏ None/rỗng, ép str, giữ thứ tự, bỏ trùng."""

   Yêu cầu:
   - Input None trả về []
   - Bỏ code None hoặc chuỗi rỗng
   - Giữ nguyên thứ tự xuất hiện đầu tiên
   - Không tự động viết hoa/lowercase vì code phải chính xác

5. Tạo helper:

   def codes_to_messages(codes: list[str] | tuple[str, ...] | None) -> list[str]:
       """Đổi code sang message tiếng Việt. Code chưa biết thì trả chính code đó."""

6. Tạo helper:

   def append_code(target: list[str], code: str | None) -> None:
       """Append code vào target nếu code hợp lệ và chưa tồn tại."""

7. Tạo test mới:
   tests/test_reason_codes_phase9.py

8. Test cần kiểm tra:
   - Các constant quan trọng tồn tại và bằng chính chuỗi code.
   - normalize_codes bỏ trùng nhưng giữ thứ tự.
   - normalize_codes xử lý None, chuỗi rỗng.
   - codes_to_messages trả message tiếng Việt cho code đã biết.
   - codes_to_messages trả lại code gốc nếu code chưa biết.
   - append_code không append trùng.

9. Chạy test:
   python -m pytest tests/test_reason_codes_phase9.py -q

10. Sau khi code xong, tự kiểm tra thật cẩn thận:
   - Không có syntax error.
   - File core/reason_codes.py không import ngược các module engine để tránh circular import.
   - Không thay đổi behavior của app vì chưa tích hợp vào engine.

Sau khi hoàn thành tất cả việc trong prompt này, báo đúng câu:
ĐÃ HOÀN THÀNH PROMPT 1


================================================================================

PROMPT 2 — PHASE 9.2: TÍCH HỢP REASON CODES VÀO signal_engine.py, KHÔNG ĐỔI CÔNG THỨC SCORE

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Prompt trước đã tạo core/reason_codes.py. Bây giờ tích hợp reason code vào core/signal_engine.py.

Mục tiêu:
core/signal_engine.py không còn tự append các chuỗi code rời rạc như "MACRO_CONFLICT", "MACRO_UNCLEAR", "MACRO_ALIGNED", "CHOCH_AGAINST_DIRECTION". Phải dùng constant từ core/reason_codes.py.

Yêu cầu rất quan trọng:
- Chỉ được sửa core/signal_engine.py và test liên quan tới signal engine.
- Không được đổi công thức tính điểm.
- Không được đổi trọng số DYNAMIC_WEIGHTS.
- Không được đổi logic macro modifier.
- Không được đổi logic SMC CHOCH cap.
- Không sửa trade_gate_engine.py, account_guard.py, entry_engine.py, analysis_engine.py, risk_engine.py, scanner.py hoặc UI trong prompt này.
- Không làm Phase 10 hoặc các phase sau.
- Không đổi format output hiện có ngoài việc bảo đảm reason_codes và penalty_codes được normalize/deduplicate.

Việc cần làm:
1. Mở file:
   - core/signal_engine.py
   - core/reason_codes.py
   - tests/test_signal_engine.py
   - tests/test_macro_modifier_phase4.py
   - tests/test_smc_choch_cap_phase5.py

2. Trong core/signal_engine.py, import các constant/helper cần thiết từ core.reason_codes:
   - MACRO_ALIGNED
   - MACRO_UNCLEAR
   - MACRO_CONFLICT
   - CHOCH_AGAINST_DIRECTION
   - normalize_codes hoặc append_code nếu phù hợp

3. Thay các chuỗi hard-code hiện tại bằng constant:
   - "MACRO_CONFLICT" -> MACRO_CONFLICT
   - "MACRO_UNCLEAR" -> MACRO_UNCLEAR
   - "MACRO_ALIGNED" -> MACRO_ALIGNED
   - "CHOCH_AGAINST_DIRECTION" -> CHOCH_AGAINST_DIRECTION

4. Khi append code vào reason_codes hoặc penalty_codes, phải tránh trùng code.
   Có thể dùng append_code(reason_codes, MACRO_ALIGNED), append_code(penalty_codes, MACRO_CONFLICT), ...

5. Trước khi return score_scenario(), đảm bảo:
   - reason_codes là list đã normalize.
   - penalty_codes là list đã normalize.
   - block_codes nếu chưa có thì không bắt buộc thêm ở signal_engine.
   - Không làm mất các key cũ: signal_score, total, rating, macro_status, macro_modifier, smc_score_cap, smc_flags.

6. Bổ sung hoặc cập nhật test:
   - macro aligned phải có MACRO_ALIGNED trong reason_codes.
   - macro unclear phải có MACRO_UNCLEAR trong penalty_codes.
   - macro conflict phải có MACRO_CONFLICT trong penalty_codes và signal_score bị trừ như cũ.
   - CHOCH ngược hướng phải có CHOCH_AGAINST_DIRECTION trong penalty_codes và signal_score không vượt quá 60.
   - Không có code bị lặp nếu điều kiện bị xử lý nhiều lần.

7. Chạy test:
   python -m pytest tests/test_reason_codes_phase9.py tests/test_signal_engine.py tests/test_macro_modifier_phase4.py tests/test_smc_choch_cap_phase5.py -q

8. Dùng thêm một script Python nhỏ hoặc pytest để test dữ liệu giống thật:
   - Tạo technical dict đầy đủ cho EUR/USD.
   - Tạo macro_context aligned/conflict/unclear.
   - Tạo smc dict có CHOCH ngược hướng.
   - Gọi score_scenario() và in/kiểm tra reason_codes, penalty_codes.

9. Sau khi code xong, tự kiểm tra thật cẩn thận:
   - Không có circular import.
   - Không đổi score ngoài phần đã có từ Phase 4/5.
   - Không đổi tên key output.
   - Không sửa file ngoài phạm vi cho phép.

Sau khi hoàn thành tất cả việc trong prompt này, báo đúng câu:
ĐÃ HOÀN THÀNH PROMPT 2


================================================================================

PROMPT 3 — PHASE 9.3: TÍCH HỢP REASON CODES VÀO trade_gate_engine.py VÀ account_guard.py

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Reason code module đã có. signal_engine.py đã bắt đầu dùng constant. Bây giờ chuẩn hóa các gate code.

Mục tiêu:
Các block_codes và warning_codes trong core/trade_gate_engine.py và core/account_guard.py phải dùng constant từ core/reason_codes.py, không dùng chuỗi hard-code rời rạc. Output vẫn giữ nguyên dạng dict hiện tại.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  - core/trade_gate_engine.py
  - core/account_guard.py
  - các test liên quan đến trade gate/account guard
- Không sửa signal_engine.py trong prompt này, trừ khi cần chỉnh import do lỗi trực tiếp từ prompt trước.
- Không sửa entry_engine.py, analysis_engine.py, risk_engine.py, scanner.py hoặc UI.
- Không đổi logic gate.
- Không đổi độ ưu tiên decision_cap.
- Không đổi allowed/blocked behavior.
- Không đổi nội dung reasons tiếng Việt nếu không cần thiết.
- Không làm Phase 10 hoặc các phase sau.

Việc cần làm:
1. Mở các file:
   - core/trade_gate_engine.py
   - core/account_guard.py
   - core/reason_codes.py
   - tests/test_trade_gate_engine.py
   - tests/test_trade_gate_account_guard_phase7.py
   - tests/test_account_guard_phase7.py
   - tests/test_phase7_account_guard_realistic.py

2. Trong trade_gate_engine.py, import các constant/helper cần thiết:
   - MT5_NOT_READY
   - SPREAD_ABNORMAL
   - DATA_QUALITY_WARNING
   - HIGH_IMPACT_NEWS_NEARBY
   - M15_NOT_CONFIRMED
   - M15_LOOSE_CONFIRMATION
   - EXPECTED_RR_TOO_LOW
   - DAILY_LOSS_LIMIT_REACHED
   - WEEKLY_LOSS_LIMIT_REACHED
   - BUY_SELL_SCORE_GAP_LOW
   - ZONE_BROKEN
   - append_code hoặc normalize_codes

3. Thay toàn bộ chuỗi code hard-code trong block_codes/warning_codes bằng constant.

4. Khi thêm vào block_codes/warning_codes, không append trùng.
   Ví dụ:
   append_code(result["block_codes"], SPREAD_ABNORMAL)
   append_code(result["warning_codes"], M15_NOT_CONFIRMED)

5. Khi merge account_guard vào trade_gate_engine.py:
   - Vẫn giữ khả năng merge code từ account_guard.
   - Không duplicate code nếu code đã tồn tại.
   - Không làm mất reasons.

6. Trong account_guard.py, import các constant/helper cần thiết:
   - DAILY_LOSS_LIMIT_REACHED
   - WEEKLY_LOSS_LIMIT_REACHED
   - MAX_CONSECUTIVE_LOSSES_REACHED
   - MAX_OPEN_RISK_REACHED
   - append_code hoặc normalize_codes

7. Thay các chuỗi code hard-code trong account_guard.py bằng constant.

8. Trước khi return trong check_account_guard(), đảm bảo:
   - block_codes đã normalize/deduplicate.
   - warning_codes đã normalize/deduplicate.
   - reasons không bắt buộc normalize nhưng không được trùng quá nhiều nếu dễ xử lý.

9. Bổ sung/cập nhật test:
   - spread abnormal => block_codes có SPREAD_ABNORMAL.
   - M15 none => warning_codes có M15_NOT_CONFIRMED.
   - M15 loose => warning_codes có M15_LOOSE_CONFIRMATION.
   - expected effective R:R thấp => warning_codes có EXPECTED_RR_TOO_LOW.
   - score gap thấp => warning_codes có BUY_SELL_SCORE_GAP_LOW.
   - zone broken => warning_codes có ZONE_BROKEN.
   - daily loss reached từ account_guard => block_codes có DAILY_LOSS_LIMIT_REACHED.
   - weekly loss reached từ account_guard => block_codes có WEEKLY_LOSS_LIMIT_REACHED.
   - max consecutive losses => block_codes có MAX_CONSECUTIVE_LOSSES_REACHED.
   - max open risk => block_codes có MAX_OPEN_RISK_REACHED.
   - Khi account_guard merge vào trade_gate, code không bị lặp.

10. Chạy test:
   python -m pytest tests/test_reason_codes_phase9.py tests/test_trade_gate_engine.py tests/test_trade_gate_account_guard_phase7.py tests/test_account_guard_phase7.py tests/test_phase7_account_guard_realistic.py -q

11. Dùng dữ liệu giống thật để test nhanh bằng Python:
   - context spread_status = "abnormal", m15_quality = "none", score_gap = 4, expected_effective_rr = 1.1.
   - check_trade_gates(context) phải trả allowed False do spread, decision_cap TRADE_BLOCKED, block_codes có SPREAD_ABNORMAL, warning_codes có M15_NOT_CONFIRMED, BUY_SELL_SCORE_GAP_LOW, EXPECTED_RR_TOO_LOW.

12. Sau khi code xong, tự kiểm tra thật cẩn thận:
   - Không đổi thứ tự gate nếu không có lý do.
   - Không làm mất hard block.
   - Không để duplicate codes.
   - Không sửa file ngoài phạm vi cho phép.

Sau khi hoàn thành tất cả việc trong prompt này, báo đúng câu:
ĐÃ HOÀN THÀNH PROMPT 3


================================================================================

PROMPT 4 — PHASE 9.4: THÊM REASON CODES VÀO entry_engine.py CHO TRẠNG THÁI M15

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 8 đã sửa entry_engine để M15 quyết định entry_status. Phase 9 cần output entry cũng có reason_codes/warning_codes/block_codes chuẩn.

Mục tiêu:
core/entry_engine.py phải trả thêm các key mã hóa lý do liên quan đến entry/M15 nhưng không phá output cũ.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  - core/entry_engine.py
  - tests/test_entry_engine.py
  - tests/test_entry_engine_phase8.py nếu có
  - có thể tạo tests/test_entry_reason_codes_phase9.py
- Không sửa trade_gate_engine.py, account_guard.py, signal_engine.py, analysis_engine.py, risk_engine.py, scanner.py hoặc UI trong prompt này.
- Không đổi logic Phase 8.
- Không được để m15_quality loose/none/unavailable thành confirmed_entry.
- Không đổi chữ ký public evaluate_entry() nếu không bắt buộc.
- Không xóa các key output cũ: entry_status, trigger_type, confirmation_score, invalid_reason, price_in_entry_zone, h1_confirmation, ready_to_trade, m15_available, m15_checked, m15_confirmed, m15_quality, m15_score_multiplier.
- Không làm Phase 10 hoặc các phase sau.

Việc cần làm:
1. Mở:
   - core/entry_engine.py
   - core/reason_codes.py
   - tests/test_entry_engine.py
   - tests/test_entry_engine_phase8.py nếu tồn tại

2. Import các constant/helper cần thiết từ core.reason_codes:
   - M15_STRICT_CONFIRMED
   - M15_LOOSE_CONFIRMATION
   - M15_NOT_CONFIRMED
   - M15_DATA_UNAVAILABLE
   - ZONE_BROKEN
   - append_code hoặc normalize_codes

3. Mở rộng helper _result() để có thêm optional parameters:
   - reason_codes: list[str] | None = None
   - warning_codes: list[str] | None = None
   - block_codes: list[str] | None = None

4. Trong output result của _result(), luôn có các key:
   - "reason_codes": list
   - "warning_codes": list
   - "block_codes": list

   Yêu cầu:
   - Nếu không có code thì trả [].
   - Các list phải được normalize/deduplicate.
   - Không làm mất key cũ.

5. Gắn code theo logic:
   - Nếu m15_quality == "strict" và confirmed_entry: reason_codes có M15_STRICT_CONFIRMED.
   - Nếu m15_quality == "loose": warning_codes có M15_LOOSE_CONFIRMATION.
   - Nếu m15_quality == "none": warning_codes có M15_NOT_CONFIRMED.
   - Nếu m15_available is False hoặc thiếu dữ liệu M15: warning_codes có M15_DATA_UNAVAILABLE.
   - Nếu entry_status invalidated do zone_broken: warning_codes hoặc block_codes có ZONE_BROKEN. Vì Phase 5 hiện coi zone broken là WATCH_ONLY/gate warning, ưu tiên để warning_codes nếu toàn hệ thống đang dùng ZONE_BROKEN như warning.

6. Không được thêm code sai ngữ cảnh:
   - Không thêm M15_STRICT_CONFIRMED nếu entry_status không phải confirmed_entry.
   - Không thêm M15_LOOSE_CONFIRMATION nếu m15_quality không phải loose.
   - Không thêm M15_NOT_CONFIRMED nếu m15_quality strict.

7. Tạo/cập nhật test:
   - strict confirmed_entry có reason_codes chứa M15_STRICT_CONFIRMED.
   - loose waiting_confirmation có warning_codes chứa M15_LOOSE_CONFIRMATION, ready_to_trade False.
   - none watch_zone có warning_codes chứa M15_NOT_CONFIRMED, ready_to_trade False.
   - thiếu M15 có warning_codes chứa M15_DATA_UNAVAILABLE và không confirmed_entry.
   - zone broken có code ZONE_BROKEN.
   - Tất cả output đều có reason_codes, warning_codes, block_codes là list.

8. Chạy test:
   python -m pytest tests/test_reason_codes_phase9.py tests/test_entry_engine.py tests/test_entry_engine_phase8.py tests/test_entry_reason_codes_phase9.py -q

   Nếu file tests/test_entry_engine_phase8.py hoặc tests/test_entry_reason_codes_phase9.py chưa tồn tại thì chỉ chạy các file tồn tại.

9. Dùng dữ liệu nến giống thật để test:
   - Tạo H1 candle bullish/bearish rõ.
   - Tạo M15 strict/loose/none.
   - Gọi evaluate_entry() và kiểm tra code đúng.

10. Sau khi code xong, tự kiểm tra thật cẩn thận:
   - Không phá Phase 8.
   - Không thay đổi công thức confirmation_score ngoài logic đã có.
   - Không sửa file ngoài phạm vi cho phép.
   - Không có duplicate code.

Sau khi hoàn thành tất cả việc trong prompt này, báo đúng câu:
ĐÃ HOÀN THÀNH PROMPT 4


================================================================================

PROMPT 5 — PHASE 9.5: TỔNG HỢP REASON CODES TRONG analysis_engine.py, KHÔNG ĐỔI DECISION LOGIC

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Các engine con đã bắt đầu trả reason_codes/warning_codes/block_codes chuẩn. Bây giờ analysis_engine.py cần gom các code này vào output phân tích tổng để UI/AI commentary/journal dùng được.

Mục tiêu:
core/analysis_engine.py phải giữ nguyên decision logic hiện tại nhưng bổ sung output chuẩn hóa:
- reason_codes
- penalty_codes
- warning_codes
- block_codes
- reason_messages nếu dễ làm, dùng codes_to_messages()

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  - core/analysis_engine.py
  - test liên quan analysis integration
- Không sửa signal_engine.py, trade_gate_engine.py, account_guard.py, entry_engine.py, risk_engine.py, scanner.py hoặc UI trong prompt này.
- Không đổi decision logic.
- Không đổi score formula.
- Không đổi trade_permission behavior.
- Không đổi cấu trúc output cũ nếu UI đang dùng.
- Chỉ bổ sung key mới, không xóa key cũ.
- Không làm Phase 10 hoặc các phase sau.

Việc cần làm:
1. Mở:
   - core/analysis_engine.py
   - core/reason_codes.py
   - tests/test_analysis_trade_gate_integration.py
   - tests/test_analysis_score_gap_integration.py
   - tests/test_analysis_account_guard_integration_phase7.py
   - các test analysis hiện có liên quan

2. Import helper từ core.reason_codes:
   - normalize_codes
   - codes_to_messages

3. Xác định nơi analysis_engine.py đã có:
   - best_side_scores hoặc scores[best_side]
   - gate_result
   - account_guard_result
   - entry scenario/result nếu có
   - trade_permission

4. Bổ sung logic gom code, ưu tiên không làm thay đổi decision:

   Gợi ý:
   combined_reason_codes = []
   combined_penalty_codes = []
   combined_warning_codes = []
   combined_block_codes = []

   Lấy từ:
   - best_side_scores.get("reason_codes", [])
   - best_side_scores.get("penalty_codes", [])
   - gate_result.get("warning_codes", [])
   - gate_result.get("block_codes", [])
   - account_guard_result.get("warning_codes", [])
   - account_guard_result.get("block_codes", [])
   - scenario/entry result nếu có reason_codes/warning_codes/block_codes

5. Trước khi đưa vào output cuối, normalize toàn bộ:
   - reason_codes = normalize_codes(combined_reason_codes)
   - penalty_codes = normalize_codes(combined_penalty_codes)
   - warning_codes = normalize_codes(combined_warning_codes)
   - block_codes = normalize_codes(combined_block_codes)

6. Nếu dễ làm và không ảnh hưởng UI, thêm:
   - reason_messages = codes_to_messages(reason_codes + penalty_codes + warning_codes + block_codes)

7. Không được xóa các key cũ như:
   - gate_block_codes
   - gate_warning_codes
   - account_guard_block_codes
   - trade_permission
   - scores
   - decision hiện tại

8. Cập nhật/bổ sung test:
   - Analysis output có reason_codes/warning_codes/block_codes là list.
   - Nếu gate spread abnormal thì block_codes có SPREAD_ABNORMAL.
   - Nếu score gap thấp thì warning_codes có BUY_SELL_SCORE_GAP_LOW.
   - Nếu macro conflict từ signal_engine thì penalty_codes có MACRO_CONFLICT.
   - Nếu account guard daily loss block thì block_codes có DAILY_LOSS_LIMIT_REACHED.
   - Duplicate code từ gate/account_guard không bị lặp.
   - Decision hiện tại không bị thay đổi chỉ vì thêm code.

9. Chạy test:
   python -m pytest tests/test_reason_codes_phase9.py tests/test_analysis_trade_gate_integration.py tests/test_analysis_score_gap_integration.py tests/test_analysis_account_guard_integration_phase7.py -q

10. Nếu analysis integration test cần dữ liệu giống thật, tạo dict data giống luồng thật thay vì mock quá sơ sài:
   - technical đủ price, EMA, RSI, MACD, ATR.
   - smc có zone/sweep/CHOCH flag.
   - gate_context có spread/news/M15/score_gap.
   - account_guard_result có block_codes/warning_codes.

11. Sau khi code xong, tự kiểm tra thật cẩn thận:
   - Không đổi quyết định cuối cùng.
   - Không xóa key backward compatibility.
   - Không sửa UI.
   - Không sửa engine khác.
   - Không duplicate code.

Sau khi hoàn thành tất cả việc trong prompt này, báo đúng câu:
ĐÃ HOÀN THÀNH PROMPT 5


================================================================================

PROMPT 6 — PHASE 9.6: TEST TỔNG HỢP PHASE 9 VÀ KIỂM TRA KHÔNG CÒN CODE RỜI RẠC QUAN TRỌNG

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 9 đã có module reason_codes.py và đã tích hợp vào signal_engine, trade_gate_engine, account_guard, entry_engine, analysis_engine. Bây giờ cần test tổng hợp và kiểm tra code để tránh bug.

Mục tiêu:
Đảm bảo reason_codes hoạt động ổn định trong toàn bộ luồng scoring/gate/entry mà không làm thay đổi logic Phase 1–8.

Yêu cầu rất quan trọng:
- Chỉ được sửa test nếu phát hiện thiếu test.
- Chỉ được sửa code production nếu có bug trực tiếp liên quan Phase 9.
- Không refactor lớn.
- Không sửa UI.
- Không làm Phase 10 hoặc các phase sau.
- Không tạo statistical_edge_engine.py, execution_quality_engine.py, decision_engine.py.
- Không đổi công thức score, gate, account guard, entry.

Việc cần làm:
1. Chạy toàn bộ các test trọng tâm:
   python -m pytest tests/test_reason_codes_phase9.py tests/test_signal_engine.py tests/test_macro_modifier_phase4.py tests/test_smc_choch_cap_phase5.py tests/test_trade_gate_engine.py tests/test_trade_gate_account_guard_phase7.py tests/test_account_guard_phase7.py tests/test_entry_engine.py tests/test_analysis_trade_gate_integration.py tests/test_analysis_score_gap_integration.py tests/test_analysis_account_guard_integration_phase7.py -q

2. Nếu máy thiếu MT5/PyQt6 khiến test không liên quan bị lỗi môi trường, không sửa code production để né lỗi. Chỉ ghi rõ lỗi môi trường. Unit test core phải pass.

3. Tạo test mới nếu chưa có:
   tests/test_phase9_reason_codes_realistic.py

4. Trong test realistic, dùng dữ liệu giống thật để kiểm tra các case:

   Case 1 — Signal macro conflict:
   - Gọi score_scenario() với macro_context ngược hướng.
   - Kỳ vọng penalty_codes có MACRO_CONFLICT.

   Case 2 — Trade gate nhiều cảnh báo cùng lúc:
   - context có spread_status abnormal, m15_quality none, score_gap thấp, expected_effective_rr thấp.
   - Kỳ vọng:
     allowed False
     decision_cap TRADE_BLOCKED
     block_codes có SPREAD_ABNORMAL
     warning_codes có M15_NOT_CONFIRMED, BUY_SELL_SCORE_GAP_LOW, EXPECTED_RR_TOO_LOW
     không có duplicate code

   Case 3 — Account guard:
   - closed_trades giống thật có daily loss vượt ngưỡng.
   - action = open_new_trade.
   - Kỳ vọng blocked True, block_codes có DAILY_LOSS_LIMIT_REACHED.

   Case 4 — Entry M15:
   - strict có M15_STRICT_CONFIRMED.
   - loose có M15_LOOSE_CONFIRMATION.
   - none có M15_NOT_CONFIRMED.

5. Viết một đoạn kiểm tra nhẹ bằng Python trong test để đảm bảo các output code list không có duplicate:
   assert len(codes) == len(set(codes))

6. Kiểm tra grep để phát hiện code rời rạc quan trọng còn sót:

   grep -R "\"MACRO_CONFLICT\"\|\"MACRO_UNCLEAR\"\|\"MACRO_ALIGNED\"\|\"SPREAD_ABNORMAL\"\|\"M15_NOT_CONFIRMED\"\|\"DAILY_LOSS_LIMIT_REACHED\"\|\"BUY_SELL_SCORE_GAP_LOW\"" core tests

   Lưu ý:
   - Trong core/reason_codes.py được phép có chuỗi constant.
   - Trong tests có thể dùng string nếu đang kiểm tra backward compatibility, nhưng ưu tiên import constant.
   - Trong các engine production khác, nên dùng constant từ core.reason_codes.

7. Nếu grep phát hiện engine production còn hard-code những code quan trọng, sửa tối thiểu sang constant.
   Không sửa message tiếng Việt tự do, chỉ sửa reason code string.

8. Chạy lại test:
   python -m pytest tests/test_reason_codes_phase9.py tests/test_phase9_reason_codes_realistic.py -q

9. Nếu ổn, chạy thêm:
   python -m pytest tests/test_signal_engine.py tests/test_trade_gate_engine.py tests/test_account_guard_phase7.py tests/test_entry_engine.py -q

10. Sau khi code xong, tự kiểm tra thật cẩn thận:
   - Không có import vòng.
   - Không duplicate code.
   - Không phá output cũ.
   - Không đổi decision logic.
   - Không sửa phần không liên quan.

Sau khi hoàn thành tất cả việc trong prompt này, báo đúng câu:
ĐÃ HOÀN THÀNH PROMPT 6

Sau khi hoàn thành hết các công việc trong prompt này, báo đúng câu:
ĐÃ HOÀN THÀNH PHASE 9
