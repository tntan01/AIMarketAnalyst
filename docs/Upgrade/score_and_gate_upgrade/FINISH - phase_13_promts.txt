PROMPT 1 — PHASE 13.1: KIỂM TRA HIỆN TRẠNG SAU PHASE 12 VÀ KHÓA PHẠM VI PHASE 13

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh chung:
Hệ thống đã hoàn thành Phase 12: có core/trade_mistake_detector.py để tự phát hiện và gắn tag lỗi hành vi giao dịch. Bây giờ bắt đầu Phase 13: FINAL SCORE MỚI.

Mục tiêu Phase 13:
Tạo lớp final_score mới dựa trên 3 lớp điểm:
- signal_score: điểm tín hiệu hiện tại từ rule engine.
- evidence_score: điểm lợi thế thống kê từ core/statistical_edge_engine.py.
- execution_quality_score: điểm chất lượng thực thi từ core/execution_quality_engine.py.

Công thức giai đoạn đầu:
final_score = signal_score * 0.65 + evidence_score * 0.20 + execution_quality_score * 0.15

Nguyên tắc cực kỳ quan trọng:
- Phase 13 chỉ tính final_score và metadata giải thích final_score.
- Phase 13 KHÔNG quyết định READY_TO_TRADE.
- Phase 13 KHÔNG thay đổi trade gate.
- Phase 13 KHÔNG thay đổi decision hiện tại.
- Phase 13 KHÔNG tạo decision_engine.py, vì đó là Phase 14.
- Phase 13 KHÔNG sửa UI/scanner, trừ khi prompt sau yêu cầu rất rõ.
- Phase 13 KHÔNG sửa database/migration.
- Phase 13 KHÔNG sửa logic scoring hiện tại trong signal_engine.py.
- Phase 13 KHÔNG sửa logic phát hiện lỗi trong trade_mistake_detector.py.

Mục tiêu riêng của prompt này:
Chỉ kiểm tra hiện trạng code sau Phase 12, xác định file được phép sửa trong Phase 13, và bổ sung reason code cho final_score nếu còn thiếu. Chưa tạo engine tính final_score đầy đủ.

Yêu cầu rất quan trọng:
- Chỉ được đọc code và bổ sung constant reason code nếu thiếu.
- Không sửa signal_engine.py.
- Không sửa analysis_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa account_guard.py.
- Không sửa statistical_edge_engine.py.
- Không sửa execution_quality_engine.py.
- Không sửa trade_mistake_detector.py.
- Không sửa services/journal_service.py.
- Không sửa migration/database.
- Không sửa UI/scanner.

Việc cần làm:
1. Kiểm tra các file sau có tồn tại không:
   - core/reason_codes.py
   - core/statistical_edge_engine.py
   - core/execution_quality_engine.py
   - core/trade_mistake_detector.py
   - core/analysis_engine.py
   - tests/

2. Mở core/statistical_edge_engine.py để hiểu output hiện tại:
   - evidence_score
   - confidence
   - sample_size
   - reason_codes hoặc reason_code
   - warning_codes nếu có
   Không sửa logic file này.

3. Mở core/execution_quality_engine.py để hiểu output hiện tại:
   - execution_quality_score
   - reason_codes
   - penalty_codes
   - warning_codes
   - score_breakdown
   Không sửa logic file này.

4. Mở core/reason_codes.py để kiểm tra đã có các code final score chưa. Nếu thiếu thì chỉ bổ sung đúng các constant sau:
   - FINAL_SCORE_OK
   - FINAL_SCORE_DATA_INCOMPLETE
   - FINAL_SCORE_SIGNAL_DOMINANT
   - FINAL_SCORE_EVIDENCE_NEUTRAL
   - FINAL_SCORE_EVIDENCE_POSITIVE
   - FINAL_SCORE_EVIDENCE_NEGATIVE
   - FINAL_SCORE_EXECUTION_STRONG
   - FINAL_SCORE_EXECUTION_WEAK

5. Nếu core/reason_codes.py có REASON_CODE_MESSAGES thì bổ sung message tiếng Việt ngắn cho các code trên, ví dụ:
   - FINAL_SCORE_OK: "Đã tính final score từ signal, evidence và execution quality."
   - FINAL_SCORE_DATA_INCOMPLETE: "Thiếu một phần dữ liệu đầu vào, final score dùng fallback an toàn."
   - FINAL_SCORE_EVIDENCE_NEUTRAL: "Evidence score đang trung lập hoặc chưa đủ mẫu."
   - FINAL_SCORE_EXECUTION_WEAK: "Execution quality thấp, làm giảm final score."

6. Không đổi tên code Phase 9/10/11/12 đã có.
7. Không xóa code cũ.
8. Không refactor lớn.

Test bắt buộc:
- Chạy:
  python -m pytest tests/test_reason_codes_phase9.py -q
  nếu file này tồn tại.
- Nếu vừa thêm code mới thì tạo hoặc cập nhật test nhỏ:
  tests/test_reason_codes_final_score_phase13.py
  Test chỉ cần import được các constant FINAL_SCORE_* mới và map message được nếu có REASON_CODE_MESSAGES.
- Chạy:
  python -m pytest tests/test_reason_codes_final_score_phase13.py -q

Tiêu chí hoàn thành:
- Các constant FINAL_SCORE_* import được.
- Không ảnh hưởng behavior hiện tại.
- Không có lỗi import.
- Không có thay đổi ở engine khác ngoài reason_codes.py và test reason code nếu cần.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 2 — PHASE 13.2: TẠO MODULE RỖNG core/final_score_engine.py VÀ TEST IMPORT

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 13 cần module riêng để tính final_score từ signal_score, evidence_score và execution_quality_score. Module này chỉ tính điểm tổng hợp, không quyết định giao dịch.

Mục tiêu riêng của prompt này:
Tạo file core/final_score_engine.py có output mặc định an toàn và test import. Chưa tích hợp vào analysis_engine, chưa đổi decision.

Yêu cầu rất quan trọng:
- Chỉ được tạo/sửa 2 file:
  1. core/final_score_engine.py
  2. tests/test_final_score_engine_import_phase13.py
- Không sửa analysis_engine.py.
- Không sửa signal_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa statistical_edge_engine.py.
- Không sửa execution_quality_engine.py.
- Không sửa trade_mistake_detector.py.
- Không sửa journal_service.py.
- Không sửa UI/scanner.
- Không tạo decision_engine.py.
- Không thay đổi decision/gate hiện tại.

Việc cần làm:
1. Tạo file mới:
   core/final_score_engine.py

2. Trong file này, import các reason code từ core.reason_codes nếu đã có:
   - FINAL_SCORE_OK
   - FINAL_SCORE_DATA_INCOMPLETE
   - FINAL_SCORE_SIGNAL_DOMINANT
   - FINAL_SCORE_EVIDENCE_NEUTRAL
   - FINAL_SCORE_EVIDENCE_POSITIVE
   - FINAL_SCORE_EVIDENCE_NEGATIVE
   - FINAL_SCORE_EXECUTION_STRONG
   - FINAL_SCORE_EXECUTION_WEAK

3. Nếu import từ core.reason_codes gây lỗi do constant chưa tồn tại, quay lại prompt 1 để bổ sung đúng constant. Không hard-code duplicate constant trong nhiều file.

4. Định nghĩa constant:
   - DEFAULT_SIGNAL_SCORE = 0
   - DEFAULT_EVIDENCE_SCORE = 50
   - DEFAULT_EXECUTION_QUALITY_SCORE = 100
   - MIN_FINAL_SCORE = 0
   - MAX_FINAL_SCORE = 100
   - DEFAULT_FINAL_SCORE_WEIGHTS = {
       "signal_score": 0.65,
       "evidence_score": 0.20,
       "execution_quality_score": 0.15,
     }

5. Tạo helper:
   def clamp_score(value: object, minimum: int = 0, maximum: int = 100) -> int:
       ...

   Yêu cầu clamp_score:
   - Nhận int, float, string số như "72.5".
   - None, "", "abc", NaN, inf thì trả minimum.
   - Kết quả luôn nằm trong [minimum, maximum].
   - Dùng round hoặc int nhất quán, ưu tiên round về số nguyên gần nhất.
   - Không raise exception.

6. Tạo hàm mặc định:
   def default_final_score_result(reason: str = "final_score_not_calculated") -> dict:
       return {
           "final_score": 0,
           "score_inputs": {
               "signal_score": 0,
               "evidence_score": 50,
               "execution_quality_score": 100,
           },
           "score_weights": DEFAULT_FINAL_SCORE_WEIGHTS,
           "weighted_components": {},
           "reason_codes": [],
           "penalty_codes": [],
           "warning_codes": [FINAL_SCORE_DATA_INCOMPLETE],
           "score_breakdown": {},
           "reason": reason,
       }

7. Chưa tạo calculate_final_score đầy đủ trong prompt này nếu chưa cần. Nếu cần tạo thì chỉ cho return default.

8. Tạo test:
   tests/test_final_score_engine_import_phase13.py

9. Test cần kiểm tra:
   - import được core.final_score_engine.
   - DEFAULT_FINAL_SCORE_WEIGHTS có tổng gần bằng 1.0.
   - clamp_score(120) == 100.
   - clamp_score(-5) == 0.
   - clamp_score("72.5") == 72 hoặc 73 tùy cách round, nhưng phải ghi expected rõ theo implementation.
   - clamp_score("abc") == 0.
   - default_final_score_result() trả dict có key final_score, score_inputs, score_weights, warning_codes.

Test bắt buộc:
python -m pytest tests/test_final_score_engine_import_phase13.py -q

Tiêu chí hoàn thành:
- Module import sạch.
- Không ảnh hưởng module khác.
- Test mới pass.
- Không tạo decision_engine.py.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 3 — PHASE 13.3: THÊM HELPER ĐỌC SCORE VÀ WEIGHT AN TOÀN

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Final score cần đọc dữ liệu từ nhiều nguồn khác nhau. Dữ liệu có thể thiếu, dạng string, hoặc nằm trong nested dict của analysis_result.

Mục tiêu riêng của prompt này:
Thêm helper nhỏ trong final_score_engine để đọc score và weight an toàn. Chưa viết công thức final_score chính.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/final_score_engine.py
  2. tests/test_final_score_engine_helpers_phase13.py
- Không sửa analysis_engine.py.
- Không sửa signal_engine.py.
- Không sửa statistical_edge_engine.py.
- Không sửa execution_quality_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa UI/scanner.
- Không tạo decision_engine.py.
- Không thay đổi decision/gate.

Việc cần làm:
1. Trong core/final_score_engine.py, thêm helper:
   def safe_score(value: object, default: int = 0) -> int:
       ...

   Yêu cầu:
   - Dùng clamp_score để đưa về 0-100.
   - None, string bẩn, NaN, inf thì trả default đã clamp.
   - Không raise exception.

2. Thêm helper:
   def normalize_weights(weights: dict | None = None) -> dict[str, float]:
       ...

   Yêu cầu:
   - Nếu weights None thì trả DEFAULT_FINAL_SCORE_WEIGHTS.
   - Chỉ nhận các key:
     + signal_score
     + evidence_score
     + execution_quality_score
   - Nếu thiếu key thì bổ sung từ default.
   - Nếu weight âm, None, string bẩn thì fallback về default của key đó.
   - Sau khi có 3 weight, normalize để tổng bằng 1.0.
   - Nếu tổng <= 0 thì trả default.
   - Không mutate input weights gốc.
   - Không raise exception.

3. Thêm helper:
   def weighted_component(score: int, weight: float) -> float:
       ...

   Yêu cầu:
   - score được clamp 0-100.
   - weight ép về float an toàn.
   - Trả score * weight.
   - Không raise exception.

4. Thêm helper:
   def build_score_inputs(signal_score: object = None, evidence_score: object = None, execution_quality_score: object = None) -> dict[str, int]:
       ...

   Yêu cầu:
   - signal_score fallback 0.
   - evidence_score fallback 50.
   - execution_quality_score fallback 100.
   - Trả dict chuẩn:
     {
       "signal_score": int,
       "evidence_score": int,
       "execution_quality_score": int,
     }

5. Tạo test:
   tests/test_final_score_engine_helpers_phase13.py

Test bắt buộc:
- safe_score("80") == 80.
- safe_score("abc", default=50) == 50.
- build_score_inputs(None, None, None) trả signal=0, evidence=50, execution=100.
- normalize_weights(None) có tổng gần bằng 1.0.
- normalize_weights({"signal_score": 2, "evidence_score": 1, "execution_quality_score": 1}) normalize thành tổng gần bằng 1.0.
- normalize_weights không mutate input gốc.
- weighted_component(80, 0.5) == 40.0.

Chạy:
python -m pytest tests/test_final_score_engine_helpers_phase13.py -q
python -m pytest tests/test_final_score_engine_import_phase13.py -q

Tiêu chí hoàn thành:
- Helper ổn định với dữ liệu bẩn.
- Không có side effect.
- Test mới và test import pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 4 — PHASE 13.4: TRIỂN KHAI calculate_final_score() THEO CÔNG THỨC GIAI ĐOẠN ĐẦU

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Đã có core/final_score_engine.py với helper đọc score và weight an toàn. Bây giờ triển khai công thức final_score chính của Phase 13.

Mục tiêu riêng của prompt này:
Tạo calculate_final_score() độc lập, chỉ tính điểm, không quyết định giao dịch.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/final_score_engine.py
  2. tests/test_final_score_engine_basic_phase13.py
- Không sửa analysis_engine.py.
- Không sửa signal_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa statistical_edge_engine.py.
- Không sửa execution_quality_engine.py.
- Không sửa trade_mistake_detector.py.
- Không sửa UI/scanner.
- Không tạo decision_engine.py.
- Không thay đổi decision/gate.

Việc cần làm:
1. Trong core/final_score_engine.py, thêm hoặc hoàn thiện hàm:
   def calculate_final_score(
       signal_score: object,
       evidence_score: object = None,
       execution_quality_score: object = None,
       *,
       weights: dict | None = None,
   ) -> dict:
       ...

2. Logic bắt buộc:
   - score_inputs = build_score_inputs(signal_score, evidence_score, execution_quality_score).
   - score_weights = normalize_weights(weights).
   - weighted_components gồm:
     {
       "signal_score": signal_score * weight_signal,
       "evidence_score": evidence_score * weight_evidence,
       "execution_quality_score": execution_quality_score * weight_execution,
     }
   - raw_final_score = tổng weighted_components.
   - final_score = clamp_score(round(raw_final_score)).
   - Không raise exception với dữ liệu bẩn.

3. Output bắt buộc:
   {
       "final_score": int,
       "score_inputs": {
           "signal_score": int,
           "evidence_score": int,
           "execution_quality_score": int,
       },
       "score_weights": {
           "signal_score": float,
           "evidence_score": float,
           "execution_quality_score": float,
       },
       "weighted_components": {
           "signal_score": float,
           "evidence_score": float,
           "execution_quality_score": float,
       },
       "reason_codes": list[str],
       "penalty_codes": list[str],
       "warning_codes": list[str],
       "score_breakdown": {
           "raw_final_score": float,
           "formula": "signal_score*0.65 + evidence_score*0.20 + execution_quality_score*0.15",
       },
       "reason": str,
   }

4. Reason/warning code logic:
   - Luôn có FINAL_SCORE_OK trong reason_codes nếu tính thành công.
   - Nếu evidence_score == 50 thì thêm FINAL_SCORE_EVIDENCE_NEUTRAL.
   - Nếu evidence_score >= 65 thì thêm FINAL_SCORE_EVIDENCE_POSITIVE.
   - Nếu evidence_score <= 40 thì thêm FINAL_SCORE_EVIDENCE_NEGATIVE vào penalty_codes.
   - Nếu execution_quality_score >= 85 thì thêm FINAL_SCORE_EXECUTION_STRONG.
   - Nếu execution_quality_score < 70 thì thêm FINAL_SCORE_EXECUTION_WEAK vào penalty_codes.
   - Nếu signal_score chiếm weight lớn nhất thì thêm FINAL_SCORE_SIGNAL_DOMINANT.
   - Nếu input nào phải fallback do None hoặc bẩn thì thêm FINAL_SCORE_DATA_INCOMPLETE vào warning_codes.

5. Không dùng final_score để sửa hoặc cap decision.
6. Không gọi trade_gate_engine.
7. Không gọi decision_engine.

Test:
Tạo file:
tests/test_final_score_engine_basic_phase13.py

Test bắt buộc:
1. Case chuẩn:
   signal_score=80, evidence_score=50, execution_quality_score=100
   expected = round(80*0.65 + 50*0.20 + 100*0.15) = round(77) = 77
   final_score == 77.

2. Evidence tốt:
   signal_score=70, evidence_score=80, execution_quality_score=100
   final_score đúng công thức.
   reason_codes có FINAL_SCORE_EVIDENCE_POSITIVE.

3. Evidence xấu:
   signal_score=80, evidence_score=30, execution_quality_score=100
   penalty_codes có FINAL_SCORE_EVIDENCE_NEGATIVE.

4. Execution yếu:
   signal_score=80, evidence_score=50, execution_quality_score=40
   penalty_codes có FINAL_SCORE_EXECUTION_WEAK.

5. Dữ liệu thiếu:
   signal_score="abc", evidence_score=None, execution_quality_score=None
   không crash.
   warning_codes có FINAL_SCORE_DATA_INCOMPLETE.

6. Custom weights:
   weights={"signal_score": 0.5, "evidence_score": 0.25, "execution_quality_score": 0.25}
   final_score đúng công thức sau normalize.

Chạy:
python -m pytest tests/test_final_score_engine_basic_phase13.py -q
python -m pytest tests/test_final_score_engine_import_phase13.py tests/test_final_score_engine_helpers_phase13.py -q

Tiêu chí hoàn thành:
- Công thức đúng.
- Output ổn định.
- Reason/warning/penalty codes không duplicate.
- Không thay đổi behavior hệ thống hiện tại.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 5 — PHASE 13.5: THÊM HÀM calculate_final_score_from_payload()

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Analysis result hiện tại có thể chứa score trong nhiều vị trí:
- scenario_scores.buy.signal_score
- scenario_scores.sell.signal_score
- decision_summary.best_score
- evidence/evidence_score nếu đã có
- execution_quality/execution_quality_score nếu đã có

Mục tiêu riêng của prompt này:
Thêm helper đọc final_score từ payload dict giống analysis_result hoặc scanner row tương lai. Chưa tích hợp vào analysis_engine.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/final_score_engine.py
  2. tests/test_final_score_engine_payload_phase13.py
- Không sửa analysis_engine.py trong prompt này.
- Không sửa scanner.py.
- Không sửa UI.
- Không sửa decision/gate.
- Không tạo decision_engine.py.

Việc cần làm:
1. Trong core/final_score_engine.py, thêm helper:
   def pick_signal_score(payload: dict | None, side: str | None = None) -> int:
       ...

   Logic:
   - Nếu payload không phải dict thì trả DEFAULT_SIGNAL_SCORE.
   - Nếu side là "buy" hoặc "sell" và payload["scenario_scores"][side]["signal_score"] có giá trị thì dùng.
   - Nếu không có signal_score nhưng có total thì dùng total làm fallback backward-compatible.
   - Nếu không có side thì ưu tiên:
     + payload["decision_summary"]["best_score"]
     + payload["best_score"]
     + payload["signal_score"]
     + 0
   - Không raise exception.

2. Thêm helper:
   def pick_evidence_score(payload: dict | None) -> int:
       ...

   Logic:
   - Ưu tiên payload["evidence_score"].
   - Sau đó payload["evidence"]["evidence_score"].
   - Sau đó payload["statistical_edge"]["evidence_score"].
   - Fallback DEFAULT_EVIDENCE_SCORE = 50.
   - Không raise exception.

3. Thêm helper:
   def pick_execution_quality_score(payload: dict | None) -> int:
       ...

   Logic:
   - Ưu tiên payload["execution_quality_score"].
   - Sau đó payload["execution_quality"]["execution_quality_score"].
   - Sau đó payload["execution"]["execution_quality_score"].
   - Fallback DEFAULT_EXECUTION_QUALITY_SCORE = 100.
   - Không raise exception.

4. Thêm hàm:
   def calculate_final_score_from_payload(payload: dict | None, *, side: str | None = None, weights: dict | None = None) -> dict:
       ...

   Logic:
   - Dùng 3 helper pick_* ở trên.
   - Gọi calculate_final_score().
   - Thêm vào score_breakdown:
     "source": "payload"
     "side": side hoặc None
   - Không mutate payload gốc.

5. Tạo test:
   tests/test_final_score_engine_payload_phase13.py

Test bắt buộc:
1. Payload có scenario_scores:
   {
     "scenario_scores": {
       "buy": {"signal_score": 82},
       "sell": {"signal_score": 60}
     },
     "evidence": {"evidence_score": 50},
     "execution_quality": {"execution_quality_score": 100}
   }
   side="buy" => signal_score input = 82.

2. Payload chỉ có total cũ:
   {"scenario_scores": {"buy": {"total": 75}}}
   side="buy" => signal_score input = 75.

3. Payload có decision_summary.best_score:
   {"decision_summary": {"best_score": 78}}
   side=None => signal_score input = 78.

4. Payload thiếu evidence/execution thì fallback evidence=50, execution=100.

5. Payload None không crash.

6. Hàm không mutate payload gốc.

Chạy:
python -m pytest tests/test_final_score_engine_payload_phase13.py -q
python -m pytest tests/test_final_score_engine_*_phase13.py -q

Tiêu chí hoàn thành:
- Đọc payload linh hoạt.
- Backward-compatible với total.
- Không tích hợp vào analysis_engine trong prompt này.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 6 — PHASE 13.6: THÊM BATCH VÀ SUMMARY CHO FINAL SCORE

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Scanner hoặc journal sau này có thể cần tính final_score cho nhiều payload. Phase 13 chỉ cung cấp utility, chưa đổi ranking scanner.

Mục tiêu riêng của prompt này:
Thêm hàm batch và summary độc lập cho final_score_engine.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/final_score_engine.py
  2. tests/test_final_score_engine_batch_phase13.py
- Không sửa scanner.py.
- Không sửa analysis_engine.py.
- Không sửa UI.
- Không sửa decision/gate.
- Không tạo decision_engine.py.

Việc cần làm:
1. Trong core/final_score_engine.py, thêm hàm:
   def calculate_final_score_batch(payloads: list[dict] | None, *, side: str | None = None, weights: dict | None = None) -> list[dict]:
       ...

   Logic:
   - Nếu payloads không phải list thì trả [].
   - Với mỗi item:
     + Nếu item là dict thì gọi calculate_final_score_from_payload(item, side=side, weights=weights).
     + Nếu item không phải dict thì vẫn trả result default có warning FINAL_SCORE_DATA_INCOMPLETE, không crash.
   - Không mutate input.

2. Thêm hàm:
   def summarize_final_scores(results: list[dict] | None) -> dict:
       ...

   Output gợi ý:
   {
       "count": int,
       "average_final_score": float,
       "min_final_score": int | None,
       "max_final_score": int | None,
       "strong_count": int,
       "weak_count": int,
       "warning_code_counts": dict,
       "penalty_code_counts": dict,
   }

   Logic:
   - strong_count: final_score >= 80.
   - weak_count: final_score < 50.
   - average làm tròn 2 chữ số.
   - Dữ liệu bẩn không crash.
   - Đếm warning_codes và penalty_codes không duplicate trong từng result nếu có thể.

3. Tạo test:
   tests/test_final_score_engine_batch_phase13.py

Test bắt buộc:
- Batch 3 payload:
  + signal 80, evidence 50, execution 100 => 77.
  + signal 60, evidence 50, execution 100 => 64.
  + signal 40, evidence 30, execution 40 => thấp.
- calculate_final_score_batch trả 3 result.
- summarize_final_scores count=3.
- average_final_score đúng gần với trung bình.
- Có weak_count đúng.
- Payload None hoặc item bẩn không crash.

Chạy:
python -m pytest tests/test_final_score_engine_batch_phase13.py -q
python -m pytest tests/test_final_score_engine_*_phase13.py -q

Tiêu chí hoàn thành:
- Batch/summary hoạt động độc lập.
- Không đổi scanner ranking.
- Không đổi decision.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 7 — PHASE 13.7: THÊM HELPER BUILD PAYLOAD LƯU JOURNAL/EXPORT CHO FINAL SCORE, KHÔNG MIGRATION

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 13 cần chuẩn hóa payload final_score để sau này journal/export có thể lưu. Nhưng migration database không thuộc prompt này.

Mục tiêu riêng của prompt này:
Thêm helper tạo payload phẳng cho final_score. Không ghi database, không sửa journal_service.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/final_score_engine.py
  2. tests/test_final_score_engine_journal_payload_phase13.py
- Không sửa services/journal_service.py.
- Không sửa data/migrations.
- Không sửa JournalEntry dataclass.
- Không sửa UI.
- Không sửa analysis_engine.py.
- Không sửa scanner.py.
- Không tạo decision_engine.py.

Việc cần làm:
1. Trong core/final_score_engine.py, thêm hàm:
   def build_final_score_journal_payload(payload: dict | None, *, side: str | None = None, weights: dict | None = None) -> dict:
       ...

2. Hàm này:
   - Gọi calculate_final_score_from_payload(payload, side=side, weights=weights).
   - Trả payload phẳng, dễ lưu:
     {
       "final_score": int,
       "final_score_signal_score": int,
       "final_score_evidence_score": int,
       "final_score_execution_quality_score": int,
       "final_score_weights": dict,
       "final_score_weighted_components": dict,
       "final_score_reason_codes": list[str],
       "final_score_penalty_codes": list[str],
       "final_score_warning_codes": list[str],
       "final_score_breakdown": dict,
     }

3. Không ghi file, không gọi database.
4. Không thêm field vào JournalEntry.
5. Không tạo migration.
6. Không mutate input payload.

7. Tạo test:
   tests/test_final_score_engine_journal_payload_phase13.py

Test bắt buộc:
- Payload có signal/evidence/execution đầy đủ thì journal payload có final_score đúng.
- Payload thiếu evidence/execution thì fallback đúng.
- Payload None không crash.
- Payload gốc không bị mutate.
- Output có đủ key bắt buộc.

Chạy:
python -m pytest tests/test_final_score_engine_journal_payload_phase13.py -q
python -m pytest tests/test_final_score_engine_*_phase13.py -q

Tiêu chí hoàn thành:
- Có payload chuẩn cho journal/export tương lai.
- Không sửa database/journal_service.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 8 — PHASE 13.8: TÍCH HỢP METADATA-ONLY VÀO analysis_engine, KHÔNG ĐỔI DECISION/GATE

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Đã có core/final_score_engine.py độc lập. Bây giờ có thể đưa final_score vào output của analyze_symbol để UI/API sau này đọc được. Tuy nhiên Phase 13 không được dùng final_score để thay đổi decision.

Mục tiêu riêng của prompt này:
Tích hợp final_score vào result của core.analysis_engine.analyze_symbol ở mức metadata-only.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/analysis_engine.py
  2. tests/test_analysis_final_score_integration_phase13.py
- Chỉ được sửa core/final_score_engine.py nếu phát hiện bug nhỏ rõ ràng khi tích hợp.
- Không sửa signal_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa account_guard.py.
- Không sửa statistical_edge_engine.py.
- Không sửa execution_quality_engine.py.
- Không sửa trade_mistake_detector.py.
- Không sửa UI/scanner.
- Không tạo decision_engine.py.
- Không thay đổi classify_decision.
- Không thay đổi decision_summary["action"] hiện tại.
- Không thay đổi trade_permission.
- Không thay đổi gate_result.

Việc cần làm:
1. Mở core/analysis_engine.py và tìm nơi return dict cuối cùng trong analyze_symbol().

2. Import nhẹ:
   from core.final_score_engine import calculate_final_score

3. Xác định signal_score:
   - best_side đã có trong analyze_symbol.
   - scores[best_side]["signal_score"] nếu có.
   - fallback scores[best_side]["total"] nếu chưa có.

4. Xác định evidence_score:
   - Phase 10 statistical_edge_engine có thể chưa được tích hợp vào analyze_symbol.
   - Trong prompt này, nếu analyze_symbol chưa có evidence_score thực, dùng fallback trung lập 50.
   - Không gọi journal_service ở core.
   - Không thêm dependency service vào core.
   - Không tự lấy trades trong analysis_engine.

5. Xác định execution_quality_score:
   - Đây là điểm sau khi trade đóng, analysis live thường chưa có.
   - Dùng fallback an toàn 100.
   - Không gọi trade_mistake_detector.
   - Không suy luận execution quality từ setup live.

6. Gọi:
   final_score_result = calculate_final_score(
       signal_score=best_signal_score,
       evidence_score=50,
       execution_quality_score=100,
   )

7. Thêm vào result return:
   "final_score": final_score_result["final_score"],
   "final_score_detail": final_score_result,

8. Nếu result đã có field "final_score" thì không overwrite bừa. Đọc kỹ code trước. Nếu chưa có thì thêm mới.

9. Không dùng final_score để:
   - đổi decision_action.
   - đổi decision_summary.
   - đổi trade_permission.
   - đổi scanner_action.
   - đổi gate_result.

10. Tạo test:
    tests/test_analysis_final_score_integration_phase13.py

Test gợi ý:
- Dùng dữ liệu candle fake giống các test analysis_engine hiện có. Nếu đã có fixture/helper trong tests hiện tại thì reuse.
- Gọi analyze_symbol với dữ liệu đủ D1/H4/H1.
- Assert:
  + result có key "final_score".
  + result có key "final_score_detail".
  + final_score_detail["score_inputs"]["evidence_score"] == 50.
  + final_score_detail["score_inputs"]["execution_quality_score"] == 100.
  + decision_summary["action"] vẫn tồn tại.
  + trade_permission vẫn tồn tại.

11. Nếu tạo candle fake:
    - D1 tối thiểu 70 nến.
    - H4 tối thiểu 70 nến.
    - H1 tối thiểu 40 nến.
    - Giá biến động nhẹ, realistic, không để ATR = 0.

Test bắt buộc:
python -m pytest tests/test_analysis_final_score_integration_phase13.py -q
python -m pytest tests/test_final_score_engine_*_phase13.py -q

Tiêu chí hoàn thành:
- analyze_symbol output có final_score metadata.
- Không đổi decision/gate.
- Không gọi services từ core.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 9 — PHASE 13.9: TÍCH HỢP EVIDENCE_SCORE NẾU CONTROLLER CUNG CẤP CLOSED_TRADES, KHÔNG GỌI SERVICE TRONG CORE

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 13.8 đã thêm final_score metadata với evidence_score fallback 50. Bây giờ cần cho analyze_symbol có thể dùng evidence_score thật nếu caller đã truyền closed_trades, nhưng core không được tự gọi database/service.

Mục tiêu riêng của prompt này:
Dùng core.statistical_edge_engine.calculate_evidence_score trong analysis_engine nếu closed_trades có dữ liệu, để final_score có evidence_score thật hơn.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/analysis_engine.py
  2. tests/test_analysis_final_score_evidence_phase13.py
- Chỉ được sửa core/final_score_engine.py nếu phát hiện bug nhỏ rõ ràng.
- Không sửa services/journal_service.py.
- Không sửa controllers.
- Không sửa signal_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa UI/scanner.
- Không tạo decision_engine.py.
- Không thay đổi decision/gate.

Việc cần làm:
1. Trong core/analysis_engine.py, import:
   from core.statistical_edge_engine import calculate_evidence_score

2. Trong analyze_symbol(), sau khi xác định best_side và market_regime:
   - Nếu closed_trades là list và không rỗng:
     evidence_result = calculate_evidence_score(
         closed_trades,
         symbol=request.symbol,
         direction=best_side,
         regime=market_regime.get("primary") nếu market_regime là dict
       )
   - Nếu không có closed_trades:
     evidence_result = {"evidence_score": 50, ... fallback trung lập}

3. Dùng evidence_score từ evidence_result để calculate_final_score().

4. Thêm vào output:
   "evidence": evidence_result

   Nếu result đã có key evidence thì đọc kỹ trước, không overwrite bừa nếu đã có cấu trúc khác.

5. final_score_detail["score_inputs"]["evidence_score"] phải phản ánh evidence_result["evidence_score"].

6. Không dùng evidence_score để thay đổi:
   - decision_action
   - trade_permission
   - gate_result
   - scenario sorting
   - scanner ranking

7. Tạo test:
   tests/test_analysis_final_score_evidence_phase13.py

Test bắt buộc:
- Tạo closed_trades 35 lệnh cùng symbol/direction với expectancy tốt.
- Gọi analyze_symbol(..., closed_trades=closed_trades).
- Assert:
  + result["evidence"]["evidence_score"] tồn tại.
  + final_score_detail["score_inputs"]["evidence_score"] == result["evidence"]["evidence_score"].
  + final_score khác với case evidence fallback nếu evidence_score không phải 50.
  + decision_summary["action"] vẫn tồn tại và không bị assert theo final_score.

Test dữ liệu giống thật:
- closed_trades gồm dict:
  {
    "symbol": "EUR/USD",
    "direction": "buy",
    "status": "closed",
    "closed_at": "2026-06-04T10:00:00+07:00",
    "result_r": 1.2 hoặc -1.0,
    "market_regime": "trend_up" hoặc "trending_up"
  }
- Nên có ít nhất 35 mẫu để statistical_edge_engine không fallback do thiếu data.

Chạy:
python -m pytest tests/test_analysis_final_score_evidence_phase13.py -q
python -m pytest tests/test_analysis_final_score_integration_phase13.py -q
python -m pytest tests/test_final_score_engine_*_phase13.py -q

Tiêu chí hoàn thành:
- Evidence thật được dùng khi caller truyền closed_trades.
- Core không gọi service/database.
- Không đổi decision/gate.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 10 — PHASE 13.10: HỖ TRỢ EXECUTION_QUALITY_SCORE ĐẦU VÀO TỪ CALLER, KHÔNG TỰ SUY LUẬN LIVE

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Execution quality thường chỉ có sau khi trade đóng. Tuy nhiên một số caller hoặc test/journal payload tương lai có thể truyền execution_quality_score vào analyze_symbol hoặc final score payload. Phase 13 chỉ nhận dữ liệu nếu caller cung cấp, không tự suy luận từ trade live.

Mục tiêu riêng của prompt này:
Cho analysis_engine nhận optional execution_quality_score nếu đã có, để final_score dùng được. Không tự gọi trade_mistake_detector.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. core/analysis_engine.py
  2. tests/test_analysis_final_score_execution_quality_phase13.py
- Chỉ được sửa core/final_score_engine.py nếu phát hiện bug nhỏ rõ ràng.
- Không sửa execution_quality_engine.py.
- Không sửa trade_mistake_detector.py.
- Không sửa controllers nếu chưa cần.
- Không sửa UI/scanner.
- Không sửa decision/gate.
- Không tạo decision_engine.py.

Việc cần làm:
1. Xem chữ ký hàm analyze_symbol() trong core/analysis_engine.py.

2. Thêm tham số keyword-only mới, backward-compatible:
   execution_quality_score: int | float | str | None = None

   Lưu ý:
   - Thêm ở cuối danh sách keyword-only để không phá call hiện có.
   - Không đổi thứ tự các positional args.

3. Khi tính final_score:
   - Nếu execution_quality_score được truyền và hợp lệ thì dùng.
   - Nếu None hoặc bẩn thì fallback 100.
   - Dùng helper từ final_score_engine nếu có, hoặc để calculate_final_score tự safe_score.

4. Thêm vào output:
   "execution_quality": {
       "execution_quality_score": giá trị đã dùng,
       "source": "provided" nếu caller truyền hợp lệ, ngược lại "fallback_no_closed_trade_execution_data"
   }

   Nếu output đã có key execution_quality thì đọc kỹ trước, không overwrite bừa.

5. Không gọi calculate_execution_quality().
6. Không gọi detect_trade_mistakes().
7. Không dùng execution_quality_score để đổi decision.

8. Tạo test:
   tests/test_analysis_final_score_execution_quality_phase13.py

Test bắt buộc:
- Gọi analyze_symbol(..., execution_quality_score=40).
- Assert:
  + result["execution_quality"]["execution_quality_score"] == 40.
  + final_score_detail["score_inputs"]["execution_quality_score"] == 40.
  + final_score thấp hơn case fallback 100 nếu signal/evidence giống nhau.
  + decision_summary vẫn tồn tại.
  + trade_permission vẫn tồn tại.

- Gọi analyze_symbol(..., execution_quality_score="bad").
  + Không crash.
  + execution_quality_score fallback 100.

Chạy:
python -m pytest tests/test_analysis_final_score_execution_quality_phase13.py -q
python -m pytest tests/test_analysis_final_score_evidence_phase13.py tests/test_analysis_final_score_integration_phase13.py -q
python -m pytest tests/test_final_score_engine_*_phase13.py -q

Tiêu chí hoàn thành:
- analyze_symbol nhận optional execution_quality_score an toàn.
- Không tự detect lỗi execution.
- Không đổi decision/gate.
- Test pass.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 11 — PHASE 13.11: TEST REALISTIC TỔNG HỢP CHO FINAL SCORE ENGINE

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Final score đã có engine độc lập và tích hợp metadata vào analysis_engine. Cần test realistic bằng dữ liệu giống thật để đảm bảo công thức ổn định, không tạo score cao giả.

Mục tiêu riêng của prompt này:
Tạo test tổng hợp cho final_score_engine với nhiều tình huống thực tế.

Yêu cầu rất quan trọng:
- Chỉ được sửa:
  1. tests/test_phase13_final_score_realistic.py
  2. core/final_score_engine.py nếu phát hiện bug nhỏ rõ ràng từ test.
- Không sửa analysis_engine.py trong prompt này.
- Không sửa engine khác.
- Không sửa UI/scanner.
- Không sửa decision/gate.
- Không tạo decision_engine.py.

Việc cần làm:
1. Tạo test file:
   tests/test_phase13_final_score_realistic.py

2. Case 1 — Setup kỹ thuật mạnh nhưng evidence trung lập và execution tốt:
   signal_score=84
   evidence_score=50
   execution_quality_score=100
   expected final_score = round(84*0.65 + 50*0.20 + 100*0.15)
   Assert đúng công thức.

3. Case 2 — Signal mạnh nhưng evidence xấu và execution yếu:
   signal_score=88
   evidence_score=30
   execution_quality_score=45
   Assert:
   - final_score thấp hơn signal_score rõ ràng.
   - penalty_codes có FINAL_SCORE_EVIDENCE_NEGATIVE.
   - penalty_codes có FINAL_SCORE_EXECUTION_WEAK.

4. Case 3 — Signal vừa phải nhưng evidence tốt và execution tốt:
   signal_score=68
   evidence_score=80
   execution_quality_score=95
   Assert:
   - final_score tăng so với signal_score một phần nhưng không vượt vô lý.
   - reason_codes có FINAL_SCORE_EVIDENCE_POSITIVE.

5. Case 4 — Dữ liệu bẩn:
   signal_score="abc"
   evidence_score=None
   execution_quality_score="nan"
   Assert:
   - Không crash.
   - warning_codes có FINAL_SCORE_DATA_INCOMPLETE.
   - final_score nằm trong 0-100.

6. Case 5 — Batch realistic:
   Tạo list 5 payload:
   - EUR/USD buy signal 82 evidence 55 execution 100.
   - GBP/JPY sell signal 76 evidence 35 execution 70.
   - XAU/USD buy signal 90 evidence 50 execution 60.
   - USD/JPY sell signal 58 evidence 80 execution 95.
   - AUD/USD buy signal 45 evidence 50 execution 100.
   Gọi calculate_final_score_batch và summarize_final_scores.
   Assert:
   - count=5.
   - average_final_score hợp lệ.
   - penalty_code_counts có ít nhất FINAL_SCORE_EVIDENCE_NEGATIVE hoặc FINAL_SCORE_EXECUTION_WEAK khi phù hợp.

Chạy:
python -m pytest tests/test_phase13_final_score_realistic.py -q
python -m pytest tests/test_final_score_engine_*_phase13.py -q

Tiêu chí hoàn thành:
- Realistic test pass.
- Engine chống dữ liệu bẩn tốt.
- Không thay đổi ngoài phạm vi.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 12 — PHASE 13.12: REGRESSION TEST PHASE 10/11/12 VÀ GATE ĐỂ ĐẢM BẢO KHÔNG PHÁ HỆ THỐNG

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Phase 13 chỉ thêm final_score metadata. Cần chạy regression để chắc chắn không phá evidence_score, execution_quality_score, trade_mistake_detector và gate/account guard.

Mục tiêu riêng của prompt này:
Chạy test liên quan Phase 10/11/12 và gate/account guard. Chỉ sửa bug nhỏ nếu bug nằm trong code Phase 13 vừa viết.

Yêu cầu rất quan trọng:
- Không thêm feature mới.
- Không refactor.
- Không sửa UI.
- Không sửa database.
- Không sửa signal_engine.py.
- Không sửa trade_gate_engine.py.
- Không sửa account_guard.py.
- Không sửa statistical_edge_engine.py.
- Không sửa execution_quality_engine.py.
- Không sửa trade_mistake_detector.py.
- Chỉ được sửa nếu thật sự có bug Phase 13:
  1. core/final_score_engine.py
  2. core/analysis_engine.py nếu lỗi do tích hợp final_score metadata
  3. tests/test_*phase13.py

Việc cần làm:
1. Chạy toàn bộ test Phase 13:
   python -m pytest tests/test_final_score_engine_*_phase13.py tests/test_analysis_final_score_*_phase13.py tests/test_phase13_final_score_realistic.py -q

2. Chạy test Phase 10 nếu tồn tại:
   python -m pytest tests/test_statistical_edge_engine_*phase10.py tests/test_phase10_statistical_edge_realistic.py -q
   Nếu glob không match trong PowerShell, chạy từng file hiện có bằng rg hoặc Get-ChildItem.

3. Chạy test Phase 11:
   python -m pytest tests/test_execution_quality_engine_*phase11.py tests/test_phase11_execution_quality_realistic.py -q

4. Chạy test Phase 12:
   python -m pytest tests/test_trade_mistake_detector_*phase12.py tests/test_reason_codes_mistake_detector_phase12.py -q

5. Chạy gate/account guard quan trọng:
   python -m pytest tests/test_trade_gate_engine.py tests/test_account_guard_phase7.py tests/test_trade_gate_account_guard_phase7.py -q

6. Nếu test fail:
   - Đọc kỹ lỗi.
   - Chỉ sửa lỗi do Phase 13 gây ra.
   - Không thay đổi expected behavior của phase cũ để test pass.
   - Nếu lỗi do môi trường thiếu MT5/PyQt6 thì ghi rõ, không sửa bừa.

7. Nếu tất cả ổn, không sửa gì thêm.

Tiêu chí hoàn thành:
- Test Phase 13 pass.
- Phase 10/11/12 không bị ảnh hưởng.
- Gate/account guard không bị ảnh hưởng.
- Không có sửa ngoài phạm vi.

Sau khi hoàn thành, báo đúng câu:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT


================================================================================

PROMPT 13 — PHASE 13.13: RÀ SOÁT CUỐI PHASE 13 VÀ BÁO HOÀN THÀNH

Bạn đang làm trong project ai-market-analyst-v2.0 trên VSCode.

Bối cảnh:
Đây là prompt cuối Phase 13. Không thêm tính năng mới nữa, chỉ rà soát cuối.

Mục tiêu riêng của prompt này:
Xác nhận Phase 13 đã hoàn thành đúng phạm vi: có final_score mới, nhưng chưa lấn sang Phase 14 decision engine, chưa đổi gate, chưa đổi UI/scanner.

Yêu cầu rất quan trọng:
- Không thêm feature mới.
- Không refactor code.
- Không sửa UI.
- Không sửa database/migration.
- Không tạo decision_engine.py.
- Không thay đổi decision/gate.
- Không đổi scanner ranking.
- Chỉ được sửa lỗi import/test fail nhỏ nếu phát hiện rõ ràng.

Checklist cần kiểm tra:
1. Có file:
   - core/final_score_engine.py

2. Module có tối thiểu các hàm/helper:
   - clamp_score
   - safe_score
   - normalize_weights
   - weighted_component
   - build_score_inputs
   - calculate_final_score
   - pick_signal_score
   - pick_evidence_score
   - pick_execution_quality_score
   - calculate_final_score_from_payload
   - calculate_final_score_batch
   - summarize_final_scores
   - build_final_score_journal_payload

3. calculate_final_score có behavior:
   - Dùng công thức 0.65 / 0.20 / 0.15 mặc định.
   - signal_score fallback 0.
   - evidence_score fallback 50.
   - execution_quality_score fallback 100.
   - final_score luôn trong 0-100.
   - dữ liệu bẩn không crash.
   - reason_codes/penalty_codes/warning_codes không duplicate.

4. analysis_engine nếu đã tích hợp:
   - result có final_score.
   - result có final_score_detail.
   - result có evidence nếu prompt 9 đã làm.
   - result có execution_quality nếu prompt 10 đã làm.
   - Không dùng final_score để đổi decision_summary["action"].
   - Không dùng final_score để đổi trade_permission.
   - Không dùng final_score để đổi trade_gate.

5. Không có file:
   - core/decision_engine.py
   Nếu file này tồn tại từ trước thì không sửa trong Phase 13, nhưng nếu Phase 13 vừa tạo thì phải dừng và báo lỗi phạm vi.

6. Không có thay đổi có chủ đích ở:
   - core/signal_engine.py
   - core/trade_gate_engine.py
   - core/account_guard.py
   - core/statistical_edge_engine.py
   - core/execution_quality_engine.py
   - core/trade_mistake_detector.py
   - ui/
   - data/migrations/

Test bắt buộc:
1. Chạy toàn bộ test Phase 13:
   python -m pytest tests/test_final_score_engine_*_phase13.py tests/test_analysis_final_score_*_phase13.py tests/test_phase13_final_score_realistic.py -q

2. Chạy import nhanh bằng python:
   python - <<'PY'
   from core.final_score_engine import calculate_final_score
   result = calculate_final_score(80, 50, 100)
   print(result)
   assert result["final_score"] == 77
   assert "score_inputs" in result
   assert "score_weights" in result
   PY

3. Nếu đủ thời gian, chạy thêm:
   python -m pytest -q
   Nếu full test fail do môi trường thiếu MT5/PyQt6 hoặc network thì ghi rõ test nào fail do môi trường, không sửa code bừa.

Báo cáo cuối cần nêu ngắn gọn:
- Đã tạo module nào.
- Đã có công thức final_score nào.
- Đã tích hợp vào analysis_engine ở mức metadata-only chưa.
- Test nào đã chạy.
- Có lỗi môi trường nào không.

Sau khi hoàn thành prompt cuối cùng, bắt buộc báo đúng 2 câu sau:
ĐÃ HOÀN THÀNH CÔNG VIỆC TRONG PROMPT
ĐÃ HOÀN THÀNH PHASE SỐ 13
